# 自动回复授权与覆盖配置（smart-message-autoreply 技能读取）

> **默认模式：`auto`**（2026-09-21 用户指令）——普通 / 明确 / 低风险消息，过 §九 九步检查后**自动发送**。
> **高风险永远不自动发送**（§十四：金钱/付款/转账/合同/重要承诺/账号安全/密码·验证码/敏感信息/争议/重大后果），无论本文件怎么配。
> 本文件用于**精细覆盖**：把特定联系人 / 群聊 / 场景降级或排除。

## 一、全局默认
- 默认模式：`auto`

## 二、联系人 / 群聊 覆盖（按需填写，不填即跟随全局默认）

| 对象（备注名/群名） | 类型 | 模式 | 备注 |
|---|---|---|---|
| （待填示例）重要客户A | 私聊 | draft | 先给我看再发 |
| （待填示例）某营销群 | 群聊 | skip | 不回 |

> 模式取值：`auto`（自动回）/ `draft`（只草拟）/ `skip`（不回、只读）。

## 三、场景覆盖（按需填写）

| 场景 | 模式 | 备注 |
|---|---|---|
| （待填示例）预约确认 | draft | 涉及时间安排，先确认 |

## 四、授权变更记录（append-only）

- 2026-09-21：用户指令「开 auto」→ 全局默认由 `draft` 改为 `auto`；高风险仍走 §十四 转人工确认。
