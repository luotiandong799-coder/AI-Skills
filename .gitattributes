# 统一 LF 换行：CRLF 可能破坏 YAML frontmatter 解析，导致 skill 静默不加载、不报错、永不触发
# 本机 core.autocrlf=true，若无本文件，git 会在 checkout 把 .md 转成 CRLF
*.md text eol=lf
# 2026-09-18 补：co-learn 的 .txt 快照实测被 autocrlf 写成 CRLF，与 LF 副本漂移 444 行
*.txt text eol=lf
# 2026-09-18 补：脚本/配置/自描述文件也锁 LF（本机 core.autocrlf=true，不锁会在 checkout 转 CRLF；
# CRLF 的 .gitattributes 还会破坏最后一条 pattern 的解析）。已核对索引内这些类型均无 CR，锁定不触发重归一化。
*.py text eol=lf
*.sh text eol=lf
*.ps1 text eol=lf
*.json text eol=lf
*.yaml text eol=lf
*.svg text eol=lf
.gitattributes text eol=lf
.gitignore text eol=lf
