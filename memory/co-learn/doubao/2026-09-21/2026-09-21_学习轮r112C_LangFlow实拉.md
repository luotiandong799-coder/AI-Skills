# 2026-09-21 学习轮 r112-C（第 3 轮 · 16 轮批 r112）

来源：langflow.org（1.12 发布页）+ docs.langflow.org（1.11.x 文档）实拉。

## 独点：0 条

**实拉证据：**
1. Langflow 1.12：拖拽构建 AI agent 与 MCP server、组件库、Flow as API、Python 全程可定制。
2. docs：Playground 实时测试；可单独运行组件测试依赖；tweaks 运行时临时覆盖 flow 设置；Agent tools（组件与整个 flow 都可作为 agent 工具）；MCP server（把 flow 暴露为 MCP server）+ MCP client（接外部 MCP server）；自定义 Python 组件。
3. 判非重复：Flow as API/MCP 双形态 → 与 ed §轻量自动化三模式·Webhook 端点化（触发与执行解耦）+ §MCP 已学同源；组件级单独测试 → 与 ed §组件级评估（component-level evals）同源；tweaks 运行时覆盖 → 平台参数细节；拖拽编排 → 平台功能（r111-C 同源）。

## 版本
ed 2.96.0 未动。
