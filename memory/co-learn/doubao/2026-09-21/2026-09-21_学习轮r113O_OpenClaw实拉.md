# 2026-09-21 学习轮 r113-O（第 15 轮 · 16 轮批 r113）

来源：docs.openclaw.ai 实拉（tools 总览 zh-CN/fr/pl/uk 多语言版 + skill-workshop fr 全文 + providers/deepseek）；WaytoAGI/agentskills.io/HF/ModelScope 已在前轮实拉（知识库社区/技能目录/模型库），本轮不重拉。

## 独点：0 条

**实拉证据：**
1. OpenClaw Skill Workshop（fr 全文）：技能创建/更新走提案队列——agent 不直接写 SKILL.md，起草 PROPOSAL.md（含 name/description/status/version/date frontmatter）→ 用户 review → apply/reject/quarantine（可带 reason，如 "Doublon"）；支持 propose-update 更新既有技能；自动捕获可复用指令（"下一次…""别忘了…"+ 失败轮次的纠正）→ 下轮提议沉淀；autonomous.enabled 可自动创建待批提案。
2. OpenClaw 工具三选一总览：工具=动作（exec/browser/web_search）、Skills=指令包（SKILL.md 加载进 prompt）、Plugins=运行时能力（代码/凭证/hook）；模型只看到过 profile+策略+沙箱过滤后的工具。
3. 插件常见工具：Tokenjuice（压缩 exec/bash 噪音输出）、tool_search（不把每个 schema 放 prompt）、Lobster（带可恢复审批的类型化工作流）。
4. skills 加载优先级 6 层（工作区 > 项目 agent > 个人 agent > 托管 > 内置 > 额外目录）；允许列表非空即最终集合不合并。
5. DeepSeek provider（V4 flash/pro，1M 上下文，384K 输出；reasoning_content 自动回放保证思维+工具多轮链）。
**判非重复：** 提案队列 → 与 Git 审查+commit 工作流等效（同源>60%），自动检测可复用指令沉淀=本学习批机制同源；工具/Skills/Plugins 三分 → OpenClaw 生态已学；Tokenjuice → ctx 压缩同源；tool_search → 本系统已实现；加载优先级/允许列表 → 平台配置；reasoning_content 回放 → provider 层实现非个人可落地方法。不落。

## 版本
ed 2.96.0 未动。
