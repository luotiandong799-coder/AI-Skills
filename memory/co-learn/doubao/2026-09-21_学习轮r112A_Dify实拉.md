# 2026-09-21 学习轮 r112-A（第 1 轮 · 16 轮批 r112）

批前 WB 新学核查：已读 workbuddy 全部留痕（r99/r100/r115-r126）+ git log 远端 r120-r126 批次 + 7 个技能最新小节（sa 2.42/av 1.65/ctx 3.30/pt 1.51/sd 1.49/db 1.22/mts 1.32），本轮判重以此为基准。

来源（本轮 15 源实拉）：dify.ai blog×3（New Agent/Human Input/Trigger）+ dify.ai 首页×3 + aisharenet（Agent Node+Strategy）+ dify-hosting 2026 + aisrc + agenticindex + ai-gallery v1.17.1 + theplanettools + easyai + gstars + alibabacloud + hivebook。

## 独点：0 条（逐站实拉证据与判非重复理由如下）

**逐站实拉结果：**
1. New Agent 聊天构建（自动生成可复用 skills+保留上下文+入 workflow）→ 与 r111-A §Agent 资源化同源判重。
2. Agent Node + Strategy 解耦（workflow 内嵌自主 agent）→ 平台功能，与 Agent 节点已学同源。
3. Human Input node（workflow 暂停人工审核→批准/编辑/改道）→ 与 r111-B 工具级 HITL 同源判重。
4. Trigger（后台监听事件自动触发）→ 与 ed §轻量自动化三模式·事件触发同源判重。
5. Agentic RAG（迭代意图/选源/改查询/评估/重试）→ 与 r111-A 同源判重。
6. E2B Sandbox/Home Snapshots/Skill Management → 与 r111-A 同源判重。
7. 多模态检索/数据集限定 API key/v1.17.1 → 平台细节。

## 版本
ed 保持 2.96.0（WB 工作树进行中版本，未触碰）。
