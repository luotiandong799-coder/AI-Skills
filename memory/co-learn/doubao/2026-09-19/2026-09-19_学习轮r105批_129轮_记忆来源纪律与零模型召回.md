# 2026-09-19 学习轮 r105 批（129 轮 · 工程类 · 手动执行第 1 波）

来源（本波 4 页）：OpenClaw memory-architecture（大丰收）、CrewAI training、Dify agent 节点、n8n advanced-ai 索引。

## 本批独点：4 条（已并 ed 2.41.0）
1. **记忆晋升的门槛是来源不是分数**（OpenClaw）：provenance 闭集四分类（owner/agent/untrusted/system）；cron/heartbeat/子代理会话不产记忆候选；网络工具结果污染本轮转述降 untrusted；被注入过的内容永不重提取（防召回反馈环）。→ §来源纪律。
2. **零模型调用召回靠写入侧元数据**（OpenClaw）：importance 写入时 1-10 打分 + trigger 短语（≥0.72 才注入、每轮≤3 条）+ 30 天半衰期指数衰减；注入仅限 curated 层。→ §写入侧元数据召回。
3. **Agent 经验+人反馈可固化重训**（CrewAI train → .pkl）：定期重训保持新鲜；绑定配置，改结构失效。→ §经验固化。
4. **Agent 推理策略按模型能力选**（Dify）：强函数调用→FunctionCalling（快）；弱/要透明轨迹→ReAct（思维→行动→观察）。→ §策略选型。

## 判重/发现
- n8n advanced-ai 索引：**确认此前 8 次死链中 3-4 个是路径猜错**——索引明确存在 "What is memory?" / "Light evaluations" / "Human-in-the-loop for tool calls" 等页面，真实 slug 待提取，**死链计数需修正**；本波先记发现，下波验证正确路径。

## 信源健康度
LangChain **10（删）** / LlamaIndex **10（删）** / AutoGen **10（删）** / n8n（待修正）/ OpenClaw **4** / CrewAI **5** / Dify（死链 3）/ smolagents **1** / LangFlow **1** / Pipedream 无收获 1 / WaytoAGI 无收获 1。

## 版本变更
- wb-execute-discipline **2.40.0 → 2.41.0**（+4 条，本轮）。
