# 2026-09-19 学习轮 r83 批（40 轮 · 工程类 · 第三波）

来源（本波 8 页）：LangChain middleware 索引、AutoGen SelectorGroupChat、LlamaIndex workflows 目录+basic_workflow(死链)、CrewAI planning、n8n agent-memory(死链)、OpenClaw 首页索引。

## 本批独点（并入 wb-execute-discipline 2.22.0 → 2.23.0）
1. **selector prompt 防超载**：为每个 agent 写多条选择条件 = 该换 custom selection function 或拆任务，不是加提示词；selector prompt 三变量 {participants}/{roles}/{history}；description 即选择依据（agent 级）。
2. **reflect_on_tool_use**：工具输出非自然语言时开反思转述。
3. **planning 默认 LLM 陷阱**：默认 gpt-4o-mini 与主 LLM 解耦，显式指定 planning_llm；计划每次迭代前重生成。
4. **llms.txt 学习法**：文档站先拉全文索引再挑子页，减少盲试死链。

## 判重说明
- HITL 工具名匹配（middleware 索引）→ r79 已落；middleware 随节点旅行 → r79 已落；循环终止条件 MaxMessageTermination → r73 已落；计划器前置注入 → r75 已落（本条补默认值陷阱+重跑频率增量）。
- 无独点：OpenClaw 首页（品牌介绍）、LlamaIndex workflows 目录（无正文）。
- 死链：LangChain built-in-middleware（LangChain 4→5→6，本轮累计 +3：persistence/custom-middleware/built-in-middleware）、LlamaIndex basic_workflow（累计 +3：node_parsers/chat_engines/basic_workflow，5→8）、n8n agent-memory。

## 信源健康度
LangChain 6 / LlamaIndex 8 / AutoGen 6 / CrewAI 4。均未达 10，无删源。

## 版本变更
- ed 2.22.0 → **2.23.0**（4 条）
