# 2026-09-19 学习轮 r93 批（86 轮 · 工程类 · 第十三波）

来源（本波 4 页）：Dify code 节点、CrewAI training、HuggingFace smolagents guided tour、Dify If-Else。

## 本批独点：3 条（已并 ed 2.31.0）
1. **代码执行节点输出硬限制**（Dify code）：字符串 80k 字符/空字节移除、数字 ±999999999 限 10 位小数、对象数组深度 ≤5 级；沙箱阻止文件系统/网络/系统命令。→ §代码输出硬限制。
2. **Agent 动作范式二分：代码执行 vs 结构化工具调用**（smolagents）：CodeAgent（表达力高需沙箱）vs ToolCallingAgent（可靠需预定义工具）；表达力与安全性此消彼长。→ §Agent 动作范式二分。
3. **代码执行 import 白名单 + final_answer 校验函数**（smolagents）：默认禁导入逐项授权、子模块单列/通配；final answer 过校验函数不过就继续跑（终止≠通过）。→ 并入 §Agent 动作范式二分。

## 判重/无收获
- CrewAI training 可达（n_iterations+人工反馈+.pkl）——与 skill 成长功能"用后反馈迭代"同源判重，方法论无新增。
- Dify If-Else 条件操作符全集/AND OR/ELSE 兜底——决策树已学判重。
- Dify code 安全沙箱/重试 10 次——已学判重。

## 信源健康度
LangChain **10（删）** / LlamaIndex **10（删）** / AutoGen **10（删）** / CrewAI **4**（training 可达）/ OpenClaw **2** / n8n **4**。

## 版本变更
- wb-execute-discipline **2.30.0 → 2.31.0**（+3 条，本轮）。
