# 2026-09-19 · 技能合并通报（浏览器域 + 蒸馏域）——致豆包（也给我自己留痕）

> 本轮不是学习轮，是**结构收敛轮**。用户指令：「该合并合并哈」。合并改动了技能目录结构，你（豆包线）也是这个仓库的写者，**必须知道**，否则下一轮可能重建我刚删掉的目录、或把内容加到错误的技能上。
> 落地 commit：`beb112d`（远端 `main`）。

## 1. 已合并（唯一一处文件级合并）

| 动作 | 前 | 后 |
|---|---|---|
| 合并 | `engineering/wb-browser-reuse` v1.0.0 + `system/browser-automation` v1.2.0 | `system/browser-automation` **v2.0.0**，旧目录**已删除** |

**为什么**：两者是同一功能位（浏览器自动化）。`browser-automation` 自 v1.2.0 起就自称「统一入口」，而 `wb-browser-reuse` 覆盖的正是它缺的第五条路径「复用你已登录的真实浏览器」；两者共享触发词空间（「浏览器自动化 / 抓网页」），并存 = 路由二义。

**合并方式 → 你需要注意的点**：
- `wb-browser-reuse` 的五条方法论（借不抢标签页借用/归还、确认开关锚定扩展设置不可绕过、类型化人助拒绝即停、本地优先数据边界、观测-动作纪律）**原样保留**在 `browser-automation` 的**第五节**，分 5.1–5.5，一小段没删。
- 因此：**不要再新建任何 `browser-reuse` / 浏览器桥接类技能**。浏览器域的落点只有一个 —— `system/browser-automation/SKILL.md`。有新方法论 → 追加到它的第五节，并升 version。
- bsk CLI 自身的命令参考仍在 `browser-skill/`（由 `bsk` 自行安装维护，**不在本仓库版本控制内**，你读不到属正常）。

## 2. 未合并：只划边界（原因是对方是市场技能 / 第三方技能，改了会被上游覆盖）

| 域 | 两个技能 | 处置 |
|---|---|---|
| 蒸馏 | `writing/cangjie-skill` 2.5.0（**市场技能**，`_skillhub_meta.json` `"source":"marketplace"`）⊕ `engineering/distill-cat`（自建） | **不合并文件**。只把判据写进我们自己的 `distill-cat` → **1.1.0**：书 / 长视频 / 播客 / 课程这类「长内容→技能组」批量重流水线走 `cangjie-skill`；个人经验 / 工作流 / 访谈 / 会议记录 + 任何「给已有 skill 补成长闭环」走 `distill-cat`。 |
| 抓取 | `research/web-scrape` ⊕ `research/agent-reach` | **不合并文件**（两者都是第三方技能，各有上游更新链）。互加分工声明：**`web-scrape` = 抓取引擎**（本地 scrapling/crawl4ai，三档难度 + `--fields`，无需登录）；**`agent-reach` = 平台路由器**（15 平台 + 登录态后端 + 全网调研）。只给一个普通网址、无平台特征 → 优先 `web-scrape`。 |
| GitHub | `system/github` 1.0.0（**市场技能**）⊕ `system/github-ssh-over-443` 1.2.0（自建） | **不合并**——域根本不同：前者是 `gh` CLI 用法（Issue/PR/CI），后者是网络连通诊断（SSH over 443）与「网络不通 vs 本地 .git 损坏」分流。名字像不等于职责像。 |

**判据（建议你以后沿用）**：①两技能**同一功能位**且触发词互相覆盖 → 才叫该合并；名字相近但域不同 → 不合并。②只要有一方是 `_skillhub_meta.json` 标记的市场技能（或带上游更新链的第三方技能），**不做文件级合并**，改为「边界声明 + 把内容写进自建那份」。

## 3. 目录 / 计数变化

- 仓库可见 `SKILL.md`：**34 → 33**（删 `engineering/wb-browser-reuse`，未新增）。
- 本机 live：33 → 34 里多出的 1 个仍是 `browser-skill/`（bsk 自管，不入库）。
- `README.md` 分类表已补 `distill-cat`；`memory/co-learn/00_先读我_致豆包.md` 的技能表已按各 `SKILL.md` **实测重算**为 33 条 + 新增变更记录。

## 4. 顺带记录的环境坑（重要，你也会踩）

- **`git rm -r <目录>` 在本仓工作树上疑似连删父目录其余文件**：2026-09-19 实测一次 —— 执行 `git rm -r engineering/wb-browser-reuse` 后，`engineering/` 下**其余 9 个技能文件全部从工作树消失**（`git status` 显示 9 条 ` D`），但 index/HEAD 完好。`git restore -- engineering/` 一条命令全部复原，lost 0。**原因未定论，不断言**。建议：删技能目录后**立刻 `ls` 父目录 + `git status --short` 复核**，异常就 `git restore -- <父目录>/`。
