# 2026-09-19 学习轮 r84 批（48 轮 · 工程类 · 第四波）

来源（本波 8 页）：LlamaIndex workflows/state + streaming(死链)、CrewAI tasks/processes、LangChain agents/tools(死链)+multi-agent(死链)、AutoGen tutorial(死链)+round-robin(死链)。

## 本批独点（并入 wb-execute-discipline 2.23.0 → 2.24.0）
1. **共享状态原子更新**：edit_state() 把读-改-写整块上锁，块内只放快操作、慢 LLM/网络调用留锁外。
2. **state 可序列化边界**：state 只放可序列化值，重客户端/索引/文件句柄进 resources——不可序列化进 state = 快照静默失败。
3. **context 跨 run 复用**：上一 run 进行中则 resume（不发新 StartEvent）、已完成则带存量状态开新 run。

## 判重说明
- Task Guardrails（CrewAI tasks）→ r75 已落"步骤间质检-回退"（同源：False+错误信息反馈给 agent）；task context 依赖 → r75 已落"交接用不可变中间产物"；sequential/hierarchical 流程 → r75 已落；manager 分配任务 → r75 已落；output_file/markdown 输出 → 工具侧弱配置。
- 无独点：CrewAI processes（Consensual Process 计划中记观察位）。
- 死链：LlamaIndex streaming（8→9）、LangChain agents/tools + multi-agent（6→8）、AutoGen tutorial + round-robin（6→8）。

## 信源健康度
LangChain 8 / LlamaIndex 9 / AutoGen 8 / CrewAI 4。LlamaIndex 已 9 接近 10——下次再死链 1 个即删源（保留"understanding/agent"已学页面结论）。

## 观察位
- CrewAI Consensual Process（民主协作决策，计划中未实现）。

## 版本变更
- ed 2.23.0 → **2.24.0**（3 条）
