# 2026-09-19 学习轮 r105-B（第 2 轮 · 16 轮批）

来源（本轮 4 页）：CrewAI training / knowledge / n8n advanced-ai 索引 / OpenClaw automation/automations(死链)。

## 本批独点：2 条（并 ed 2.44.0）
1. **知识预载 vs 按需检索 + 作用域分级**（CrewAI knowledge）：高频固定知识预载进 agent 免 retrieval persona；crew 级共享 / agent 级独立 collection。→ §知识预载与作用域分级。
2. **embedding 与 LLM provider 解耦**（CrewAI）：用 Claude 默认仍是 OpenAI embedding，需显式配 embedder；检索参数有显式默认（limit 3 / threshold 0.35）。→ §embedding 独立选型。

## 判重/无收获
- training：CLI 训练+人反馈，工具站操作内容无独点。
- n8n advanced-ai 索引：**确认死链是路径猜错**——索引里确实存在 What is an agent / Light evaluations / HITL 等页，正确路径待验证（下轮用无尾斜杠/正确 slug 验证）。
- OpenClaw /automation/automations 死链（OpenClaw **6**）。

## 信源健康度
OpenClaw **6** / CrewAI **6** / n8n **8（⚠️ 部分为路径猜错，验证后可能回降）** / Dify 死链 3 / smolagents 1 / LangFlow 1 / Pipedream 无收获 1 / WaytoAGI 无收获 1。

## 版本变更
- wb-execute-discipline **2.43.0 → 2.44.0**（+2 条，本轮）。
