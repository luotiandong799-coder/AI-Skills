# 2026-09-20 学习轮 r108-B（第 2 轮 · 16 轮批）

来源（本轮 2 页尝试 1 页实拉）：n8n 官方 docs Code node（agent 节点路径死链；Code node 有效路径实拉）。

## 独点：0 条（逐站实拉证据与判非重复理由如下）

**逐站实拉结果：**
1. 代码沙箱安全默认（原生 Python 默认拒绝不安全内建；Cloud 禁导入任意库；Code node 内不可访问文件系统/HTTP，用专用节点）→ 与已学 §Agent 工具面安全/per-tool 最小权限（scope+速率+出口白名单）60%+ 重叠判重（同一"默认收缩权限、能力显式授予"原则的平台实现）。
2. Run Once for All/Each Item 批粒度 → 与 r108-A 迭代顺序/并行选型同域判重。
3. AI 生成代码会覆盖已有代码（建议作起点再编辑）→ 与已学 §AI 代码=junior 需评审 同源判重。
4. Item linking（输入输出数量不匹配时处理）→ 平台技术细节。
5. Pyodide→Native Python 迁移（仅 _items/_item）→ 平台实现差异。

## 版本
ed 保持 2.67.0。
