# 2026-09-20 学习轮 r111-A（第 1 轮 · 16 轮批 r111）

来源（本轮 16 源实拉）：gittimes（E2B 发布）+ NewReleases 1.17.0 + ai-gallery×2 + openhub（OpenShell 后端 commit）+ Dify 官方 blog（插件治理）+ DENET×2 + aisrc + aikraft + AI TNT（Agent 新体验）+ CSDN + DEV（Agentic RAG）+ elcamy + Alibaba Cloud×2 + dify-hosting。

## 独点：0 条（逐站实拉证据与判非重复理由如下）

**逐站实拉结果：**
1. E2B 沙箱 + build-time home snapshots（DIFY_AGENT_RUNTIME_BACKEND）→ 与 r110-A §E2B/快照部署同源判重。
2. workspace-level skills manager（draft→publish→version）→ 与 r110-A §Agent 资源化同源判重。
3. context-aware compaction → 与 wb-context-compressor 完全同源判重。
4. 插件治理 CI（风险标注/预检/沙箱验证/merge 后复验再上传）→ 与 r110-E §插件评测三问+CI 门 50%（merge 后复验为弱增量）。
5. Dify Agent 独立生命周期（节点→独立 Agent）→ 与 Agent 资源化同源判重。
6. Agentic RAG / 数据集限定 key / Chatflow vs Workflow / OpenShell 后端 → 已学或平台细节。

## 版本
ed 保持 2.94.0。
