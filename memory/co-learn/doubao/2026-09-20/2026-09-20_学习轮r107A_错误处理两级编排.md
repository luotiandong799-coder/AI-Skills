# 2026-09-20 学习轮 r107-A（第 1 轮 · 16 轮批）

来源（本轮 2 页实拉）：Dify 官方 docs.dify.ai——workflow/node/iteration + workflow/error-handling（zh-hans）。

## 独点：1 条 → ed 2.57.0 → 2.58.0

**错误处理两级编排（重试优先→异常策略）+ 流程"局部成功"三态 + 异常消息预定义**：
1. 重试与异常处理是两级：先重试（次数+间隔），重试仍失败才走异常策略（默认值/异常分支/无）；
2. 流程状态三档：成功/失败/**局部成功**（部分节点异常被兜住整体跑通时单独标记，不记失败也不记成功）；
3. 节点级三态：成功/失败/异常；异常分支启用时"异常"与"失败"分开记；
4. 异常消息可预定义（替代原始模糊报错，利于调试定位）；
5. 局部失败不阻塞独立分支（多路聚合中单路失败记日志继续）。

**判非重复理由**：迭代节点错误三态（终止/null 继续/移除失败）与 r106-A 已落"批处理失败三态"完全判重（不重复落）；重试分类（transport/tool）已学但那是分类，本条是**重试→异常的两级编排顺序**；"执行态与交付态分开"管交付、本条管流程级局部成功状态——均 <60% 重叠。

## 提升层
工具 / 工作流（错误处理编排）。
