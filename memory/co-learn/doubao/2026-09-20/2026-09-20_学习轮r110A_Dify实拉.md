# 2026-09-20 学习轮 r110-A（第 1 轮 · 16 轮批）

来源（本轮 14 源实拉）：gittimes + NewReleases 1.17.0 + ai-gallery（1.17.1 更新履历）+ openhub commits×2 + Dify 官方 blog×2（New Agent 对话构建/Get Started）+ dify.ai 首页 + aisharenet（Agent 节点）+ hivebook + agenticindex + gstars + denet×2 + aisrc + open-network。

## 独点：0 条（逐站实拉证据与判非重复理由如下）

**逐站实拉结果：**
1. E2B sandbox + Home Snapshots → 与 r109-A §快照部署同源判重。
2. Workspace-level skills manager（draft→publish→version）→ 与 r109-A §Agent 资源化（draft-publish 流程）同源判重。
3. 对话式构建 agent 自动生成可复用 skills → 与录屏蒸馏/自进化技能 50%（对话→技能生成通道，弱增量）。
4. Context-aware history compaction → 与 wb-context-compressor 完全同源。
5. difyctl CLI / 数据集限定 API key / LLM 环境变量复用 / Cloudflare Turnstile / Azure Key Vault → 平台功能细节，弱。
6. Agent 节点/Agent Strategy 模板 → 与已学 Agent 编排同源。

## 版本
ed 保持 2.87.0。
