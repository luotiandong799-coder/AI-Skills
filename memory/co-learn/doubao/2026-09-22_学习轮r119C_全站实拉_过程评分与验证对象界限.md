# 2026-09-22 学习轮 r119-C（Agent 编排可靠性 + 轨迹评测前沿角度 · 10 次实拉 · 落地 4 独点）

批前基准：WB r129 + 库内 D1-D51（r119-A/B 已含）；本角度=编排可靠性/轨迹评测，与前两轮错开。git 基线含 r119-A/B 已 commit。

## 10 次实拉（逐站留痕）
1. 编排可靠性批：MetaCTO 错误对象规范（class/retryable/retry_after）、n8n 硬计数器+fallback chains、Self-Healing Orchestrators（信号→类→恢复）、LangGraph 三原语（RetryPolicy/TimeoutPolicy/error_handler）、Microsoft 编排模式
2. OpenClaw：Skill Workshop（治理化技能创建：review queue/审计/哈希绑定/回滚）、XML 转义 token 开销、v2026.7.1 平台功能
3. 轨迹评测批：ClawTrack（Task+Process 双评估）、TRACE（分层轨迹效用+最小指导量）、Counsel（评委元评估三分类）、LangChain 三级评估（Run/Trace/Thread）、Anthropic evals
4. ClawTrack 深拉：**Process Grader 四维（goal alignment / efficiency / information utilization / result verification）**，12541 条 rubric，**result verification 是系统性最弱维**，过滤 lucky passes
5. deepseek 生态：API 功能/FIM/Thinking Mode（平台）+ caveman 模板（已有）
6. agentskills.io：**description 触发率评测流程（train/validation 集、每条 3 次、trigger rate>0.5 过）**；MCP 再审计数据（106/192 有源码）
7. TRACE/Claw-Eval 深拉：**Scaffolded Capability Assessment（最小指导量测潜在能力）**；Claw-Eval 三维（Completion/Safety/Robustness，**安全违规黑箱归零**）
8. UBC web-agent-environments：**Verifier/Judge/Acceptance policy 三对象界限表**（各对象能支持什么结论、不能确立什么）
9. n8n Production AI Playbook：sub-workflows 独立测试/复用、Deterministic+AI 混合（已覆盖）、Max Iterations=最长工具链+2（小点）
10. AgenticSkills/Bright Coding：MCP 再审计、Compose Skills not Monolith（与 sa 已有重叠）

## 落地（4 独点）
| 编号 | 落点文件 | 独点 | 来源 | 提升层 |
|---|---|---|---|---|
| D52 | av | 过程评分四维：goal alignment / efficiency / information utilization / result verification——逐轮打分，过滤结果评估看不到的 lucky passes；result verification 是系统性最弱维 | ClawTrack（arXiv 2607.28037） | 可复用 Skill（评测） |
| D53 | av | 最小指导量协议（Scaffolded Capability Assessment：测"最少给多少指导能成功"量化潜在能力）+ 安全违规黑箱归零（任一违规整题零分） | TRACE（arXiv 2602.21230）+ Claw-Eval（arXiv 2604.06132） | 可复用 Skill（评测） |
| D54 | av | 验证对象三分类界限：Verifier（确定后置条件）/ Judge（开放文本）/ Acceptance policy（终态+成本+风险+人工升级）——各自"能支持什么结论、不能确立什么" | UBC web-agent-environments | 可复用 Skill（验证选型） |
| D55 | sa | description 触发率评测协议：train/validation 集 + 每条查询跑 3 次 + should-trigger 率>0.5 过、should-not-trigger<0.5——量化"该响没响/不该响却响" | agentskills.io/skill.md | 可复用 Skill（description 优化） |

## 判不落（逐条理由）
- 错误对象规范（class/retryable/retry_after）：与 ED"LLM 友好错误消息六要素"（失败类别/可恢复/参数/策略/重试意义/下一步）重叠 >60%。
- n8n fallback chains / Self-Healing 编排器：与 ED"故障注入恢复路径五选 + 三层恢复 + fallback 链"重叠 >60%。
- LangGraph error_handler 原语：与 ED"重试优先于兜底"同源，编排器平台细节。
- Counsel 评委元评估三分类：与 av D33（评委可靠性协议：翻转率/双评委/噪声预算）重叠 >50%，小增量并入 D33 语境。
- LangChain Run/Trace/Thread 三级评估：与 av D43（错误传播追踪）+ D52（轨迹级评分）重叠，概念已覆盖。
- n8n progressive disclosure in n8n：progressive disclosure 已在非独点清单。
- Skill Workshop 哈希绑定/回滚：与 WB r129 ctx"批准绑清单指纹"重叠 >60%。
- XML 转义 token 开销/Compose Skills/Max Iterations：小点，与已有描述写法/迭代上限重叠。
- 生态数据（MCP 再审计数字）：非独点。

## 功能套件检查
av 落 D52/D53/D54（过程四维/最小指导量/验证对象界限）；sa 落 D55（触发率评测）；ctx/ED/pt/mts 无新材料；套件维持 3 件套判据不凑数。

## 垃圾清理
本轮零临时文件；保留：本留痕 + 被并入的 av/sa。
