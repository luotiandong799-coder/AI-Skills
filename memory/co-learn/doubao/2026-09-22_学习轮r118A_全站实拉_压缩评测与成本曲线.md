# 2026-09-22 学习轮 r118-A（上下文/记忆工程角度 · 10 次实拉 · 落地 4 独点 + 2 并入）

批前前置：WB r128 已盘点（留痕 + 远端 e8431df 共学双写 + 技能最新小节：ctx 3.32.0 / sa 2.44.0 / av 1.67.0 / dl 1.23.0），本地=远端=85a1387，判重基准=D1-D35 + WB r123-r128 全部要点。

## 10 次实拉（逐站留痕）
1. arXiv 压缩/记忆批：ARC 2607.25066（ID 可寻址回放）、2608.16370（压缩交互成本）、ACC 2601.11653、Context Codec 2605.17304（已在 D26）、ACM Maximem Synap（智源）、2604.19572 TACO、Observational Memory（Mastra）、near-lossless RL 压缩
2. Anthropic 官方：compaction（已在 r117 覆盖）、context-editing（适用/不适用清单——**新点**）
3. RAG/检索批：2602.22216（recursive>semantic 实证——**修正证据**）、2512.05411（metadata RAG）、TeaRAG、MarsDevs 生产指标阈值（**新点**）、memorypapers ARM、FutureAGI
4. OpenClaw：memory overview（四层架构，已有）、compaction 配对（已有）、memory-architecture Prospective 层
5. GitHub 生态：Copilot Code Review Skills GA（团队级，不投入）、Agent Skills Context Engineering（Muratcan）、GitHub Agentic Workflows、dev.to 30 repos 趋势（headroom 66K）
6. 国内：腾讯 ContextPilot-14B（模型层自管上下文）、DeepSeek V4.1 Flash（平台动态）、TencentDB Agent Memory（符号压缩+**召回超时 5s**——新点）、腾讯开源记忆系统
7. headroom 专拉：压缩中间件 60-95%，原始可检索——与 ctx 检索式召回/context-mode 重叠 >60%，判不落
8. agentskills.io：Hermes 131 技能、EvoSkill 2603.02766 自动技能发现、Skill Workshop
9. skills.sh/SkillsMP：Anthropic skill-creator 官方（**SKILL.md ≤500 行 / 参考文件 >300 行 TOC**——新点，sa 侧）、smartscope P0/P1（重叠不落）、The Production Line（**forked context 子代理隔离**）
10. 工作流平台：Langflow 1.10 Memory bases、n8n 报告/2.37（平台功能，不落）

## 落地（4 新独点 + 2 并入）
| 编号 | 落点文件 | 独点 | 来源 | 提升层 |
|---|---|---|---|---|
| D36 | ctx | 压缩评测必须双指标：completion + reacquisition cost（任务完成率不变≠无损，压缩会放大 agent 重新获取已丢状态的交互成本） | arXiv 2608.16370 | 工作流（评测口径） |
| D37 | ctx | 压缩策略三类成本曲线：朴素累积=二次方成本 / 粗略摘要=线性但精度断崖 / 验证压缩=线性+高保真——按任务选策略形态 | 智源 ACM Maximem Synap | 工作流（策略选择） |
| D38 | ctx | context editing 适用边界：产物外化任务适合编辑；需要精确回忆早期细节 / 大量 server-side tools / 多变量精确状态的任务不适合 | Anthropic 官方 context-editing | 工作流（适用判定） |
| D39 | av | RAG 生产指标阈值表：Faithfulness≥0.9 / Answer relevancy≥0.85 / Context precision≥0.8 / Context recall≥0.85——生产基线而非相对提升 | MarsDevs 2026 生产指南 | 可复用 Skill（评测基线） |

**并入（不新编号）**：
- 分块方法修正证据：2602.22216 病理实验室实证 recursive 256/512 在 relevance/faithfulness/context recall 全面超 semantic——并入既有"分块粒度匹配"条目加限定"recursive vs semantic 按域验证，勿默认 semantic 最优"（新证据修改结论，说明依据）。
- 记忆召回超时上限：TencentDB 5000ms 超时保证会话不被慢召回阻塞——并入 ctx 记忆区"召回必须有超时上限，慢召回不阻塞主会话"。

## 判不落（逐条理由）
- ARC ID 可寻址回放：与 ctx"检索式召回（事件入库建索引）"重叠 >60%（都是"压缩后按指针/ID 取回"），不装。
- ACC 未验证内容不入持久记忆：ctx"记忆提取四策略（提取→校验）"已覆盖，重叠 >60%。
- TACO/观察式压缩：辅助 LLM 成本 <2% 是数据点，无独立方法论，不装。
- headroom：压缩+原始可检索=ctx context-mode 已有，重叠 >60%；四部署模式是工程形态信息不构成方法论，不装。
- ContextPilot-14B：模型层趋势，调用侧无直接可复用点（其决策点清单=我们 ctx 已有 checklist），不装。
- Langflow Memory bases / n8n 2.37 / DeepSeek V4：平台功能清单，无技能系统可复用方法论，不装。
- EvoSkill/Skill Workshop：技能自动发现——留 C 轮（技能创作角度）再评估，本轮不装。
- SKILL.md 500 行阈值：sa 侧点，留 C 轮技能创作实拉后合并评估（避免单轮过载）。
- forked context 子代理隔离：ED 侧点，留 B 轮（编排角度）实拉后合并评估。

## 功能套件检查
ctx（3.32.0 侧 + D8-D38 + r128 全部）无降级点；mts/ponytail 无新材料可优化；sa/av 本轮轻量（av 落 D39）；套件维持 3 件套判据（功能位覆盖完整），不凑数。

## 垃圾清理
本轮搜索零临时文件（general_search 无落盘）；无 __pycache__/log/tmp 产生；保留：本留痕 + 被并入的 ctx/av。
