# 2026-09-23 学习轮 r151-A（提示工程长输入 / 技能文件治理 / 独立评审 / 执行安全）

## 实拉证据（十站全量，2026-09-23 19:45-19:47 实拉）
1. Anthropic prompting（platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices 2026-09-23 + docs.anthropic.com/en/docs/let-claude-think）：黄金法则"show your prompt to a colleague"；**Long context tips：超 20k tokens 时"Put long data near the top of the prompt, above the query, instructions, and examples. This improves performance across all models. Queries at the end can improve response quality"**（长数据置顶、query 置底）。
2. OpenAI prompting（developers-openai.com/docs/prompt-guidance + help.openai.com 6654000）：指令放 prompt 开头并用 ###/""" 分隔指令与上下文；system 消息放总体角色、user 消息放任务细节与示例；结构化输出用 JSON mode/schema——均已覆盖（§分层预算/结构化输出），判重复留痕。
3. Claude Code best practices（code.claude.com/docs/en/best-practices 2026-09-17）：四阶段 Explore（plan mode）→Plan→Implement→Verify；**Scaling/Advanced Patterns PDF：Keep files < 200 lines——longer files consume more context and can negatively affect instruction adherence；.claude/rules/ 按文件路径组织多文件规则并可按路径作用域**。
4. Claude 官方博客 subagents（claude.com/blog/subagents-in-claude-code 2026-04-07）：**Independent review——After implementing something complex, verification from a subagent that hasn't been influenced by the implementation journey catches what familiarity obscures；评审子代理不看实现过程（不知道考虑了哪些 tradeoff/拒绝了哪些方案/作了哪些假设）**。
5. Karpathy 4 rules（Forrest Chang CLAUDE.md，错误率 41%→11%）：Think before coding / Simplicity first / No speculative abstractions（内容截断，未取全不作落地）。
6. OpenClaw（docs.openclaw.ai/reference/AGENTS.default + concepts/memory 2026-09-12/15）：USER.md（带日期的活跃/被取代偏好指令）+ MEMORY.md（持久非画像事实）+ 小写 memory.md 仅 legacy 修复输入、**禁止刻意同留大小写两个根文件**；memory_search/memory_get/intent 三工具。连带：chenguangliang OpenClaw memory best-practices（2026-02-23）：**混合检索 Vector 70%+BM25 30%、时间衰减 halfLifeDays=30、MMR 去重 lambda=0.7、压缩前自动 memory flush**。
7. agentskills.io（agentskills.io/skill.md + skill-creation/best-practices 2026-08-11/09-11）：name 1-64 小写+数字+连字符须匹配目录名、description 1-1024 imperative "Use when"、**"Skill instructions focus on what the agent wouldn't know without the skill"（只写模型不知道的）**——与 wb-skill-authoring 现有覆盖判重复；Start from real expertise（不用 LLM 通用知识造技能）。
8. skills.sh / localskills / GitHub CLI（skills.sh/docs/faq + docs.localskills.sh + github.blog 2026-04-16）：`npx skills add owner/skill` / find / check / update；localskills install --target cursor claude --project --symlink（平台/作用域/安装方式选择）；**`gh skill publish` 发布时按 agentskills.io spec 校验 + 检查 tag protection / secret scanning / code scanning 远程安全设置；immutable releases 防供应链篡改**。
9. Dify（dify.ai/blog + dev.to Dify 5 hidden uses 2026-07-14）：Agentic RAG 已覆盖（r150-C 判重）；工作流可导出 YAML 进 Git 版本控制 + 内置 tracing API 逐历史执行回放；上线前 5 层验证（单元级每 agent prompt/工具链/输出 Schema 断言 + 集成级路由/状态传递/收敛 + 可观测性埋点完整性）。
10. n8n（blog.n8n.io/llm-tool-calling-error-handling 2026-07-03 + n8noneclick 转述官方两层架构 2026-07-04）：**Orchestration owns infrastructure-level retries silently. The LLM owns reasoning-based recovery for application-level problems that require context（编排层静默重试基础设施错误，LLM 层做需上下文的推理式恢复）**；单一视觉执行轨迹显示哪个工具调用失败、为何失败、LLM 传了什么参数。maxIterations 硬上限（默认 10、简单 agent 设 5）——与 mts §工具循环 O(n²) 同面，不重复落。
11. HF smolagents（hf.co/blog/smolagents + noqta.tn 2026-05-26 + PyPI 1.26.0）：**默认解释器拦截所有 import，仅白名单 math/statistics/datetime，工具依赖的必须显式声明（additional_authorized_imports）——这是生成代码安全的机制**；planning_interval 周期回顾更新策略（与 r150-C 配置表点同源判重复）；memory 截断 max_steps。

## 独点清单 + 判非重复理由（4 点落地 mts 1.42.0→1.43.0）
1. **长输入编排：文档置顶、query 置底**（来源：Anthropic 官方 prompting best-practices Long context tips，2026-09-23 实拉）：超 20k tokens 时把长数据/文档放在 prompt 顶部、query 放底部（instructions/examples 居中）——官方称全模型性能提升、末尾 query 改善回答质量。判非重复：已有 §上下文分层（system→retrieval→tools→user）管"组装顺序面"、检索预算管"数量面"；本条给"**大输入时的具体位置规则**"（长数据置顶+query 置底）——位置面细节无覆盖，增量 ≥40%。提升层=提示工程/工作流。
2. **规则文件 200 行上限 + 按路径作用域拆分**（来源：Claude Code Advanced Patterns 官方 PDF + code.claude.com best-practices，2026-09-23 实拉）：文件 >200 行会耗更多 context 且降低指令遵从；用 .claude/rules/ 目录按文件路径组织并作用域到特定路径。判非重复：wb-doc-writing §膨胀是症状 管"术语表删到 lean 再拆"（内容面）；本条给"**指令/规则文件的规模上限（200 行）与按路径作用域的组织方式**"（治理面可执行规格），增量 ≥40%。提升层=可复用 Skill（指令文件治理）。
3. **实现后独立评审子代理：不带实现上下文**（来源：Claude 官方博客 subagents-in-claude-code，2026-09-23 实拉）：复杂实现完成后，用**没参与实现旅程**的子代理评审——它不知道考虑过哪些 tradeoff、拒绝了哪些方案、作了哪些假设，旁观视角抓熟悉性盲区。判非重复：ED §步骤间质检-回退 管"每步质检函数"；WB sa §评委团（r146-C）管"评测用 LLM judge"；本条管"**评审者必须与实现者信息隔离（无实现上下文）**"——信息隔离面无覆盖，增量 ≥40%。提升层=工作流（实现后独立评审）。
4. **代码执行 import 白名单：默认最小集 + 显式声明**（来源：HF smolagents 官方 PyPI/blog + noqta 转述，2026-09-23 实拉）：解释器默认拦截所有 import，仅白名单 math/statistics/datetime；工具依赖的包必须显式声明（additional_authorized_imports）。判非重复：WB mcp-builder §沙箱三件套（r150）管"网络/凭证/资源隔离"；本条管"**生成代码的 import 白名单机制**"（依赖面最小集+显式声明）——对象与机制不同，增量 ≥40%。提升层=工具（代码执行安全）。

## 功能套件检查
wb-ponytail / wb-max-token-saver / wb-context-compressor：本轮 mts 1.42.0→1.43.0（+4 节）；其余两个文件在 WB 在途未碰。

## 垃圾清理
本轮仅 general_search 无临时文件；mts 无 .bak。无删除项。
