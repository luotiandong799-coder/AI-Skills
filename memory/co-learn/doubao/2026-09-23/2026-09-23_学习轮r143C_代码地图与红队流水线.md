# r143-C 全站实拉 · repo map + 浏览器失败重试 + 安全红队流水线落地

> 日期：2026-09-23｜批次：r143-C（定时触发）｜模式：AI技能库学习批
> 判重基准：WB r142 九点 + WB r138 九点 + 豆包 r135/r139/r140/r141/r143-A/B 已落

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | 浏览自动化（Kanopy/cissychen/techiehub/browserize） | **retry with variation（失败不重试同点击，重新截图重新 prompt）；act_and_verify（等条件不等时间，reality 回传重规划，子目标 checkpoint）；DOM vs vision（DOM 可靠 12-17pp）；选择器优先级 data-testid/id/name→text→CSS tolerance；hybrid 脚本 80%+AI 20%** | **C1：失败变化重试+act_and_verify（落 ED）** |
| 2 | 代码库理解（Agent Patterns/Aider/13labs/Kilo） | **repo map（tree-sitter AST+PageRank 符号排序+binary-search 塞 token 预算）；launch point 决定可见性（窄目录启动=上下文自限）；显式 file set；AST+code graph 与 embeddings 互补** | **C2：repo map+launch point（落 mts）** |
| 3 | 语音 Agent（anhtu/burki/assemblyai） | S2S 直接音频 sub-500ms vs 级联 STT→LLM→TTS；总往返 <1.5s；endpointing 150-300ms 基于转录；streaming 强制；first-token 关键指标 | 工具站操作知识（语音助手构建），个人非核心，不落 |
| 4 | 文档智能（AI Learning Guides/harshith/zalt） | 表格失败模式（合并单元格膨胀/多页断行/旋转文本）；分层准确率；多信号置信度（OCR+LLM 自报+交叉验证）；VLM 优于传统 OCR | 与 r139 视觉提取三纪律重叠 ~50%，不落 |
| 5 | 安全红队（PI-Hunter/pentest.qa/rafter/MUZZLE） | **测试面枚举（RAG 文档/网页/email/DB/工具输出/MCP 描述）；红队流水线（target+独立红队模型+payload 库；static suite+自动演化）；轨迹审计定位脆弱模式；反模式用例（编码指令不遵从）** | **C5：红队流水线（落 ED）** |
| 6 | 意图路由（besthub/xbstack/DoorDash/DO router） | 三路线错误率（keyword 15-40%/embedding 8-15%/LLM）；multi-label 意图（主+次+情绪）；Dual-intent 有序集合+消歧；SLM+ranking 两阶段；per-intent/OOD/drift | 与 r141-A 评估粒度/mts 难度路由判重不落 |
| 7 | 超级浏览器理论（arXiv SUPERBrowser） | vision-first bounding-box 候选区+选择+只记目标所需（感知-认知-行动三元） | 认知层，与 ctx 记忆分层同源，不落 |
| 8 | 浏览器六角色（zharfai） | Observer/Planner/Policy/Executor/Verifier/Evidence recorder | ED 已有动作前五检查/验证纪律覆盖，不落 |
| 9 | 文档 VLM（noqta/parseur） | 整页理解/跨布局泛化/阅读顺序 | 工具选型事实，不落 |
| 10 | 意图分类评估（FutureAGI） | per-intent precision-recall 比 aggregate 可靠；OOD 检测；drift gates | 与 r141-A pass^k/评估粒度判重不落 |

## 二、判重与落地
- **C1 浏览器失败重试（落 · ED）**：ED 已有动作前五检查（动作前等待）+ 单步成功率^步数（拆流程）——"retry with variation（重截图重 prompt）+ act_and_verify（等条件不等时间/reality 回传）"是失败后路径增量 → 落 ED（工作流层），与五检查/拆流程分工注明。
- **C2 repo map+launch point（落 · mts）**：mts 有三级加载/检索排序（内容级）——"符号级结构地图（AST+PageRank+token 预算）+ 启动点上下文自限"是代码库上下文新机制 → 落 mts（工具/工作流层），与三级加载合流注明。
- **C5 安全红队流水线（落 · ED）**：ctx 注入预检管"运行时筛查"、r141-C 输出四不管"输出边界"、r140-C 记忆投毒管"写入门禁"——"开发期主动红队（测试面枚举+payload 库+自动演化+轨迹审计）"是测试方法论增量 → 落 ED（工作流层），与运行时预检分工注明。
- 语音 Agent/文档 VLM：工具站操作知识 → 不落。
- 表格失败模式：与 r139 视觉提取三纪律重叠 → 不落。
- 意图路由评估：与 r141-A 评估粒度判重 → 不落。

## 三、功能套件检查
- 三件套：mts 新增"repo map+launch point"（代码库上下文加载，与三级加载/检索排序成上下文加载三角）；pt 无改动；ctx 在途不碰 → 无需增删。
- 工程技能：ED 新增"浏览器失败重试"+"安全红队流水线"，浏览可靠性链（五检查→retry variation）+ 安全测试链（运行时预检→输出四不→开发期红队）补全。
- 垃圾清理：本轮无临时文件；无 __pycache__ 产生。

## 四、批汇总（r143 三批）
- r143-A `36a4472`：mts 记忆写入时序 + ED PACT 通信压缩 + ED RAG 新鲜度两档/三级监控
- r143-B `7cda128`：ED judge 三类偏见缓解 + ED prompt 版本生命周期 + ED deterministic spine
- r143-C `（本批 commit）`：mts repo map/launch point + ED 浏览器失败重试 + ED 红队流水线
- 共 9 真独点（全带来源与提升层）；批末 push 一次（预期因 WB 已推 r142 被拒，只试一次如实报告）。
