# r148-A 全站实拉 · 结构化输出两态判据

> 日期：2026-09-23｜批次：r148-A（定时触发，批前已盘点 WB 全量 r146 为判重基准）
> 判重基准：WB r145A/B/C+r146A/B/C（2026-09-23 无 r147 新留痕）+ 豆包已落全清单（含 r147-A/B）

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | GitHub Trending 今日 | pacifio/atlas（agent 源码控制）；认知核心技能分类（感知/记忆/推理/规划/行动/验证/学习/治理 159 技能卡）；plannotator/effective-html | atlas 与回滚面重叠；分类法 skill-authoring 面；html 可视化面 doubao-visualization 覆盖 → 不落 |
| 2 | Anthropic Skills | Record a Skill（录屏转技能）；**write-for-your-agent（指令用祈使句"Run npm test"、显式默认"未指定分支用 main"、不做选项菜单给决定）**；14 设计模式；context-is-public-good（启动只预载 metadata） | Record a Skill=r145-C 动作固化覆盖；write-for-agent 与 ED 现文（CLI 别做成菜单/USER.md 祈使指令）重叠>60% 判不落；context 预算 r147-A 覆盖 → 不落 |
| 3 | docs.openclaw.ai | **Skill Workshop（proposal 待审草稿含 target binding/hashes/rollback→applied 才 live；后台自动学习直接编辑不走提案）** | 生成技能审批治理与 WB release 输入集版本化+审批门控重叠>60% → 不落 |
| 4 | WaytoAGI | Coze 输出格式段（变量名/内容/格式须前后一致）；华为 Coactive 双态（Interactive 目标不清任务/Proactive 可重复任务动态迁移） | 输出格式段=输出四不覆盖；双态太泛无判据 → 不落 |
| 5 | n8n | Agent-as-Tool 委派；Thinking Space 草稿板；结构化输出 parser→clean fields | 委派=ED 编排覆盖；草稿板=上下文隔离覆盖；parser 已覆盖 → 不落 |
| 6 | Dify | Agent 节点/Strategy 解耦；Agentic RAG；Human Input Node；Trigger 事件驱动；自动生成可复用 skills 保留上下文 | 解耦=r146-B 策略守卫面；RAG/HITL/Trigger/自动生成全部既有覆盖 → 不落 |
| 7 | LangFlow | CodeAct Agent（smolagents 迭代写码沙箱执行）；aMap/aReduce/aGenerate 表格处理 token 三型 | CodeAct=r147-B 覆盖；表格三模式太小 → 不落 |
| 8 | MCP | **progressive least-privilege scope（最小初始 mcp:tools-basic 只读→特权操作首次尝试时增量提升）**；iss 校验/application_type/never token passthrough；签名工具清单；CSAlliance 四级 | 渐进 scope 属 WB ctx per-tool 权限主战场；OAuth 硬化企业级；签名清单与 r147-B canary 面近 → 不落 |
| 9 | Agent 记忆 | HORMA（文件系统式层级摘要链接原始轨迹）；Hermes L0-L2（FTS5 session search）；HMAT（working/episodic/semantic 重要性加权）；recall-as-first-class | 全部属 WB ctx 记忆分层/摘要锚点已覆盖面 → 不落 |
| 10 | 输出校验 | **SOB 结构化输出基准（valid JSON vs correct JSON 差距大：图像 67.2%/音频 23.7%）；**VERDI（claim extraction→per-claim→nothing-to-verify 早停）；GANDR 双 agent；UnVot NLI+CoT 双一致；FAE 证据消融 | **A3：valid≠correct 两态判据（落 ED）**；VERDI=r147-B B2 覆盖；GANDR=r145-B 三角色覆盖；UnVot=r143-B/评委团覆盖；FAE 评测发现中低 → 不落 |

## 二、判重与落地
- **A1 write-for-your-agent（判重不落）**：祈使句指令/不做菜单均与 ED 现文重叠（§CLI 别做成菜单、§USER.md 偏好写成祈使指令），增量只剩"显式默认值"一点，重叠>60%。
- **A2 Skill Workshop（判重不落）**：生成技能提案审批+回滚快照与 WB release 输入集版本化/审批门控同类，个人场景重量级治理不必要。
- **A3 结构化输出两态判据（落 · ED）**：Grep mts/ED 无 valid/correct JSON 同类；"解析通过≠可交付、schema 合法≠值级正确、多源/跨域合法-正确差距放大"是新验收线。与 §输出四不分工（形态禁 vs 合格线）。提升层：工具/工作流。版本 bump：ED 2.98.0→**2.99.0**（mts 无内容变化保持 1.38.0）。

## 三、功能套件检查
- 三件套维持：mts +6 点（r147-A×2/r147-B×2/r148-A 无）；ED +1（r148-A 两态判据）；pt/ctx 无新料。
- 垃圾清理：无临时文件；无 __pycache__。
