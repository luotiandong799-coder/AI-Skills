# r149-B 全站实拉 · 技能生态与平台面复核轮（0 落地）

> 日期：2026-09-23｜批次：r149-B（判重基准同 r149-A）

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 判非重复理由 |
|---|---|---|---|
| 1 | Hugging Face smolagents | 工具合并减少 LLM 调用（return_spot_information 一次调双 API）；agent.memory.get_full_steps()+step callbacks 动态改记忆；load_tool 从 Hub 导入/push_to_hub 分享 | 工具合并=mts 成本面约 50%（调用次数 vs 输出精简）不足 60% 阈值但无独立落地价值；memory 回调=WB ctx 覆盖；Hub 分享=平台面 → 不落 |
| 2 | ModelScope 创空间 | Agent 创建字段（name/avatar/instruction/prompt suggestion）；API 注册为 tool（smart API）；发布流程（构建→创空间→API-KEY） | 产品功能面（2023 旧内容为主），无方法独点 → 不落 |
| 3 | Anthropic skill-creator 深挖 | 基线通过无技能=技能技术已被模型内化→不再必要（退役判据）；Description Optimizer（20 query 正反样例+train/test 60/40 防过拟合选最佳）；Benchmark Mode（全 eval 记录 pass rate/时间/token 作基线，模型更新后对比）；Eval/Improve Mode | 基线退役=r148-C 两实例闭环"基线对照区分模型本来就会的"延伸重叠>60%；描述优化=WB skill-authoring 激活计分板面；Benchmark Mode=WB r146-C 评测续跑面；Eval/Improve=两实例闭环 → 不落 |
| 4 | skills.sh | Vercel 开源包管理器；Top 技能安装量（vercel-react-best-practices 7.87 万）；遥测仅名称/文件名/时间戳（DISABLE_TELEMETRY） | 平台面，已拉多轮无新方法 → 不落 |
| 5 | 腾讯 SkillHub | 企业版三重防线（AI 扫描+静态分析+沙箱验证→管理员审核→审计）；SkillPay 发布流程；meeting-decision-amplifier（Step A/B 不依赖外部 skill，腾讯会议 Skill 仅 Step C 可选增强） | 三重防线=企业级不投入；SkillPay=商业面；外部依赖可选增强=ED 降级路径面边缘（约 40%）→ 不落 |
| 6 | n8n 模板/Complex Patterns | 蓝图先行（deterministic nodes vs LLM calls vs agent reasoning 是架构）；AI Agent Tool 委托（agent 作工具被调用）；HITL 学习（从人工纠错学习） | 蓝图=ED 编排模式覆盖；委托=ED 多 agent 覆盖；HITL=ED HITL 覆盖 → 不落 |
| 7 | Dify 插件 | OpenPlugin v3.1 双向能力契约（注册期静态声明 schema/认证/配额）；Creator Center 模板分佣；Asqav 防篡改 receipts；Plugin Runtime v3 热更新（独立 HTTP+OpenAPI 契约） | 能力契约=ED Agent 接口契约面（约 50-60%）无独立增量；分佣=商业面；receipts=r147-B 主张级审计覆盖；热更新=工具面 → 不落 |
| 8 | MCP Registry/工具发现 | 官方注册表（registry.modelcontextprotocol.io）+社区目录（PulseMCP/Glama/mcp.so/Smithery）分层；MCPfinder（agent-readable search+trust scoring）；MCP v2.1 Server Cards 自动发现；arXiv 对 registry 全量元数据扫描研究 | 全部平台/协议/生态面，非方法 → 不落 |
| 9 | Pipedream | AI scaffolding（描述→自动搭结构→自动生成代码）；AI-powered API transforms；Agent Builder+MCP 3000+ APIs | 全部产品功能面 → 不落 |
| 10 | Anthropic 完整指南（白皮书 PDF 实拉） | 单任务先迭代到成功再提炼再扩测试集；三种测试级别（manual/scripted/programmatic）+三测试面（triggering/execution/quality）；MCP 厨房 vs Skills 食谱；description 硬规范（<1024 字符/无 XML/含 what+when+触发短语）；skill 结构（kebab-case/无 README.md） | 单任务迭代=r148-C 两实例闭环覆盖；测试级别=WB skill-authoring 评测面；厨房食谱=r147-A 选型面；description 规范=WB 描述优化面近（约 40-50%）无独立增量；结构规则=工具格式面 → 不落 |

## 二、落地结论
- 本批 0 个真独点：全部为平台功能面/产品面/既有已落覆盖，无新方法论增量。
- 不做 idle commit 编造：逐站判非重复理由见上表。

## 三、功能套件检查
- mts/ED/pt/ctx 四件套均无需更新。
- 垃圾清理：无临时文件、无 __pycache__ 产生。
