# r143-A 全站实拉 · 记忆写入时序 + PACT 通信压缩 + RAG 运维落地

> 日期：2026-09-23｜批次：r143-A（定时触发）｜模式：AI技能库学习批
> 判重基准：WB r142 九点（av 1.75 评测判据两层/dl 1.35 断点坐标/ctx 3.36 压缩三段 keep_first/doc-writing 1.10/sd 1.66）+ WB r138 九点 + 豆包 r135/r139/r140/r141 已落

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | Agent 记忆（Ascheriit/OpenAI SDK/alivedise） | **先响应后提取（提取加延迟不阻塞响应）；提取用便宜模型（分类任务非推理）；收尾合并 session→global（去重+冲突 recency wins+清空）**；LazyMem 写时不构造查询时构造；confidence decay | **A1：记忆写入时序（落 mts）** |
| 2 | MCP 生态（arXiv 2603.13417/Claude 官方） | **MCP 三缺失原语（identity propagation/adaptive tool budgeting/structured error semantics）**；**group tools around intent 不按端点 1:1 镜像**；remote server 跨端形态；10k server/97M SDK 下载 | 与 sa 工具设计/WB r138 MCP 判重不落（intent 分组与"工具少而精"同源） |
| 3 | 多 Agent 通信（arXiv PACT/A2A/COMPEL） | **PACT：消息只投影接收面 action+state+artifact，不暴露完整生成转录**；A2A 异步结构化；rate limits per pair；payload 当敌意 | **A2：通信压缩只传接收面（落 ED）** |
| 4 | Agent 测试调试（Kanopy/Braintrust/FutureAGI） | 工具选择准确率 vs golden trace；step efficiency（实际/最优）；cost per task；trace→test 回归；三层观测 metrics/traces/logs | 与 r141-A 数据集纪律/r140-B eval 门禁判重不落 |
| 5 | 工具设计（AI-TLDR/explainx/anthropic） | **~10-15 工具同时呈现上限，超出动态路由 5-8**；consolidation principle；flat/typed/minimal 参数；attention funnel（53→14） | 与 mts 三级加载（r140-A 呈现子集）重叠 50-60%，不落 |
| 6 | RAG 生产化（Rag About It/belsoft/superml） | **新鲜度两档（高利害 last_verified 每块+定期重索引；普通指纹变化检测每周比对）**；**三级监控（合成健康检查固定调度+阈值告警）**；retrieval 占 ~58% 质量回归根因；component-level eval | **A4：RAG freshness 两档+三级监控（落 ED）** |
| 7 | Agent 记忆分层（Sabaoon/zalt/agentloop） | 四层记忆（in-context/vector/KV state/procedural）不同写成本读延迟；retrieve before generate | ctx 记忆分层已覆盖，不落 |
| 8 | 记忆框架选型（atlan/Mem0 官方） | retrieve→LLM→store 三步接线；Mem0/Zep/LangGraph Store 对比 | 工具选型事实，与 ctx 记忆已落同源，不落 |
| 9 | Agent 观测（LangChain/AWS AgentCore） | trace 查看器看工具选择/推理偏离；metrics+traces+structured logs 三层；非确定性下同输入不同行为 | 与 ED 诊断流/观测 opt-in 判重不落 |
| 10 | 记忆评估（dataaihub/AWS S3 Vectors） | curate writes/retrieve narrowly/expire stale/measure recall；consolidation 定期摘要合并；confidence decay 排序 | ctx 两级沉淀/软衰减已覆盖，不落 |

## 二、判重与落地
- **A1 记忆写入时序（落 · mts）**：ctx 管"提取什么/何时提取"（策略+校验）、mts r140-B 管"记忆 token 分层"——"先响应后提取+便宜模型+收尾合并 session→global（recency wins）"是时序与成本增量 → 落 mts（工作流层），分工注明。
- **A2 PACT 通信压缩（落 · ED）**：ED 多 Agent 接口契约管"消息结构化"，双模型隔离管"模型层不见 raw"——"agent 间消息只传接收面 action/state/artifact、不暴露完整转录"是通信层增量 → 落 ED（工作流层），与两者分工注明。
- **A4 RAG 新鲜度两档+三级监控（落 · ED）**：r141-A 管"构建期检索排序与评测"——"运行期新鲜度策略+合成健康检查"是运维增量 → 落 ED（工作流层），衔接注明。
- 工具 10-15 上限/路由：与 mts 三级加载（r140-A 呈现子集）重叠 50-60% → 不落。
- MCP intent 分组：与 sa 工具少而精/WB r138 MCP 判重 → 不落。
- 记忆四层/框架选型/观测三层：ctx/ED 已覆盖 → 不落。

## 三、功能套件检查
- 三件套：mts 新增"记忆写入时序"（与 r140-B 记忆 token 分层 + r140-A 三级加载成记忆-加载-写入闭环）；pt 无改动；ctx 在途（WB r142 已推）不碰 → 套件无需增删。
- 工程技能：ED 新增"PACT 通信压缩"+"RAG 运行期运维"，多 Agent 纪律链（协作→契约→通信压缩）补全。
- 垃圾清理：本轮无临时文件（纯搜索+内存追加）；无 __pycache__ 产生。
