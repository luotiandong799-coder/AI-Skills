# r144-B 全站实拉 · GitHub 生态 + 输出落库指针 + 长期指令强制机制

> 日期：2026-09-23｜批次：r144-B（定时触发）｜模式：AI技能库学习批
> 判重基准：WB r144A/B/C 独点 + WB r142 九点 + 豆包 r135/r139/r140/r141/r143/r144-A 已落

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | GitHub Trending（agent-native/smolvm/hermes-agent/ECC） | BuilderIO agent-native（TS agent 框架）；smolvm 可嵌入可分支 VM；ECC 190k 星（15-68 agents/56-286 skills/hooks 管线） | 平台形态/生态事实，不落 |
| 2 | agentskills.io | **Agent Skills 开放标准（Linux Foundation AAIF 治理/35+ 平台/40k+ 技能）**；agentskill.sh CLI 商店；673 技能质量研究（结构/行为/交叉污染） | 标准细节属 sa 域（WB 主战场），不落 |
| 3 | HuggingFace smolagents | **code-first（模型写可执行 Python 而非 JSON tool call；单次生成多动作；同等准确率少 30% 步骤）**；DANGEROUS_MODULES 导入过滤；remote sandbox 优先；**工具合并原则（合并减少 LLM 调用次数）**；**测试拒绝能力（危险指令应请求授权）+ 故意首次失败看恢复** | code-first 是执行格式决策；工具合并/拒绝测试与已落重叠，均判重不落 |
| 4 | DeepSeek Harness | 一切皆插件（models/tools/skills/sessions/sandboxes/loops/scheduling/UI 可换）；dsh.do 插件市场 7302 个 | 平台架构事实，不落 |
| 5 | docs.openclaw.ai automation | **standing orders（固定指令无触发=建议不强制；强制靠 cron 绑定；一个程序一个领域）**；inferred commitments 实验已移除 | **B1：长期指令建议vs强制分离（落 ED）** |
| 6 | ECC 深度 | hooks/memory-persistence 会话开始结束自动存重载；MCP servers 预配置；AgentShield 安全 | session 钩子与 ctx 重启续跑重叠，不落 |
| 7 | context-mode | **工具输出沙箱隔离（原始输出留子进程/SQLite，模型只见 5.4KB 指针；FTS5+BM25 检索召回；98% 压缩）**；**会话续接（事件落库，compact 后按需检索召回不重放整条历史）**；ctx_batch_execute 批量替换 30+ 调用；触发式优化信号（利用率>80%/注意力退化/质量跌破基线） | **B2：输出落库指针+按需检索（落 mts，升级 r140-A）**；触发式信号属 ctx 域不落 |
| 8 | DeerFlow 2.0 | Lead Agent 拆子任务委派并行；**双线程池（调度池排队编排+执行池带超时）**；沙箱/长期记忆事实提取 | 调度/执行分离与 deterministic spine 重叠，不落 |
| 9 | smolagents 官方指南 | going off-task 三修复（crystal-clear system prompt 目标+禁止动作/工具极简描述/max_iterations 早停）；工具合并原则 | 与 ED 已有动作约束/工具描述纪律重叠，不落 |
| 10 | GitHub AI 标签生态 | awesome-agent-skills（VoltAgent 1500+ 官方技能）；anthropics/skills 文档套件；openai/skills（Codex）；addyosmani/agent-skills 85k 星；Git Commit Generator | 生态事实/低价值工具型技能，不落 |

## 二、判重与落地
- **B1 长期指令建议vs强制分离（落 · ED）**：ED 定时记账管"排期怎么写对"、轻量自动化管"自动化形态"——"固定指令无触发=建议，强制靠 cron 绑定；一个程序一个领域"是"长期指令如何获得执行力"增量 → 落 ED（工作流层），分工注明。
- **B2 工具输出落库指针+按需检索（落 · mts）**：r140-A 输出隔离管"代码不进上下文只回输出"——"输出比摘要大时落 SQLite/FTS5 指针库，BM25 按需召回，compact 后重放事件检索而非整段重放"是输出通道升级 → 落 mts（工具层），与 r140-A 分工注明。
- 判重不落（逐条）：code-first 执行格式（与 ED 代码执行/JSON tool call 两路已覆盖，决策依据缺失）、工具合并（r143-A consolidation 判重已述）、拒绝能力测试（r140-C 已知坏输入重叠）、session hooks（ctx 重启续跑）、双线程池（deterministic spine）、standard 字段（sa 域）、going off-task（ED 动作约束已落）。

## 三、功能套件检查
- 三件套：mts 新增"输出落库指针+按需检索"（上下文隔离通道补全：代码隔离→输出指针）；pt/ctx 无改动（ctx 在途不碰）→ 无需增删。
- 工程技能：ED 新增"长期指令建议vs强制分离"，自动化纪律链补全（排期→形态→强制力）。
- 垃圾清理：本轮无临时文件；无 __pycache__ 产生。
