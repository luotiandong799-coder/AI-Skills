# r145-A 全站实拉 · 框架生产模式 + 提示精简实证 + 错误处理分层

> 日期：2026-09-23｜批次：r145-A（定时触发）｜模式：AI技能库学习批
> 判重基准：WB r144A/B/C 独点 + 豆包 r135-r144 已落（含 r144-C 基准审计/RAG 管线）

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | LangGraph 官方/生产模式 | **双存储层分离（checkpointer=[summary+last N]给 LLM 工作上下文 vs 自有表=append-only 全历史给 UI）**；entrypoint.final 解耦；生产需 durable checkpointer；重试循环 error_count 上限 | 双存储层与 ctx/mts 记忆分层重叠，判重不落 |
| 2 | CrewAI 官方 | **Flow vs Crew 判据（路径开放→Crew/序列确定→Flow，Flow 内嵌 Crew）**；**Router 必须处理每个 case 含 do-nothing 路径（无效分支缺 listener=静默失败）**；loop-back 加迭代上限；max_iter 25→5-8；窄角色 2 工具 | Router 兜底与 ED 失败处理重叠 60%；Flow/Crew 与 ED 编排决策树重叠；max_iter 与工具循环终止重叠 → 均不落 |
| 3 | AutoGen→MAF | AutoGen 维护模式（无新特性仅安全补丁）；MAF 1.0 GA；迁移 deadline Q3 2026；企业六项清单；Selector 描述像招聘启事 | 框架生命周期事实/清单已覆盖 → 不落 |
| 4 | Dify 官方 | **有界智能（AI 在受控检查点提供意图解读，路由仍规则驱动）**；Human Input 节点；节点级错误处理（继续/日志/替代路径） | 有界智能=deterministic spine 同源 → 不落 |
| 5 | n8n 官方/社区 | **两层错误处理（节点级 Retry On Fail 瞬时 + Error Trigger 剩余：告警/死信/记录）**；retry-from-failed=重跑非续跑；safe default；死信表 attempt_count/幂等标记 | **A1：错误处理两层分治（落 ED）** |
| 6 | 提示工程 2026 | **Anthropic 80% prompt cut（删 80% system prompt 无评估损失→精简提示+工具+渐进披露+记忆+结构化引用）**；Simplicity Paradox；MPO/PSAO/RPT 研究 | **A2：精简优先实证（落 mts）**；Simplicity Paradox 与 mts 已有重叠 |
| 7 | 推理时计算 | **自适应分配（CoBa：生成/验证/停止三选一；廉价验证广撒+强验证只给不确定）**；弱 verifier 封顶；Plan-and-Budget | 按难度分配 mts 已落；验证分层与 judge 已覆盖 → 不落 |
| 8 | 长上下文 | **Lost in the Middle（U 型首尾好中间差，2026 收窄未消除；有效上下文不均匀）**；重排文档/精简/不信更多 token；评估扫深度 | **A3：注入顺序关键放首尾（落 mts）** |
| 9 | 语音 Agent | 全双工（去轮次检测器）；turn-taking 系统（barge-in 200ms 检测/300ms 停止/端点按呼叫类型校准）；延迟预算 300-800ms；mask unavoidable delay | 语音产品域与豆包办公技能域不匹配 → 不落 |
| 10 | GitHub Trending 本周 | agentic tooling 主导（agent-native/claude-code/ZCode/ECC/archify/ponytail）；OpenMAIC；self-driving-agents | 生态事实无新方法论 → 不落 |

## 二、判重与落地
- **A1 错误处理两层分治（落 · ED）**：ED 已有"transport/tool 分开重试"（重试对象维度）——本条补"重试层级"：节点级瞬时就地重试 + 工作流级死信/告警，attempt_count 让重试次数变数据。落 ED（工作流层），分工注明。
- **A2 精简优先实证（落 · mts）**：mts 已有 system prompt 分层预算（各层字数）——本条补"总量该持续瘦身"：Claude Code 删 80% 无评估损失，工具/记忆/引用替代指令。落 mts（模型/工具层）。
- **A3 注入顺序（落 · mts）**：mts 已有"只注入相关信息"（注入什么）——本条补"注入顺序"：U 型注意力，关键信息置首尾。落 mts（工具/工作流层），与 r144-C RAG 检索管线（召回选择）分工。
- 判重不落（逐条）：双存储层（ctx/mts 记忆分层已覆盖）、Router 兜底（ED 失败处理已覆盖）、Flow/Crew 判据（ED 多 agent 决策树已覆盖）、max_iter（工具循环终止已覆盖）、有界智能（deterministic spine 同源）、验证预算分层（mts 按难度+judge 已覆盖）、语音 turn-taking（域不匹配）、框架生命周期/生态事实（无方法论增量）。

## 三、功能套件检查
- 三件套：mts 新增"精简实证+注入顺序"（上下文管理链补全：预算→瘦身→注入顺序）；pt/ctx 无改动 → 无需增删。
- 工程技能：ED 新增"错误处理两层分治"（错误处理链补全：对象→层级→死信）。
- 垃圾清理：本轮无临时文件；无 __pycache__ 产生。
