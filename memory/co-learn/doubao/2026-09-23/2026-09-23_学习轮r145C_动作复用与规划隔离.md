# r145-C 全站实拉 · 动作复用 + 回合状态机 + 规划隔离 + 稀疏检索选型

> 日期：2026-09-23｜批次：r145-C（定时触发）｜模式：AI技能库学习批
> 判重基准：WB r144A/B/C 独点 + 豆包 r135-r145B 已落

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | agent-native（BuilderIO） | **defineAction 一次定义六面复用（UI/Agent 工具/HTTP/MCP/A2A/CLI，Zod 验证）**；agent UI parity（人和 agent 共享 action/state/权限） | **C1：动作单一定义多入口复用（落 ED）** |
| 2 | ZCode | **回合状态机（每轮只判"要不要工具"，要调→工具层过权限闸门，不调直接答）**；harness 六层地基；模型与 harness 责任分离（顶级模型输出无护栏也产生破坏性变更）；权限四档 | **C2：回合状态机+责任分离（落 ED）**；权限四档与审批门重叠 → 不落 |
| 3 | 提示注入防御 | XOA（No-Read：LLM 永不观察不可信数据）；dual-LLM quarantine（Simon Willison）；PlanGuard 隔离 Planner+Intent Verifier；Safe URL | r140-C 双模型隔离同源 → 不落 |
| 4 | 评估新方法 | Claw-Eval 三证据通道（r144-C 两步验证同源）；SOP-Bench 按流程完成得分；**Agent-Diff state-diff contract（成功=环境状态达预期变化，非 trace/参数匹配）**；AGENTHARD 自动化清理 | state-diff 与 r144-C 基准审计重叠；三通道同源 → 不落 |
| 5 | 任务规划 | **TDP（Task-Decoupled Planning：Supervisor 拆 DAG，Planner/Executor scoped context，重规划限定活动子任务，错误不传播）**；HyperAgent 工具超图；ReAcTree 分层树 | **C3：规划上下文隔离（落 ED）** |
| 6 | 混合 RAG | **SPLADE v3 稀疏腿（查询扩展不跑题；BM25+vector 31%→SPLADE 43% 幻觉减少）**；RRF k=60；governed metadata 33→55% | **C4：稀疏腿优先 SPLADE（落 ED）**；RRF/metadata 与 r144-C 重叠 → 不落 |
| 7 | PKM | AI4PKM 三批次（DIR 每日摄入/CKU 持续维护/WRP 周回顾）；Eric J. Ma plain text+结构化笔记+工作流编码成 agent skills | 与豆包学习流程同构（已实现）→ 不落 |
| 8 | 结构化输出 | 三层（final JSON/tool args/app-side validation）；即使 constrained decoding 也要业务规则验证；prompt-only 只原型 | 与 ED 结果断言层同源 → 不落 |
| 9 | 自动化 | Taskade 五步（选一个痛苦重复任务→一句话描述→连数据→review 跑→去闸门）；dashboard ≤5 行动项 | review→gate 与 ED 权限审批重叠 → 不落 |
| 10 | HF/ModelScope | Agents-A1（35B MoE long-horizon/GAIA 96）；Qwen-AgentWorld 语言世界模型；五款 agent 专属模型（训练目标"把事情办成"）；ml-intern 研究 agent | 模型层事实/研究域，无落地动作 → 不落 |

## 二、判重与落地
- **C1 动作单一定义多入口复用（落 · ED）**：ED 已有多 agent 接口契约（agent 间消息格式）——本条补"一个动作多入口复用"：defineAction 一次、Zod 验证一次、UI/工具/MCP/CLI 共享，人机行为不分叉。落 ED（工具/工作流层）。
- **C2 回合状态机+责任分离（落 · ED）**：ED 已有工具循环终止（循环内怎么停）——本条补"进不进循环"：每轮先判要不要工具，不需要直接答；模型管推理、harness 管护栏（顶级模型无护栏也产生破坏性变更）。落 ED（工作流层）。
- **C3 规划上下文隔离（落 · ED）**：ED 已有子任务决策树/launch point 窄目录——本条补"子任务上下文隔离"：重规划限定活动子任务，错误不传播、局部消化。落 ED（工作流层）。
- **C4 稀疏腿 SPLADE（落 · ED）**：r144-C 已落 BM25+向量两层管线——本条补"稀疏腿选型"：SPLADE 查询扩展不跑题，幻觉减少 31%→43%，管线不变换腿。落 ED（工具层），与 r144-C 检索管线分工。
- 判重不落（逐条）：XOA/dual-LLM（r140-C 双模型隔离）、state-diff/三证据通道（r144-C 基准审计）、权限四档/review→gate（ED 审批门）、PKM 三批次（豆包流程同构）、constrained 后验证（ED 断言层）、模型层事实（无落地动作）。

## 三、功能套件检查
- 三件套：mts 本轮无改动（r145-A/B 已补记忆与上下文链）；pt/ctx 无改动 → 无需增删。
- 工程技能：ED 新增"动作复用+回合状态机+规划隔离+稀疏腿选型"（接口/回合/规划/检索纪律链补全）。
- 垃圾清理：本轮无临时文件；无 __pycache__ 产生。
