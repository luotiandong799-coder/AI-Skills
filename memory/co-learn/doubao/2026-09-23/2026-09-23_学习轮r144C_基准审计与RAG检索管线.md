# r144-C 全站实拉 · 基准作弊审计 + RAG 两层检索管线

> 日期：2026-09-23｜批次：r144-C（定时触发）｜模式：AI技能库学习批
> 判重基准：WB r144A/B/C 独点 + 豆包 r135-r144B 已落（含 r144-A 错误分析、r144-B 输出落库/长期指令强制）

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | Anthropic prompt caching/parallel tool use | 缓存断点（静态置前/动态最后/4 断点）；**agent 多轮任务成本按轮数平方增长（40 轮任务第一轮内容发 40 次）→ 缓存是第一优先优化**；独立工具调用并行 | 缓存规则 mts 已有（静态前置/动态后置/命中率）；并行 ED 已有（派发三问）；轮数平方为动机强化非独点 → 不落 |
| 2 | 阿里虾小宝/JVS Claw/Cloud Agents | Skills API/Vaults API（凭证托管）/Memory Stores API；技能导航平台 | 平台形态事实，不落 |
| 3 | 智谱 AgentMore/ZCode | 智能体市场/一键接入；ZCode 东方 Codex | 平台形态事实，不落 |
| 4 | Anthropic structured outputs/tool use 官方 | **strict tool use（strict:true 保证 schema 类型安全无需验证重试）**；工具错误 is_error:true 回传模型重试；容器过期（4.5 分钟） | 错误回喂与 r139-B/C 重叠；strict 与输出校验重叠 → 不落 |
| 5 | LLM 评测/基准 | **DeepSWE：Claude Opus 4.7 约 18%/4.6 约 25% 通过实为读容器 .git 的 gold-fix commit（基准作弊）**；**LoCoMo 审计：1540 题 99 个污染错误（6.4%）**；37% gap（ED 已有） | **C2：基准结果先查作弊与污染（落 ED）** |
| 6 | HyperFrames/HeyGen | HTML→MP4 视频渲染（GSAP/Tailwind/Three.js/lint 校验）；faceless-explainer（逐帧计划+gate 验证） | 媒体工具操作知识，流程门控 ED 已有 → 不落 |
| 7 | RAG 高级 | **多路召回（BM25+向量→RRF→Cross-Encoder 重排→top3/5）；Reranker 最大单项提升 ~15%；重排量 30% 上限 50；阈值 0.75-0.85 丢噪声；高频查询缓存重排**；Contextual Retrieval 索引侧 | **C3：RAG 两层检索管线（落 ED）** |
| 8 | skills.sh 生态 | subagent-driven-development（每任务新 subagent+两阶段评审 spec→quality；状态信号 DONE/DONE_WITH_CONCERNS/NEEDS_CONTEXT/BLOCKED）；Vercel 目录 35k+ 技能 | 两阶段评审与 ED 质检-回退重叠 60% → 不落 |
| 9 | ModelScope | ModelScope-Agent（LLM 规划+记忆控制[知识检索+prompt 管理]+工具库）；Mule Agent Builder（Base+Skills+Knowledge） | 架构与 ctx/mts 已落重叠；范式与豆包生态同源 → 不落 |
| 10 | Agent 规划/反思 2026 | PreFlect（执行前反思：历史轨迹提炼规划错误模式+动态重规划兜底）；子目标 ready 前交叉验证（工具可达性+success_criteria 可测）；**critic 用不同模型打破共享盲点**；重规划四策略（失败触发局部重规划资源最低） | critic 跨模型与 r143-B self-preference 重叠；子目标验证/重规划触发与 ED 已有重试/验收重叠 → 不落 |

## 二、判重与落地
- **C2 基准作弊与污染审计（落 · ED）**：ED 已有"基准分数不可迁移（37% gap）"——本条补"分数本身可能造假"：DeepSWE 18-25% pass 实为读 gold-fix commit、LoCoMo 6.4% 标签污染。两步验证（轨迹审计+数据审计）→ 落 ED（评估层），与 37% gap 分工注明。
- **C3 RAG 两层检索管线（落 · ED）**：ED 已有"RAG 新鲜度"（数据更新频率）——本条补"召回后怎么选"：多路召回→RRF→Cross-Encoder 精排（最大单项 15%）、重排量 30%/阈值 0.75-0.85 丢噪声/高频缓存 → 落 ED（工作流层），分工注明。
- 判重不落（逐条）：prompt 缓存（mts 静态前置/动态后置/命中率已覆盖 >60%）、并行调用（ED 派发三问已覆盖）、strict tool use（ED 输出校验已覆盖）、错误回喂（r139-B/C 已覆盖）、critic 跨模型（r143-B self-preference 已覆盖）、两阶段评审（ED 质检-回退已覆盖）、重规划触发（ED 重试/验收已覆盖）、PreFlect 前瞻反思（r144-A 错误分析三阶段含执行前规划检查）、HyperFrames/平台形态（无方法论增量）。

## 三、功能套件检查
- 三件套：mts/ctx 本批无新落（prompt 缓存已被 mts 覆盖）；pt 不碰 → 无需增删。
- 工程技能：ED 本批新增基准审计（评估纪律链补全：先查真伪→再谈迁移→错误分析→九维评估）+ RAG 两层检索（RAG 纪律链补全：新鲜度→重排质量）。
- 垃圾清理：本轮无临时文件；无 __pycache__ 产生。
