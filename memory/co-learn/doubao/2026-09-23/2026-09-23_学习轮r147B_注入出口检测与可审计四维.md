# r147-B 全站实拉 · 注入出口检测两法 + 主张级可审计四维

> 日期：2026-09-23｜批次：r147-B（定时触发，独立于 r147-A 重新全站实拉）
> 判重基准：WB r146A/B/C + 豆包已落全清单（含 r147-A 快照/选型判据）

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | WaytoAGI/方法论集 | Agent=模型+Harness（模型 20%/harness 80%）；先接追踪评估再交付；生产失败构建回归测试集；大上下文不是文件系统 | 前三均 ED/av/WB ctx 已覆盖；"上下文不是文件系统"WB ctx 覆盖 → 不落 |
| 2 | deepseek-plugin.org/Harness | Everything is a plugin（模型/工具/技能/会话/沙箱/存储/循环/调度/UI 可插拔）；defineTool 规范（exec.signal 中止信号/卸载自动清理）；危险工具调用前暂停确认 | 工具注册/中止/确认 ED 已覆盖 → 不落 |
| 3 | agentskills.io 生态 | agentskillslist #01 Self-Improving Agent（捕获失败/用户纠正→持续改进，触发清单）；trust score/safe_to_try 平台质量信号 | 自改进 r146-B 判重不落；质量信号 r147-A A2 已落 → 不落 |
| 4 | OpenClaw memory/context | Pruning vs Compaction 分工（pruning 只丢 in-memory 旧工具结果、不重写 transcript、历史可查）；daily reset（本地 4AM 会话重置，过期在下条消息评估）；模型只记住落盘内容无隐藏状态 | pruning/compaction WB ctx 覆盖；会话重置与 ED cron 触发窗口面重叠 → 不落 |
| 5 | Hugging Face smolagents | CodeAgent 写 Python 执行（同准确率少 30% 步数）；planning_interval 周期后退评估事实；final_answer_checks 校验函数；max_steps 10-20 | code-first r144-B 判重不落；planning/校验已覆盖 → 不落 |
| 6 | GitHub AI 仓库 | MemHarness（记忆带来源状态+成功率平滑率）；AHE（observability-driven 自动改进 harness 组件冻结 base model）；Google Agents CLI | 记忆面 WB ctx 覆盖；自改进 r146-B 判重不落 → 不落 |
| 7 | 提示注入 2026 | **canary token（system prompt 埋唯一不可猜字符串，输出/工具调用/外发网络出现=成功外泄证明，把"识别所有载荷"变"识别一个字符串"）**；**PromptArmor（LLM 当注入检测器，AgentDojo FP/FN<1%）**；ARGUS 因果溯源审计；输出过滤/结构化 | **B1：注入出口检测两法（落 mts）**；ARGUS 与 r146-B 不可信区同向；结构化输出 ED 覆盖 |
| 8 | 上下文工程 | **Compaction 摘要按 schema（决策及理由/当前计划态/发现的约束/产出工件/放弃方法及原因——"为什么放弃 A"是防重跑死路的关键）**；CLEAR/COMPACT/REMEMBER 决策框架；sliding window N=10-20 | 摘要 schema 属 WB ctx 主战场（不可编辑）且重叠>60%；tool clearing r144-B 已落 → 不落 |
| 9 | deep research | **AAR 主张级可审计四维（provenance coverage/soundness/contradiction transparency/audit effort）**；NVIDIA 五阶段+引用验证 deterministic 管线；Shannon claim extraction+confidence scoring | **B2：主张级可审计四维（落 mts，与 ED 引用纪律分工=引用真实 vs 可审计）**；五阶段管线 ED 引用覆盖；AREX 递归自改进 r146-B 判重不落 |
| 10 | GitHub Trending 今日 | ECC 又上榜（同源）；cursor/plugins（产品面）；utopia document-to-ontology（产品面）；humanizer 去 AI 味（doubao-human-signal 覆盖）；agent-native（站 8 已拉） | 全部判重不落 |

## 二、判重与落地
- **B1 注入出口检测两法（落 · mts）**：Grep mts/ED 无 canary/检测器同类——"出口抓成功外泄"是新面；与 WB ctx 工具面安全分工（入口预检 vs 出口证据）、与 ED 注入模式预检清单分工（入口防 vs 出口抓）。提升层：工具/可复用 Skill。
- **B2 主张级可审计四维（落 · mts）**：Grep 无"provenance coverage/soundness/矛盾透明/审计成本"同类——报告级可审计标准是新；与 ED 引用纪律分工（引用真实可查 vs 逐主张可审计）。提升层：工作流/可复用 Skill。
- 版本 bump：mts 1.37.0→**1.38.0**（ED 本轮无内容变化保持 2.98.0）。
- 判重不落（逐条）：Agent=模型+Harness（ED 回合状态机覆盖）；Everything-is-a-plugin（ED 工具面覆盖）；Self-Improving 触发清单（r146-B 判重不落）；pruning/compaction 分工（WB ctx 覆盖）；摘要 schema 弃用原因（WB ctx 主战场+重叠）；CodeAgent/code-first（r144-B 同源）；MemHarness 记忆面（WB ctx 覆盖）；ARGUS（r146-B 不可信区同向）；NVIDIA 引用验证管线（ED 引用覆盖）；humanizer（doubao-human-signal 覆盖）。

## 三、功能套件检查
- 三件套：mts +2（出口检测/可审计四维）+bump；pt/ctx 无新料 → 维持 3 件套；ED 保持 2.98.0 无内容变化。
- 垃圾清理：无临时文件产生；无 __pycache__。
