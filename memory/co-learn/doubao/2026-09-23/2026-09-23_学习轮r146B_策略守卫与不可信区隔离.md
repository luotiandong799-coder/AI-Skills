# r146-B 全站实拉 · 策略守卫两阶段 + 不可信区零权限化

> 日期：2026-09-23｜批次：r146-B（定时触发）｜模式：AI技能库学习批
> 判重基准：WB r145A/B/C + 豆包 r135-r146A 已落

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | LangFlow | **Policies 组件（自然语言规则→Guarded Tools：Generate 生成守卫代码→人审→Guard 运行，每个工具包保护层）**；CUGA Plan 四步；Flow DevOps Toolkit；Assistant 自然语言生成组件 | **B1：策略守卫两阶段（落 ED，与 hook 担保/最小权限分工）**；CUGA 规划与 TDP 重叠 → 不落 |
| 2 | Activepieces | Agent 可复用可聊天；提示词库版本化+prompt ID 审计；approval where it counts | prompt 库版本化与 r143-B 版本生命周期重叠；审批 gate 已覆盖 → 不落 |
| 3 | Make | 五 agent 模式（Conversational/Synthesizer/Routing）；场景转工具（scenario→tool，AI 定参数 vs 常量）；radical transparency | 场景转工具与 r145-C 动作复用同源；五模式与五种编排重叠 → 不落 |
| 4 | Pipedream | Connect SDK+MCP server 1 万+工具；Docs MCP server；AI-driven scaffolding；配额断→fallback | resilient fallback 与错误处理已覆盖；Docs MCP 产品面 → 不落 |
| 5 | MCP 2026-07-28 | **stateless 核心（无状态协议，HTTP 水平扩展）；server/discover 能力发现；list cacheable；授权对齐 OAuth/OIDC（agent 身份独立、子 agent 权限比父更窄）**；extensions 框架（Apps 服务端 UI/Tasks 长运行） | **子 agent 授权递减链并入 B2 讨论（权限面，与最小权限分工）**；无状态协议事实 → 不落 |
| 6 | 自我改进研究 | ERL（轨迹反思→启发式→测试时检索注入）；Llama Stack 确定性自改进（每 N 次 evaluate→重写 system prompt）；SAMULE 三级反思 | ERL 与 WB dl 失败经验索引重叠；Llama Stack 与 r145-B 上下文演化三角色重叠 → 不落 |
| 7 | 存储型注入防御 | **untrusted zone（读取器无工具/凭证/网络，typed boundary 返回窄 schema 事实/引用/风险标记）；DualView 双视图（AgentView 符号化/HumanView 保真）；lethal trifecta（不持私有数据+不可信内容+出口工具）** | **B2：不可信读取区零权限化（落 ED，与隔离模型分工）**；DualView 实现面（符号化）判重不落 |
| 8 | 工具选择发现 | Tool Search Tool（按需扩展，34-64% token 减）；**CMTF 因果最小过滤（相关性≠必要性，precondition-effect 契约）**；描述是路由主源 | CMTF 与入口最小化重叠>60% 判重不落；描述质量 r140-A 已覆盖 → 不落 |
| 9 | 多轮上下文摘要 | MT-OSC（Condenser+Decider）；保留最近 5-10 轮全文；progressive summarization | 与 WB ctx 压缩触发/keep_first 重叠 → 不落 |
| 10 | GitHub Trending（今日） | cloudflare/security-audit-skill；alibaba/open-code-review（确定性+LLM 混合）；Agent Beacon 遥测；WeKnora 自维护 Wiki | open-code-review 与 archify 确定性产物同面；其余产品事实 → 不落 |

## 二、判重与落地
- **B1 策略守卫两阶段（落 · ED）**：Grep 确认 ED 无"守卫工具/Generate/Guard"同类——"自然语言规则→LLM 生成守卫代码→人审→Guard 运行，守卫包在工具外层调用前拦截"是新流程。与 §硬约束 hook 担保分工（拦截点 vs 守卫产生启用）；与 §per-tool 最小权限分工（权限量 vs 规则执行化）。落 ED（工具/可复用 Skill 层）。
- **B2 不可信读取区零权限化（落 · ED）**：ED 已有隔离模型（quarantined/privileged 模型分离，2044 行）与记忆 taint——本条补"读取器能力边界与返回形态"：读取器无工具/凭证/网络、typed boundary 只回窄 schema 事实/引用/风险标记、lethal trifecta 三权不同持。与隔离模型分工（模型分离 vs 读取器权限+输出边界）。落 ED（工作流层）。
- 判重不落（逐条）：CMTF 因果工具过滤（与入口最小化重叠>60%）；Tool Search Tool（同源）；ERL 启发式索引（WB dl 失败经验索引覆盖）；Llama Stack 自改进（r145-B 三角色覆盖）；prompt 库版本化（r143-B 覆盖）；场景转工具（r145-C 动作复用覆盖）；MT-OSC/5-10 轮保留（WB ctx 覆盖）；五 agent 模式（五种编排覆盖）；open-code-review 混合（archify 确定性产物同面）；MCP 无状态协议/子 agent 递减授权（授权递减并入 B2 备注，协议事实不落）。

## 三、功能套件检查
- 三件套：mts/pt/ctx 本轮无新料 → 维持 3 件套。
- 工程技能：ED +2（策略守卫两阶段 + 不可信区零权限化）。
- 垃圾清理：本轮无临时文件；无 __pycache__ 产生。
