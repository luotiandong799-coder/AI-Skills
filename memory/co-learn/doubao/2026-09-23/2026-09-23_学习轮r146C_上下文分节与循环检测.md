# r146-C 全站实拉 · 上下文分节标签 + 参数哈希循环检测

> 日期：2026-09-23｜批次：r146-C（定时触发）｜模式：AI技能库学习批
> 判重基准：WB r145A/B/C + 豆包 r135-r146B 已落

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | 腾讯 SkillHub | 三线并行安全审核（内容合规+科恩漏洞扫描+云鼎 AI 模型安全评估）；SkillPay 支付体系；TRACE 评测体系；团队空间管理 | 三线审核平台治理面（个人非上架方）→ 不落；SkillPay/TRACE 商业事实 → 不落 |
| 2 | 阿里虾小宝 | JVS Claw 万能 skill 自进化（"没有这个技能请搜索并创建"）；技能先检测本地再安装 | 万能 skill 与入口最小化+技能发现重叠>60% → 不落 |
| 3 | 智谱 AgentMore/ZCode | ZCode 东方 Codex（桌面+微信派活）；AutoGLM 50+ 步跨 app；解耦任务规划与动作执行 | 解耦规划/执行与 r145-C 回合状态机重叠 → 不落；产品事实 |
| 4 | charonhub/deeplearning.ai | 新课程 Building Adaptive AI Agents（08-26）；视频生成 agent 自检查自修复；AI Prompting for Everyone | 视频自修复=输出校验已覆盖；课程事实 → 不落 |
| 5 | 上下文工程 | **Layered Prompt（[SYSTEM]/[RETRIEVED]/[SUMMARY] 显式分节不混块）**；Input Eviction（85% 触发）；Context Engineering 学科（Write/Select/Compress/Isolate）；position-optimized 注入 | **C1：上下文显式分节标签（落 mts，与注入顺序分工=结构 vs 排序）**；Input Eviction 与 WB ctx 压缩触发重叠 → 不落 |
| 6 | 工具调用可靠性 | **argument hashing 抓推理循环（同参数重复调用哈希追踪）**；separate completion check；schema 先验证再进推理链；五层（retry/fallback/error context/obs/chaos） | **C2：参数哈希检测重复调用（落 ED，与 stop_reason/幂等分工）**；completion check 与质检-回退重叠 → 不落 |
| 7 | Deep research | DuMate（全局自修正路线图+递归两层执行+rubric 优化）；Thinker-Actor fractal tree；IterResearch 20 步（+31.2%）；Reflector 覆盖检查 | 递归两层与 r144-C 两层检索管线重叠；Reflector 与质检-回退重叠 → 不落 |
| 8 | 智能体测试 | task replay（轨迹录制→stub 重放）；行为断言（right tool/grounded/persisted）；AgentAssay 三值；三层金字塔 | task replay 与 r145-C 动作缓存同源；行为断言/三值与 av eval 重叠 → 不落 |
| 9 | 个人生产力 | Copilot Agent Mode（one prompt→guided workflow）；Inbox Zero 四类模板；Muse 长期目标→行动；跨时区会议提示词 | Inbox Zero 模板属提示词应用非方法论 → 不落 |
| 10 | GitHub Trending（今日） | Google ax（编排 runtime +188%）；Understand-Anything（代码库→知识图谱）；addyosmani 97k★；scientific-agent-skills 40k★ | 编排/知识图谱与已落同面 → 不落；star 事实 |

## 二、判重与落地
- **C1 上下文显式分节标签（落 · mts）**：Grep 确认 mts 无"分节/SYSTEM INSTRUCTIONS/RETRIEVED"同类——"系统指令/检索上下文/对话摘要分块标注、不混单块"是新结构决策。与 r145-A 注入顺序分工（结构标签 vs 首尾排序）；与 WB ctx 压缩触发分工（组装结构 vs 压缩时机）。落 mts（工作流层）。
- **C2 参数哈希检测重复调用（落 · ED）**：ED 已有幂等观察（重复后果安全）、runaway 拦截（hook 机制）、stop_reason（循环终止）——本条补"循环怎么被识别"：同参数哈希追踪，规范化（排除时间戳）+分档处置（容忍有限次→提示→硬终止）。与 stop_reason/幂等/runaway 三方分工明确。落 ED（工具层）。
- 判重不落（逐条）：万能 skill 自进化（入口最小化覆盖）；解耦规划/执行（r145-C 覆盖）；视频自修复（输出校验覆盖）；Input Eviction（WB ctx 覆盖）；递归两层检索（r144-C 覆盖）；Reflector 覆盖检查（质检-回退覆盖）；task replay（r145-C 动作缓存覆盖）；行为断言/AgentAssay（av 覆盖）；Inbox Zero 模板（提示词应用非方法论）；三线安全审核（平台治理面）；Google ax/Understand-Anything（编排/repo map 同面）。

## 三、功能套件检查
- 三件套：mts +1（上下文分节标签）；pt/ctx 本轮无新料 → 维持 3 件套。
- 工程技能：ED +1（参数哈希循环检测）——r146 三轮合计 ED +3 节。
- 垃圾清理：本轮无临时文件；无 __pycache__ 产生。
