# 2026-09-23 学习轮 r150-A：缓存运行时纪律与输出侧瘦身面

轮次：r150-A（全库轮号续接，2026-09-23，18:40 批）
判重基准：WB r144-r149 留痕 + 豆包 r135-r149 留痕 + mts 1.39.0 全量既有覆盖（不可变前缀/脚本输出隔离/工具输出落库指针/记忆分层/两实例闭环等）

## 一、实拉证据（10 站全量逐站）

| # | 站 | URL | 关键原文 / 事实 |
|---|---|---|---|
| 1 | Anthropic/Claude | platform.claude.com/docs/en/build-with-claude/prompt-caching | 自动缓存=顶层 cache_control，系统自动把断点移到最后一个可缓存块；显式断点最多 4 个 |
| 2 | Anthropic/Claude | claude.com/blog/lessons-from-building-claude-code-prompt-caching-is-everything | "Don't change tools or models mid-conversation. Use tools to model state transitions (like plan mode) rather than changing the tool set. Defer tool loading instead of removing tools. Monitor your cache hit rate like you monitor uptime. We alert on cache breaks and treat them as incidents. Fork operations need to share the parent's prefix." |
| 3 | 缓存参数面 | developersdigest.tech/blog/prompt-caching-claude-api-production-guide | break-even≈前缀 5 分钟内复用 >1-2 次就缓存；>2k token 的 system prompt 每调用必带是显性收益 |
| 4 | OpenClaw | docs.openclaw.ai/reference/token-use | 上下文计费含不可见项：Provider wrappers or safety headers (not visible, but still counted)；/status 与 /usage tokens\|full 给每次回复的 usage footer |
| 5 | OpenClaw | openclaw-docs.beaverslab.xyz/en/gateway/config-agents | Context budget ownership map：多个高容量预算按子系统拆分（bootstrapMaxChars 等），不走单一旋钮 |
| 6 | WaytoAGI | waytoagi.com/question/91629 | deepseek 时代"关键诉求直通车"：完整提示词可能不如片段有效，过长提示带来 token 浪费与上下文污染 |
| 7 | deepseek-plugin.org | deepseek.com/harness/en + sitepoint 评测 | DeepSeek Harness 一切皆插件；router 先设 routing key，记忆注入在路由决策之后，output 插件格式化 |
| 8 | GitHub 生态 | startupcorners digest 2026-09-23 + trendshift + heatdrop | CowAgent 47K★ / Orca 平行 agent ADE / deepseek-harness / siyuan 等均为产品面，无个人可内化方法增量 |
| 9 | deeplearning.ai | community.deeplearning.ai 新课程 | Agent Memory (Oracle)、Building AI Browser Agents (MCTS+自批评+DPO)、Voice agents（每阶段指标）；课程面，方法已被既有覆盖 |
| 10 | HF smolagents | dev.to/wartzarbee/smolagents-replays-its-whole-memory-every-step + mlhive/aionda | CodeAgent 省 30% LLM 调用（r149-B 已判不装）；**新捕获：smolagents 每步重放全量记忆=O(n²) token 账单，固定窗口截断回 O(n)** |

## 二、独点清单 + 判非重复理由（本轮落地 4 点 → mts 1.39.0→1.40.0）

| 独点 | 来源 | 判非重复理由（对 WB/豆包既有） | 提升层 |
|---|---|---|---|
| 1 运行时工具集稳定性纪律：不中途换工具/模型；用工具模拟状态迁移（plan mode）而非换工具集；延迟加载而非删除 | Claude Code 官方博客 | mts §不可变前缀管"字节级前缀稳定"（缓存布局面），本条管"**会话运行时工具/模型集合不变**"（调用面+状态机代替换工具集+延迟加载）——增量 >60% | 工具 / 工作流 |
| 2 缓存命中率当 uptime 监控：缓存破坏按事故告警；fork/分支操作必须共享父前缀 | 同上 | mts §保持轮次流动防缓存过期管"停顿超 TTL"（时间面），本条管"**命中率观测告警 + 分支共享前缀**"（运维面+并行分支面）——增量 >60% | 工作流（成本观测） |
| 3 工具返回字段裁剪：工具只回 LLM 需要的字段（property_name/price/availability），非完整 API JSON | n8n 社区实测 | mts §脚本输出隔离/落库指针管"输出不占上下文或转移"，本条管"**输出内容源头瘦身（字段级）**"——不同维度，增量 >60% | 工具 / 工作流 |
| 4 工具循环 O(n²) 隐藏账单：每步重放全量历史→固定窗口截断回 O(n)，且要故意为之而非账单上发现 | smolagents 社区实测 | mts §记忆 token 分层/上下文预算管"常驻量硬预算"，本条给**工具循环的定量成本机制**（O(n²)→O(n)）+ "故意截断"决策点——增量 >50% | 工作流 / 工具 |

判不装（实拉后双键判重 >60%）：关键诉求直通车（与 mts 精简面/去 AI 味重叠）；CodeAgent 省 30%（r149-B 已判）；context budget ownership map（与 mts §system prompt 分层预算同面，增量不足 40%）；Harness 一切皆插件（r148 前已覆盖）；GitHub 当日榜全产品面（无方法增量）；新课程面（被既有方法论覆盖）。
