# r149-A 全站实拉 · 生态面复核轮（0 落地）

> 日期：2026-09-23｜批次：r149-A（判重基准：WB r144-r147 全量 + 豆包 r135-r148 已落全清单，含 r148-C 新增 mts 两实例闭环/ED Gotchas）

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 判非重复理由 |
|---|---|---|---|
| 1 | GitHub Trending/生态 | ECC（+5078★：Skills/instincts/memory/security/harness performance，/harness-audit、/quality-gate、/model-route 命令）；pacifio/atlas（agent 源代码控制：并行会话+共享内存+可搜索历史）；superpowers/JeecgBoot/deepseek-harness（历史已拉）；headroom（token 优化） | ECC instincts=从会话学模式带置信度→WB ctx 记忆面；rules/hooks/agents=分层/多agent 已覆盖；atlas=产品面（IDE）；headroom=mts token 面 → 不落 |
| 2 | Anthropic Skill 生态 | 内部扩散流程（工程师自建→Slack 分享→他人试用反馈→traction 后 PR 入市→merge 前轻量策展）；敏感操作人机闭环（删除/支付前强制确认）+失败安全降级路径；版本回滚（delete_versions）；side-by-side 四指标（首次可接受稿时间/编辑次数/脚本抓到错误/人工置信度） | 扩散流程=WB release 审批门控面；敏感操作确认=ED guardian/审批面；版本回滚=WB release 版本化覆盖；四指标=r148-C 基线对照已落 → 不落 |
| 3 | docs.openclaw.ai | Be concise（instruct what not how）；exec 防任意注入；test locally（openclaw agent --message）；Skill Workshop proposal→apply→rollback | concise=write-for-agent 面（r148-A 判重）；注入=ED 安全面；本地测试=r148-C 两实例闭环；workshop=r148-A 判重不落（WB 审批门控）→ 不落 |
| 4 | ECC 深挖 | Agents=scoped workers（隔离 plan/impl/review）；Rules=durable standards always loaded；Hooks=harness 事件脚本（model context 外）；NanoClaw v2（model routing/skill hot-load/session branch） | agents/rules/hooks=ED 多 agent+分层预算覆盖；hot-load=工具面 → 不落 |
| 5 | atlas 深挖 | 编码代理的版本控制系统（alpha-0.3.0：共享设备本地语义记忆/并行代理/独立流式输出/Git 历史聚合） | 产品面（IDE/工具），无方法独点 → 不落 |
| 6 | n8n Production Playbook | Structured Output Parser（schema 约束+不符合 retry/error handling）；Error recovery（retry/fallback model/provider/human escalation）；pin data at Set node before inference（调试不烧 API credits）；Context→Inference→Action validation 三阶段 | Parser=r148-A 两态判据覆盖；error recovery=ED 错误处理覆盖；pin data=mts 成本面重叠>60%；三阶段=ED 管线面 → 不落 |
| 7 | Dify 深挖 | View cached variables（改缓存变量局部重测免全跑）；异常重试（retry_times/interval/timeout）；跨节点变量显式契约（源声明输出+全路径引用，漏声明静默消失）；对话变量状态管理 | cached variables=与 n8n pin data 同面（调试省重跑）且边缘；重试=ED 覆盖；跨节点契约=ED Agent 接口契约（r147 前落"输入格式固定/输出结构化/不可变中间产物"）覆盖 → 不落 |
| 8 | LangFlow 深挖 | Memory Base（向量化语义检索 vs Message History 时间序 vs 知识库人工填充三分工）；ConversationBuffer/Window/Summary/VectorStoreRetriever 类型；skills=reference library 比喻（按需加载） | 三检索分工=WB ctx r145-B 语义检索/SPLADE 覆盖；memory 类型=WB ctx 覆盖；reference library=r140-A 三级加载覆盖 → 不落 |
| 9 | MCP 安全 | OWASP MCP Cheat Sheet（mcp-scan 检测 poisoned tools/应用层签名/pin 工具定义哈希每次验证）；OAuth 2.1+PKCE 强制/mTLS/SSRF egress 白名单/敏感操作人机确认 | 企业级安全面（OAuth/mTLS/SSRF）；哈希 pin/poisoned tools=r147-B 注入出口+canary 面；人机确认=站 2 同点（ED guardian 覆盖）→ 不落 |
| 10 | WaytoAGI + deeplearning.ai | Astra 提示词课（控制 vs 放手）；提示词最简化（去作者/版本信息，防分类混淆）；新课程：Building Adaptive AI Agents（trajectory-to-skill learning/continual learning）、Evaluating AI Agents、Agent Skills with Anthropic、Building Data Agents（Measure Agent's GPA） | 控制vs放手=ED 约束块显式化面；最简化=mts 精简面；**trajectory-to-skill 与 mts §上下文演化工件（Generator 留全轨迹→Reflector 提炼教训→Curator 转 deltas）核心机制重叠>60% 判不装**；Agent GPA=WB skill-authoring 评测面 → 不落 |

## 二、落地结论
- 本批 0 个真独点：唯一候选 trajectory-to-skill 经 Grep 判重与 mts §439 行"上下文作为演化工件"（轨迹提炼教训→结构化增量更新）重叠>60%，核心机制已覆盖。
- 不做 idle commit 编造：逐站判非重复理由见上表。

## 三、功能套件检查
- mts/ED/pt/ctx 四件套均无需更新：无新方法输入。
- 垃圾清理：无临时文件、无 __pycache__ 产生。
