# 2026-09-22 学习轮 r122-A（延迟评分归因 + 评测校准 + 指纹与记忆整理角度 · 10 次实拉 · 落地 4 独点）

批前基准：WB r131 9 落点（5 文件，主动避开 ctx/ED）+ WB 给豆包 5 条建议落点 + r121 批 D71-D82 + D1-D70。

## 10 次实拉（逐站留痕）
1. Inngest：**durable step（step.run 持久化每一步结果，崩溃/冷启动恢复跳过已完成）**、**step.score() durable+memoized（延迟评分：结果先出信号后到，分数归到产生它的 run，重放不重复记分）**、standalone scorer（agent/run.completed 事件驱动，**agent 不知道自己在被评估**）
2. CrewAI fingerprinting：**指纹（UUID+时间戳，自动生成不可手动覆盖，内容改了指纹不变=身份与内容分离）**；Gen skill fingerprinting（**Skill ID 只随有意义内容变化，忽略时间戳/目录名/顺序/路径分隔符**）；content-addressed memory（同内容同 ID，二次写=touch 非重复）
3. 在线/离线评测：**离线=发布前可复现固定数据集 gate prompts/models/tools/retrievers/policies；在线=回答后采样流量，可用慢 judge**；trace-level evaluation（工具调用 spans 排序发 LLM judge 判 correct/incorrect）；evaluator 运行本身被 trace（judge 决策过程可审计）；judge 用 suppress_tracing 防污染
4. LLM judge 校准（强）：**judge 偏差（长度偏好/位置偏差/啰嗦偏好）→ 人类标注样本校准（50-100 条）**；**要求 judge 每条标准引证据**；**双独立参考校准（human + 不同模型家族独立 judge）+ 漂移监控（judge 与被评系统同谱系时重新校准）**；**部署前可靠性测试（<0.65 路由更强 judge 或 ensemble 多数投票）**；Anthropic：确定性 grader 优先、**别总检查精确工具顺序（过刚脆弱）**
5. 记忆去重/整合（强）：**Mem0 Dream 三态（新包含旧全部信息则旧标 merged 指向替换；过期标 superseded 不丢历史；高频行为摘要成高层记忆）**；**Infini Memory 事实冲突规则（仅 seq 不同保留新；矛盾保留新；用户来源优先于 AI 来源）**；Auto-Dreamer（离线整合器只读证据+有限检查）
6. 平台：n8n 2.40 force tool call（已见）/n8n Agents（agent 定义一次随处用）/task isolation sidecar/nobody user；Langflow 1.11 HITL checkpoint/A2A/AG-UI；Activepieces 审批日志——平台
7. HITL/checkpoint：**中断四组件（gate 拦截+checkpoint 保存+notification 通知+resume 重建，四缺一即坏）**；**中断点前已执行代码 resume 时重执行（副作用要幂等）**；run 是 store 行不是活进程
8. Prompt 2026：**instruction hierarchy（System>Developer>User>Tool，安全约束放 system/developer 防低优先输入覆盖）**；**instruction-first（任务指令第一行，长上下文后置，埋在第 2400 行=丢）**；长上下文标记（分段+语义标签+锚点）
9. GitHub 生态：deepseek-harness（Everything is a Plugin）、vercel/eve、smolvm、NVIDIA/OpenShell——生态
10. Skills 生态：**Agent Plugins 开放标准（OpenAI/MS/AWS/GitHub/Cursor/Vercel 达成，包装 Skills+MCP）**、Skills API GA、**@skills 协议（安装捆绑三功能 content/persistence/auto-triggering 分离，只有 auto-triggering 需要 prompt）**

## 落地（4 独点）
| 编号 | 落点文件 | 独点 | 来源 | 提升层 |
|---|---|---|---|---|
| D83 | av | 延迟评分与归因：结果先出信号后到，分数归到产生它的 run（step.score durable+memoized 重放不重复记分）；scorer 与 agent 解耦（被评者不知道被评） | Inngest agent-evals + WB r131 建议 1/2 | 可复用 Skill（评测归因） |
| D84 | av | 在线/离线评测分层 + judge 校准纪律：离线=发布前固定集可复现、在线=回答后采样流量慢 judge；judge 引证据/双独立参考校准/漂移监控/同谱系警惕/部署前可靠性测试 | slavadubrov + MS Learn + BabelJudge + Anthropic evals | 可复用 Skill（评测校准） |
| D85 | ctx | 指纹双型分离：身份指纹（UUID+创建时间，内容改不变）vs 内容指纹（hash，内容改就变）；规范化内容指纹忽略无关变化（时间戳/目录名/顺序/路径分隔符）；同内容同 ID 二次写=触达非重复 | CrewAI fingerprinting + Gen skill fingerprinting + content-addressed memory + WB r131 建议 3 | 工作流（记忆完整性） |
| D86 | ctx | 记忆整理三态：新包含旧全部信息→旧标 merged 指向替换；过期标 superseded 不丢历史；高频行为摘要成高层记忆；事实冲突规则（仅 seq 不同保留新/矛盾保留新/用户来源优先于 AI 来源） | Mem0 Dream + Infini Memory + WB r131 语境 | 工作流（记忆整合规则） |

## 判不落（逐条理由）
- 中断四组件+resume 重放：与 ED D58（HITL 审批精确 payload+幂等继续）重叠 >50%，只补完整性判据不补机制。
- 指令层级/指令前置/长上下文标记：与 ctx D61（SPEAR prompt 视图）+ D77（提示布局）相邻，机制重叠。
- Agent Plugins/@skills 协议/ecosystem 数字：生态非方法。
- durable step 本身：平台功能（与 D72 后台任务相关）。
- n8n Agents/task isolation/Langflow 1.11/Activepieces：平台功能。
- deepseek-harness/eve/smolvm/OpenShell：生态。
- trace-level LLM judge 轨迹评测：与 D79（轨迹回归三档）互补但 D84 已收 judge 校准，轨迹 judge 具体实现不另落。

## 功能套件检查
av 落 D83/D84（评测归因/校准）；ctx 落 D85/D86（指纹/记忆整理）；pt/mts 无新材料；三件套覆盖完整不凑数。

## 垃圾清理
零临时文件；保留：本留痕 + 被并入的 av/ctx。
