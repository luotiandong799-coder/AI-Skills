# 2026-09-20 学习轮 r109-B（第 2 轮 · 16 轮批）

来源（本轮 9 源实拉）：n8n.io changelog（dead，转搜索）+ n8n docs release-notes + megachangelog 2.40/2.39 + releasebot + kt.team + datapath + n8n community×2 + n8n blog（Gateway credits）。

## 独点：0 条（逐站实拉证据与判非重复理由如下）

**逐站实拉结果：**
1. n8n Agents（2.32.3 Preview）：一次创建处处使用+跨对话续接 → 与 r109-A §Agent 资源化（一处维护多端复用）同源判重。
2. AI Agent Tool node 主代理监督委派 specialists、多层嵌套 → 与已学 §多 Agent 协作纪律（orchestrator-worker/层级）同源判重。
3. AI Agent Node force tool call on first iteration → 与已学工具调用模式 50%（循环首轮控制，弱增量不落）。
4. per-principal sandbox isolation → 与 per-tool 最小权限 50% 判重。
5. Schedule Trigger "If Execution Is Missed" → 与定时任务记账判据 50%（漏跑处理，弱增量不落）。
6. Changes diff / workflow version endpoints → 版本审查手段，弱。
7. Gateway credits 免 key 计费 → 计费模式，弱。
8. 自动模型选择 → 弱。

## 版本
ed 保持 2.78.0。健康度：n8n 官网 changelog 连续 dead（转 docs 路径正常），未到删除门槛。
