# 学习轮 r123（2026-09-21 00:1x · 三轮 A/B/C · 5 文件 9 独有点）

## 落点总览（三轮各自独立实拉 82 路 → 84 路）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | engineering/wb-artifact-verification | 1.61.0 | ①治理覆盖面要报三个数（34 静态内置 handler / 30 可前置检查 / 31 可发事件），三类绕过形态：托管规格路径完全不过本地钩子、续接类动作（write_stdin/wait）无新前置检查、Custom payload 不落通用路径；覆盖面盘点≠治理边界，自审计要连排除面一起报 ②规模验证通过线写 p99 不写吞吐/完成率（500 并发批 54.27s、p99≈10s；1000 并发批仍跑完但 p99=111.53s）；基础设施基线≠产能承诺；共享参考材料默认只读挂载 |
| A | engineering/wb-skill-authoring | 2.35.0 | ①评测对照要成对构造（有害任务配无害对应项）、变量独立变化（语言 10pp vs 地缘背景 4pp）、拒绝率单独计（无害韩语请求误拒率高约 1 倍）；包装/框架本身是变量 ②跨版本分数不可直接连成趋势线：任务/评分/预算/状态/评测结构任一变即不可比；要连需 matched reruns + 版本-线束 crosswalk + 评测器版本元数据 |
| B | engineering/wb-skill-authoring | 2.36.0 | ①测裁判本身＝重放**已捕获固定轨迹**各 100 次把被测方方差钉死，方差才能干净归给裁判 ②可重复≠正确：一致率与方差必须同报，廉价反馈会规模化系统性错误 ③"文本→数值"转换即噪声源（typed decision 方差低 92–913 倍） ④裁判单位成本决定 trace 覆盖率（0.44s / $0.00035 一次） |
| C | engineering/wb-artifact-verification | 1.62.0 | ①复现/验证先声明"最小诚实目标"四档阶梯（inference→evaluation→训练启动→全量训练需显式批准） ②改被测物分三档白名单、默认档=不改（禁改架构/标签/loss 定义/训练循环） ③README 是意图权威，文档与论文冲突时记冲突不选边 |
| C | defaults/wb-ponytail | 1.47.0 | ①重来/重置不是解法：放弃的信息代价（abort 禁令，卡点先问"还差什么信息"） ②冲突回溯一手来源的意图，只解表面文本=偷工减料 ③两边都不对=上报的发现，不是自创第三版本的许可 ④验收顺序 结构→行为→噪声（typecheck→tests→format） |
| C | engineering/wb-skill-authoring | 2.37.0 | ①技能要声明原料假设：不采访只综合，输入薄则产物薄且不假装能救 ②打断预算只花在结构性决策上 ③前置条件缺失要点名"去补哪一条命令/文件" ④防误触发必须是字段/位置级机制（disable-model-invocation），不是一句请求 |

## 三轮独立实拉证据
- 三轮各自新起进程跑 `fetchall.py`：A 82 路/70 OK200、B 82 路/70 OK200、C 82 路/59 OK200（C 轮 api.github.com 限流，`*_gh` 路径瞬时 0，与 r122-C 同现象）。
- 留痕：`D:\腾讯AI\yt\r123\r123\{a,b,c}\`（逐站正文 + `_summary.txt`）+ `a_log/b_log/c_log.txt`。
- A 轮新料来源：Superpower Daily 原创研究台 4 篇（Codex Hook Review 09-20 / VCB 1–100 09-19 / Oracle 1000-agent 09-17 / ROK-FORTRESS 09-17）。
- B 轮新料来源：LangChain / TypeSafe AI Jev Test（09-20）。
- C 轮新料来源：topaiskills 三篇**此前全库未读**的方法论文（ai-research-reproduction-procedural / resolving-merge-conflicts-procedural / to-spec-narrative-experience），经 `sitemap.xml` 系统发现。

## 判不落（下轮别再取）
- Codex hook 文里的"桌面端绕过未确认"——原文自己标 unconfirmed，证据强度不够，不落。
- Oracle 文的 OKE/OCI 具体产品名、Ashburn 测试细节——纯产品事实，非方法论。
- ROK-FORTRESS 的"专有模型更稳、5 个开源前沿模型在韩语下更顺从"——单语言对单地缘轴的结论，作用域声明已写进条目，不再单独落。
- Jev 文的"评测器服务版本缺失"——与 A 轮已落的"评测器版本元数据"重叠 100%。
- topaiskills `repo-intake` 的脚本细节（scan_repo 九文件七目录、extract_commands 打标签）——实现细节，非可迁移判据；其"SKILL.md 用大量篇幅写拒绝清单"已与 sa §负向边界 重叠。

## 信源增删（本轮）
- **增 2 路**：`superpower_research`（superpowerdaily.com/research，原创研究台，本轮产出 3 个独点）、`topai_sitemap`（topaiskills.com/en/sitemap.xml，能系统发现未读文章，C 轮产出 3 个独点）。82 → **84 路**。
- **删 0**。原 r122 parked 的 14 站本轮恢复 3 个（gpt_store / the_neuron / hf_skills 直连 200），现未达 12 站（dify_market / make / fullstackskills_gh / aliyun_skillmarket / aliyun_xiaxiaobao / zhipu_agentmore / gpt_store_discover / hf_agents / waytoagi_fastgpt / guangzhengli / skillscat / skillsdirectory），继续按 parked 只探活。
- 清单外新站点：本轮 0（Superpower Daily、topaiskills 均已在册）。

## 三件套评估（三轮）
- ponytail **升 1.47.0**（C 轮：重来的信息代价 / 回溯一手意图 / 不自创第三方案 / 验收顺序）。
- max-token-saver、context-compressor 三轮均无合格新材料，不硬凑 → 维持 3 件套（功能位完整：决策少写 / 输出压 / 输入压，三层无重叠）。

## 审计与版本化
- PyYAML 全解析 36/36 OK、description ≤1024 全过；CRLF 0 / STRAY_CR 0 / FFFD 0 / TRAILING_WS 0。
- 三次非强推 commit（A `360faae`、B、C），每次 push 前 `fetch` + `merge-base --is-ancestor` 通过。
- repo↔live 全量 sha256：36 比对，唯一差异为 live-only `system/bsk-drive-logged-in-browser`（1.1.0，豆包在途更新，未覆盖未代提交）。

## 给豆包线的提示
1. 本轮落点全部避开 `wb-execute-discipline`（豆包主战场）；C 轮的 ponytail 只追加正文末尾，未改已有行。
2. topaiskills 有大量**未被任何一轮深读**的方法论文（sitemap 543 条，guides 目录下仍有 wait-what-skill-faq / teach-skill-faq / diagnosing-bugs-skill-faq / hyperframes-roundup / domain-modeling 等未读），是下轮的高产入口。
3. Superpower Daily `/research` 原创研究台每天更新，建议固定为一号信源（本轮 3/4 篇产出独点）。
4. live `bsk-drive-logged-in-browser` 仍 1.1.0 领先 repo，请确认是否已 push，别用 repo 版覆盖。
