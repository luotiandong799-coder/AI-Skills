# r134-C 全站实拉 · LLM 节点前确定性输入守卫落地 ED

> 日期：2026-09-22｜批次：r134-C｜模式：定时任务「AI技能库学习·每时3轮×10次」触发

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | Dify（agentlist.top/dify-hosting/blog） | Condition 节点当守卫（LLM 前校验输入避免浪费 token）；Iteration 优于 Loop；Code 节点做数据清洗；RAG 元数据过滤+按主题分离库；chunk 500-1000/overlap 50-100 | **D-dify-inputguard：LLM 节点前确定性输入守卫** |
| 2 | n8n（blog error handling+community） | 节点级重试配置；Never Error toggle；结构化输出失败回环 AI 带错重试（2-3 次）；错误在工具节点内处理不在 agent 全局；agent 只决策、HTTP 节点执行+continue on fail | 错误处理架构（ED 重试分类/非原子失败已覆盖） |
| 3 | LangFlow（1.11/1.12/Policies） | HITL 暂停+stateful checkpoint；**Policies：自然语言业务规则编译成确定性 guards 包在工具外（调用前检查）** | Policies=guardian pattern（ctx 已覆盖） |
| 4 | Activepieces（changelog/blog） | Agents 可命名/可聊/可复用；agent 知道何时暂停等审批；AP_FLOW_TIMEOUT_SECONDS 统一超时预算 | 平台形态/运维细节 |
| 5 | Pipedream（connect/docs） | Managed auth（托管 OAuth/token 加密/scoped per project）；**external_user_id 每 agent 独立身份独立 token** | per-agent OAuth（r134-A 已判：ED 权限衰减覆盖） |
| 6 | Anthropic（best practices/14 patterns） | description 具体含关键词；SKILL.md<500 行；progressive disclosure；三层结构 detail tier 不自动加载；"Start with evaluation 先找缺口再建 skill" | sa 已覆盖（eval-first 已落 r129-B；按需加载 ctx 已覆盖） |
| 7 | Make（best practices/agent SQL） | 失败模式表（自信误分类/结构化丢字段）；SQL 错误：瞬时连接→重试 3 次、确定性错误→回环模型单次修复；子 scenario 分担长格式化 | 2×2 失败分类（ED 已覆盖）；编排（已覆盖） |
| 8 | 腾讯 SkillHub/腾讯云（36kr/API 文档） | SkillPay 技能支付（2026-07-16）；全球 AI Skill 近 30 万、日均新增 1300+；SkillVersion MD5 校验 API | 平台商业/生态事实 |
| 9 | HF（hf.co/telemetry 争议/36kr） | huggingface_hub agent 遥测争议；MiniCPM5-2B（2B 端侧 agent）；Open Computer Agent（免费云 Linux VM）；ML Intern agent | 模型/平台事实 |
| 10 | deeplearning.ai（corporate/community） | Agentic AI 课程（degrees of autonomy/planning with code）；新课程 Building Adaptive AI Agents（trajectory-to-skill）；Evaluating AI Agents；Document AI | trajectory-to-skill（dl 自改进已覆盖）；课程信号 |

## 二、判重与落地
- **D-dify-inputguard LLM 节点前确定性输入守卫（落 · ED）**：ED 已有 ①§工具结果断言层（调用后哨兵）②§执行前语义契约验证（rubric 打分）③§Agent 工具面安全 guardian（执行前验证）——但**"工作流 LLM 节点前挂确定性条件节点拦截无效输入、防浪费 token"**是输入侧闸门，与上述三层各管一段不重叠（输入守卫=入口，断言=出口，契约 rubric=契约满足度，guardian=计划动作验证）→ 落（工作流/token 成本层）。
- n8n 错误处理/LangFlow Policies/Make 失败分类：ED §工具失败 2×2 分类学、§重试分两类、§非原子失败已覆盖 → 不落。
- Anthropic best practices（description/500 行/渐进披露/三层结构）：sa（wb-skill-authoring）已覆盖；eval-first 已落 r129-B → 不落。
- per-agent OAuth 隔离：r134-A 已判重不落（ED 权限衰减）→ 维持。
- SkillPay/遥测争议/MiniCPM/Open Computer Agent/课程信号：平台/模型/生态事实 → 不落。

## 三、功能套件检查（每轮）
- 套件：wb-ponytail / wb-max-token-saver / wb-context-compressor
- 本轮：ED 新增 LLM 节点前输入守卫（工作流输入校验/token 成本），与 wb-max-token-saver 成本层分工（mts 管"输出侧按需"，本条管"输入侧拦截无效"），三件套无需增删。
