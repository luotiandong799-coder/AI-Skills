# r135-B 全站实拉 · handoff 边界上下文蒸馏落地 ED

> 日期：2026-09-22｜批次：r135-B（手动触发）｜模式：AI技能库学习批

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | Dify（CSDN/media/stepcodex） | rerank 阈值调优实测（Recall@10 随阈值升高下降、MRR 最敏感阈值 0.6→0.65 降 12.7%、NDCG@10 在 0.4~0.6 最稳定适合作锚点）；hybrid 权重向量 0.7/BM25 0.3 起调 A/B 优化 | RAG 调优参数（平台实测数据） |
| 2 | LangFlow（docs/blog 1.10/1.11） | knowledge base 多向量后端（Chroma/OpenSearch/pgvector）；1.11 多向量检索（ColBERT late interaction + ColPali 视觉文档检索） | 平台能力事实 |
| 3 | HF smolagents（PyPI/deepwiki） | 1.26：Remote WasmExecutor 移除（远程执行器漏洞修补）、Exa 搜索接入；代码 agent（actions=Python snippets，省 30% token） | 平台/安全事实 |
| 4 | deeplearning.ai（corporate Evaluating AI Agents） | 按组件选 evaluator（code/LLM-judge/人工）；从 trace 造测试样例；convergence score（高效步数内响应）；**reflection 修当次 run vs evaluation 是可复用打分器抓跨运行回归**；error analysis 按阶段归因预算 | 评测方法论（av/WB dl 已覆盖） |
| 5 | OpenClaw（concepts/memory） | MEMORY.md 长期/每日日志/会话转录三层；Honcho 记忆服务（user modeling）；token-limit 策略 | ctx 已覆盖（记忆分级/两级沉淀） |
| 6 | AWS Well-Architected / LangChain / BlckAlpaca（handoff） | **交接摘要对象替代完整历史（任务规格+相关事实+约束）；边界上下文蒸馏（小模型提取最小充分上下文）；版本化 schema 拒绝畸形交接；子 agent 结果 typed schema 回传（never pipe full transcript）；显式教委托五要素** | **D-handoff-distill：handoff 边界上下文蒸馏** |
| 7 | Pipedream/Activepieces（changelog/首页） | Activepieces Agents 命名/对话/复用；Pipedream 2500+ 集成（并入 Workday）；NL 生成 workflow 代码可检查 | 平台事实（r134 已判 Agents） |
| 8 | prompt engineering 结构化输出（explainx/sureprompts/next-gen） | 三层验证（语法/schema/业务）；缺失数据行为策略（required fallback/nullable/optional）；repair only formatting never meaning | av 结构化三指标/WB r130A 已覆盖 |
| 9 | 阿里/智谱（云栖大会/aliyun/ModelScope） | 千问 Personal Agent（3 亿用户）；Qwen3.7 rerank/embedding；PAI Token 统一配置；云 Agent Plugin 搜索安装 | 平台/企业级事实 |
| 10 | ModelScope（AgentFabric/框架） | 0 代码创建 Agent；ModelScope-Agent 框架（记忆控制/工具模块） | 平台事实 |

## 二、判重与落地
- **D-handoff-distill handoff 边界上下文蒸馏（落 · ED）**：ED §多 Agent 协作纪律已覆盖"接口契约（typed schema 输出/不可变中间产物）"，但**"交接边界用小模型蒸馏最小充分上下文 + 版本化 schema 拒绝畸形交接 + 永不回传子 agent 完整 transcript + 委托五要素显式声明"**是交接时刻消息形态的增量 → 落（工作流/多 Agent 交接）。
- RAG 阈值实测 / LangFlow 多向量 / smolagents / OpenClaw 记忆 / 结构化输出三层：已覆盖或平台绑定 → 不落。
- 千问 Personal Agent / PAI Token / ModelScope：企业级/平台罗列 → 不落。

## 三、功能套件检查
- 套件：wb-ponytail / wb-max-token-saver / wb-context-compressor
- 本轮：ED 新增 handoff 蒸馏（交接上下文成本），与 ctx（压缩时机/规则重声明）分工：ctx 管会话内压缩，本条管多 Agent 交接边界压缩 → 三件套无需增删。
