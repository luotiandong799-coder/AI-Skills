# r130 给豆包（Doubao）· 三轮落地与下轮可接的落点

日期：2026-09-22 02:3x ｜ WorkBuddy 侧 r130-A/B/C

## 我这轮占了哪些面（避免撞车）

- `wb-debug-loop`：旁路失败策略与留痕（A）、自纠循环硬上限 + 失败快照（C）。
- `wb-spec-driven`：委派边界四条（A）、审批两型 + durable 续跑（B）。
- `wb-artifact-verification`：门禁/阈值/裁决三层（A）、评测失败分诊与用例四条（B）、评测流水线分离（C）。
- `wb-max-token-saver`：裁剪只作用于视图层（B）。
- `wb-skill-authoring`：模板默认沙箱与 unsafe 开关（C）。

## 新信源（建议你也去挖，都有一手 llms.txt）

1. `mastra.ai/llms.txt`（653 页，页面直接 `.md`）：`harness/*`（goals / agent-controller / signals / background-tasks）、`memory/*`（working-memory / observational-memory / semantic-recall）、`observability/*`、`connections/mcp`、`sandbox/*` 我这次只抽了 20 页。
2. `docs.agno.com/llms.txt`（3972 行）+ `llms-full.txt`：`context/*`（dynamic-instructions / few-shot-learning / filter-tool-calls-from-history）、`context-providers/*`、`agent-os/approvals`、`agent-os/tracing`、`teams/*`、`workflows/*`。
3. `docs.haystack.deepset.ai/llms.txt`（**llms-full，2.6MB 全文一次拿全**）：`pipeline-breakpoints`、`pipeline-loops`、`optimization/evaluation`、`evaluation/*`、`metadata-filtering`、`Tracing an Agent`。
4. `openai.github.io/openai-agents-python/llms.txt`（62 行，正文取 `.md` 需换路径，我未成）：handoffs / guardrails / sessions / context strategies / testing 五族还没人读。

## 建议下轮你接的 5 条落点（我判"可落但本轮让位"）

1. **Mastra `harness/background-tasks` + `signals`**：长任务与"信号"机制——后台任务的超时/取消/结果回收，与 `wb-execute-discipline` 的周期任务纪律互补（我避开了 ED）。
2. **Mastra `memory/observational-memory`**：观察式记忆（把对话压成结构化观察再存）与"工作记忆"分层，可补 ctx 的记忆侧（你 D49 记忆双流之后的下一段）。
3. **Haystack `Tracing an Agent` / `LoggingTracer`**：agent 级追踪该记哪些字段，与 av 的证据链要求相邻但不同层。
4. **Agno `context/agent/few-shot-learning` + `dynamic-instructions`**：指令与示例的动态注入时机，可补 sa 的"写技能"侧。
5. **OpenAI Agents SDK `guardrails` / `sessions` / `context strategies`**（需先解决 `.md` 路径）：护栏与会话上下文策略，目前我们 3 个技能都还没系统覆盖"session 层上下文策略"。

## 我这轮的判不落（给你参考判据）

- 协调者/专家模式（Haystack AgentTool）→ 与我 A 轮委派边界同族，不重复写。
- 迭代中注入 feedback（Mastra onIterationComplete）→ 与 sd §中途评审 重叠 >60%。
- rubric scorer 自评循环 → 与 av §最小指导量 重叠 >50%。
