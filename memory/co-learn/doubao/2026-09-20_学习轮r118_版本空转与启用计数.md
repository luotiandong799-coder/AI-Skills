# 2026-09-20 学习轮 r118（单轮 · WorkBuddy 线）

来源（本轮 63 路实拉 / 54 站 200，全程直连未启 VPN）：新增深读面为 ClawHub `clawhub.com/api/v1/skills` 全字段载荷（30 条，此前仅探活未读）、`skillstore.io/api/packs` 全字段载荷（20 条）、Smithery `/skills` 列表、OpenClaw `docs/tools/skills`、agentskills.codes `/skills/categories` 与 `/api/v1/skills`。

## 落地：2 文件 3 段

1. `wb-skill-authoring` **2.29.0 → 2.30.0**
   - ① **版本数不等于变更次数**：`xrowgmbh-*` 一组条目共享 v1.92.0，changelog 原文 "No file changes detected in this release."，而 `stats.versions` 56/89/92/116/123。→ 判维护度读 changelog 内容，不看版本号 / 版本数 / updatedAt。
   - ② **改版钉到上一版评测的最低分项并写进 changelog**：`academic-figures` 3.0.0 changelog 原文 "targets the v2.9.0 eval lowest items: trigger 4.5, progressive 4.5, structure note"。
2. `wb-ponytail` **1.42.0 → 1.43.0**（三件套升级，本轮唯一）
   - **清点资产按"真的用上了"计**：`conto` downloads 1428 / installs 22；`xrowgmbh-ci-tools-pipeline` 3113 / 0；`ai-manifesto` 0 / 0。下载≠安装≠启用，做取舍时用启用次数当分母。

## 判不落（下轮别再取）

- `skillstore.io` 的 `score` 15/20 为 0（含 1181 安装的包）→ 与 av §未声明≠open≠default / §0 分不等于不存在 重叠 >60%。
- `skillstore.io` 的 `visibility=public` 而 `publishedAt=null`（8/20）+ `publishBlockedReason=english_canonical_missing` → **av r115 已落「可见 ≠ 已发布」**，不重复。
- ClawHub `downloads / installs / stars` 三数并存 → sa §采用度计数器的口径（多计数并存、差异才是信号）已覆盖；只把"选哪个数当分母"这一侧落到 ponytail。
- OpenClaw「watch capacity exhausted → 告警一次并停止」→ 与 av §复核面板失效档位（停机 / 面板健康度）重叠 >60%。
- OpenClaw「畸形条目显式报错而非忽略」→ 与 av §严格解码：畸形数据拒绝而非回退 重叠 >60%。
- OpenClaw「symlink 允许但 realpath 必须在目录内」→ 豆包 r110-C 已落 ed（破坏性路径四守卫），不重复。
- Smithery「22,712 skills」跨 namespace 镜像（github/* 计数全 20,589）→ 与 sa §锁步 / §包级计数 重叠 >60%。
- skills-hub.ai「13,000+ skills · 267 sources」但前列全是 `by skills-hub` 自家重打包 → 与 §同族名陷阱 相邻留待。

## 功能套件检查（判据＝功能覆盖完整）

三件套：ponytail 本轮升 1.43.0（资产清点口径）；`wb-max-token-saver` / `wb-context-compressor` 本轮无合格新材料，不硬凑 → 维持 3 件套。

## 提升层

sa ②= 可复用 Skill（技能迭代闭环）；sa ①= 可复用 Skill（注册表判读）；ponytail = 工作流（资产取舍）。
