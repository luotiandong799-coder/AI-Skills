# 2026-09-21 学习轮 r128（三轮 A/B/C · 4 文件 8 独点 · 已推送）

批前前置：远端领先（豆包 r113–r116 已落 D4–D25），本地 `reset --hard FETCH_HEAD`(=99f7b50) 快进，落点避开 `wb-execute-discipline` 与 D13–D25 已占面。

## 三轮独立实拉（各自新进程，非复用）
- A：90 路 / **57 OK200**；B：90 路 / **65 OK200**；C：90 路 / **66 OK200**。
  未达站三轮不一致（A 未达 activepieces/clawhub_api，B/C 达；B 未达 activepieces/agentrank/smithery，A/C 或达或否）→ 证明确为独立重拉。
- 未达主因：代理 `127.0.0.1:33210` 拒绝连接（WinError 10061）+ 部分站直连超时；`gpt_store` 直连/代理均失败（已开 Edge 之外的两条通道核验）。

## 深拉主源（三轮各不相同）
- **A：OpenClaw 官方 docs（llms.txt 索引 265 页中筛 14 页 .md 实拉）** —— `cli/security` / `cli/approvals` / `clawhub/skill-format` / `automation/taskflow` / `cli/doctor/recovery`。
- **B：LangChain 官方 docs（新增源页）** —— `deepagents/fault-tolerance` / `human-in-the-loop` / `permissions` / `context-engineering` / `memory` / `rubric` + `langgraph/thinking-in-langgraph` / `interrupts` / `persistence` / `errors/*`。
- **C：Promptfoo 官方 docs（新信源首读）** —— `docs/red-team/` + `docs/configuration/expected-outputs/`（deterministic / model-graded / agent-rubric）。

## 落地（4 文件 8 独点）
| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | defaults/wb-context-compressor | 3.31.0 | 审计/体检的默认执行面必须冷于被审对象（`security audit` 默认不加载插件运行时，`--deep` 才开） |
| A | engineering/wb-skill-authoring | 2.43.0 | ①前置条件两级声明（requires.env 硬前置 vs envVars 可选）②共享状态写入带预期修订，陈旧即拒 |
| A | engineering/wb-artifact-verification | 1.66.0 | 标识符有歧义即拒绝，不得择一执行（附：紧凑视图后必跟无损全文块） |
| B | engineering/wb-debug-loop | 1.23.0 | 错误处置四档（瞬时/模型可自愈/需人/意外）+ 接不住就冒泡 + 重试按调用对象收窄 |
| B | defaults/wb-context-compressor | 3.32.0 | ①限制必须绑真正执行通道（路径白名单挡不住 shell）②拒绝必须走 reject 专用通道（用"回复"拒绝会被模型当成功） |
| C | engineering/wb-skill-authoring | 2.44.0 | 安全审查重点按架构形态分（chatbot / RAG / agent 三类固有弱点不同） |
| C | engineering/wb-artifact-verification | 1.67.0 | 复用的判定带指纹：strategy+plugin+assertion+input/output 全匹配才复用，否则重判 |

## 判不落（去重证据）
- OpenClaw `cli/doctor/recovery`「探测失败≠证明」→ av 已有 §控制查询 / §未检测是一等状态，重叠 >60%。
- DeepAgents「未用工具 schema 常驻」→ ctx 触发词已有「工具定义与输出是常驻固定开销」。
- 「memory 常驻注入无渐进披露」→ ctx §上下文分层 +「可检索不等于该常驻」已覆盖。
- promptfoo「judge temperature=0 / 显式 judge」→ sa §指标有噪声 + §评测器版本元数据已覆盖。
- AWS agentic / microsoft_foundry / freeCodeCamp 首页 → 营销页与产品清单，无技能系统可复用方法论。

## 三件套评估
`wb-context-compressor` 连升两级（3.30.0→3.31.0→3.32.0，占 3 点）；`wb-ponytail` / `wb-max-token-saver` 三轮均无合格新材料，不硬凑 → **维持 3 件套**（功能位判据，不凑数）。

## 版本化
3 次非强推 push（SSH over 443、无 VPN、不重试、不补推）：`08243d9`(A) → `758352c`(B) → `353323b`(C)，每次 push 前 `fetch` + `merge-base --is-ancestor` 均 OK。cp 回 live 后全量 sha256：4 个改动文件 repo/live 一致；CONTENT_DIFF 1 = `wb-execute-discipline`（豆包在途，未覆盖未代提交）；ONLY_LIVE 4 均为市场/第三方技能。
