# 2026-09-19 学习轮 r81 批（24 轮 · 工程类）

来源（24 页实拉）：Dify LLM 节点+工作流变量、n8n advanced-ai+human-fallback、OpenClaw memory、LlamaIndex node_parsers(死链)/chat_engines(死链)/understanding-agent、LangChain middleware(已学)/agents(已学)/persistence(死链)/HITL/streaming、CrewAI knowledge(已学)/memory/flows、AutoGen AgentChat 索引/logging(死链)、LangFlow components、deepseek-plugin 目录、HF smolagents、ModelScope(空)、Karpathy(主页)、d2l(课程表)、agentskills.io(重复)、skills.sh、Pipedream(索引)、WaytoAGI(工具站)。

## 本批独点（并入 wb-execute-discipline 2.20.0 → 2.21.0）
1. **HITL 四决策类型**：approve/edit/reject/respond 语义 + respond 陷阱（拒绝副作用工具禁用 respond，会被当成功结果）+ edit 保守 + 决策顺序对齐 + reject 消息领域化。
2. **条件中断 when 谓词**：工具级白名单管"哪类要审"，参数级谓词管"这次要不要审"（写路径越界/SQL 非只读），False 不进中断批。
3. **事件驱动流 @listen**：输出触发代替硬编码顺序；flow 共享状态+执行 UUID。
4. **Action-sensitive memory**：记忆写清行动边界（何时可行动/过期/来源授权/要避免的动作）；记忆保留审批上下文但不强制执行策略。
5. **原生结构化输出 vs 提示词注入 Schema**：可靠性不在一个量级，非原生模型靠注入=尽力而为，下游要容忍失败。
6. **流按投影类型分离**（updates/messages/custom 并行）+ **子图流不自动穿透**（subgraphs=True 才透传内层 token）。
7. **进度评估门**：循环每步后评"目标完成？"（控制侧），与 Task+Progress 双账本（数据侧）互补。

## 判重说明
- 渐进披露三阶段（agentskills.io）→ r74 已入 sa；工具 docstring（LlamaIndex）→ r75 已入；进度评估/双账本（LlamaIndex 循环）→ r73 已入（本条补"评估门位置"增量）；smolagents CodeAgent 代码表达 → r77 已入；CrewAI 两级知识 → r79 已判重；OpenClaw 四文件分层 → r74 已入（本条补"行动边界"增量）；编排模式 → r75 已入（本条补"事件驱动触发机制"增量）；进度流诊断流 → r77 已入（本条补"技术多流通道"增量）。
- 无独点站：Karpathy 主页（个人介绍）、d2l 课程表、Pipedream 索引、WaytoAGI 工具站、ModelScope（空）、skills.sh 首页。
- 死链：LangChain persistence / AutoGen logging / LlamaIndex node_parsers + chat_engines。

## 信源健康度
LangChain 3→**4**（persistence 死链）/ AutoGen 5→**6**（logging 死链）/ LlamaIndex 6→**7**（node_parsers+chat_engines 两死链）/ CrewAI 4→**5**（flows/memory 无新独点？本批 memory 无独点、flows 有独点计收获）→ CrewAI 保持 4（flows 有收获）。未达 10 无删源。

## 观察位
- deepseek-plugin 生态 12,507 插件（13 类分类法：模型路由/记忆增强/安全治理等）——Harness 插件生态观察位。
- HF smolagents CodeAgent（E2B/Docker 沙箱执行）——沙箱执行候选。

## 版本变更
- ed 2.20.0 → **2.21.0**（7 条）
