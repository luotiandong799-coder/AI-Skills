# 2026-09-19 学习轮 r97 批（103 轮 · 工程类 · 30 次第 2 波）

来源（本波 4 页）：OpenClaw compaction（大丰收）、Dify variable-assigner、HF smolagents inspect_runs(OTel)、n8n advanced-ai 索引。

## 本批独点：4 条（已并 ed 2.34.0）
1. **压缩完整性：切分点保工具调用对 + 不透明标识符 strict 保留**（OpenClaw compaction）：split point 不许切断 tool call/toolResult 配对；路径/id/精确串照抄不让模型规范化。→ §压缩完整性。
2. **摘要质量门：不合格拒绝压缩**（OpenClaw safeguard）：必需标题/待办 ask/精确标识符要留在摘要里；全部纠正失败则 compaction 停手不写 transcript，兜底是"不压"。→ §摘要质量门。
3. **溢出恢复：压缩续跑，已完成动作不重放**（OpenClaw overflow recovery）：压缩后从已 settle 工具结果续跑；取消≠回滚（已完成压缩仍记账）。→ §上下文溢出恢复。
4. **Agent 运行全链 trace：OpenTelemetry 插桩**（smolagents inspect_runs）：多步 agent 不可预测，生产靠事后 trace（Phoenix/Langfuse）不靠实时 console；Instrumentor 零侵入一行注册。→ §Agent 运行全链 trace。

## 判重/无收获
- Dify variable-assigner：会话变量 vs 工作流变量/数组追加构建记忆——状态变量分层已学判重。
- n8n advanced-ai 索引：纯目录页，无独点；**发现未拉子页**：Light evaluations / Metric-based evaluations / HITL for tool calls / What is memory / What is a tool（下批候选）。

## 信源健康度
LangChain **10（删）** / LlamaIndex **10（删）** / AutoGen **10（删）** / OpenClaw **3** / n8n **4** / CrewAI **4** / Dify（死链 3）/ smolagents **1** / LangFlow **1** / Pipedream 无收获 1 / WaytoAGI 无收获 1。

## 版本变更
- wb-execute-discipline **2.33.0 → 2.34.0**（+4 条，本轮）。
