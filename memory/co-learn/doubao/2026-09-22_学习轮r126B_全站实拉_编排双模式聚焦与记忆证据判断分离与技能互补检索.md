# 2026-09-22 学习轮 r126-B（编排双模式聚焦 · 记忆证据判断分离 · 技能互补检索 · 10 次实拉 · 落地 3 独点）

批前基准：WB r134/r135 批 + r124 D115-D137 + r125 A/B/C D138-D161 + r126-A D162/D164/D165/D166。B 角度=Agent 编排/工具/工作流/安全/技能检索侧。

## 10 次实拉（逐站留痕）
1. 多 Agent 编排（Velocity/QuantumFlow/padiso/MetaCTO/nasscom/Focused/agentcorps/NovaKit/BestHub/shogo）：**handoff 两错：传太少（只传最终结果丢推理与不确定性）vs 传太多（12K 全会话）**；角色重叠冲突+共享状态争用；circuit breaker 防 A→B→C→A 无限循环（30% 问题）；supervisor 与 orchestrator-worker 差异（supervisor 每步后留在循环可自适应）；orchestrator 计划错误→worker 忠实执行（验证计划再派发）；worker 无状态+幂等，orchestrator 拥有记忆
2. 函数调用可靠性（ChenKai/StochasticSandbox/DEV/AI-TLDR/richards/Bitsfolio/NeuralBase/llmversus/asoasis）：**schema drift（工具返回形状变但 schema 没更新→模型按旧 schema 请求→opaque 失败；fix=dispatcher 校验输出+报 version-skew；更佳=从工具实现生成 schema（Pydantic 反射））**；结构化输出 API 级强制；retry limit per tool+per conversation（3 次停止）；失败返回结构化错误字符串非异常；validate-before-execute；schema 校验 2-5% 静默损坏率需重试
3. 工作流错误处理（agent-works/MetaCTO/WillSpurlock/Microsoft/dev.n8n/XBSTACK/ILMTEC/ThinkBot）：幂等键+状态变更动作类型清单（send_email/create_ticket 等集合判断）；支付状态不明挂起人工对账；retry budget+fallback+circuit breaker；LLM 当不可靠依赖处理（校验/只重试可恢复/安全回退）
4. 上下文工程（arXiv C4/Focus/Anthropic/LangChain/agentpatterns/arXiv DACS/Acon）：**C4 配置（last 5 tool calls + summarization window=3）91.6% vs 纯剪枝 79.0%（+12.6pp）**；**DACS 编排双模式：Registry 模式只持 ≤200 token/agent 状态摘要→agent 发 SteeringRequest→Focus 模式注入其全上下文压缩他者**；源代码压缩反例（minify 切 42% token 但 SWE-bench 50%→38% -12pp）；LangChain 阈值触发压缩（offload 先→summarization 后）
5. 记忆架构（arXiv 记忆综述/Redis/Oracle/NVIDIA NemoClaw/AgeMem/CSDN）：Pattern A/B/C（monolithic/context+retrieval store/…）；**NemoClaw self model：Markdown 存派生知识+SQLite ledger 存义务/排名/纠正/审计事件——证据与判断分离**；AgeMem 记忆操作暴露为工具（store/retrieve/update/summarize/discard agent 自主）；read-before-reasoning write-after-acting
6. 安全（OWASP/CodeGuilds/Zylos/Datalaria/ClawGuard/TigerGate）：**sandbox 减少权限提示 84%+消除跨工具污染（Claude Code bubblewrap/Seatbelt）**；ClawGuard 用户确认规则集+工具调用边界拦截；OWASP Agent Memory Guard（memory poisoning）；skill 权限 manifest（过度授权被下游注入武器化）
7. 技能检索（arXiv SkillFlow/SkillRouter/AgentSkillOS/SkillStreaming/Graph-of-Skills/Milvus）：**SkillFlow 四阶段（dense→两轮 cross-encoder rerank→LLM 选择）36K SKILL.md**；**AgentSkillOS：Skill Tree 层级组织 coarse-to-fine + Complementarity-Aware Retrieval（选互补技能集而非 embedding 近重复）+ DAG 编排**；**SkillStreaming：70% 余弦相似度+30% SupaScore 质量先验重排**；SkillRouter 1.2B 74% Hit@1（13× 少参数 5.8× 快）；Hermes 学习循环用 hybrid search 连起跨会话改述
8. 平台（Langflow/n8n/n8n 报告）：**n8n 2.37 Anthropic prompt caching（系统 prompt/工具定义/历史复用，TTL 5min/1h，默认关）**；n8n Agents（定义一次到处用）；Langflow 1.10 Memory bases；n8n 报告工具对比表（n8n 72% codability）
9. deeplearning.ai（Agentic AI 课程/CrewAI 课程）：四设计模式+skills 章节（开放标准格式创建可复用技能）；**CrewAI A2A 协议（agent-to-agent 通信标准）**；LangGraph 记忆课程
10. Anthropic Skills 官方（Claude platform docs/best practices/完整指南）：**frontmatter 规则：name ≤64 字符小写+连字符+禁 XML 标签+禁保留词（anthropic/claude）；description ≤1024 必填**；trigger phrases 写法（描述含用户实际会说的话）；audit logging；预置办公套件（powerpoint/excel/word/pdf）；开放标准 2025-10 发布 12 月开源

## 落地（3 独点；初判 4 点，双键检索剔除 1 点）
| 编号 | 落点文件 | 独点 | 来源 | 提升层 |
|---|---|---|---|---|
| D168 | ED | 编排器双模式聚焦：Registry 模式只持 ≤200 token/agent 状态摘要（保持响应）→agent 发 SteeringRequest→Focus 模式注入其全上下文压缩他者（多 Agent 大系统不常驻全上下文） | arXiv 2604.07911 DACS | 工作流（编排） |
| D169 | ctx | 记忆证据与判断分离：派生知识（self model）存 Markdown+义务/排名/纠正/审计事件存结构化 ledger——证据与判断两套存储防判断污染证据 | NVIDIA NemoClaw | 工具（记忆） |
| D170 | sa | 互补性感知技能检索（选互补技能集覆盖任务不同侧面，而非 embedding 近重复）+质量先验加权重排（70% 相似度+30% 质量先验，质量是查询无关先验） | AgentSkillOS + SkillStreaming | 可复用 Skill（检索） |

## 复核剔除（双键检索命中既有落点）
- D167 schema drift 检测/从实现生成：sa/ED L1821 已落"工具 schema 版本化：registry 管理+schema_version+破坏性变更判定+加性默认"——防 drift 方法已覆盖，从实现生成为弱增量，不装。

## 判不落（其余，逐条理由）
- handoff 两错/传不确定性：ED D147 已落 handoff 最小上下文（传结果不传全会话+显式携带目标未完成）——不装。
- orchestrator 计划验证/worker 幂等：ED §多 Agent 协作已落——不装。
- 幂等键/状态变更清单：ED §重试分两类已落写操作幂等——不装。
- 支付状态不明挂起：ED 已落写操作幂等相邻——不装。
- C4 配置量化：ctx 已落压缩策略（按任务状态压）——不装。
- 源代码压缩反例：mts 已落"不省的部分：验证一步不省"——不装。
- sandbox 84%/ClawGuard：ctx §Agent 安全纵深已落 guardian/per-tool 权限——不装。
- memory poisoning：ctx 已落（OWASP Agent Memory Guard）——不装。
- SkillFlow 四阶段：ctx §检索管线已落 RRF——不装。
- n8n prompt caching：ctx 已落 stable-prefix 缓存（L1365）——不装。
- A2A 协议：平台标准事实，个人用户不投入——不装。
- frontmatter 规则/trigger phrases：sa 已落（D153/D160 相邻）——不装。

## 功能套件检查
ctx 落 D169（记忆证据判断分离）；三件套覆盖完整。ED 落 D168（编排双模式）。与 WB 新 commit、r124、r125、r126-A 无重叠。

## 垃圾清理
零临时文件；保留：本留痕 + 被并入的 ED/ctx/sa。
