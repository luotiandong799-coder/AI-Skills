# r129-C 全站实拉 · 不可变前缀/自改进三阶段/记忆完整性/输出过滤/运行时组合落地 · AgentCard三态判重

> 日期：2026-09-22｜批次：r129-C｜模式：定时任务「AI技能库学习·每时3轮×10次」触发

## 一、逐站实拉（10 次，全量信源覆盖）
| # | 信源 | 实拉内容 | 提炼 |
|---|---|---|---|
| 1 | arXiv/AgentPatterns/LangChain/tRPC | prompt caching | 不可变前缀模式；token 计数前置；缓存 TTL |
| 2 | Self-Harness/Hermes/Llama Stack/Agentic Flywheel | 自我改进 | 三阶段闭环；确定性自改进；review fork；窄白名单 |
| 3 | A2A v1.0/MCP | 协议 | Agent Card 名片；MCP 管工具 A2A 管 agent |
| 4 | OWASP Memory Guard/workos/iternal | 安全 | 记忆哈希基线+快照回滚+来源标签+写前验证；输出过滤 |
| 5 | AgentProg/zircote/DeepThink | 工作流 | STP DSL 控制流剪枝；动态工作流脚本只回传最终值 |
| 6 | DeepSeek Harness/Hermes/freeCodeCamp | 生态 | Everything is a Plugin；三层记忆+自改进 |
| 7 | OpenClaw skill workshop/docs | 技能流程 | 不可变 revision+stale 冲突；apply/reject/quarantine 三态 |
| 8 | AWS/Copilot Studio/Blck Alpaca | 子代理 | scopedRequest；写入单线程；typed 结果回传 |
| 9 | Dify marketplace/n8n/Upstash | 平台 | 800+ 插件市场；平台功能 |
| 10 | AgentAssay/AgentPatterns/zylos | 验证 | 三值回归判定；确定性校验先于 LLM；金集管理 |

## 二、判重与落地
- **D221 不可变前缀+token 计数前置（落 · mts）**：mts 不可变前缀 0/byte-identical 0/token 计数 0；稳定前缀布局+append 不 rewrite+发送前数 token 为独有增量 → 落。
- **D223 Harness 自改进三阶段（落 · dl）**：dl held-out 0/失败模式聚类 0；聚类→绑定失败机制的最小修改→留出验证+确定性 N 次触发为独有增量 → 落。
- **D224 后台 review fork（落 · dl）**：dl review fork 0/白名单 0/迭代计数器 0；答案交付后静默复盘+窄工具白名单只许动记忆技能为独有增量 → 落。
- **D226 记忆完整性防护（落 · ctx）**：ctx 哈希基线 0/来源标签 0/写前验证 0；哈希基线+快照回滚+provenance+写前验证组合为独有增量 → 落。
- **D227 输出过滤与内容标记（落 · av）**：av 输出过滤 0/秘密 0；确定性响应扫描最后防线+datamarking 输入标记为独有增量 → 落。
- **D228 任务程序 DSL+动态工作流脚本（落 · ED）**：ED DSL 0/动态工作流 0/控制流剪枝 0；控制流显式化+按流剪枝+只有最终值进上下文为独有增量 → 落。
- **D229 组件插件化（落 · ED，并入 D228）**：ED 插件化 0；everything is a plugin 显式依赖连接为独有增量 → 合并入 ED 运行时组合小节。
- **D232 作用域控制输入+写入单线程（落 · ED）**：ED scoped 0/单线程 0/碎片化 0；scopedRequest 防未答复请求干扰+并行思考串行写入为独有增量 → 落。
- **D225 Agent Card（不落）**：ED Agent Card 3 处已覆盖（子代理身份/接口契约）→ 重叠。
- **D230 技能不可变修订（不落）**：sa 不可变修订 1/stale 1 已覆盖 → 重叠。
- **D231 quarantine 三态（不落）**：sa 隔离 10 已覆盖隔离概念，三态为平台流程 → 不落。
- **D233 三值回归（不落）**：av INCONCLUSIVE 10 已覆盖三态分级；仅顺序分析为细分 → 不落。

## 三、逐站判非重复理由（未落地站点）
- A2A/MCP：协议层（平台）→ 淘汰；Agent Card 已覆盖。
- 平台类（Dify marketplace/n8n/Upstash/LangFlow）：平台功能 → 淘汰。
- 金集管理（人工评审驱动）：sa 已落示例集评测集不相交 → 重叠。
- 生产评估三档：av 已落影子评测 r128-A → 重叠。

## 四、功能套件检查（每轮）
- 套件：wb-ponytail / wb-max-token-saver / wb-context-compressor
- 本轮：mts 新增不可变前缀（成本布局），ctx 新增记忆完整性防护（安全）；三件套覆盖完整，无需增删。
