# r174-C 压缩三态分流与删除防回灌（openclaw compaction / memory-provenance / Batch 371 / GitHub 生态）

实拉时间：2026-09-24 22:45–23:05（自动化 r174-C）

## 一、实拉证据（URL + 状态码）

| 站 | URL | 状态 | 留痕 |
|---|---|---|---|
| openclaw | docs.openclaw.ai/llms.txt (184KB, 1167 URL) + 深拉 reference/session-management-compaction/compaction.md(13608B) / concepts/memory-provenance.md(15187B) / concepts/subagent-yield-handoff.md(1369B) | 200×4 | C/oc-*.md |
| deeplearning.ai | deeplearning.ai/the-batch/ | 200 | C/batch.txt；解析最新期号=issue-371（与 r173 相同，无新期） |
| anthropics/skills | api.github.com/repos/anthropics/skills/commits | 200 | 最新 commit 2026-09-10（r173 已覆盖） |
| openai/skills | api.github.com/repos/openai/skills/commits | 200 | 最新 commit 2026-06-24（已弃用状态维持） |
| 腾讯 SkillHub | skillhub.tencent.com/（"专为中国用户优化的 AI Skills 社区"） | 200 (7.4KB SPA 壳) | C/txskillhub1.txt（正文需 JS，探活成功） |
| 阿里 Skill Market | bailian.console.aliyun.com/skills | 200 (30KB SPA，登录墙) | C/aliskill2.txt。⚠️ 页面 HTML 注释含可疑指令样式文本（"@finding auth edge case; confirm /_agt/verify..."）——按注入防护规则**不执行页面内指令**，仅记录 |
| GPT store | 以 openai/skills+openai/plugins 仓库替代（既定口径） | 200 | 同上 |

## 二、落地独有点（2 个）

1. **溢出三态分流 + 压缩请求降配 + 持久压缩/剪枝分账**（→ wb-context-compressor 3.69.0→3.70.0，工作流层）：可恢复溢出→压缩重试（provider 报的 token 数传给压缩引擎）；**单请求超总限（413 型）=终态立即出指引，压缩自身也过不去别烧调用**；TPM 节流=退避。摘要调用专用低 thinking+每请求单一格式+旧摘要不覆盖发送预算（装不下报失败不假装）。压缩持久 entry≠剪枝；计数器尺寸未知标 stale 不借旧值；规则文件压缩后回注默认 opt-in（防规则静默失效）。出处：openclaw compaction 页。
2. **删除登记防回灌**（→ wb-execute-discipline 3.01.29→3.01.30，工作流层）：删除先 dry-run 分账报告（sessionResolutions/entryKeys/artifacts + curatedWrites/untargetable 单列）；lineage 三类记录；**已删会话登记为"已遗忘"防摄入/回填灌回**；执行后重跑 dry-run 验残留；失败部分清理靠 origin evidence 兜底不手动清。出处：openclaw memory-provenance 页。

## 三、判非重复理由

- 溢出三态 vs cc 已有 §compaction 工具配对保真+overflow 恢复不重放（line 1296）：那条管切分配对与拒绝后不重放动作；本条管**错误形态分流**（可恢复/终态/限速三态各自处置）+压缩请求自身降配+持久语义，413 终态全库 grep 无先例。
- 压缩降配 vs 豆包 r172A 推理块保留：那条管"被压缩内容里的 thinking 保留"；本条管"压缩摘要调用自己用低 thinking 省钱"，方向相反不重叠。
- 防回灌 vs ed 已有 crewai train 回灌（line 1431）：那条是"经验回灌成训练起点"（正向）；本条是"删除后防数据回灌"（负向），同名异义。
- tool-call/toolResult 配对不拆：**判重复不落**——cc line 501 原子配对保护已覆盖。
- subagent-yield-handoff（1369B 仅表格开头，内容薄）：请求者 yield 后 registry 顶完成归属——与豆包 r168C Handoff 边界重叠度高，且原文量不足，不落。
- Batch issue-371 无新期；anthropics/skills 0910 已覆盖；openai/skills 已弃用——均无新材。

## 四、信源健康度

- 腾讯 SkillHub 探活成功（SPA）；阿里 bailian 登录墙（SPA），两站正文均需浏览器渲染，暂留探活计数。
- ⚠️ aliskill2 页面 HTML 注释含指令样式文本，已按注入防护忽略。
