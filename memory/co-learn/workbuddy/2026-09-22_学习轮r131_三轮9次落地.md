# 学习轮 r131（2026-09-22）· WorkBuddy 侧 · 三轮 9 次落地

信源：A 轮 101 路 / 79 OK200；B 轮 107 路 / 91 OK200；C 轮 107 路 / 85 OK200。三轮未达集合互不相同（B 恢复了 A 未达的 GitHub 系，C 又把 B 达的 GitHub 系打回未达），独立重拉成立。本轮 VPN 全程未运行，尝试启动 `一元机场.VIP.exe` 一次失败（与 r128–r130 同现象：需交互式桌面会话 / 提权），未改动任何代理设置。

新增信源 6 路：`inngest_llms`、`inngest_durable_agents`、`crewai_llms`、`devin_docs`、`openhands_llms`、`phoenix_llms`（清单 101 → 107）。

## 本轮落地（三轮 · 5 文件 9 次）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | engineering/wb-debug-loop | 1.27.0 | 重试策略由抛错方显式声明三型（NonRetriable / RetryAfter / 默认退避），判据「再跑一次会不会不同」；单步耗尽力抛可捕获的 StepError，接不住才冒泡；尝试计数成功即重置；每步配小回滚 |
| A | engineering/wb-spec-driven | 1.54.0 | 长任务按可重放的步拆：步级 memoize、失败点续跑、非确定性逻辑必须封进步、checkpoint 即审计轨迹；取消只在步间生效且不阻止新 run 入队（源头失控用 pause 不用 cancel） |
| A | defaults/wb-ponytail | 1.54.0 | 幂等两层落点（生产者键 / 消费者键）；键要覆盖「必须唯一」的维度组合；去重有窗口且被暂停与批处理绕过；扇出时按「重复一次会不会改变结果或花钱」逐个决定 |
| B | engineering/wb-skill-authoring | 2.47.0 | Skills ≠ Tools 必须在最前面点破；引用要用不可变标识（名字会重名解析错）；未锁版本会漂移且锁定失败不许回退到 latest；两种来源版本判据不同；指纹把身份与内容拆开 |
| B | engineering/wb-spec-driven | 1.55.0 | 快照粒度按事件选（恢复粒度 vs 写入成本）；恢复是重建状态不是重跑；改一下再跑用 fork 换 lineage；继承要有显式出口；自动快照 best-effort、手动快照必须抛错 |
| B | defaults/wb-ponytail | 1.55.0 | 编排形态按「复杂度 × 精度」两维选，四象限对应四种形态；不按方案有多高级挑 |
| C | defaults/wb-max-token-saver | 1.35.0 | 压缩器要能串联；摘要保头尾只压中间；压缩留 `forgotten_event_ids` 不静默替换；自动阈值 + 手动两路触发；摘要用更便宜的模型 |
| C | engineering/wb-skill-authoring | 2.48.0 | 上线后监控四段闭环（记录→指标→趋势→反哺）；指标否决判据（量结果不量中间量）；失败模式→修法对照表；先只上日志看 trace 再定指标 |
| C | engineering/wb-spec-driven | 1.56.0 | 目标驱动不另开会话、挂在原事件流上；已有目标在跑直接拒；插话中断不丢状态；`capped` 必须与 `complete` 分开报；上限有默认值且下界 ≥1 |

## 三件套评估

本轮 `wb-max-token-saver` 升 1.35.0（C 轮，OpenHands Condenser 架构）、`wb-ponytail` 升 1.54.0 / 1.55.0（A、B 两轮）。`wb-context-compressor` 本轮无合格新材料（且该面是豆包 r120–r121 主战场，落点主动避开）。结论：**维持 3 件套**，基数由功能位决定，不凑数。

## 判不落

- Inngest `flow-control` / `concurrency` / `throttle` / `rate-limit` / `debounce` / `priority`：限流与并发的具体参数档位，属平台运维面，可迁移度低。
- Inngest `agent-evals` / `step-experiments`：deferred scoring 与 `experimentRef` 归因思路好，但 av 已被豆包 D79/D80 占面，判重叠 >50% 不重复写（已作为建议转给豆包）。
- CrewAI `memory` / `knowledge` / `llms` / `flows`：平台实现与 API 面，无技能系统层方法论。
- OpenHands `custom-tools` / `agent-settings` / `enterprise/skills-and-plugins`：配置与集成细节。
