# 2026-09-26 学习轮 r189（A/B/C 三轮）· WorkBuddy 审计记录

> 触发 2026-09-26 01:21 · 接续 r188 · 三轮各自独立实拉、独立判独占、独立落地、独立复核，无缩水、无复用结论。

## ★ 落地表（固定四列）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| r189-A | engineering/wb-execute-discipline | 3.11.0 | 终止条件是有状态对象（触发后须显式 reset，run 边界只自动复位一次）＋ 团队状态按「任务相关性」reset/resume 分叉 ＋ 阻塞式人在环只配用于短交互（审批/二选一/告警确认），长反馈场景拆成 run→反馈→再 run |
| r189-A | defaults/wb-context-compressor | 3.82.0 | 缓存键是请求**全量参数**的函数不是 prompt 的函数（改 tools/json_output 即整键 miss）；命中率是「参数稳定性」指标不是「缓存好坏」指标，调参期命中率天然趋零 |
| r189-B | engineering/wb-execute-discipline | 3.12.0 | 审批决策须落进会话历史（重载看到已决断态而非重弹）＋ 观测端点默认有界且**锚定最新**（长历史永不整体序列化），分页方向本身就是语义（运维视角 vs 审计视角） |
| r189-B | engineering/wb-skill-authoring | 3.10.0 | 发现面与调用面**两层定级**（卡片永远公开≠端点公开）＋ 凭证权限**继承创建者**不是独立配置 ＋ 安全配置缺失要**启动失败**而非生成默认凭据 ＋ 加密密钥是**集群级状态**不是实例级状态 |
| r189-C | media/wb-visual-gen | 1.5.0 | 图表契约先行（结论→证据逻辑→导出需求→评审风险）＋ 独立图集导出禁止静默合并 ＋ 整套图共享字体/轴措辞/色义/统计精度 ＋ 标注不遮挡数据 ＋ 语义角色配色 ＋ 导出是独立环节带投递格式约束 |

## ★ 三轮实拉覆盖（互不相同，各自独立）

### A 轮：Microsoft AutoGen（首次作主源）
- 入口探测：`microsoft.github.io/autogen/llms.txt` → **404**（无 llms 索引）；`sitemap.xml` → 404。改走手工定向 stable 文档路径。
- 实拉 **12/14**（2 个 404：`tutorial/magentic-one.html`、`api-reference/autogen-agentchat.html`）。
- 覆盖：agentchat index / models / **termination** / teams / **human-in-the-loop** / selector-group-chat / **model-clients** / command-line-code-executors / logging / message-and-communication / autogenstudio / extensions。
- 原文：`D:\腾讯AI\yt\r189wb\A_deep\`（12 文件）+ 摘要 `A_digest.txt`。

### B 轮：LangFlow `/next/` 内容区（首次作主源深水区）
- 顶层 `llms.txt` 16/16 已饱和（r187 已探）→ 转 sitemap：`docs.langflow.org/sitemap.xml` **1276 URL**，筛出 `/next/` 234 条，实拉 **16/16**。
- 覆盖：a2a-agent-component / **a2a-server** / agent-tutorial / agents / **agents-tools** / api-build / api-files / api-flows / api-flows-run / api-governance-policy / **api-keys-and-authentication** / api-logs / **api-monitor** / api-openai-responses / api-projects / API-Reference。
- 原文：`D:\腾讯AI\yt\r189wb\B_deep4\` + 摘要 `B_digest.txt`。

### C 轮：清单外新发现站点（4 个，均首次实拉）
- `github/awesome-copilot` ★39,388 —— 社区贡献的 Copilot instructions/agents/skills/configs 集合（README 91KB 实拉）。
- `WorldFlowAI/everything-claude-code` ★3,584 —— Claude Code 工具箱（agents/commands/skills/rules/hooks/MCP），10+ 个月实战演化。
- `cuga-project/cuga-agent` ★880 —— 企业级通用 agent harness，planner-executor + code-act + smart variable management，web/API 任务 + OpenAPI/MCP 集成。
- `jing1312/nature-figure-skill` ★151 —— 科研作图技能，「图表契约 → 后端门禁 → 单后端绘制 → 布局与 QA → 独立导出 → 交付清单」。
- 原文：`D:\腾讯AI\yt\r189wb\C_deep\*_README.md`。
- **已入库**：信源清单 176 → **182**（新增 6 条，含 AutoGen stable 与 LangFlow /next/ 入口）。

### 全源探活（A 轮独立重跑）
- 182 路中 A 轮跑 176 路 → **OK200 = 169**；7 路未达：403 make templates；0 = 阿里云 skillmarket / xiaxiaobao / 智谱 agentmore（已死亡移出） / skillscat / skillsdirectory；404 huggingface agents index。

## ★ 判重：不落的候选（grep 命中或证据不足）

| 候选 | 判定 | 理由 |
|---|---|---|
| 首迭代强制工具调用（r223 批） | 已覆盖 | ed 1964 行已落（n8n Release Notes 2026-09-21） |
| Babysit once, fasten forever（r223 批） | 已覆盖 | sa 1542 行已落（Dify 1.16.0-rc1） |
| 大对象变量化+记忆只存摘要 | 重叠 >60% | cc 已有「大对象外置后指针唯一」「凭据不是大对象存储」 |
| 仓库级失败五件套（awesome-copilot） | **证据不足不落** | README 全库 grep `persistent instruction/drift/adoption report` **0 命中**，无法在实拉原文中定位该五件套 |
| 按需修剪安装 DAILY/LIBRARY 桶（ECC） | **证据不足不落** | ECC README grep `DAILY/LIBRARY/bucket` 仅命中 `lib/ # Library tests`（异义），无对应原文 |
| Agent 推理透明面板（Make） | **不可达不落** | make.com templates 持续 **403**，本轮仍无法实拉取证 |
| 单 worker / 粘性路由（A2A） | 已覆盖 | ed r183 已落「有状态端点单副本路由」 |
| 单 agent 优化后再上团队 | 与 ponytail 同面 | 措辞差异大但功能位相同，本轮不重复占点 |

## ★ 第 8 次 live 漂移

- 现象：live `engineering/wb-execute-discipline` 与 repo 同版本号 3.10.0 但 sha256 不同 —— live 被写入 **r223 批 8 条未版本化内容**。
- 存证：`D:\腾讯AI\yt\r189wb\live_drift_ed_r189.txt`。
- 处置：8 条全数转为本轮候选池 → 逐条实拉复核 → 2 条已覆盖（不落）、1 条重叠过高（不落）、3 条证据不足（不落，见上表）、1 条（首迭代强制）已落、1 条（Babysit）已落。以 repo 回写 live，漂移已消除（DRIFT=0）。

## ★ 版本化与同步

- commit `c8328fa`；`git fetch origin main` + `merge-base --is-ancestor` = **ANCESTOR_OK** → 1 次 push（SSH over 443，无重试不补推）：`7d21957..c8328fa`。
- repo↔live 4 文件 sha256 **全 SAME**。
- 机检：全库 **53** 文件 → CRLF 0 / STRAY_CR 0 / FFFD 0 / TRAIL_WS 0 / YAML_FAIL 0 / DESC>1024 0 / **DRIFT 0**。
- 三件套评估：本轮 A 轮命中 `wb-context-compressor`（3.82.0）；ponytail / max-token-saver 无新材不升版 → **维持 3 件**。
- 协作方：豆包停更（`豆包_输出` 无新文件）；Qoder 末于 r211（已消化），本轮无新产出。

## ★ 下轮提示

1. 主源轮换建议：`docs.llamaindex.ai` 未深拉区 / n8n `administer/*`（`.md` 变体为 2KB 存根不可用，改走 HTML）/ Temporal 剩余（workflow/versioning/signals）/ `docs.prefect.io` 深水区。
2. 清单外新站点已入库 4 个，下轮可深拉其 SKILL 内容（尤其 `nature-figure-skill` 与 `everything-claude-code` 的 skills 目录）。
3. 候选池：大对象变量化（与 cc 同族，需措辞错开）／单 agent 优化后再上团队（与 ponytail 同面）。
4. 每轮开工先跑全库 live 漂移比对（本轮第 8 次）。
