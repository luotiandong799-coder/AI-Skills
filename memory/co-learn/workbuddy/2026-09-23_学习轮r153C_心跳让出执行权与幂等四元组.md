# 2026-09-23 学习轮 r153-C · WorkBuddy 审计

> 本轮主题：**心跳要靠让出执行权 / 幂等键的四元组身份 / 覆盖型开关退役顺序与版本一致性路线**
> 主源：trigger.dev 官方 `trigger.dev/docs`（清单内 165 路之一，此前从未深拉；`llms.txt` 129/130 页实拉，含 `ai-chat/patterns/*` 十九页）
> 执行线：WorkBuddy（WB）。

## 一、实拉证据（独立进程，第三个独立进程）

- 全量：`fetchall.py c` → **165 路 / 142 OK200 / 23 未达**（未达集合与 A、B **不同**，多出 make_templates / openclaw_gh / langchain_gh / openai_agents_gh / autogen_gh / metagpt_gh / agentfootprint / e2b_artifacts / sim_studio / deepwiki_mcp_python 等 10 项 → 佐证三次独立进程）。
- 主源深拉：`https://trigger.dev/docs/llms.txt` → **129/130 页全 200**，原文落 `yt/r153wb/deep_c/`。

| # | 页面 | 关键原文（英文原样） |
|---|---|---|
| 1 | `runs/heartbeats.md` | `We send a heartbeat from your task to the platform every 30 seconds. If we don't receive a heartbeat within 5 minutes, we mark the run as stalled and stop it`；`Code that blocks the event loop for too long ... can prevent heartbeats from being sent`；修正动作＝`use heartbeats.yield() inside the loop` |
| 2 | `runs/max-duration.md` | `maxDuration` 比的是**CPU 时间**，`does not include time spent waiting`（`wait.for` / `triggerAndWait` / `batchTriggerAndWait`）；`exceeds the maxDuration` 时 `the lifecycle functions cleanup, onSuccess, and onFailure will not be called` |
| 3 | `queue-concurrency.md` | 队列对象带 `concurrency.base` / `override` / `current` + `overriddenAt` / `overriddenBy`，并有独立 `resetConcurrencyLimit`；`You can pause a queue to prevent new runs from starting. Runs that are currently executing will continue to completion` |
| 4 | `idempotency.md` | `Same key + "run" scope in different parent runs = different hashes = both tasks run`；`Same key + "global" scope in different parent runs = same hash = only first task runs`；`Idempotency keys, even the ones scoped globally, are actually scoped to the task and the environment`；`If you use the same idempotency key for triggering different tasks, the tasks will not be idempotent`；外部重置 run/attempt 作用域键必须补 `parentRunId`（+`attemptNumber`）否则报错；重置只影响当前环境 |
| 5 | `deployment/version-skew-protection.md` | `TRIGGER_VERSION` 指向存在的版本时 `simply keeps winning`（两机制共存无中间坏状态）；`The moment it names a version that environment doesn't have, every trigger fails outright with a 422`；迁移第 4 步（移除变量）`is not optional, and don't do it before step 2`；对比表：原子部署＝每次发布两次部署/阻断上游/须关自动域名分配/仅生产环境 vs 版本偏斜保护＝一次部署/从不阻断/不改上游开关/覆盖生产+预发+预览，`None — the id is the contract` |

## 二、落地汇报（强制四列表格 · 轮 / 文件 / 版本 / 独有点）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| C | engineering/wb-debug-loop | **1.48.0** | 心跳靠「让出执行权」才发得出去（30s 发/5min 判停；紧循环饿死心跳被误判 stalled 并杀掉 → 修法是在循环里插显式 yield，**不是调大窗口**）；**越限终止会跳过全部收尾钩子**（「收尾一定会跑」是错假设）；**计量口径**：越限比的是 CPU 时间、等外部回调不算；「暂停」只挡新的、**不等于中止**；并发上限有基线/覆盖双层且覆盖留痕可一键回基线 |
| C | engineering/wb-spec-driven | **1.75.0** | **幂等键不是一串字符，是四元组**（字符串 + 作用域 + 任务 + 环境）：作用域决定哈希里还掺什么（run 含父运行 ID / attempt 还含尝试号 / global 不掺）→ 同 key 不同作用域行为**相反**；同 key 用于不同任务则**两边都不幂等且不报错**；重置是四元组上的精确定位，缺上下文报错、只管当前环境；**统一入口会让语义退化**（后端触发时所有作用域等同 global） |
| C | engineering/wb-release-maintain | **1.9.0** | **覆盖型配置的拆除顺序是硬约束**：`TRIGGER_VERSION` 指向存在的版本时持续获胜（两机制共存无中间态），一旦指向不存在的版本**每次触发直接 422**，被它压制的机制救不了 → 顺序＝①确认接管者生效 ②再删覆盖者；迁移完成与否看「现在是谁在赢」，不是「新的已部署」；**同一类一致性问题两条相反路线优先选「等待式」**（阻断式代价在耦合与失败面，等待式代价在延迟；`the id is the contract` ＝契约压在必然存在的东西上，消除「忘了配置」这个失败模式） |

## 三、逐点判重（双键：来源标识 + 概念关键词）

| 候选点 | 来源 | 判定 | 落点 |
|---|---|---|---|
| 心跳靠让出执行权、紧循环饿死心跳 | trigger.dev heartbeats | **新增**（av §判定「卡死/僵死」用心跳 + 期待进展事件对 管「用哪两类信号判活、窗口怎么容忍丢包」；本条管**心跳为什么发不出去以及怎么改代码修好**） | dl ✅ |
| 越限终止跳过收尾钩子 | trigger.dev max-duration | **新增**（「无论如何都要执行的清理不能挂在正常收尾钩子上」既有技能无对应；dl §收尾超时与干活超时是两个旋钮 管默认值取向，非钩子是否被调用） | dl ✅ |
| maxDuration 计 CPU 时间不含等待 | trigger.dev max-duration | **新增**（「等不算跑」的计量口径，此前无） | dl ✅ |
| 暂停只挡新 run、暂停≠中止 | trigger.dev queue-concurrency | **新增**（止血 vs 清场是两个动作，此前无） | dl ✅ |
| 幂等键作用域决定哈希与去重语义 | trigger.dev idempotency | **新增**（sd 只在 §非平凡判据 把「幂等」列为编译器无法验证的性质；本条给出**怎么写才无歧义**。rel §幂等键的并发语义 管并发覆盖 vs 顺序重放，与本条**身份构成**不同面） | sd ✅ |
| 覆盖型开关退役顺序 + 阻断式 vs 等待式 | trigger.dev version-skew-protection | **新增**（rel §版本是不可变快照、端点决定谁跟最新版 管漂移；§关键环境的准入闸门做成动作级硬约束 管门禁；本条管**部署期间跨系统版本错配怎么消、以及覆盖者怎么安全退场**） | rel ✅ |
| ai-chat/patterns/* 十九页（compaction / sub-agents / tool-result-auditing / trusted-edge-signals / version-upgrades 等） | trigger.dev ai-chat | **未深读**（本轮 llms.txt 仅按序抓取前 130 条，ai-chat 段落位于清单中部；**下轮首读优先级第一位**） | — |
| run metadata / realtime / wait-for-token | trigger.dev | **判不落**（平台实时面，与 av §异步结果对不上号 / cc §工具大返回 同面，重叠 >60%） | — |

## 四、功能套件检查（三件套）

- 本轮三件套覆盖：**C → 无**（三个落点都在 engineering 面）。三轮合计覆盖：A → cc、B → cc、C → 无；pt / mts 三轮均无合格新材料。
- 结论：**保持 pt + mts + cc 三件套不变**，不因「轮次要动三件套」而硬凑（用户 2026-09-18 已定：基数为功能服务，不凑数）。

## 五、垃圾清理

- 产物全落 D 盘 `yt/r153wb/`（land_c.py、deep_c 129 页、c 轮抓取、日志）；**C 盘无新增**。
