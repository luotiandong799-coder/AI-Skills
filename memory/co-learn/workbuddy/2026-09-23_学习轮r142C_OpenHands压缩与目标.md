# r142-C 学习记录（WorkBuddy 侧）

- 日期：2026-09-23｜轮次：r142-C（定时触发 · 三轮第 3 轮）
- 独立重拉：新进程 `fetchall.py r142/c` → **110 路 / 94 OK200 / 16 未达**（未达集合与 A、B 又互异：C 轮 `e2b_artifacts`/`sim_studio` 未达、`deepwiki_mcp_python` 被 GitHub 403 限流，A/B 轮均可；A 轮未达的 `make` 在 B 可达、C 又回落）→ 三轮未达集合互不相同，独立重拉成立。
- 主源：**OpenHands 官方 `docs.all-hands.dev/llms.txt`（46,920 字节 / 267 个 `.md` 页）** —— 首读深拉 24 页全 200：`sdk/arch/{condenser,llm,events,security,conversation,agent,agent-server,tool-system,mcp}`、`sdk/guides/{context-condenser,agent-server/*}`、`overview/skills/{creating,adding,repo,public}`、`usage/tips/prompting-best-practices`、`usage/agent-canvas/*`、`enterprise/conversations-and-sandboxes`。

## 落地（3 文件 3 独点）

| 文件 | 版本 | 独有点 | 提升层 |
|---|---|---|---|
| defaults/wb-context-compressor | 3.36.0 | 压缩三段结构（keep_first 头部不压 / 中间才摘要 / 最近不动）；触发按事件条数阈值不按 token；压缩的模型调用单独打 usage_id 记账 | 工作流（上下文压缩实现） |
| writing/wb-doc-writing | 1.10.0 | 文档详略由最小读者决定（自己/团队/公开三档）；正文量级锚点 1,500–2,000 词；动笔前范围四问 | 可复用 Skill（文档投入决策） |
| engineering/wb-spec-driven | 1.66.0 | 长期目标不 fork 历史（挂同一条事件流）；终态四档且 capped 是终态不可续；驱动状态与被驱动数据同存；插话先中断后追加；停止优雅（在途调用跑完） | 工作流（长期任务编排） |

## 判重不落（附理由）

- **UNKNOWN 是一等风险级 + `confirm_unknown` 独立开关 + 三档确认策略**：av 核心判据"判不出结果时'不确定'是一等判定，不得默认通过"已覆盖 >60% → 不落。
- **技能创建的范围/触发词/渐进披露/references 外置/祈使句写作**：sa §渐进披露 + §触发词设计 + §文件拆分 已全部覆盖 → 不落。
- **prompting best practices（具体/给位置/单特性≤100 行）**：sd §规约先行 + §任务自包含 + r132-A 已落 → 不落。
- **events 事件溯源 / tool-system / mcp**：平台实现与 API 面 → 不落。
- **agent-canvas llm-profiles / enterprise sandboxes**：产品与部署面 → 不落。

## 候选池（未落，下轮可接）

1. OpenHands `sdk/arch/security` 的 LLM 自评风险字段（`security_risk` 随动作参数产出、由分析器提取）→ 若 av 后续要做"不确定分级"可接。
2. OpenHands `sdk/arch/tool-system`（25KB，工具注册/执行/结果回灌）→ 可落 sa 工具面。
3. OpenHands `overview/skills/{repo,public}`（技能仓库级与组织级分发）→ 可落 sa 分发边界。
4. Haystack `pipeline-loops` 与 Haystack `optimization/evaluation/statistical` → 与 OpenHands 目标驱动四态可合成"循环任务的终止与验收"专题。

## 环境

- VPN 仍为用户自行开启（uniproxy 33210 监听、ProxyEnable=1）→ 三轮均未启未关，未改动代理设置。
- 落地前核验 sd / sa / doc-writing / ctx 四处 repo 与 live sha256 全 SAME，无并发未推内容；落点继续避开豆包主战场 `wb-execute-discipline` / `wb-max-token-saver` / `wb-ponytail`。
