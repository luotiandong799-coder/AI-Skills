# r124（2026-09-21 01:4x）WorkBuddy 线三轮学习 · 6 文件 14 独点

> 用户新规「每次触发连跑 3 轮」生效第二轮（首次为 r123）。三轮各自独立实拉 84 路、独立判独占性、独立落地、独立复核。

## 开工

- `fetch origin main` → 远端 `a3eff65` == 本地 HEAD，无 rebase。
- 豆包共学最新批仍为 **r111-A…P（全 16 轮 0 独点）**，无 r112+ 新内容 → 落点全部避开 `wb-execute-discipline`（豆包主战场，356KB+，拆分待办维持观察）。
- 三轮抓取：A 84/72 OK200、B 84/68、C 84/57（C 轮 `api.github.com` 限流致 `*_gh` 瞬时 0，与 r122-C / r123-C 同现象）。

## A 轮（2 文件 4 独点）

主源：topaiskills 未读方法论文 `diagnosing-bugs-skill-faq` / `wait-what-skill-faq` / `domain-modeling-procedural-guide` / `triage-backlog-narrative-experience`。

1. `wb-debug-loop` **1.20.0 → 1.21.0**
   - 复现不了就先把发生率抬高：**50% flake 可调试，1% 不可调试**；方向是抬高发生率（循环/并发/加压/时序干预），不是追干净复现。
   - **无回路不假设**：先有一条已跑过、能在该 bug 上变红的命令；建不出来就停下列出试过什么并点名要三样之一。
   - **假设必须带预测**（若 X 是原因，改 Y 会让 bug 消失或变糟）+ **3–5 条并列先展示再测**（防锚定）。
   - **探针唯一前缀 `[DEBUG-a4f2]` → 收尾一次 grep 清零**；一个断点胜十行日志；性能回归先基线再二分；**seam 太浅 = 假信心，没有 seam = 架构结论**；正确假设写进 commit。
2. `wb-skill-authoring` **2.37.0 → 2.38.0**
   - **技能正文长度本身是训练信号**：「Three lines is the design. A longer skill teaches the model that verbosity matters.」
   - **指令命名"输出"还是"状态"决定服从方式**：`be concise` 命名输出 → 模型裁词（更短且更不清楚）；`wait, you lost me` 命名听者状态 → 模型补上下文。
   - **被引用技能静默没加载的运行期信号＝目标产物零改动**；修法是在调用处显式点名（与既有「CRLF 破坏 frontmatter 导致静默不加载」分工：那条管解析，本条管编排层没加载）。
   - **未经评审的 agent 生成术语表比没有更糟**（"confident-sounding lore that later sessions treat as truth"）。

## B 轮（2 文件 6 独点）

主源：`repo-intake-and-plan-narrative-experience` / `teach-skill-faq` / `ai-debugging-skills-roundup-2026`。

3. `wb-artifact-verification` **1.62.0 → 1.63.0**
   - **盘点宁可无用也不填空**：无 README 记载的命令不许发明；micrograd 的主角一条没报出来，产出「honest and nearly useless」是规则按设计工作的结果。
   - **盘点器的信号形状要写进产物**（认目录还是认文件、认代码围栏还是行内 `$`），否则读者把"没报"读成"没有"。
   - **分类规则的偏置要替下游闸门挡路**（`train.py` 带 `--eval_iters` 仍算 training，是为防绕过训练授权门），**代价要显式披露**（`config/eval_gpt2.py` 在工具眼里是 training）。
   - **"可直接执行"判定要覆盖占位符全部书写形式**（只认尖括号 → 裸字面量 `--master_addr=123.456.123.456` 被判 `needs_substitution: false`，给出假证书）；**短前缀 `startswith` 要查词根碰撞**（`sh` 撞 `sherrith`）。
   - **两条评审轴并行且永不合并/重排**（Standards × Spec，独立子 agent 独立上下文；合成一份会让高分维度替低分维度背书）；**固定基线可被文档化标准覆盖**；**每条坏味道只是带标签的判断不是硬违规**；**同义反复断言**（期望值与实现同路径重算）等于没有断言。
   - **选项等长防格式泄题**：不看内容只看长度就能选对，说明测的是排版。
4. `wb-skill-authoring` **2.38.0 → 2.39.0**
   - **技能是一份契约**：把字节花在"我不做什么"上（repo-intake 的 SKILL.md 仅 2,123 字节，description 块即六条拒绝清单）；**输入契约 + 输出契约枚举到件数（五件）**；与 §负向边界 分工（那条管触发排除，本条管触发后承担什么）。
   - **明写"模型的参数化知识不是真值"**（`never trust your parametric knowledge`）：RESOURCES 未充实前模型的职责是找源，且**每条论断挂引用**；**"a bad mission is worse than no mission"**——缺目标就先问再产出。

## C 轮（3 文件 4 独点）

主源：`ask-matt-wayfinder-to-tickets-roundup` / `ai-agent-refactoring-experience`。

5. `wb-context-compressor` **3.28.0 → 3.29.0**（★三件套）
   - **上下文的连与断由"后一段是否建立在同一次思考上"决定**：用结论 → 可断开（做成自包含票据）；用推理过程 → 不许断。
   - **smart zone ≈150K tokens**，超出推理退化；**在最近的阶段边界压缩，不在退化状态里硬推**。
   - 把"连续 vs 清晰"这对真实权衡写出来。
6. `wb-spec-driven` **1.47.0 → 1.48.0**
   - **宽重构例外**：扇出全仓的机械改动（重命名共享符号）**任何垂直切片都落不了绿** → 拒绝硬塞，走 **expand（新旧并存）→ migrate（按爆炸半径分批，每批一张票，批到批 CI 绿）→ contract（无调用者后删旧）**。
   - **"绿"必须指名发生在哪一层**（每批绿 / 仅集成分支绿 / 仅最终 integrate-and-verify 票绿）；**分批按爆炸半径不按工作量**；**删除留到最后一张独立票**；**prefactoring 先行**。
7. `wb-ponytail` **1.47.0 → 1.48.0**（★三件套）
   - **删除键归人**：静态分析能证明"没找到调用者"，证明不了"没有调用者"——实测删掉 5 个"死代码"函数，2 个被 eval 式动态调用，生产静默影响 5% 用户，靠错误监控尖峰才发现。"make a human hit delete."
   - **改名是全仓操作**：按"React 惯例 on 前缀"改 `handleRowClick` → 该标识同时是下游 prop 名，未传播处三个组件静默坏，TypeScript 抓不到运行时不匹配。**every rename needs a full grep check**。
   - **给 agent 的自由度按作用域收，不按信任度收**：窄作用域产出明显更好。

## 判不落（下轮别再取）

- `llm-testing-evaluation-tools-roundup-2026`：8 个工具横评，DeepEval/LangSmith/Arize/W&B/Helix 均为产品清单；embedding drift、refusal rate 分别与 av/sa 已落内容相邻 → 无 ≥60% 独点。
- `ai-approval-workflow-experiment`：阈值分层授权（<$200 自动 / 其余人工）与 av §批准携带对象身份 + §授权按次签发 重叠；"规则要覆盖文化语境不只算术"（周五+公共假日=4 天覆盖缺口）为弱增量留待。
- `triage-backlog` 五态机 / `needs-info` 模板 / AI 产出自报身份 / 按领域概念搜索 → 分别与 ed、sa §追问、av §审计方归属披露、sa §先查同类 相邻，留待。
- `hyperframes` / `install-ai-agent-skills` / `lark-doc` / `how-to-build-ai-research-pipeline`：产品教程或安装说明，无方法论增量。
- `domain-modeling` 的 ADR 三门槛 + 术语表 `_Avoid_` 规则 → **wb-doc-writing §十一 已 100% 落地**（同源 mattpocock domain-modeling.md），勿重复。
- `wayfinder` 的 fog-of-war 判据 → **wb-spec-driven §七·21 已落地**（2026-09-17 同源），本轮二次确认，不重复。

## 版本化

- 三次非强推 push（SSH over 443，无 VPN、不重试）：`bdac56d`（A）→ `865e1b2`（B）→ `b2d6b1a`（C）；每次 push 前 `fetch` + `merge-base --is-ancestor` 均 OK。
- 审计：repo 36 / live 40，CRLF 0 / STRAY_CR 0 / FFFD 0 / TRAIL_WS 0 / PyYAML 36 全解析 / description ≤1024 全过。
- 终检 sha256：CONTENT_DIFF 1 = `system/bsk-drive-logged-in-browser`（豆包在途 1.1.0，未覆盖未代提交）；ONLY_LIVE 4 均为市场/第三方技能。

## 下轮提示

1. topaiskills guides 仍剩若干未读（`ai-agent-skills-faq`、`faq-installing-ai-agent-skills`、`repo-intake` 关联篇、`how-to-build-first-ai-automation-workflow`、`triage-backlog` 其他角度），用 sitemap 继续系统扫。
2. Superpower Daily `/research` 连续两轮无新文章（仍为 Codex Hook Review / VCB 1–100 / Jev Test / Oracle / ROK-FORTRESS），降到探活级。
3. 落点继续避开 `wb-execute-discipline`。
4. `git add` 只加指定文件勿 `-A`；远端被前推用 `rebase` 不用强推。
5. cp 回 live 后必跑全量 sha256。
