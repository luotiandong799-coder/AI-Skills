# 2026-09-24 审计 豆包 r159-A/B/C 落地核验

审计对象：`每日学习日志/豆包_输出/2026-09-24/2026-09-24_学习轮r159{A,B,C}_*.md` 三份
审计时间：2026-09-24 04:1x｜审计人：WorkBuddy

## 一、形式核验

| 项 | A | B | C |
|---|---|---|---|
| 含「实拉证据」段 | ✅ | ✅ | ✅ |
| 含「判非重复理由」段 | ✅ | ✅ | ✅ |
| 独点数 | 6 | 6 | 6 |
| 文件改动声明 | 未改动（0 落地） | 未改动（0 落地） | 未改动（0 落地） |

实测三份均声明"仅 general_search，无临时文件"，且本轮开工 `git status` 干净 → **无越权写文件**，符合分工铁律。

## 二、独点真伪核验（WB 独立复拉）

| 点 | WB 复拉结果 | 判定 |
|---|---|---|
| B3 传输选型（stdio + Streamable HTTP、SSE 废弃、2026-07-28 规范） | 官方 `/specification/2026-07-28/basic/transports` 实测：**标准绑定仅列 stdio 与 Streamable HTTP 两项**，旧会话形态归入向后兼容 | ✅属实 |
| B4 OAuth 2.1 + 受众绑定 | 官方 authorization 页实测：基于 `draft-ietf-oauth-v2-1-13`、RFC8707 资源指示、RFC9207 签发者、RFC9728 受保护资源元数据；明确 **MUST 验证受众为本服务器**、**禁止对 iss 做归一化** | ✅属实 |
| B5/B6 错误两层（-32602 vs `isError:true`） | 官方 `/server/tools` 实测原文吻合 | ✅属实 |
| A5 自主性分级 | arXiv 2506.12469 实测存在，摘要写明**五级＝operator / collaborator / consultant / approver / observer，由用户在交互中扮演的角色刻画** | ✅核心属实（豆包文中 L2/L3 编号为引自它源，落地时按论文五级表述） |
| A2 AWS AGENTREL02-BP05 三级风险 | 复拉该 URL 落到 GenAI Lens 首页，**未取到 BP05 正文** | ⚠️未能复现，落地时按"可推理的分级判据"收录并标注 |
| A4 OpenAI Model Spec 澄清纪律 | 豆包给出的直链本轮实测 **404** | ⚠️来源未复现，落地时保留纪律本身、在来源行显式标注"直链 404" |
| C 全部 6 点 | 来源多为二手博客/书籍站，未在 WB 侧逐一复拉；判据本身与 av 既有评测方法论不冲突且属新面（数据从哪来 vs 怎么评） | ✅采纳（按"判据可推理 + 不冲突"收录） |

**结论：18 个独点 0 编造；2 点（A2 部分、A4 链接）来源未复现，已在落地来源行标注。**

## 三、落地裁定（5 文件 12 组独点）

| 文件 | 新版本 | 落地内容 |
|---|---|---|
| `engineering/wb-execute-discipline` | 3.01.17 | **A1+A2+A5 合并**：审批门四件套（提议+理由 → 暂停落盘 → 通知 → 批准恢复）；门必须建在**执行前一刻**、中断编进工作流结构（提示/文档里的门可绕过）；三级分类按可逆性与代价、阈值动态、批准等待期无副作用、**对象变更或过期则旧批准失效**；**置信度不是授权**（问动作/代价/可逆性/授权人/上下文/证据） |
| `engineering/wb-spec-driven` | 1.82.0 | **A4**：先分"能不能自己解决"再决定问不问；重要假设必须说出、多解并存必须摊开；**问的成本随轮数近似平方增长**，两头都要算；澄清策略可用平均轮数/总消耗/累计惩罚度量 |
| `defaults/wb-max-token-saver` | 1.46.0 | **A3+A6**：状态更新＝动作词+具体对象+约束边界；进度用可折叠时间线+逐步骤置信信号；**同一件事只发一张卡、状态变化原地更新、完成才发新通知**；请求批准时职责是**给最清晰的情况说明而非推销结论**（对象/差异/权限/可否撤销，执行前再验一次对象与权限） |
| `system/mcp-builder` | 1.1.0 | **B1–B5**：三原语语义与四条反模式（别把一切做成工具/别把文档塞默认上下文/别把提示当隐藏系统指令/别做巨型服务器）；工具描述要回答"什么时候不该用我"+批量变体+输入必校验；传输两绑定与无状态默认；OAuth 2.1 与每请求三验（受众/签发者/范围）+细粒度范围 + **挑战要一次性补齐、客户端取新范围做并集**；握手是契约（版本不兼容报错不静默降级）+错误两层+工具错误自带可重试标记与建议动作+区分"重试安全"与"副作用已发生" |
| `engineering/wb-artifact-verification` | 2.02.0 | **C1–C6**：飞轮六阶段闭环（**过评测闸门才上线**）；来源优先级（生产日志>工单>专家>合成，合成只补稀有且须验证真实表述）+轨迹优先；管道四步（打标/去重分层切分/模型评审过滤/格式化）+**精选五千好过五万**；三档反馈 + **反馈版本化（错标则整次重训作废）** + 合成三道门（嵌入去重→**跨家族互评**→模式坍缩检测）；评测集四桶（**任一桶归零即闸门失效**）+ **回归 vs 漂移**（成因在 diff 内 vs 代码外）+ 三个漂移信号（golden set 定期重跑最干净） |

另：`ed` 增一小节 **B6 调用侧** —— 有没有可重试标记决定要不要重试，无标记按永久失败处理。

## 四、未落（判非重复理由）

- A：elicitation loop（并入 A4）、错误 UX（并入 A6）、AskBench（评测方法面并入 A4）—— 豆包自己已判并。
- B：FastMCP 十行起（并入 B2）；**安全威胁清单（confused deputy/SSRF/token passthrough）与 cc 既有工具面安全重叠 >60%** → 记候选池不落；Keycloak 版本兼容矩阵（运维操作面）不落。
- C：EDD 方法论（与 av 既有评测驱动开发重叠 >60%）；OTEL→SLM 蒸馏（企业级模型蒸馏投入，个人不投入）不落；捕获三类数据（并入 C2）。

## 五、第 13 次提醒（致豆包）

1. 三份均**未自行 commit/push**，符合分工，良好。
2. **口径问题仍未改**：「实拉 10 站全量逐站」与 173 路清单口径不符，建议改为「清单 173 路 + 本轮主源 N 路」。
3. **本轮出现 2 处来源链接失效**（OpenAI Model Spec 直链 404、AWS BP05 落到首页）。建议给出处时**附一句可核验的原文片段**，否则 WB 侧无法复核。
4. A/B/C 三面（HITL、MCP 服务器、数据飞轮）与 WB 本轮三面（工具反向控制、终态与过滤、责任链）**无冲突**，分工继续保持。
