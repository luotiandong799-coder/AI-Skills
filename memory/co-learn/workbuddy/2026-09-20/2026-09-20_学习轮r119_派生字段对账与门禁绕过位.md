# 2026-09-20 学习轮 r119（WorkBuddy 线 · 单轮）

信源：71 路探测 / 60 站 200（全程直连未启 VPN）。本轮新增探测面：`glama.ai/mcp/servers`（89,683 MCP servers 注册表）、`claudeskillhub.com`、`glama.ai`、`VoltAgent/awesome-agent-skills`、`agentskills.codes/skills/categories` 首次深读、`skillstore.io` 单 pack 详情首次深读、OpenClaw `docs/tools/skills`「Revisions / Gating」首次深读。

## 独点（3 文件 6 段）

1. **wb-skill-authoring 2.30.0 → 2.31.0**
   - 派生字段必须能被源字段或源端点复算：skillstore 详情 `lastUpdatedDays: 0` 而 `updatedAt` 停在两个月前；同一 pack 列表 1181 / 详情 876；正例 glama「相对量 + 绝对 UTC」并列。
   - 分类视图是全集的子集：agentskills.codes 总量 19,296，九类之和 11,518，约 40%（7,778 条）无分类 → 只逛分类页看不到。
   - 什么变化算一版要有正负清单：OpenClaw 修订哈希纳入路径/内容/大小/可执行位，排除分享/归属/ZIP 时间戳/归档顺序；只改 helper 脚本也算改版；陈旧编辑报冲突不覆盖。

2. **wb-artifact-verification 1.56.0 → 1.57.0**
   - 写了依赖声明 ≠ 依赖被校验过：`metadata.openclaw.requires`（bins/env/config/os 四类独立）+ `always: true` 可整体绕过；「Enablement does not bypass … prerequisites」。
   - 同一入口两条路径要过同一套校验：`command-dispatch: tool` 绕过模型直达工具、`command-arg-mode: raw` 原参直传；模型触发与用户触发是两个开关。

3. **wb-ponytail 1.43.0 → 1.44.0**（本轮三件套升级项）
   - 库里有 ≠ 能用：新会话最多选 64 个技能，personal 优先 + 稳定 ID 序，超限落选 ≠ 不合格；容量上限要写成数字。

## 本轮额外修复（非学习，属审计发现）

`wb-skill-authoring` 与 `wb-ponytail` 的 frontmatter 是 `description: >-内容…`（折叠标量与首行内容同行），PyYAML 解析报 "while scanning a block scalar" —— **HEAD 版本即已存在，非本轮引入**。已把首行内容移到 `>-` 的下一行，两个文件 frontmatter 现在均可解析（desc 长度 2879 / 1669）。全库 36 个 SKILL.md 现在只剩 `research/user-centric-research` 缺 name（用户自建技能，已知）。

## 判不落（下轮别再取）

- skillstore `reviewedBy: "automation"` + `reviewIssueUrl` → 与 av §审计方归属要披露 相邻留待。
- `usageGuideRaw` vs `usageGuide` 双份 + `usageGuidePolishedAt` → 与 sa §双 URL 溯源 重叠 >60%。
- ClawHub `moderation.verdict: clean` 的 reasonCodes 只有 `review.llm_review` → 与 sa §扫描过替代不了扫到了什么 同源。
- glama facet 计数不可相加（Remote 37,638 / Python 37,603 … 总和 ≫ 89,683）→ 与 sa §锁步/包级计数 重叠 >60%。
- OpenClaw「会话保留已选修订、新版本不自动生效」→ 与 ed §技能钉版本 重叠 >60%。
- OpenClaw「节点技能掉线即从清单消失」→ 与 sa §路由指向幽灵 重叠 >50%。

## 结论

sa 与 ponytail 的两个 frontmatter 缺陷修好后，默认技能集的元数据可被正常解析；本轮主源从"统计数字"转向"派生值与门配置"，落点全部避开豆包本轮（r111 批 16 轮 0 独点，ed 保持 2.94.0）。
