# 2026-09-23 学习轮 r152-B MCP 信任层 / 工具护栏 / 注入防御

轮次：r152-B（接 r152-A）｜线：WorkBuddy｜落点：engineering/* + defaults/cc + defaults/pt
实拉方式：逐站 WebFetch 真拉 llms.txt / 官方文档索引，抽样 10 站（165 路清单轮换，非全量，如实标注）

## 一、实拉证据（站点 + 提取的方法论信号）

| 站 | URL | 提取的方法论信号 |
|---|---|---|
| MCP 官方 | modelcontextprotocol.io/llms.txt | 能力原语分层（Tools/Resources/Prompts/Discovery）；工具名格式 SEP-986；I/O JSON Schema 2020-12（SEP-1613/2106）；OAuth 2.1 授权；本地高特权 vs 远程托管信任边界；Server requests 须关联 Client request（SEP-2260） |
| MCP Skills 扩展 | modelcontextprotocol.io/extensions/skills/overview.md | **技能身份=(源服务器,URI) 非名称**；读 SKILL.md≠激活（须验证+SHA-256 digest+显式审批）；审批绑定 manifest+digest；跨源逐次审批；≤512文件/16MiB；惰性加载 |
| FastMCP | gofastmcp.com/llms.txt | **按需工具搜索替代全量目录**；Tool Transformation 改 schema；**沙箱代理不持长期凭证**；多认证源汇聚；Middleware 横切错误处理；Tool Fingerprinting |
| Pydantic AI | ai.pydantic.dev/llms.txt | **Guardrails 链作用于模型工具调用本身**；**畸形 JSON 参数先修后验**；**工具大返回截断/落盘/总结**；间接注入分类工具结果；子代理委派绑预算；Step Persistence 快照 |
| Langfuse | langfuse.com/llms.txt | trace 作审计账本；Eval 数据集+实验；Prompt Versioning；CI 门禁；每页 .md 可取 |
| Phoenix | docs.arize.com/phoenix/llms.txt | OpenInference 11 类 span；多轮 session 聚合；LLM Jury 加权降偏；Agent 轨迹/工具选择/推理路径评测；CI pytest 门禁 |
| Opik | comet.com/docs/opik/llms.txt | trace/span/thread 三级；离线缓存重放；**从生产失败建回归测试**；轨迹评测下沉到工具选择/推理路径；数据集版本化可回退 |
| Ragas | docs.ragas.io/llms.txt | Agentic/Tool-use 专项指标；Rubric-Based + Aspect Critique；Noise Sensitivity 抗噪声；Testset 生成（单跳/多跳/Persona） |
| Braintrust | braintrust.dev/llms.txt | Eval 三元组（Dataset/Task/Scorer）；5 阶段持续改进闭环；生产日志→Topics→数据集；Online Scoring；Prompt Versioning |
| Vellum | docs.vellum.ai/llms.txt | **节点级容错/重试（Node Adornments）**；子工作流模块化；Map 并行；内联 Guardrail 门禁；Escalation to Human；多环境晋升+Release Review |

## 二、落地汇报（强制四列表格 · 轮 / 文件 / 版本 / 独有点）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| B | wb-skill-authoring | 2.68.0 | 技能身份与加载信任层：名称非唯一、身份=源+URI、读≠激活须验证+审批、digest 锁版本、跨源逐次审批、≤512文件/16MiB（MCP Skills 扩展 SEP-2640） |
| B | wb-context-compressor | 3.46.0 | 间接注入防御：工具返回结果也按注入源分类后再入模型（Pydantic AI） |
| B | wb-context-compressor | 3.46.0 | 执行沙箱不持长期凭证：只拿时效/代理性逐次授权（FastMCP） |
| B | wb-context-compressor | 3.46.0 | 工具大返回处理：超限截断/落盘/总结不滞留历史（Pydantic AI） |
| B | wb-execute-discipline | 3.01.3 | 工具调用护栏链：对模型发起的工具调用本身做 allow/block/替换/重试/审批+秘密/PII 检测（Pydantic AI） |
| B | wb-execute-discipline | 3.01.3 | 畸形工具参数先修后验：schema 校验前插修复层（Pydantic AI） |
| B | wb-execute-discipline | 3.01.3 | 容错要到节点级，不只全局 Agent 层（Vellum Node Adornments） |

## 三、逐点判重（双键：来源标识 + 概念关键词）

- **MCP 技能信任层（身份二元组/读≠激活/digest/跨源审批）**：库内 sa 有「工具别全塞进 prompt（语义检索分发）」「frontmatter 闭合」「重命名全链路同步」，但**无"技能身份不以名称唯一标定、内容靠 digest 验证、激活靠显式审批"的加载信任层**——完全空缺，增量≥60%，不重（MCP Skills 扩展首读）。
- **间接注入防御（工具结果作注入源）**：ctx 有 §AI 应用安全、§注入与裁剪可观测性（管用户输入侧/整体面），本条管"工具/检索返回被低估的注入通道" → 互补非重复，增量≥40%。
- **沙箱不持长期凭证**：ctx 有 §凭据读穿不复制、§公开渠道不承载密钥（管主环境），本条管"执行沙箱这一特殊场景须把凭证降级为短时效" → 相邻非重复，增量≥40%。
- **工具大返回处理**：ctx §工具输出沙箱（原始输出收口）、§撑满之后四条（上下文满）相邻，但"工具返回这一具体对象的尺寸治理——溢出落盘不进历史"是细化新角度 → 增量≥40%，不重。
- **工具调用护栏链（罩在模型→工具这步）**：exec §安全钩子管"执行前后"，本条管"调用决策本身要不要放行" → 前置不同面，不重。
- **畸形参数先修后验**：exec/av 有「结构化输出两态（valid≠correct）」，本条管"调用工具前的参数整形——先能解析再谈对错" → 相邻非重复，增量≥40%。
- **节点级容错**：exec §失败恢复有序阶梯管"失败后往哪升级"，本条管"失败在哪一层被接住（节点级隔离）" → 互补，不重。

## 四、候选未落地点（判非重复 / 低迁移 / 已覆盖理由）

- **按需工具搜索替代全量目录（FastMCP/Mastra）**：与 sa §工具别全塞进 prompt（语义检索式工具分发 gateway 模式）重叠 >60%，不落。
- **Eval 轨迹下沉/LLM Jury 加权/多 run 冻结/数据集版本化/生产失败→回归测试 / Online Scoring**：av 已深覆盖（r1018 工具轨迹回归、r1035 生产失败→回归、r1323 轨迹同构、r1346 多 run 冻结、r517 两类 eval 目标通过率），**全部判已在库，不落**（遵守库治理"优先并入、不重复追加"）。
- **子代理委派绑预算 / Step Persistence 快照**：与 exec「并行派发」、A 轮 Temporal 耐用执行（事件溯源快照）重叠 >60%，不落。
- **Escalation to Human / Release Review 门禁 / 多环境晋升**：与 wb-release-maintain 已有发布治理相邻，留 r152-C 或后续评估（本次未读该文件，保守不越界落）。
- **Prompt Versioning / 提示缓存 / 动态模板**：与 AGENTS.md 版本化 + ctx 上下文治理重叠 >60%，不落。

## 五、三件套评估（用户每轮要求）

- `wb-skill-authoring` 本轮升 2.68.0（1 点：MCP 技能信任层）。
- `wb-context-compressor` 本轮升 3.46.0（3 点：间接注入/沙箱凭证/工具大返回）。
- `wb-execute-discipline` 本轮升 3.01.3（3 点：工具调用护栏链/畸形参数先修后验/节点级容错）。
- `wb-ponytail`、`wb-max-token-saver`：本轮无合格新材料（ponytail 的 YAGNI 与 Vellum 节点容错重叠 >60%；mts 归豆包线），不硬凑。

## 六、垃圾清理

- 生成 `r152b_append.py`（落盘脚本），收尾前删除。
- 未启 VPN；无临时文件残留。
