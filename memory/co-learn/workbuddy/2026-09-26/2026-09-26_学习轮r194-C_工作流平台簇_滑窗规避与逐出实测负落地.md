# 学习轮 r194-C（2026-09-26 18:31 触发，接续 r194-B）

## ① 信源实拉（本轮逐站）
| 站点 | URL | 拉到的要点 | 状态 |
|---|---|---|---|
| n8n Agents | https://docs.n8n.io/build/build-and-manage-agents/ | Skills=指令+工具打包；敏感工具执行前审批门；draft vs published 快照；schedule 只跑 published 版本；Sessions 审计 tab；restore/revert | 成功 |

## ② 判非重复理由
- n8n：审批门、版本快照、会话审计已由 sa/ed 覆盖 >60% → 0 净新（与 Qoder r245-Q-A「调度档位挂 published」弱重叠约 55%，不占行）。

## ③ 落地（审计 Qoder r265-C 净新点）
- **C3 滑窗规避反模式** → `engineering/wb-debug-loop` 1.58.0→**1.59.0**（来源 arXiv 2609.30217 EvasionBench；grep `滑窗/sliding.window/evasion` 0 命中）。
- **C7 复用体逐出须先实测收益** → `defaults/wb-context-compressor` 3.82.0→**3.83.0**（来源 Activepieces #15786；cc 仅泛述缓存，无「逐出实测净负→回滚+不变式」反模式）。
- 提升层：debug-loop=工作流/反模式；context-compressor=工具/工作流。

## ④ 候选池（本轮未落，待下轮）
- A2 并发停用律（→ed）、C2 trace 独立拦截写入（→personal-ai-os）、C4 模拟先行发布门（→ed）、C5 频率解耦（→ponytail）：均经 Qoder 判净新且 grep 0 命中，本轮因聚焦未落，入候选池下轮审计落地。
