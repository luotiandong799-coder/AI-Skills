# 2026-09-23 学习轮 r154-B（WorkBuddy 侧独立实拉 + 落地）

轮次：r154-B ｜ 接 r154-A，**独立重拉**（新进程，不复用 A 的抓取结果）。

## 一、实拉证据（174 路全量，逐站留痕）
- `fetchall.py b` → **174 路 / 145 OK200 / 29 未达**（未达集合同 A，如实记录；逐站名见 `yt/r154wb/b/_summary.txt`）。
- **★主源（清单内 174 路，此前从未深拉）**：**Kestra 官方 `kestra.io/llms.txt`** → `deep.py` 无过滤 CAP=700 → **521 页全 200**（计入 `_index.txt`）。深读：`workflow-components/{concurrency,errors,retries,subflows}.md`、`best-practices/{flows,loop,credentials-vs-secrets-vs-kv-store}.md`。
- 原文片段（可核）：`Once an execution occupies a concurrency slot, it keeps that slot until it reaches a terminal state. This includes executions in the PAUSED state while they wait for manual approval.`／`Concurrency uses database locks to hold slots, so heavy contention ... can increase database load and slow scheduling.`／`The larger the flow execution context, the longer the serialization process takes.` + `there may be a hard limit on the size of this context, since it's stored as a single row or message (often around 1 MB)`／`a nested loop of 20 × 20 tasks results in 400 sub-executions`／`a Subflow task creates a new execution, its tasks are isolated and do not affect the parent flow's performance`／`Ensure retry.interval is smaller than maxDuration, or retries may never run.`。

## 二、落地（3 文件 3 独点）
| 文件 | 版本 | 独有点 |
|---|---|---|
| `engineering/wb-execute-discipline` | 3.01.3 → **3.01.4** | 并发槽按「终态」释放（等人工/挂起仍占槽，`limit:1` 会被一个挂起执行堵死）；超限行为必须显式三选一（QUEUE/CANCEL/FAIL），排队消耗共享锁不免费；并发只管「同时在飞数」，不是 CPU/内存/时间窗速率 |
| `engineering/wb-spec-driven` | 1.75.0 → **1.76.0** | 执行状态容器的成本按「状态变更次数」放大（每次状态变更整份取回-反序列化-写回），约 1MB 硬墙 → 大产物只能留引用；任务数本身是性能变量（>100 退化，20×20=400 子执行）；要「可独立重跑」就拆子执行；循环局部产出默认不外泄须显式声明出口且按规模选 FETCH/STORE |
| `engineering/wb-debug-loop` | 1.49.0 → **1.50.0** | 参数间硬约束导致「配了永不触发」（`retry.interval` 必须小于 `maxDuration`），只能靠主动一致性校验抓；`timeout` 与 `retry.maxDuration` 是两个刻度须分别设值；`warningOnRetry` 让「重试过」成为可观测状态；兜底处理器管辖范围＝所在容器子树（挂在 t2 上抓不到 t1） |

## 三、判非重复（不落项）
- **`credentials` / `secrets` / `KV Store` 三类值分工**（可代轮换的认证对象 / 静态密钥 / 运行时动态共享值）：**本轮不落**，记入候选池——与 cc 既有「凭据读穿不复制 / 沙箱不持长期凭证 / 敏感输入走带外」三层部分相邻，需先做一次边界整合再落，避免并列成第四条同类规则。
- **`errors` vs `afterExecution` 的选择**：不单列，已并入上表 dl 条目（兜底分层）后半句。
- **子流程 `revision` 默认 latest 可钉版本**：与 rel「版本是不可变快照、端点决定谁跟最新版」重叠 >60%，不落。
- **递归流程不支持**：平台约束事实，非可迁移方法论，不落。

## 四、三件套评估
- `wb-ponytail` / `wb-max-token-saver` / `wb-context-compressor`：本轮三件套均无合格新材料 → **版本全部不动**（不硬凑，符合「1..N 件套按功能而非数量」）。
- 结论：**维持 3 件套**。

## 五、版本化
- cp live → repo，sha256 三文件全 **SAME** → commit **`9a79d22`** → `merge-base --is-ancestor` = **ANCESTOR_OK** → push `5b15b37..9a79d22`（SSH over 443，非强推、无重试）。
