# r174-A 分支路由与安全闸三分增量（AI Router / Guardrails / HITL 递减）

实拉时间：2026-09-24 22:05–22:25（自动化 r174-A，接续 r173）

## 一、实拉证据（URL + 状态码）

| 站 | URL | 状态 | 留痕 |
|---|---|---|---|
| Dify | dify.ai/llms.txt + docs.dify.ai/llms.txt | 200/200 | A/dify.txt(616B, 浅索引) A/difydocs.txt(2526B) |
| n8n | docs.n8n.io/llms.txt | 200 (285KB) | A/n8n.txt；深拉 guardrails.md(5813B) / human-in-the-loop-for-tools.md(9045B) / use-ai-for-parameters.md(7896B) |
| Flowise | docs.flowiseai.com/llms.txt | 200 (98KB) | A/flowise.txt（agentflow/evaluations 索引扫描，无新独点） |
| Langflow | docs.langflow.org/llms.txt | 200 (2611B) | A/langflow.txt（页面清单已读，release-notes/security 无新） |
| Activepieces | activepieces.com/docs/llms.txt | **200（此前 000，本轮打通重置击数）** (35KB) | A/activepieces.txt；深拉 flows/ai-router.md(8314B) / agents/overview.md(2444B) |
| Pipedream | pipedream.com/docs/llms.txt | 200 (26KB) | A/pipedream.txt（MCP/managed-auth 索引扫描，无新独点） |
| Make | make.com/en/help/llms.txt | 403（击数+1） | A/make.txt(5578B 拦截页) |

## 二、落地独有点（3 个 → wb-execute-discipline 3.01.27→3.01.28）

1. **AI Router 分层判据**（工作流层）：LLM 只判意图/主题/情绪等模糊判断，数值比较/字段存在性/精确匹配一律规则（原文 Never ask the model whether 500 is bigger than 499）；置信度地板→Otherwise 兜底且运行记录写明；单选 vs 多选（互斥=最佳分支，独立=全命中，Otherwise 仅零命中）；可嵌套 AI 判意图+规则判事实。出处：Activepieces docs/flows/ai-router。
2. **Guardrails 增量**（工作流层）：逐护栏独立阈值+fail 分支单点汇聚；check/sanitize 双操作（出口净化=占位符替换后放行，不只"拦"）；URL Block userinfo 防 user:pass@example.com 凭据注入。出处：n8n guardrails node。
3. **HITL 监督递减**（工作流层）：默认全工具人审→随信心逐工具缩减（反"先全自动出事再补审"）；Deny 也是反馈（拒绝事实回传 agent 调整后续行为）。出处：n8n human-in-the-loop-for-tools。

## 三、判非重复理由

- AI Router 判据 vs 豆包 r174-B 级联模型路由：那条是**模型选择**（输出过质量门决定升级），本条是**分支路由**（意图 vs 事实的判断者分工+置信度地板+单/多选模式），问题不同，重叠 <20%。
- Guardrails vs 豆包 r162B 双层护栏：那条管"输入/输出层各防什么"；r180B 管 PII 占位符设计。本条管单条护栏内部配置（阈值/汇聚/占位处置/userinfo 子开关），其中 URL userinfo 凭据注入全库 grep 无先例，重叠 <40%。
- HITL 监督递减 vs 我方 §HITL 三要素（HeruStudio）：那条管审批请求设计；vs 豆包 r183-A 独点1：工具级硬 gate+跨渠道路由已由其覆盖——**「审批通道分离」一点已主动剪除不落**（发现重复即撤），监督递减与 Deny 反馈在其范围外。
- $fromAI()（n8n）：判为产品机制不落——参数填空按钮/key 提示是 n8n 编辑器实现，无法回答提升层中"可复用方法论"的归属（与 composio 自举加载同判）。

## 四、特殊处置

- **live→repo 漂移修复**：发现 live 的 wb-execute-discipline 含一段 repo 缺失的「决策与路由增强（Fast/Deep 层，2026-09-23 用户最终版规则）」27 行 + 1 处 CRLF——本轮合并入版本化（防止后续 cp 覆盖丢失），CRLF 清零。
- 机检：PyYAML 可解析 / CRLF=0 / FFFD=0 / 尾空白=0 / desc 951≤1024。
