# r145-A 学习记录（WorkBuddy）

> 日期：2026-09-23｜轮次：r145-A（三轮第 1 轮）｜信源：**115 路 / 102 OK200 / 13 未达**
> 主源（本轮首读）：Letta 官方 `docs.letta.com`（llms.txt 27,665 字节 / 130 页）· LangChain 官方 `docs.langchain.com`（llms.txt 24,320 字节 / 119 页）
> 深拉：56 页直出 Markdown 全 200（Letta 28 + LangChain 28）

## 一、本轮落地

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | agent/agent-guild | 1.1.0 | 私有记忆仓 vs 组织共享仓双仓分离（判据＝换一个 agent 还成不成立）+ 共享仓根下 `skills/<name>/SKILL.md` 供全协会 + 写了不 push 等于没共享 + 记忆整理走 git worktree 不阻塞主线 |
| A | defaults/wb-context-compressor | 3.38.0 | 常驻的是目录树不是内容（路径即索引）+ 默认无向量索引（先问 grep 找不找得到）+ 不常驻的文件必须在 AGENTS.md 显式点名才存在 + 记忆体检三查（放置/重复/system-prompt 占用） |
| A | engineering/wb-skill-authoring | 2.61.0 | 子代理 = Markdown + YAML frontmatter（与技能同构）+ `tools`/`memoryBlocks` 默认是 all（默认开 vs 默认关是隔离真伪的分水岭）+ 内置子代理按职能分型含只读 recall + 覆盖链自定义>内置 项目级>全局 + 后台完成通知 |

## 二、逐站实拉证据（选摘，全量见 `D:/腾讯AI/yt/r145/a/_summary.txt`）

- `letta_docs_llms` 200 / 27,665 B（direct）
- `langchain_docs_llms` 200 / 24,320 B（direct）
- `temporal_llms` 200 / 13,793 B｜`claude_docs_llms` 200 / 68,759 B｜`agentcore_devguide_llms` 200
- `docs.letta.com/concepts/memfs/index.md` 200 / 4,078 B —— 目录树常驻、默认无语义索引、worktree 并发
- `docs.letta.com/concepts/shared-memory/index.md` 200 / 2,902 B —— 双仓对照表、legacy memory block 迁移
- `docs.letta.com/configuration/subagents/index.md` 200 / 7,762 B —— 7 种内置子代理、frontmatter 默认 all、覆盖链
- `docs.letta.com/configuration/memory/index.md` 200 / 2,145 B —— `/doctor` 三查
- `docs.langchain.com/oss/deepagents/code/memory-and-skills` 200 / 9,658 B —— 附加文件必须被 AGENTS.md 引用
- 未达 13 路（与上一轮集合互异）：`dify_market`(404) / `make`(403) / `fullstackskills_gh`(404) / `aliyun_skillmarket`(10053) / `aliyun_xiaxiaobao`(10054) / `zhipu_agentmore`(10054) / `gpt_store_discover`(404) / `hf_agents`(404) / `waytoagi_fastgpt`(302 循环) / `guangzhengli`(404) / `skillscat`(SSL EOF) / `skillsdirectory`(SSL EOF) / `bedrock_agentcore_llms`(404，starter-toolkit 入口，devguide 入口 200)

## 三、判重不落（逐条理由）

| 点 | 来源 | 判非重复理由（→ 不落） |
|---|---|---|
| `/init` 自举 + `/remember` 显式教学 | Letta memory | 与 agent-guild §M0/M2 已覆盖 |
| 7 种内置子代理逐项能力 | Letta subagents | 只取"按职能分型 + 只读型存在"这个判据，清单本身是产品事实 |
| skills vs memory 频谱（通用偏好→memory / 任务相关→skill） | LangChain | 与 sa §固化 vs 检索、§记忆答我们是谁 同源 >60% |
| 技能发现目录优先级（7 层目录后覆盖前） | LangChain | 与 sa §技能优先级/覆盖链 已覆盖 |
| OpenWiki personal mode（连接器/凭据/cron） | LangChain | 产品域，非 agent 技能系统底层方法论 |
| deepagents CLI 参数、hooks、plugins、remote sandbox | LangChain | 工具配置面，无可复用判据 |

## 四、候选池（下轮可接）

1. Letta `configuration/skills`（技能来源与优先级，8,477 B，本轮未细读）
2. Letta `configuration/mods`（agent 自改 harness，9,788 B）
3. Letta `agent-sdk/permissions`（5,205 B）、`agent-sdk/sessions`（6,075 B）
4. LangChain `oss/python/deepagents/memory`（SDK 记忆后端）、`oss/python/deepagents/skills`
5. `docs.letta.com` 的 Context Constitution（GitHub `letta-ai/context-constitution`）——上下文宪法：放什么、什么顺序、什么粒度、留多久

## 五、三件套评估（A）

`wb-context-compressor` 升 3.38.0（输入侧再得一点：常驻清单的形态）。`wb-ponytail` / `wb-max-token-saver` 本轮无新料 → **维持 3 件套**。
