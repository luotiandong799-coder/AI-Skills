# 2026-09-24 学习轮 r157-C：运行中的轨迹评审 / 技能库是构造期快照 / 工具搜索自适应 / 缺口驱动发现

轮次：r157-C（每小时 3 轮之第 3 轮）｜信源：**173 路独立重拉 → 167 OK200 / 6 未达**（三轮未达集合完全一致，稳定）。

## 一、独立实拉证据

- 全量：`fetchall.py c`。
- **主源（清单内，此前未深拉 harness / capabilities 面）**：Pydantic AI `ai.pydantic.dev/llms.txt` → 过滤 `harness/` + `capabilities/` + `overview/coding-agent-skills` → **60/60 全 200**（`deep_c/`）。
- 关键原文：
  - `harness/trajectory-judge`：`runs concurrently with the agent, so the run is never blocked waiting on a judge`；每 `every` 次模型请求取最近 `window` token（约 4 字符/token）轨迹；`delivers exactly one verdict per evaluation`（`AllGood` / `Steer`），且裁决是**输出类型不是它调用的工具**；`At most one evaluation per judge is in flight at a time`。
  - `harness/skills`：`Discovery happens once at construction. The catalog and parsed instructions are a snapshot. Construct a new Skills instance to rescan`；`It does not load bundled resources or run scripts`；不自动搜索 `.agents` / `.claude` / 家目录；`include` / `exclude` 裁剪暴露面。
  - `capabilities/tool-search`：`defer_loading=True` 标记；按激活模型自适应（原生服务端搜索 vs 本地 `search_tools`），**存在可搜索延迟工具时自动注入**；策略 `keywords` / `bm25` / `regex` / 自定义；`A search that finds no matches returns normally and never spends a retry`；重试预算跟随 agent 工具预算。
  - 审计取证（豆包 r156-C）：MCP 官方 client best practices `Offer multiple detail levels` / `Cache tool definitions` / `Refresh on list_changed`；arXiv 2506.01056 MCP-Zero 主动能力请求 + 双级评分 `score=(s_server x s_tool) x max(s_server,s_tool)`；腾讯 SkillHub 三线并行审核 + edgeone 本地不出设备扫描。

## 二、本轮落地（4 文件 5 独点）

| 文件 | 版本 | 独有点 | 提升层 |
|---|---|---|---|
| engineering/wb-artifact-verification | 1.99.0 | 长任务在线评审：并发不阻塞、窗口按 token 截断使单次成本有界、一次评估只给一个裁决、裁决异步入队下一次请求、同时最多一个评估在飞 | 工作流 |
| engineering/wb-skill-authoring | 2.75.0 | 技能目录是构造期快照（改了不生效先查刷新）；加载只带正文不带资源与脚本；库路径显式声明不靠约定目录；暴露面在加载器层裁剪 | 可复用 Skill |
| engineering/wb-skill-authoring | 2.75.0 | （豆包 r156-C 审计落地）平台上架三线并行任一不通即拒；本地不出设备扫描是另一种风险取舍 | 工作流 |
| defaults/wb-context-compressor | 3.55.0 | 工具搜索按激活模型自适应选路、有可延迟工具就自动注入、策略可选；**空结果是正常返回不消耗重试** | 工具 |
| defaults/wb-context-compressor | 3.55.0 | （豆包 r156-C 审计落地）客户端三纪律：多级细节 / 缓存定义 / list_changed 才重索引 | 工具 |
| defaults/wb-ponytail | 1.65.0 | （豆包 r156-C 审计落地）缺能力时先声明缺口再检索；匹配分 server 级与 tool 级两级合成 | 工作流 |

## 三、判重不落

- Mastra ToolSearch 与 pydantic ToolSearch 属同一功能位的两个实现 → 只落一次，互为佐证（cc 3.54.0 / 3.55.0 分别取各自独有的细节）。
- 豆包 r156-C 点 3（向量检索 top3-5 / token 降 70-90% / 迭代守卫 / RBAC）→ 与 cc 3.54.0「阈值是准入闸门」重叠约 60%，记候选池不重复落。
- n8n MCP 双角色 / Streamable HTTP 取代 SSE / MCP 广场生态面 → 平台操作面与协议事实面，不落。

## 四、三件套评估

cc 迭代（3.54.0→3.55.0）、pt 迭代（1.64.0→1.65.0）；mts 本轮无合格新材料（豆包主战场，落点已避开）。维持 3 件套。

## 五、版本化

commit+push `52f1602`（ANCESTOR_OK，非强推）；repo→live 4 文件 sha256 全 SAME。
