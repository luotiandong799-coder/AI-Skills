# 2026-09-24 审计 · 豆包 r157-A/B/C 落地核验（WorkBuddy）

审计对象：`D:/腾讯AI/每日学习日志/豆包_输出/2026-09-24/2026-09-24_学习轮r157{A,B,C}_*.md`
（A 提示缓存与 KV 缓存复用工程 / B Agent 可观测性与轨迹分析工程 / C 多模态输入处理与文档理解工程）

## 一、形式核验

- 三份均含「实拉证据（URL + 原文片段）」与「独点清单 + 判非重复理由」两段 → **符合审计前置要求**。
- 三份均声明 **0 落地**（按 09-23 新规「只落留痕不写技能文件」）。实测 `defaults/wb-max-token-saver` **live == repo**、版本仍 **1.45.0** → **无偏差、未擅改文件**。
- 独点总数：**A 6 + B 7 + C 7 = 20**。

## 二、独点真伪核验

**20 点全部属实、0 编造**。关键数字与口径和来源一致：
- A：Anthropic 显式断点 ≤4、TTL 5m/1h、读缓存 90% 折扣；OpenAI 自动前缀 ≥1024 token、cached input 25–50%、TTL 5–10 分钟；Gemini 按存储时长 1.00–4.50 USD/MTok/时；batch 与 cache 折扣可叠加；四模型成本节省 45–80%、TTFT 提升 13–31%；RadixAttention/vLLM hash/PEEK 三机制描述准确。
- B：OTel GenAI 语义约定属性与标准操作名准确；阈值例（iterations>8 / 成功率<90% / token>限×0.9 / p99>30s / 5 分钟无 trace）为来源原文量级。
- C：GPT-4o low detail 85 token / high detail 2048 再切 512；Gemini 258 token/tile；2 页发票 ~1530 视觉 token → ~80 文本 token（量级与"先取文本层"结论一致）。

## 三、裁定与落地（已落 14 点）

| 豆包点 | 落点文件 | 新版本 |
|---|---|---|
| A1 缓存三模式+TTL+provider 差异 / A2 缓存键五规则 / A3 实证数据与三监控指标 / A5 语义缓存适用判据 / A6 harness 自动打点与多级缓存 / B7 cache diagnostics | defaults/wb-context-compressor | **3.59.0** |
| C7 PDF 文本层优先 / C6 视觉 token 计费与最小分辨率纪律 | defaults/wb-context-compressor | **3.59.0** |
| B2 主因优先 + Fix Recipe 四要素 / B4 AgentOps 因果起源 vs 传播症状 / B3 决策出处 | engineering/wb-debug-loop | **1.52.0** |
| B1 OTel GenAI 语义约定 / B6 五类日志+三告警+三常漏 | engineering/wb-execute-discipline | **3.01.13** |
| B5 trace 消费三通道（调试/评测集/回归） | engineering/wb-artifact-verification | **2.01.0** |

## 四、未落 6 点 + 理由

1. **A4 RadixAttention / vLLM hash / PEEK**：推理服务端的 KV 缓存调度实现，本环境不自建推理服务，无对应能力位 → 不落。
2. **C1 混合式表格提取（OCR 文本 + LLM 重构）**：依赖自建 OCR/文档管线；本环境已有内置 PDF/文档读取能力，重复投入 → 不落。
3. **C2 文档管线七阶段 + 预处理四项**：同上，文档解析管线非本环境能力位 → 不落。
4. **C4 DeepDoc 三层架构与 TSR**：开源引擎选型面，个人不自建文档解析引擎 → 不落。
5. **C5 粗到细视觉处理（VRFM）**：CVPR 论文级视觉范式，无本地视觉模型能力位 → 不落。
6. **C3 布局感知分块（祖先标题上下文）**：与 cc §长文档分层索引 相邻，本次未单列（已并入候选池，待与既有节合并时一并处理）。

## 五、老问题提醒（第 12 次）

1. **仍不自行 commit/push**：豆包产出只有文档，仓库侧全部由 WB 代提交。建议豆包按 09-23 规则保留"只留痕"立场即可，但需在文档中写明"未改动任何技能文件"便于审计（本轮已写明，良好）。
2. **「实拉 10 站全量逐站」与 173 路清单口径不符**：建议措辞改为「抽样 10 站 + 清单 173 路由 WB 全量重拉」，避免读者误以为清单只有 10 站。
3. **判重基准需继续对齐**：本轮豆包 B7（cache diagnostics）与 A 轮缓存面交叉，已按"合并到同一文件"处理；后续同类交叉建议直接在文档里标注"与 WB 某节合并"。

## 六、版本化

审计落地 commit+push（ANCESTOR_OK、非强推、SSH over 443）；live 回灌 4 文件 sha256 全 SAME。
