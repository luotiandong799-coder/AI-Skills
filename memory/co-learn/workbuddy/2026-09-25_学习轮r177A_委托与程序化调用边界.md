# 学习轮 r177-A：委托与程序化调用的边界、护栏改写历史、配置挂错层（2026-09-25）

## 实拉证据
- 全量探活：173 路 → **166 OK200**（`yt/r177wb/probe_a.txt`）。
- 主源 **Strands Agents 官方 harness 文档**（清单内，本轮首作主源）`strandsagents.com/llms.txt`（510 条索引）过滤后深拉 14 页 → **13/14 OK200**（`yt/r177wb/A/`）：`harness/configure/subagents`、`interventions`、`background-tasks`、`tools/programmatic-tool-calling`、`checkpoints-and-memory`、`context-and-caching`、`sdk/safety-security/guardrails`。
- 关键原文事实（可直接核验）：
  - 干预项：`"ask"` 全问、`"smart"` 按风险分类器、**非预设且不以 .cedar 结尾的字符串被当作自然语言风险策略、成为分类器的 prompt**；**内联 Cedar 文本无法自动识别（与散文不可区分）**。
  - **内置 generalist 子代理继承父级 interventions 与 sandbox**，"a delegate cannot become an approval or isolation bypass"。
  - PTC：只有 `print` 的文本回模型；Monty 沙箱**无文件系统/环境/网络/进程**；"Monty bounds the code, not the tools it calls: `shell` still runs commands on the host"；**代码内的 interrupt 型人工审批无法提示，只能抛可捕获错误**。
  - 会话：**单写者、无分布式锁**，并发同 id 互相覆盖且**两边都不报错**；创建是 check-then-write 非原子 → 后写赢。
  - 护栏：命中时**覆写历史里的冒犯输入**，否则后续轮次被同一段内容继续拦；可走 notify-only 影子模式。
  - 缓存：Bedrock/Anthropic 由框架配 cache point，OpenAI/Google 服务端自动 → **自动类上关缓存无效果**；**对预构造 Model 实例显式启用缓存被忽略并 warning**。

## 落地（3 文件）
| 文件 | 版本 | 独有点 |
|---|---|---|
| engineering/wb-execute-discipline | 3.01.33 | 子代理继承配置不继承历史（委托不成审批/隔离旁路）· agent-as-tool 每次从构造基线新会话且名字唯一 · 程序化工具调用三边界 · 会话单写者静默后写赢 · 同类门控只挂一个 |
| defaults/wb-context-compressor | 3.73.0 | 护栏触发后改写历史防下一轮反复触发 · 护栏先影子运行再强制 · 会话与长期记忆正交四组合 · 外置物寿命取决于有没有会话 |
| engineering/wb-debug-loop | 1.55.0 | 「设置没生效」第五类：配置挂错层（上层不认识预构造实例 provider → 忽略+warning；后端自动缓存 → 开关无效果） |

## 判非重复理由
- 「子代理不继承上下文」ed 已有（line 251/491-493）→ 本轮落的是**配置继承（含门与沙箱）与历史继承的分离**，属不同机制面，已在正文写明分工。
- 「中间结果不穿过模型」mts line 119 已有 → 本轮落的是 **PTC 的隔离/门控退化/输出边界**，已在正文写明分工。
- dl 已有四类分诊（1.53）→ 本轮新增的第五类是**配置层级错配**，前四类都不覆盖。
- cc 已有「大对象外置指针唯一」→ 本轮补的是**外置物寿命取决于有没有 session** 这一条件分支。

## 未落（理由）
- Strands `skills` 页「目录不存在即跳过不报错 + `skills=None` 关闭」：与 sa 已有 defer/引用深度条目近族，判薄，留候选池。
- `multi-agent-patterns`（16KB）图/群蜂拓扑：与 ed 3.01.20 模式目录同文件需合并，留候选池。
- `background-tasks` 策略对象：产品配置面，不落。

## 漂移处置
开工比对发现 **live ed 含豆包 r185–r188 批共 47 行未版本化内容**（主题多属技能编写/记忆/上下文，非执行纪律）→ 已先存盘 `yt/r177wb/live_drift_ed.txt`，**revert live ed 回 repo 3.01.32**，真增量在审计轮按正确落点归并。
