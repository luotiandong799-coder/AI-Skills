# 学习轮 r169-C · Agent 生态实拉（agentskills.io / OpenClaw / deeplearning.ai）

> 轮次：r169-C｜日期：2026-09-24｜实拉：WorkBuddy
> 提升层：可复用 Skill（技能加载架构）

## 一、逐站实拉留痕
| 信源 | 状态 | 关键内容 |
|---|---|---|
| agentskills.io（Agent Skills 开放标准） | 200 | **三阶段渐进式披露**：① Discovery 启动只加载 `name`+`description`；② Activation 任务匹配 `description` 才读完整 `SKILL.md`；③ Execution 按需执行 scripts/ 或加载 references/。技能＝含 `SKILL.md`(name/description/instructions) 的文件夹 + 可选 scripts/references/assets/。完整指令仅任务需要时加载，可挂大量技能而上下文极小 |
| OpenClaw docs（docs.openclaw.ai） | 200 | 自托管 Gateway：单一网关为 session/routing 唯一真相源；「可信网关 / 不可信执行 / 确定性策略」架构；Skills「教 agent 可重复流程、按需加载」；cron/hooks/webhooks 自动化；ClawHub 插件市场含发布/策展/信任指引 |
| deeplearning.ai The Batch（issue-371, 2026-09-18） | 200 | 本期主题 Meta Agent Security / Navier-Stokes 争议 / Claude 欺诈——属新闻，无新工程方法论（与 r166-A 已覆盖 issue-371 重叠） |
| GitHub agent 生态 | 沿用 | 沿用 r167-A（VoltAgent/claudeskillhub/OpenClaw）不重拉 |

## 二、落地独点（wb-skill-authoring 2.86.0）
1. **渐进式披露三阶段（Discovery/Activation/Execution）**：采用 Agent Skills 开放标准模型——`description` 是唯一路由面，决定 Discovery 能否命中；厚内容外置 `references/`，SKILL.md 只留入口与判据。与 2.80 束级门控互补：门控定「何时加载」，三阶段定义「加载时各自发生什么」。

## 三、判非重复理由
- 既有「按需加载束级门控」(2.80) 只讲加载门，本轮补的是**开放标准的三阶段命名模型 + Discovery 仅加载 name/description 的硬规则 + SKILL.md 作为入口、scripts/references/assets 为可选捆绑物**的 canonical 结构，重叠 <60%。
- OpenClaw「可信网关/不可信执行/确定性策略」属基础设施架构，与「agent 技能系统方法论」边界较远，留候选池不落。
- The Batch 新闻无方法论，不计。
