# r175-B 长时程任务跨窗口交接工件 + 评测环境是一等混淆变量（Anthropic 工程博客实拉）

实拉时间：2026-09-24 23:22–23:35（r175 三轮中的 B 轮，主源与 A 轮完全不同，逐站重拉）

## 实拉证据（逐站）

主源：Anthropic 工程博客（**先按 slug 在技能库全量查重，挑 12 篇里 12 篇未被消费过的**）。实拉 6 篇，全部 200，HTML 落盘后去标签转纯文本（原文留存 `D:/腾讯AI/yt/r175wb/B/`）：

1. `anthropic.com/engineering`（索引页 176,137 B）→ 解析出 24 篇 slug，逐篇比对技能库命中数，取 0 命中的新篇。
2. `.../effective-harnesses-for-long-running-agents`（182,601 B / 去标签 15,181 字）
3. `.../advanced-tool-use`（273,660 B / 27,930 字）
4. `.../code-execution-with-mcp`（209,968 B）
5. `.../a-postmortem-of-three-recent-issues`（189,325 B）
6. `.../how-we-contain-claude`（227,262 B）
7. `.../infrastructure-noise`（164,246 B / 14,066 字）

探活失败留痕：`cookbook.openai.com/llms.txt` **403**（累计第 2 次）、`platform.openai.com/docs/llms.txt` **403**（累计第 2 次）——OpenAI 侧两站本轮仍未打通。

## 关键原文摘录（可核验）

- 「Claude's failures manifested in two patterns. First, the agent tended to try to do too much at once—essentially to attempt to one-shot the app… A second failure mode… a later agent instance would look around, see that progress had been made, and declare the job done.」
- 四件套：「an `init.sh` script, a `claude-progress.txt` file that keeps a log of what agents have done, and an initial git commit」+「a comprehensive file of feature requirements… all initially marked as "failing"」。
- 「we landed on using JSON for this, as the model is less likely to inappropriately change or overwrite JSON files compared to Markdown files」；「It is unacceptable to remove or edit tests because this could lead to missing or buggy functionality.」
- 起手四步原文：`pwd` → read git logs and progress files → read feature list、选最高优先级未做项 → 起 dev server 跑基础端到端冒烟。
- 「Absent explicit prompting, Claude tended to make code changes, and even do testing with unit tests or `curl` commands against a development server, but would fail recognize that the feature didn't work end-to-end.」
- 「Claude can't see browser-native alert modals through the Puppeteer MCP, and features relying on these modals tended to be buggier as a result.」
- infra-noise：「the gap between the most- and least-resourced setups on Terminal-Bench 2.0 was 6 percentage points (p < 0.01)」；「a guaranteed allocation… and a hard limit at which the container is killed. When these are set to the same value, there's zero headroom」；「Up to roughly 3x… additional resources fix infrastructure reliability problems… Above the 3x mark… additional resources start actively helping the agent solve problems it couldn't solve before」；「leaderboard differences below 3 percentage points deserve skepticism」。

## 独点清单（2 个落地点）

### 落点 1 → `wb-execute-discipline` 3.01.31 → **3.01.32**
跨上下文窗口的长时程任务交接：两类失败（one-shot 半实现 / 后期提前宣布完成）→ 首会话 initializer 建四件套（init.sh / 进度文件 / 功能清单全标未通过 / 初始 commit）→ 清单用 JSON 且只许改状态字段 + 禁删测试 → 一个会话只推一个功能 → 收尾 git commit + 进度摘要、环境留在可合入主干状态 → 起手四步（pwd / 读进度+git log / 挑最高优先级 / 先冒烟再开发）→ 完成判定必须端到端（浏览器自动化像真人一样测）且知道验证工具盲区（看不见 alert 弹窗的功能天然更 buggy）。
**提升层：工作流。**

### 落点 2 → `wb-artifact-verification` 2.10.0 → **2.11.0**
评测环境配置是一等混淆变量：Terminal-Bench 2.0 只改资源配额差 6pp（p<0.01）超过榜单头部差距 / SWE-bench RAM 5x 涨 1.54pp；容器保证值与硬 kill 阈值必须分开（设同值＝零余量，瞬时抖动 OOM）；≤3x 只修可靠性（infra 错误 5.8%→2.1%，分数提升在噪声内 p=0.40），>3x 开始改变评测在测什么（+4pp，紧配额奖励高效打法、松配额奖励暴力打法）；读榜单三纪律（<3pp 领先存疑、必须报执行配置与施加方式、跨时段多天重跑）。
**提升层：可复用 Skill（验证方法论）。**

## 判非重复理由（本轮逐条）

- ed 已有「结构化 handoff / spec 四步循环 / TDD 阶段化 handoff / lane 契约」——那些管**任务与任务之间**的交接；本轮是**同一长任务跨上下文窗口**的交接，交接载体是文件工件 + git 历史而非消息，重叠 <60%。
- av 已有 judge 噪声预算（翻转率/双评委）——那是**判官本身**的随机性；本轮是**执行环境配置**的系统性偏置，两码事；与「主张证据评级」分工已写进段落末句。
- **主动剪除**：advanced-tool-use 的 deferred loading / Tool Search 机制，与 sa line 1918「defer 挂束级 vs 单项」已落内容重叠 >60% → 不落。
- 留候选池（本轮有材但判不落）：① Programmatic Tool Calling（编排逻辑写成代码：中间结果不进上下文、循环/条件/错误处理显式化，每步自然语言调用都要一次完整推理且中间结果必然堆积）——与 cc「大对象外置」部分重叠，待判；② Tool Use Examples 作为工具定义第三层（schema 只保证结构合法，表达不了"何时带可选参数、哪些组合合理"）——与 sa「单示例 few-shot」部分重叠，待判；③ 事故复盘三则（a-postmortem）与沙箱约束（how-we-contain-claude）本轮读了但未提取够硬的独点，留待下轮。
