# 学习轮 r177-B：陈旧证据标记、留痕脱敏、评测并发独立限流（2026-09-25）

## 实拉证据
- 全量重探：173 路 → **165 OK200**（`yt/r177wb/probe_b.txt`，与 A 轮独立；未达集合与 A 轮基本一致：make_templates 403、aliyun 双站/zhipu 连接重置、hf_agents 404、skillscat/skillsdirectory SSL）。
- 主源 **n8n 官方 docs**（清单内）`docs.n8n.io/llms.txt`（1358 条索引）过滤后深拉 14 页 → **14/14 OK200**（`yt/r177wb/B/`）：`understand-dirty-nodes`、`types-of-executions`、`manage-execution-data`、`handle-binary-data`、`scaling/control-concurrency`、`debug-executions`、`security/redact-execution-data`。
- 关键原文事实：
  - 脏节点六条传播规则（改参数标自己 / 插删节点标其后第一个 / 加连线标目标 / 停用标其后第一个 / 改 pin 标其后 / **循环内任一点脏则循环首节点也脏**），重跑才清除。
  - 执行数据裁剪是**两阶段（先标记后删除）**；**二进制裁剪只作用于当前 active 的存储模式**——从 S3 切到文件系统后 S3 里的旧数据没人清；SQLite 下裁剪不释放磁盘（需 VACUUM）。
  - **pin 住的数据只在手动/部分执行生效，生产执行忽略全部 pin**；失败执行可整体拷回编辑器、pin 在首节点复现。
  - **评测并发独立于生产并发**（默认按授权档 1/3/5）；`N8N_CONCURRENCY_PRODUCTION_LIMIT` 同时控制常规模式限流与队列模式 worker 并发两套机制。
  - 部分执行**必须挂 trigger**，且工作流过大时直接拒绝（需先用 limit 节点压数据量）。
  - 执行数据脱敏：**元数据（状态/耗时/节点名）保留、payload 换占位**；生产与手动两个独立开关；实例级强制是**下限（floor）**，被覆盖的设置在工作流侧锁死；**错误详情一起脱，只留错误类型与 HTTP 状态码**。

## 落地（3 文件）
| 文件 | 版本 | 独有点 |
|---|---|---|
| engineering/wb-execute-discipline | 3.01.35（并发线已占 3.01.34） | 脏标记六条传播规则含循环绕回首节点 · 清理策略只作用于当前存储后端（换后端留遗留、SQLite 裁剪不释放空间）· 调试替身在生产被显式忽略、复现失败用真实失败数据 |
| engineering/wb-artifact-verification | 2.13.0 | 评测跑批并发独立设限不共用生产限流 · 同一变量管两套并发机制要看优先级 · 失败样例须可原样重放 · 夹具生产侧失效规则 · 部分重跑需 trigger 且受体积上限 |
| defaults/wb-context-compressor | 3.75.0 | 留痕脱敏元数据留载荷脱 · 生产与手动两个独立开关 · 实例级强制是下限不可局部放松 · 错误详情一起脱只留类型与状态码 |

## 判非重复理由
- ed 已有「历史结果继承规则」（源与条件未变才继承）→ 本轮落的是**脏标记的传播与清除机制**，是前者的可执行版本，已在正文写明分工。
- ed 3.01.22 已有并发与资源池标定 → 本轮落的是**按流量来源分桶（生产 vs 评测）各自设限**，属分桶维度。
- cc 3.61 已有脱敏四则（写盘前字段匹配）→ 本轮落的是**展示/检索侧的内容抹除与策略单调性**，两侧互补。

## 并发处置
提交前发现**并发线未提交改动**（ed 3.01.34 / cc 3.74.0 / sa 2.94.0，Qoder 审计落地）→ 先单独并入 commit `8bfda26`，再落本轮，未覆盖对方内容。

## 未落
- `convert-to-sub-workflows`、`link-data-items/item-linking-errors`：数据接线产品面，不落。
- `use-external-storage`（11KB）S3/外部存储配置：运维面，留候选池。
