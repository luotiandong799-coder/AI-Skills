# 2026-09-22 学习轮 r132（三轮 9 次落地 · Devin 官方 / Inngest ai-patterns / CrewAI v1.15.22 三源首读深拉）

## 三轮独立重拉（逐站留痕）

| 轮 | 107 路结果 | 未达集合特征 |
|---|---|---|
| A | 91 OK200 / 16 未达 | 基线未达 16 站 |
| B | 88 OK200 / 19 未达 | **比 A 多 3 个 GitHub 站未达**（mattpocock_skills / agent_rigor / skillbuddy） |
| C | 91 OK200 / 16 未达 | **把 B 多出的 3 个打回可达**，与 A 集合又不同 |

三轮未达集合互不相同 → 独立进程重拉成立，非复用结论。

## 主源（三个，本轮首次深读）

1. **Devin 官方 `docs.devin.ai/llms.txt`（39KB，212 个 `.md` 页）→ 实拉 20 页全 200**：`cli/extensibility/{rules,skills/creating-skills,hooks/lifecycle-hooks}`、`cli/subagents`、`work-with-devin/{dynamic-workflows,devin-handoff}`、`product-guides/{knowledge,skills}`、`essential-guidelines/*`。
2. **Inngest `ai-patterns/*` + 关键 `guides/*`（r131 只抽了 learn/*，本轮补未读）→ 实拉 20 页全 200**：agent-tool-loops / sub-agent-delegation / human-in-the-loop / fan-out-jobs / working-with-loops / error-handling / versioning 等。
3. **CrewAI v1.15.22 `concepts/*` + `guides/flows/*`（r131 抽 20 页，本轮补 16 页）→ 全 200**：collaboration / processes / publish-custom-tools / memory / flows / llms / knowledge。

## 落地（9 独点 · 三轮各 3）

| 轮 | 文件 | 新版本 | 独有点 | 提升层 |
|---|---|---|---|---|
| A | sa | 2.49.0 | 常驻规则只做路由表（rule 引用 skill，不留正文）+ `allowed-tools` 是免弹窗不是白名单 + 同名字段在 skill 与 subagent profile 语义相反 + 权限叠加不能提权 + 停用≠删除 | 可复用 Skill |
| A | sd | 1.57.0 | 跨 agent 编排可重放：禁依赖六类（时间/随机/生成 ID/环境变量/文件系统/网络响应）+ 改一个 prompt 的重跑影响面（它+下游）+ schema 小且平 + 何时不上编排四种 + pipeline≠parallel + 共享工作树必须不重叠 | 工作流 |
| A | pt | 1.56.0 | fan-out 前先切片验单价 + 模型由 profile 决定、只读探索走便宜档 + 后台任务必须先批完工具 + 嵌套自动降级、编排恒一层深 | 工具/成本 |
| B | sd | 1.58.0 | 委派同步/异步判据＝父需不需要答案才能继续 + 任务描述必须自包含 + 委派失败降级为可对话信息 + 并发等待相关性键 + 等待是挂起不是轮询 | 工作流 |
| B | dl | 1.28.0 | 转圈检测（同一动作连续重复即卡死，不等超时）+ 迭代上限按复杂度定档（2–3 / 15–20）+ 每个工具各成重试单元 + 并行单元 id 带序号 + 预算按累计输入 token 卡 | 可复用 Skill（排障） |
| B | sa | 2.50.0 | 一个子代理一个独立入口，不用参数分发 + 专用化靠任务描述与工具集、不新增实体 + 防递归两道（工具集 + 硬上限） | 可复用 Skill |
| C | sd | 1.59.0 | 派活与提问是两个工具不是一种 + 委派权按角色开关 + 协作故障三症状→三修法对照表 + 角色要具体互补 | 工作流 |
| C | sa | 2.51.0 | 对外能力契约显式声明不靠推断 + 角色/技能重叠反例（"General Assistant"）| 可复用 Skill |
| C | mts | 1.36.0 | 同一结果给模型与给程序两种形状 + 改视图不动本体 + 两边不取并集 | 工具（输出侧） |

## 判不落（逐条理由）

- Devin `devin_goodbad` / `instruct` / `playbooks_use`：指令好坏六要素与 wb-spec-driven 规约重叠 >60%。
- Devin `knowledge` / `knowonboard`：Knowledge 已 deprecated，机制面只剩"专用扩展名文件才被自动加载"，与 sa 已有约定文件名覆盖重叠。
- Devin `cli_hooks`：生命周期钩子 API 面，无方法论判据。
- Inngest `hitl` 主体：与 sd §人工介入阻塞/留痕两型 + 豆包 D58 重叠 >60%（只取 `match` 相关性键与超时动作）。
- Inngest `fan-out-jobs` / `step-parallelism` / `batching` / `delayed`：参数与 API 面。
- Inngest `agent-tool-loops` 的上下文修剪（保头尾丢中间）：与 r131-C mts 已落点重复。
- CrewAI `memory` / `llms` / `knowledge` / `training` / `telemetry`：平台实现与记忆实现面（且 ctx 为豆包主战场，主动避开）。
- CrewAI `flows` / `conversational-flows`：状态机 API 面。

## 三件套评估

本轮三件套占 **4 次**（pt ×1、mts ×1）。判断：**维持 3 件套**——新增内容全部能归到既有三个功能位（决策/输出/输入），没有出现需要独立覆盖的新功能位；基数由功能决定不凑数。ctx 本轮无新材料且为豆包主战场（r122 已落 D90/D95），主动避开。

## 审计

repo 45 SKILL.md；PyYAML **45/45 全解析**；CRLF 0 / STRAY_CR 0 / U+FFFD 0 / TRAILING_WS 0 / description >1024 **0** / 标点开头 **0**。

## 版本化

4 次非强推 push（SSH over 443、无 VPN、不重试不补推）：`d3ebd84`(A) → `a50347f`(B) → `b8c8f74`(C) → 共学双写。A 轮开工遇远端 AHEAD → `git rebase FETCH_HEAD` 后推，未强推。
