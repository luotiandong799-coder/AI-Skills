# 2026-09-22 学习轮 r118-C（评测/安全/提示工程角度 · 10 次实拉 · 落地 4 独点）

批前前置：WB r128 判重基准沿用；本地=远端=85a1387 + r118-A/B commit；本轮新实拉（评测/安全层）与 A/B 轮角度不同，结论不复用。

## 10 次实拉（逐站留痕）
1. arXiv 评测批：AgentEval 2604.23581（DAG step 级评测+层级失败分类+错误传播追踪）、TED 2603.15483（progress efficiency）、One-Eval、CLEAR（KPA 批量化）、KDD 三法对比、JADE 2602.06486（证据依赖门控）、AdaRubric 2603.21362（任务自适应 rubric）
2. 安全批：OWASP 注入、Microsoft agent-governance prompt_defense.py（17 向量检查单）、2604.23887（输出过滤唯一完美防御）、OWASP Memory Guard（记忆哈希+回滚）、OWASP red/blue/purple
3. 提示工程批：C.R.I.S.P.E.、techiehub 六法、BestHub few-shot、LobeHub router——均为已知技术，无独点
4. OpenClaw：exec approvals 四档（deny/allowlist/ask/auto）、multi-agent sandbox precedence（下层只能收紧不能放宽）、sandboxing 默认关
5. HF 生态：smolagents（LocalPythonExecutor 非安全边界）、LLM-Agent-Harness-Survey（通用沙箱不存在）、Cyber-Capable Agents、Phantom-B 事件
6. 技能供应链：SkillFortify 2603.00195（SMT 形式验证）、Vett 两层+签名+不可变注册表、Unit 42（5% 带攻击链）、NVIDIA verified、MCP agent trust、LiteLLM CVE（pull_request_target）
7. 国内：SkillTrustBench（扫描器检测效能基准）、DeepSeek Harness 红队 A.I.G（多路径注入面）、Agent3Sigma、华为 AgentArts、智能体安全蓝皮书
8. 智谱 Z.ai GLM-5.1（平台发布，不落）

## 落地（4 独点）
| 编号 | 落点文件 | 独点 | 来源 | 提升层 |
|---|---|---|---|---|
| D42 | av | 证据依赖门控：评估/审核结论时检查其支撑 claim 是否被推翻，建立在被推翻 claim 上的结论直接无效 | arXiv 2602.06486 JADE | 可复用 Skill（评测判定） |
| D43 | av | 错误传播追踪：评测不只定位失败步骤，要追踪错误从哪一步传播到哪一步（层级失败分类学） | arXiv 2604.23581 AgentEval | 可复用 Skill（错误分析） |
| D44 | ctx | 注入防御输出侧优先：输出过滤是唯一零泄漏防御（15000 次攻击压力测试）；配 17 向量 system prompt 缺失防御检查单；注入沿多路径进入（Skill 资产本身是攻击面） | arXiv 2604.23887 + Microsoft agent-governance + 腾讯 A.I.G 红队 | 工作流（安全纵深） |
| D45 | ctx | 通用沙箱不存在：按任务形态选/配沙箱；本地限制器不是安全边界（smolagents LocalPythonExecutor 官方声明可绕过） | HF LLM-Agent-Harness-Survey + smolagents 官方 | 工作流（执行环境） |

## 判不落（逐条理由）
- TED progress efficiency：与 r118-A D36"压缩评测双指标"同族（都要补完成率之外指标），且"用对话轮数测进展效率"偏研究侧，不单列。
- CLEAR KPA：sa D35 ESPO"错误聚类为结构性模式"已覆盖，重叠 >60%。
- AdaRubric 自适应 rubric：av D33 评委可靠性协议已覆盖"固定 rubric 的误评问题"，重叠 >60%。
- One-Eval/AgentEval 自动化系统：工程系统细节，方法论已抽 D43。
- SkillFortify 形式验证：方向价值高但个人调用侧无直接可执行形态；"技能不能超出声明能力"判据并入 av 漏斗语境，不单列。
- Vett/NVIDIA/Unit 42：生态方案与数据点，av 指纹/漏斗已覆盖同类。
- exec approvals 四档：ctx r128"限制绑执行通道"已覆盖，重叠 >60%；auto 档中间审查层有独特性但优先级低，留后续。
- sandbox precedence（只收紧不放宽）：与 D40 权限衰减同族，ED 已含。
- SkillTrustBench/Agent3Sigma：扫描器评测与模型安全榜，数据型信息，不落。
- LiteLLM CVE：实例数据，pull_request_target 教训并入 D44 语境提及。

## 功能套件检查
av 落 D42/D43（评测判定+错误追踪）补"评测因果维度"；ctx 落 D44/D45（注入防御输出侧+沙箱判据）补"安全纵深"；sa/ED 本轮无改动；mts/ponytail 无新材料；套件维持 3 件套判据不凑数。

## 垃圾清理
本轮零临时文件；保留：本留痕 + 被并入的 av/ctx。
