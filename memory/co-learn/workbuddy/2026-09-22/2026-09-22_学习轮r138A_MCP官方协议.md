# r138-A 全站实拉 · MCP 官方协议深读（2026-09-22）

## 一、实拉证据
- 全站：**110 路 / 97 OK200**（`yt/r138/a/_summary.txt`，VPN 开，代理 33210 生效）。
- 主源深拉：MCP 官方 `modelcontextprotocol.io` **24 页全 200**（`yt/r138/deep_a/`），此前未做过 MCP 官方主线深读。
- 关键页：`develop/clients/client-best-practices`、`extensions/skills/overview`（SEP-2640 Final）、`specification/2026-07-28/basic/versioning`、`server/tools`、`seps/1303`、`seps/986`、`tools/debugging`、`basic/patterns/mrtr`。

## 二、落地（3 文件 3 独点）
| 文件 | 版本 | 独有点 | 层级 |
|---|---|---|---|
| engineering/wb-debug-loop | 1.31.0 | 诊断通道与被诊断通道物理分离（stdio 禁写 stdout）+ 观测按请求 opt-in 默认不发 | 工具/工作流（诊断设施） |
| engineering/wb-skill-authoring | 2.57.0 | 技能经协议分发三段式（能力声明→列表只给 sha256 摘要→按需 read）+ 读到≠激活 + 聚合方前缀消歧、来源自报名不可信 | 可复用 Skill（分发与完整性） |
| engineering/wb-spec-driven | 1.63.0 | 兼容性不靠开场握手（每请求带版本、拒绝回 supported 清单、旧名别名保留一个大版本） | 工作流（契约演进） |

## 三、判非重复（逐条）
1. **诊断通道分离**：库内 `wb-debug-loop` 有"最小化复现""卡死检测""错误四档"，但**全部管"怎么定位问题"，没有一条管"观测本身会污染被测对象"**。stdout 污染是本机真实坑类（脚本回显混入协议/产物），且规范给了硬约束（MUST NOT）→ 落。
2. **Skills over MCP 分发**：sa 已有 §渐进披露（本地三层加载）与 §分发到多个 CLI（本地工作树装多 CLI），**没有"跨网络分发 + 摘要校验 + 读≠激活"**。SEP-2640 明确"resources/read 读 SKILL.md 不激活，须经宿主校验与批准路径"——这是**远端内容提升为可执行指令前的门**，与本地加载是不同威胁面 → 落。
3. **无握手版本兼容**：sd 有 §接口契约（一次交付双方认什么）、§委派边界，但**没有"契约随时间演进怎么不断老调用方"**。规范机制（逐请求版本 + 拒绝回 supported + 别名保留一个大版本）可直接迁移到技能/工具改名与改形状 → 落。

## 四、判重不落地（有证据）
- **渐进工具发现三层 + 1–5% 阈值 + prompt caching 追加不重排**：ctx 已有 §上下文预算量化三件套（固定比例+预留缓冲+触发阈值）与 §上下文分级治理 → 重叠 >60%，不落。
- **Elicitation / MRTR 中途反向索取**：ED §116–124 已于 2026-09-14/15 实拉落地（含 `requestState` 状态令牌与重放）→ 已覆盖，不落。
- **Programmatic tool calling / code mode（脚本在沙箱跑只回摘要）**：ctx §工具输出 offload（长输出落盘只读结论）+ r129B 已判 `code-as-action` → 已覆盖。
- **工具注解（readOnly/destructive/idempotent/openWorld）**：sd §441 工具安全标注契约已落 4 个 hint → 已覆盖。
- **工具形状阈值 3 次合并 / 8 参数拆分**：sa §806 已落（AWS MCP strategies）。
- **程序化工具调用/MCP Apps UI、Registry 审核、OAuth 安全**：平台实现面或安全细则，非技能系统底层方法论 → 不落。

## 五、新发现站点（待用户定是否计入必须项）
- `modelcontextprotocol.io` **全站**（含 `llms.txt`、specification、extensions、SEPs）——本轮首次做主线深读，方法论密度高（协议级契约/兼容/错误/分发），**建议升级为必须项**。本轮已实质深读 24 页。

## 六、三件套评估
- wb-ponytail / wb-max-token-saver / wb-context-compressor 本轮未触及；A 轮新增点分别落在 debug-loop / sa / spec-driven，与三件套无功能位冲突 → 无需增删。
