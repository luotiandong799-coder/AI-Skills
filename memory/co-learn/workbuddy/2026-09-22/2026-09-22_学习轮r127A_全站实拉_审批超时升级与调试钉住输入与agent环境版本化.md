# 2026-09-22 学习轮 r127-A（审批超时升级 · 调试钉住输入 · agent 环境版本化 · 10 次实拉 · 落地 3 独点）

批前基准：WB r134/r135 批 + r124 D115-D137 + r125 A/B/C D138-D161 + r126 A/B/C D162-D174。A 角度=平台发布/工作流引擎/HITL/技能生态侧。

## 10 次实拉（逐站留痕）
1. Dify（dify.ai blog/release 1.17.0/CSDN/DENET）：**Human Input Node（v1.13.0：工作流暂停等人工审阅，批准/编辑/改道后恢复）**；New Agent（chat 构建 agent，自动生成可复用 skills+保留上下文，就绪后加入工作流）；Creator Center+Template Marketplace（工作流模板一键采用）；多模态检索（知识库图文混合检索+LLM vision node）；agent runtime reworked（shell-based LLM agent 范式）
2. n8n（n8n blog/mech.app/synta/n8nlogic）：**HITL 工具级审批（工具启用 human review→暂停→给工具名+$fromAI()参数→批准/拒绝）**；**审批请求带上下文（agent 推理+参数，不只动作）**；**审批超时（4h）→升级另一审阅人或优雅失败+通知**；**记录谁批了什么**；cap max iterations（AI Agent 节点硬上限）；**pin input data（调试钉住输入数据，重跑不重执行前置不烧 token）**；fallback branch（LLM down/rate-limited/垃圾输出→优雅退出）；缓存查询（同题重复答→先查缓存）；agentic 设计模式（retry/fallback model/fallback provider/human escalation）
3. Langflow（langflow.org/docs）：**Memory bases（per-flow 向量存储自动摄取会话消息，跨会话语义检索——按语义相似度返回相关上下文而非最近消息）**；Assistant flow building；DB providers（Chroma/OpenSearch/pgvector 可配）；1.11.0 Multi-vector retrieval（ColBERT 晚交互+ColPali 视觉文档检索）；~89% 内存削减（依赖剪枝/worker 生命周期/COW）
4. Activepieces（activepieces.com/changelog）：**AI-ready pieces（tool search：按任务描述找动作→查 schema→运行，不只按名字）**；AI metadata+audience；human approvals（run 等待审批）；step-level logs（输入/输出/错误）；Tables（跨 run 结构化状态）；400+ MCP servers
5. Make（help.make.com/Academy）：LLM integration 模式（classify→validate→route）；不该用 agent 的清单（简单逻辑/确定性流程）；agent 工具命名与描述（动词开头"add customer email to spreadsheet"式）；逐步构建+组件独立测试；context window 校准（system prompt/context files/tool definitions/对话历史占满窗口）
6. Pipedream（pipedream.com/docs/changelog）：**remote MCP server 暴露 10,000+ 工具+toolAnnotations（read/write/destructive）**；OAuth 静态 URL；被 Workday 收购（Flowise 建 agent、Pipedream 连接系统、Sana 搜索三位一体）；credits 计费（30s 计算=1 credit）
7. OpenClaw（docs.openclaw.ai/clawdocs）：**Claws（versioned setup for one new agent：便携身份+工作区文件+技能+插件+MCP servers+cron jobs 一次声明，可复现新 agent；不替换现有 agent）**；Skill 判据（已有工具但需可重复流程/评审标准/命令序列/操作约束时用 Skill）；plugin 自带 skills（openclaw.plugin.json 声明，plugin 启用时加载）；plugin 架构（v2026.5.28-6.1 外部化官方插件+SQLite 安装索引+SecretRef 凭证契约+生命周期 hooks）
8. deeplearning.ai（learn/corporate/charonhub）：**Building Adaptive AI Agents（2026-08-26 新课程：continual learning+behavior adaptation：trajectory-to-skill learning 从轨迹自动学技能）**；Agentic AI 主线（reflection/tool use/planning/multi-agent+evals+error analysis）；CrewAI 多智能体课程（生产/调试/观察/优化）；Evaluating AI Agents（observability+组件评测+实验）
9. Anthropic Skills 官方（docs.anthropic.com/resources PDF/help center）：**"context window is a public good"：SKILL.md 每个 token 与对话历史竞争；默认假设 Claude 已很聪明只加它不知道的（50-token 好例 vs 150-token 坏例）**；启动只预载 name+description 元数据，SKILL.md 相关时才读；trigger evals（40 行标注 eval 测该触发时触发/该安静时安静）；skill-creator 官方技能（创建→起草→测试→评估→重写循环）
10. GitHub 生态（DEV/小鹏排行/trendshift/agskills/refolk）：google/ax（开源 agentic orchestrator）；alibaba/open-code-review 28.5k（代码评审 agent）；mastra 28.2k（TS agent 框架）；WeKnora 28.2k（RAG 知识平台）；claude-seo（19 子技能+12 子代理+3 扩展）

## 落地（3 独点；初判 5 点，双键检索剔除 2 点）
| 编号 | 落点文件 | 独点 | 来源 | 提升层 |
|---|---|---|---|---|
| D176 | ED | 审批超时升级与审计：审批挂起设超时（如 4h）→超时升级另一审阅人或优雅失败+通知；记录谁批准了什么（审计留痕） | n8n 官方 + n8nautomation.cloud 2026-04-10 | 工作流（HITL） |
| D177 | ED | 调试钉住输入：工作流开头 pin 输入数据，重跑从钉住点继续不重执行前置节点、不重烧 API token（调试=改判据重跑，不是重跑全部） | Synta《5 Real Patterns That Actually Ship》2026-06-28 | 工作流（调试） |
| D178 | sa | agent 环境版本化打包（Claws）：便携身份+工作区文件+技能+插件+MCP servers+cron jobs 一次声明，可整体复现新 agent，不替换现有 agent | OpenClaw 官方 docs.openclaw.ai·cli/claws 2026-09-15 | 可复用 Skill（打包） |

## 复核剔除（双键检索命中既有落点）
- D175 Human Input Node（暂停等人工，批准/编辑/改道后恢复）：ED L522-526 已落"审批三要素：风险评分触发+精确 payload+从该步幂等继续"——暂停审批→继续机制已覆盖，改道为弱增量，不装。
- D179 trajectory-to-skill（从轨迹自动学技能）：sa L296/L365/L390 已落"指令迭代用执行轨迹当输入"+"打包脚本信号（发现每次重造同一段逻辑→写测过的脚本）"——轨迹→技能/脚本链路已覆盖，自动化为课程宣传弱增量，不装。

## 判不落（其余，逐条理由）
- Human Input Node 平台实现（Dify）：平台事实——不装。
- Memory bases 语义检索记忆：ctx 已落语义召回（L1236/L175 记忆四策略）——不装。
- Multi-vector/ColPali：平台功能——不装。
- tool search/step logs/approvals（Activepieces）：平台实现，sa D161 已落工具 schema/ED 已落审批——不装。
- LLM integration classify-validate-route（Make）：ED 已落路由/校验——不装。
- 不该用 agent 清单（Make）：ED 已落"大多数多 agent 系统不需要多 agent"——不装。
- toolAnnotations 10k 工具（Pipedream）：sa D161 已落——不装。
- context window public good/concise 原则（Anthropic）：sa Gotchas 已落描述写作——不装。
- trigger evals（Anthropic）：sa D160/caliper 已落激活两方向评测——不装。
- Building Adaptive AI Agents 课程本身：平台事实——不装。
- Google ax/alibaba open-code-review/mastra/WeKnora：平台事实——不装。

## 功能套件检查
ED 落 D176/D177（审批超时+调试钉住），三件套覆盖完整。sa 落 D178（agent 环境版本化）。与 WB 新 commit、r124-r126 无重叠。

## 垃圾清理
零临时文件；保留：本留痕 + 被并入的 ED/sa。
