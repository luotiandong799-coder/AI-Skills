# 2026-09-22 学习轮 r118-B（Agent 编排/多 Agent 角度 · 10 次实拉 · 落地 2 独点）

批前前置：WB r128 判重基准沿用（ctx 3.32.0 / sa 2.44.0 / av 1.67.0 / dl 1.23.0）；本地=远端=85a1387 + r118-A commit；本轮新实拉与 A 轮角度不同（编排层），结论不复用。

## 10 次实拉（逐站留痕）
1. arXiv 编排批：2601.13671（企业编排）、2607.11138（capability fencing + lazy discovery）、2604.17009 ParaManager、2603.09619（privilege attenuation 权限衰减）、2606.05711 latent communication
2. 多 Agent 通信/记忆批：DailyAI TencentDB Team Memory、Developers Digest Fan-Out/Fan-In merge 陷阱、AWS shared task state、MaaS 三原语、ClickIT（critic 独立评估、hybrid memory）、Memori attribution、Microsoft agent-academy
3. LangGraph：handoffs context engineering（交接消息必须显式）、supervisor、Command.PARENT、migrate 指南
4. 子代理 fork 批：AgentPatterns（skill context isolation：技能在 fork 子代理窗口执行）、LangChain Organizing Context（supervisor 只收结果，推理 withheld）、Hermes delegation（同文件冲突判据）、Self-Manager、Claude Code Forked Subagent（fork 继承父级历史到分叉点）、MindStudio
5. OpenClaw：subagents（sessions_spawn、默认剥离 session/message 工具、push 通知不阻塞、嵌套深度）、workspace isolation（独立 cwd/上下文副本/session lock）、swarm
6. Dify：Agent Node 1.16-rc（Babysit once fasten forever——**新点**）、A2A Nacos、agentic RAG（已有）
7. 工作流平台：Activepieces（max steps 20、agent builder）、Pipedream（平台功能）
8. deeplearning.ai：Agentic AI / CrewAI 课程动态（无新方法论）
9. WaytoAGI / Agent-World / SelfEvolvingAgent / 模型分层：平台与教程类，无独点
10. 国内 Skill 平台：SkillHub TRACE 评测、SkillPay、阿里虾小宝、字节扣子、QClaw 实战——平台生态动态，方法论少

## 落地（2 独点）
| 编号 | 落点文件 | 独点 | 来源 | 提升层 |
|---|---|---|---|---|
| D40 | ED | 并行子代理隔离纪律五条：①forked context（子代理继承父级到分叉点的历史，或按需全新）②只回最终结果，中间推理/工具调用不进父级上下文 ③工作区隔离（独立 cwd+上下文副本+session lock）④会碰同一文件的工作由主线程自己做 ⑤子代理权限衰减（不继承父级全权，默认剥离会话/消息管理工具） | Claude Code Forked Subagent / Hermes / OpenClaw workspace isolation / privilege attenuation | 工作流（多 Agent 编排） |
| D41 | sa | 技能沉淀"陪跑一次，永久固化"（Babysit once, fasten forever）：新技能/新配置先陪 agent 跑 1-2 次测试运行，把运行中发现的有用配置、文件、技能、工具固定下来 | Dify 1.16.0-rc1 Agent Node / Build-Babysit flow | 可复用 Skill（沉淀流程） |

**并入 ED 同一条（不新编号）**：
- context bleed 防渗漏：并行 agent 共享文件"读-改-读"竞态（A 改完 B 还在用旧值）、环境变量/隐式假设不携带状态、子图 handoff 的消息传递必须显式决定（不能自然流动）——并入 D40 作为隔离纪律第⑥条。

## 判不落（逐条理由）
- Fan-Out/Fan-In merge 陷阱：与 D40 的"只回结果"同族（merge 协议=统一输出 schema），ED"接口契约"已有覆盖，不单列。
- lazy discovery/capability fencing：编排器能力可见性裁剪——ED"按技能建 agent"已含"编排者只编排不干专家活"，重叠 >60%，并入 D40 语境提及。
- hybrid memory（共享放已验证/隔离放推理）：与 r118-A 已落"记忆召回超时"+ ctx"提取→校验"同族，不单列。
- 自我进化循环（CSDN SelfEvolvingAgent）：证据源单一且低质，方向与 EvoSkill/Skill Workshop 同族未深拉，留待后续轮；不硬落。
- Team Memory / Memori attribution：平台功能清单，无调用侧新方法论，不落。
- LangGraph handoffs：交接消息显式=并入 D40 第⑥条；其余为框架 API 细节，不落。
- SkillHub TRACE / SkillPay / 虾小宝 / 扣子：平台生态动态，无技能系统可复用方法论，不落。

## 功能套件检查
ED 落 D40 后多 Agent 纪律更完整（隔离+权限+渗漏）；sa 落 D41 后沉淀流程补齐（评测 D33/D34、触发 D35、沉淀 D41）；ctx/mts/ponytail 无新材料；套件维持 3 件套判据不凑数。

## 垃圾清理
本轮零临时文件（general_search 无落盘、补丁脚本用后即删）；保留：本留痕 + 被并入的 ED/sa。
