# 2026-09-22 学习轮 r121-B（动态指令注入 + 编排状态机 + 缓存经济角度 · 10 次实拉 · 落地 4 独点）

批前基准：r121-A 4 独点（D71-D74）+ WB r130 9 点 + D1-D70。角度错开 A 轮（A 已覆盖记忆/session/后台任务）。

## 10 次实拉（逐站留痕）
1. Agno Dynamic Instructions：instructions as callable（按运行时状态生成）、session_state 模板进指令（{key} 替换）、Session Context Store=快照式（每次更新替换非累积）
2. Mastra Signals：四类投递（sendMessage 进活跃循环/queueMessage 等下轮/sendSignal 后台结果·策略提醒·外部事件/sendStateSignal 状态 lane+快照+增量）、**transient:true 只当前轮投递不保留（防每轮重注指令堆积、模型把过去提醒当历史模仿）**、signal 注入不失效 prompt cache
3. Agent 编排 2026：**supervisor 路由逻辑在 prompt 里无记忆→同一任务循环发给同一 agent；要显式状态机记"试过什么"**；Agent Contracts（编排者发子合同=任务+分配预算+成功标准）；orchestrator 大模型+worker 小模型省 40-60%；Claude Coordinator pattern（session budget 作 fan-out 护栏）
4. 结构化输出自纠错：**错误反馈机制（模型看到具体 schema 错误再重试，非盲重试）**、constrained/guided decoding（token 级 mask）、自校验步骤可拦约 30% 错误
5. 模型路由：Switchcraft（inline 最低成本路由，82.9% 准确率成本降 84%、大模型不总优于小模型 tool-use）、escalation router（小模型先跑+低置信度升级大模型省 50%+）、difficulty-aware 分类路由省 40% 大模型调用
6. deeplearning.ai Agentic AI Module 4：evals/error analysis 已学；agent-evals 反模式表（20 条深思用例>1000 条、二值标准求和>1-5 分、只端到端太噪要加组件级）
7. Anthropic Memory/Claude Code：Memory Tool（just-in-time 按需拉取）、memory 三作用域（user/project/local——与 D64 重叠）、MEMORY.md 200 行上限、skill 内置 append-only 历史日志
8. HF/ModelScope：smolagents 1.26（think in code）、Agents-A1（35B 达万亿参数性能）、BenchLM 排行榜——生态
9. Prompt Caching（强）：**KV hit rate=#1 成本杠杆（prefill 占 85-95%，90% hit→省 80-90%）；prompt 布局=stable-prefix-first+variable-suffix-last；4 缓存断点对应 system/tool schema/few-shot/长文档四静态区；一个 cache-busting 改动废全部后续缓存（会话中不换模型、不增删工具、system 保持静态、动态信息进 messages）**
10. 腾讯 SkillHub/生态：1.3 万技能、月下载 1700 万+、SkillPay、企业共享 skills——生态数字

## 落地（4 独点）
| 编号 | 落点文件 | 独点 | 来源 | 提升层 |
|---|---|---|---|---|
| D75 | ctx | transient 上下文（只当前轮投递不保留，防指令堆积被模型当历史模仿）+ 四类投递（进循环/排队下轮/信号/状态 lane） | Mastra Signals | 工作流（上下文注入） |
| D76 | ED | supervisor 显式状态机（编排记忆不进 prompt，记"试过什么"防循环派活）+ 子合同（任务+预算+成功标准随派发下发） | Agent 编排 2026 + Agent Contracts arXiv | 工作流（编排） |
| D77 | ctx | prompt 布局纪律（稳定前缀优先+可变后缀最后+缓存断点对应静态区）+ cache-busting 纪律（会话中不换模型/不增删工具/动态信息进 messages） | Prompt Caching 2026 实拉 | 工作流（缓存经济） |
| D78 | av | 结构化输出错误反馈重试（把具体 schema 错误喂回模型重试，非盲重试；区分 constrained decoding 与验证重试） | Spring AI / Instructor / Pydantic 自愈循环 | 可复用 Skill（输出校验） |

## 判不落（逐条理由）
- 动态指令 callable/state 模板：与 D61（prompt 视图/meta-prompting 条件）+ sa 已有覆盖相邻，机制非独点（transient 才是独点，已入 D75）。
- Anthropic memory 三作用域：与 D64（子代理持久记忆三作用域）重叠 >60%。
- skill 内置 append-only 日志：弱（并入 D71 工作记忆语境，不单独落）。
- smolagents/Agents-A1/BenchLM/SkillHub 数字：生态非方法。
- 模型路由 escalation/difficulty-aware：与 D63（主代理最小动作）+ 路由已有覆盖重叠 >50%，非独点。
- agent-evals 反模式表：与 av 评测纪律重叠 >60%。
- Session Context 快照式：与 D73 会话状态策略相邻，机制并入 D73 语境。
- 编排 orchestrator 大小模型成本：与 D63 重叠。

## 功能套件检查
ctx 落 D75/D77（transient 注入/缓存布局）；ED 落 D76（编排状态机）；av 落 D78（错误反馈重试）；pt/mts 无新材料；三件套覆盖完整不凑数。

## 垃圾清理
零临时文件；保留：本留痕 + 被并入的 ctx/ED/av。
