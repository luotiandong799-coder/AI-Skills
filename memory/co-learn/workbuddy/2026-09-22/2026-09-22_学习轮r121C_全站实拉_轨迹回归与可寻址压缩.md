# 2026-09-22 学习轮 r121-C（Agent 安全护栏 + 检索校验 + 成本治理 + 长上下文压缩角度 · 10 次实拉 · 落地 4 独点）

批前基准：r121-A/B 8 独点（D71-D78）+ WB r130 9 点 + D1-D70。角度错开前两轮（A=记忆/session/后台，B=编排/缓存/校验）。

## 10 次实拉（逐站留痕）
1. Agent 安全护栏：policy engine（确定性策略引擎在模型推理之外，动作执行前按固有风险/上下文/链上行为评估）、**Anthropic 测试（agent 24/25 次完成凭据窃取，内容过滤全失效，只有环境控制=沙箱/网络出口拦截）**、schema separation（工具响应 schema 校验，不匹配=错误或注入都按硬停处理）
2. Agentic RAG：reranker 最高 ROI（top50→cross-encoder→top5）、hybrid search、CRAG 纠错式（检索质量检测分级）——与 D67 重叠/实现细节
3. Agent 测试：**agent-vcr（工具调用序列录制入 git + CI drift 检测）**、evalview（golden baseline 重放 diff）、**AWS AgentCore 轨迹匹配三档（Exact/In-Order/Any-Order）**、Langfuse 评测四维
4. 声明式编排：**MS Declarative Workflows 1.0（编排 as data：YAML 评审/版本化）**、AgentSPEX（显式控制流+上下文可见性）、**Conductor（agent 间路由确定性 vs LLM 自由路由）**、workflow blueprint（参数化可版本化）
5. 长上下文压缩研究：**ARC 可寻址压缩（append-only ID 日志，压缩成紧凑引用可按 ID 寻回，无需重跑工具）**、**观察掩码 vs 摘要（JetBrains SWE-bench：掩码匹配或超摘要，~52% 成本降 +2.6% 解决率；摘要轨迹长 15%）**、**85% 阈值触发留余量（95% 触发无空间放摘要输出）+ 保留最近 10% 轮次原文作活跃工作记忆**、InfMem PreThink-Retrieve-Write
6. 平台：n8n 2.40 修复/MCP discovery、Langflow Policies（自然语言业务规则→确定性 guard）、Dify——平台
7. 多模态 agent：**semantic references vs pixel coordinates（语义描述定位目标+下游 a11y/DOM 解析坐标，像素坐标分辨率变化即失效）**、agentic vision（缩放/边缘提取验证计划）
8. 成本治理（强）：**budget alert 是烟雾探测器，agent 还需要断路器（enforcement 停止 agent 不再发 LLM 调用直到人/策略恢复）**、**六策略（max turns/墙钟/工具重复上限+root 请求美元预算先于模型调用+贵模型按功能 allowlist+服务账号受限 key+上下文摘要后重放+fan-out 显式上限）**、**硬 token 预算编排层执行（非 provider 限流器）、版本化+按意图类别**
9. OpenClaw/ClawHub skills 生态：publish/install 命令、per-release trust verdicts——平台
10. GitHub 生态：harnesses.sh（Hermes 21 万星/strands harness-sdk/ADK）、Reins（轻量 harness 安全操作 app）、agentic-harness-engineering（evaluate-analyze-improve 循环）——生态

## 落地（4 独点）
| 编号 | 落点文件 | 独点 | 来源 | 提升层 |
|---|---|---|---|---|
| D79 | av | 工具调用序列回归：golden trace 录制入 git + CI drift 检测；轨迹匹配三档（精确/保序/任意序） | agent-vcr + AWS AgentCore + evalview | 可复用 Skill（轨迹回归） |
| D80 | ctx | 可寻址压缩：append-only ID 日志+压缩成紧凑引用可按 ID 寻回（不重跑工具）；观察掩码优于摘要（数据）；85% 阈值留余量+保留最近 10% 原文作活跃工作记忆 | ARC arXiv 2607.25066 + JetBrains SWE-bench + Zylos | 工作流（压缩替代策略） |
| D81 | ED | 成本断路器：alert 是烟雾探测器、enforcement 才停 agent；硬 token 预算编排层执行+版本化+按意图类别；六策略（轮次/墙钟/工具重复上限+美元预算先于模型调用+贵模型 allowlist+受限 key+fan-out 上限） | RockB + waxell + CallIT Dev + Zylos | 工作流（成本治理） |
| D82 | ED | 声明式编排 as data：YAML 可评审可版本化；确定性路由 vs LLM 自由路由的取舍（关键路径确定性，探索用 LLM） | MS Declarative Workflows + Conductor + AgentSPEX | 工作流（编排形态） |

## 判不落（逐条理由）
- 环境控制兜底（沙箱/网络出口）：与 ctx Agent 安全纵深/工具面安全相邻，已有 guardian 三问+最小权限，仅补证据不补机制。
- CRAG 检索质量分级：与 D57/D67 检索分解+rerank 重叠 >50%。
- reranker ROI/hybrid search：与 D67 重叠/实现细节。
- 多模态语义引用定位：弱（与 ED 视觉提取三纪律相邻）。
- policy engine 外部化：与 ctx 安全层机制相近。
- harnesses.sh 生态/Reins/awesome 清单：生态非方法。
- Agentic AI evals 反模式：与 av 评测纪律重叠。
- InfMem/ACM：与 D71（观察式记忆）+ D80 同族，机制并入 D80 语境。

## 功能套件检查
ctx 落 D80（可寻址压缩）；ED 落 D81/D82（成本断路器/声明式编排）；av 落 D79（轨迹回归）；pt/mts 无新材料；三件套覆盖完整不凑数。

## 垃圾清理
零临时文件；保留：本留痕 + 被并入的 ctx/ED/av。
