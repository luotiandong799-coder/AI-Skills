# 2026-09-22 学习轮 r121-A（Session 层上下文策略 + 观察式记忆 + 后台任务信号角度 · 10 次实拉 · 落地 4 独点）

批前基准：WB r130 9 落点（dl/sd/av/mts/sa）+ D1-D70 + WB 建议 5 条（Mastra background-tasks/observational-memory、Haystack Tracing、Agno 动态指令、OpenAI SDK session 策略）。角度按建议挖，避开 WB 已占面。

## 10 次实拉（逐站留痕）
1. Mastra Observational Memory：Observer/Reflector 双后台 agent 维护稠密观察日志替换原始历史；5-40x 压缩；LongMemEval 94.87%；纯文本不依赖向量库；工作记忆=持久结构化事实块（每轮读、agent 自己更新）
2. Mastra Background Tasks + Signals：global/per-agent 并发上限、backpressure=queue（超限排队不丢）、defaultTimeoutMs、suspend→cancel/resume、abortSignal 级联取消子代理、生命周期回调
3. Agno Context：few-shot（additional_input 作为历史消息插入）、动态指令（instructions as function 按状态生成）、**max_tool_calls_from_history（只放最近 n 条工具调用）**、context 五源
4. Haystack Tracing：agent.step.llm/tool span（3.0）、**CompactionHook 追踪字段（压缩策略/估算大小/是否触发/token 目标/是否成功）**、hook 级 span 归因
5. OpenAI Agents SDK Sessions：**状态策略矩阵（to_input_list 应用内存/session SDK+存储/conversation_id 服务端共享/previousResponseId 最简续接）**、session 持久化重试不重复存
6. Dify/n8n：Dify Agent 独立化（平台）、Agentic RAG（迭代检索）、n8n 2.40 force tool call on first iteration（平台）
7. LangFlow/Activepieces/Make/Pipedream：LangFlow 1.11 HITL checkpoints（平台）、Activepieces agent pause 审批（与 D58 重叠）
8. Anthropic Skills 生态：行业标准采纳（OpenAI/Cursor/Copilot）、SkillsMP 66K skills、OpenClaw install policy（平台）
9. WaytoAGI/OpenClaw/docs 动态：v2026.7.1 平台更新（平台）
10. GitHub Trending 9/21：**Hermes Agent（agent-curated memory：agent 自己决定记什么 + periodic nudges 定期提醒整合 + 三层记忆）**、Cloudflare Security Audit Skill、agentmemory、Metis（生态）

## 落地（4 独点）
| 编号 | 落点文件 | 独点 | 来源 | 提升层 |
|---|---|---|---|---|
| D71 | ctx | 观察式记忆双 agent 机制（Observer 持续观察→Reflector 周期重组，5-40x 压缩、LongMemEval 94.87%）+ 工作记忆结构化事实块（每轮读、agent 自己更新）+"记什么"的裁决者（后台 agent vs agent 自身 curate + periodic nudges 定期提醒整合） | Mastra Observational Memory + Hermes Agent | 工作流（记忆/压缩） |
| D72 | ED | 后台任务并发背压：global/per-agent 并发上限 + 超限排队不丢弃 + defaultTimeoutMs；abort 信号级联取消全部在途子代理（父 abort → 子全取消） | Mastra Background Tasks | 工作流（任务调度） |
| D73 | ctx | 会话状态策略四选一矩阵（to_input_list 全手动/session SDK+存储/conversation_id 服务端共享/previousResponseId 最简续接：状态在哪+适合什么+下轮传什么）+ 工具调用历史限量（max_tool_calls_from_history 只放最近 n 条） | OpenAI Agents SDK Sessions + Agno | 工作流（上下文策略） |
| D74 | ctx | 压缩钩子可观测字段（compaction 策略/估算上下文大小/是否触发/token 目标/是否成功 + hook 级 span 归因）——压缩行为本身可追踪 | Haystack 3.1 CompactionHook | 可复用 Skill（压缩可观测性） |

## 判不落（逐条理由）
- Hermes self-improving loop / 三层记忆 / 自动 Skill 创建：与 D71（OM）+ ctx 记忆策略 + WB r129 重叠 >60%（只取 agent-curated + nudge 并入 D71）。
- LangFlow HITL checkpoints / Activepieces pause 审批：与 D58（HITL 审批三要素）重叠 >60%。
- Dify Agent 独立化 / Agentic RAG / n8n force tool call：平台功能。
- A2A / AG-UI / ColPali 多向量检索：平台实现。
- OpenClaw install policy / exec maxOutputBytes：平台安装策略。
- Skills 行业标准 / SkillsMP 生态数字 / agentmemory / Metis：生态数字非方法。
- Agno few-shot 作为历史插入：与 sa 已有提示工程覆盖相邻，动态指令与 D61 语境相邻，非独点。

## 功能套件检查
ctx 落 D71/D73/D74（观察式记忆/会话状态矩阵/压缩可观测）；ED 落 D72（后台背压级联取消）；pt/mts/sa 无新材料；套件维持 3 件套判据不凑数。

## 垃圾清理
本轮零临时文件；保留：本留痕 + 被并入的 ctx/ED。
