# r173-C 自动化载荷治理：预检缓存、显式失败、有界常驻授权（WorkBuddy 实拉）

实拉时间：2026-09-24 20:45
信源（C 组 Agent 生态域，逐站实拉留痕）：
- docs.openclaw.ai llms.txt **200** → automation/cron-jobs/payloads 全文实拉（113KB，四载荷类型/执行样式/模型链/审批卡/输出传递）
- deeplearning.ai the-batch **200**（最新仍 issue-371，r168-C 已覆盖，无新期）——`/the-batch/llms.txt` 路径 404，走主页拉取
- GitHub agent 生态（api.github.com **200**）：anthropics/skills 最新 commit 2026-09-10（Managed Agents 更新，r168-C 已覆盖）；openai/skills 2026-06-22 已弃用（Deprecate skills repository #496）
- docs.openclaw.ai 其余新页（channel-routing/threads-and-sessions 等）为 IM 渠道产品配置，无底层方法论可提取
- agentskills.codes **000**（探活累计+1）；GPT store 沿用 r82 定论（已迁移为 openai 仓库信源，不再开 Edge 逐拉）

## 独点清单（3 个真独点，已落 wb-execute-discipline 3.01.27）

### 独点1：定时任务前置可达性门（工作流层）
- 到期任务先探依赖端点；候选链全不可达才记 skipped；预检按**端点**缓存（官方 5 分钟）不按任务/模型——共享同一死服务器的多任务只探一次，防请求风暴；skipped 不计入执行错误退避。
- 判非重复：ed §依赖探测三型管结果分类，本条管定时场景的预检调度与缓存共享，未落。

### 独点2：模型配置解析失败显式报错（工作流层）
- 任务点名模型不存在→验证错误直接失败，不静默回退默认；空 fallback 链传空覆盖，不静默追加主模型当隐藏重试目标。
- 判非重复：ed §自动降级不覆盖显式决策管"不许夺走用户选择"，本条管"配置本身错了不许替它圆"。

### 独点3：Always-allow 有界常驻授权 + NO_REPLY 静默令牌（工作流层）
- "总是允许"= scoped standing grant（有生命周期/可列出/可撤销），第三档介于"每次问"与"永久开"之间；"没输出"三种含义（失败/不该播/没跑完）分开，静默要显式令牌。
- 判非重复：ed §审批门管设不设门，本条管门开后授权收敛与静默语义，未落。

## 判非重复理由（不落项）
- openclaw 四载荷类型本体（agentTurn/command/script/heartbeat）与脚本预算（300s/900s、50/200 工具调用）：产品机制，可参考不构成底层方法论增量。
- The Batch issue-371、anthropics/skills 0910 更新：历史轮已覆盖；openai/skills 已弃用仅探活。
