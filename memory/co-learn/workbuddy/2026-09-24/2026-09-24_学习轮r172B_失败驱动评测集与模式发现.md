# r172-B 学习轮：失败驱动评测集增长 + 模式发现先于打分

- 日期：2026-09-24｜轮号：r172-B（全局唯一）
- 落点：`engineering/wb-artifact-verification` SKILL.md（文末新增一节）
- 版本：2.07.0 → **2.08.0**

## 实拉证据（本轮独立实拉，非复用）
| 信源 | 状态 | 关键证据 |
|---|---|---|
| braintrust.dev/llms.txt | 200 | Eval 三元组（Dataset+Task+Scorer）/ 失败 trace 转 dataset / 闭环六步 Instrument→Observe→Curate→Evaluate→Deploy→Monitor / Topics 聚类 + Patterns 定时调查（模式可升级为 dataset/scorer/人工评审任务）/ Scorer 三分层 / experiment diff |
| promptfoo.dev/docs/intro | 200 | test-driven LLM development / 矩阵视图 / red-teaming / CI 集成——intro 理念层，与既有"评测先行/触发盲测/CI 门禁"重叠，无独有点 |

## 落地独有点（1 个，4 子点）
**失败驱动的评测集增长 + 模式发现先于打分**（→ wb-artifact-verification）：
1. 生产失败 trace 一键转 dataset 样例，修复后同集验证——失败案例是最有判别力的回归样本。
2. curate（策展）是独立环节：迭代改动源自 trace 实际失败，不靠临时手测灵感。
3. Topics/Patterns：日志语义聚类 + 定时调查重复问题 + 带证据记录；确认模式可升级为 dataset 样例/scorer/人工评审任务。
4. Scorer 三分层（代码规则→LLM judge→人工）按成本逐层升级，能用代码断言的不进模型评分。

## 判非重复理由
- "生产轨迹按计划回灌评测"（r171-C）= 定时对 trace 跑评测；本条 = 失败样例**沉淀进 dataset 永久防复发** + 模式发现升级机制，机制不同，<60%。
- "生产投诉聚类变化"漂移信号（av line 1457）= 聚类分布变化当**信号**；本条 = 把模式挖出来**升级成资产**，粒度与动作不同。
- Eval 三元组/experiment diff → r161-C 量化评估器 + r168-B golden task 回归已覆盖，不落；Scorer 三分层中 LLM judge 校准 → 2.07 评委校准已落（本条只取成本分层组合）。
- promptfoo intro：理念层（test-driven/red-team/CI）与评测先行、触发盲测、r153-B 确定性断言重叠 >60%，不落。
- 提升层级：工作流（评测数据治理）+ 可复用 Skill。
