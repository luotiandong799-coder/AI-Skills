# r161-C · WorkBuddy 审计记录（Haystack 评估/管线/调试面）

- 轮次：r161-C（每小时触发连跑 3 轮之第 3 轮；A/B/C 各独立实拉、独立落地、独立记录）
- 实拉时间：2026-09-24
- 主源：Haystack 官方 `docs.haystack.deepset.ai` llms.txt → deep 拉取 `evaluation,evaluators,agents,pipelines,components,retrieval` 共 80 篇正文（ok200=78，2 篇 GitHub raw 400）
- 全量探活：fetchall c 覆盖 173 路信源，OK200=167（非 200 见 run_c.log，已逐站留痕）

## 一、逐站实拉证据（节选关键源）
| 源 | 状态 | 本轮取到 |
|---|---|---|
| Haystack components/evaluators（faithfulness/context_relevance/document_recall/answer_exact_match/sas/mrr/ndcg） | 200 | 量化评估器定义、score 语义、失败跳过不计分 |
| Haystack tutorials/35_evaluating_rag_pipelines + about-pipelines | 200 | 评估需 ground-truth（答案+文档）、pipeline 先可视化再跑、组件只收所需数据 |
| Haystack components/agents/agent.py | 200 | 显式终止原因才退出、空响应不退出保留恢复、continue_run 不跨退出遗留 |
| Haystack converters/output_adapter.py + mock embedder/generator | 200 | 确定性 mock 同接口可复现、Pipeline.load 默认安全模式、unsafe=True 才解禁沙箱 |
| cookbook/sparse_embedding_retrieval + hyde | 200 | 稠密/稀疏各有短版、常需 hybrid、先评估再定 |
| 其余 171 路（见 run_c） | 167 OK200 | 仅探活留痕，未新深拉 |

## 二、筛选
- ❌ 不计：各 converter/embedder 的逐参数 API、图像/音频封装、具体模型配置。
- ✅ 取：3 个可复用于 wb-* 技能系统的底层方法论。

## 三、判非重复理由
1. **量化评估器 + 可溯源引用（→ wb-artifact-verification）**
   已有 av 2.05 讲"显式失败信号（ToolFailure/isError）+ 配置 fail-fast + 审视注入提示"。本点讲"用 faithfulness/context_relevance/recall 等量化评估器对照 ground-truth 与检索上下文验证输出，引用用 [编号] 指向具体文档"。是验证的"量化+溯源"维度，与"失败信号"不重叠。
2. **确定性替身 + 结构预检（→ wb-debug-loop）**
   已有 debug-loop 讲 repro→minimize→hypothesis。本点补"用同接口确定性 mock 让 bug 稳定复现；pipeline 先可视化确认接线；fallback 只在底层真抛错时生效"。是可复现调试的工程化手段，属新增判据。
3. **单方法不够就组合但先评估 + 契约默认严格（→ wb-ponytail）**
   ponytail 强调 YAGNI/少写；本点补"取舍时先用小评估验证再上复杂组合，不预支复杂度"与"模板变量默认全 required、显式放开可选子集"。是决策与契约严谨性的新增操作点，重叠 <60%。

## 四、落地独点（见本轮交付四列表格）
- wb-artifact-verification 2.05.0 → 2.06.0
- wb-debug-loop 1.53.0 → 1.54.0
- wb-ponytail 1.65.0 → 1.66.0

## 五、三件套评估（本轮）
- ponytail（三件套之一）本轮升级（1.65.0→1.66.0）：补"先评估再组合"与"契约默认严格"。
- max-token-saver：本轮无新压缩点需固化。
- context-compressor：本轮未改（B 轮已升级 3.66→3.67），无新点。

## 六、审计结论
C 轮非缩水：主源换为 Haystack（与 A 轮 Agno、B 轮 CrewAI 均不同源），三个落地点来自本轮实拉正文、均附可核验出处，且与 A/B 两轮所落点位不重复（A: 名字≠权限/空结果取证/大对象外置；B: 任务优先/显式失败信号/知识源外置+作用域记忆；C: 量化评估器/确定性替身/先评估再组合）。A·B·C 三轮信源、深拉、落地、版本均独立。
