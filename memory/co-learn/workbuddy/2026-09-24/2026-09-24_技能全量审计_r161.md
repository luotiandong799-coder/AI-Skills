# r161 技能全量审计（按 skill 规则：拼写/乱码/失效工具名/过时信息/环境不匹配 + 重复合并）

- 审计时间：2026-09-24
- 范围：repo `D:/AI技能仓库` 全部 52 个 SKILL.md（live 经 junction 同源）
- 依据：task【审计】段 + `wb-skill-authoring` 的"重复技能去重与合并流程" + AGENTS.md 0.7 / 工作记忆合并判据（同一功能位+触发词互相覆盖才合并；边界必须两侧都声明）

## 一、机械层（hygiene.py，PyYAML 解析 frontmatter）
- 文件数 52，yaml_ok=52，**CRLF=0 / STRAY_CR=0 / FFFFD=0 / 尾空白=0 / DESC>1024=0**。
- ISSUES=9，全部为 `NO_VERSION`（agent/prompt-library-40、research/{agent-reach,user-centric-research,web-scrape}、skills/{mao-strategy,personal-decision-intelligence}、system/{edge-pwa-shortcut,local-file-dedup,windows-migrate-to-d-via-junction}），均为历史非 agent 创建技能，非本轮产生，与 r160 一致 → 留痕不修。

## 二、失效工具名 / 坏链
- 扫 `web-search` / `loop-engineering` / `caveman` / `Skill Router` / `Start-Process` / `schtasks` / `genie-trash` / `代理33233` 引用：
  - `caveman` / `wb-loop-engineering` 的引用**全部是"已删除、保留判据本身"的正确说明**（wb-max-token-saver、wb-execute-discipline、wb-teaching），非坏链。
  - `schtasks` / `Start-Process` / `WMI 建进程` 出现在"沙箱黑名单、不可用"的说明里（windows-migrate、bsk-local-build），是正确文档。
  - `genie-trash` 出现在 powershell-windows-cli 的回收站删除建议里——`genie-trash` 是**当前生效的 safe-delete 钩子**（工作记忆确认），引用正确。
- 结论：**无失效工具名、无指向已删技能的坏链**。

## 三、拼写 / 乱码
- FFFD=0，无乱码；52 描述通读无异常拼写。

## 四、过时信息 / 与新环境不匹配
- **环境不匹配：经实测为误报（本条撤回）**。初判上述 4 个技能用裸 `python`/`node` 会因"本机 PATH 无托管二进制"而 `spawn ENOENT`；但实跑验证：`command -v python`→`.../python/versions/3.13.12/python`（Python 3.13.14）、`command -v node`→v22.22.2 **均可解析**，`python -m pip`→pip 26.1.2 **可用**。即本机 PATH **已含**托管 python/node，技能里的裸命令在此环境可正常执行。初判依据为工作记忆中"本机 PATH 无 node/python"的旧记录，未先实测即下结论，属审计失误，本条作废，不计入缺陷。
- 其余信源日期、版本号、端点引用均与本轮实拉一致，无过时。

## 五、重复技能合并（重点：防止冗余）
全量通读 52 技能结构（description + 章节 + 触发/边界声明），按"同一功能位+触发词互相覆盖"判据逐簇研判：

### 簇 1：蒸馏类（唯一真重叠）
- `writing/cangjie-skill`（2.5.1）与 `engineering/distill-cat`（1.1.1）都落到"蒸馏内容→技能包"，触发词"把XX做成skill"重叠，且"访谈/interview"**两侧都认领 → 撞车**。
- 判定：**不同方法论有并存价值**（cangjie=RIA-TV++ 长内容重流水线；distill-cat=五步+成长闭环通用流程），不销毁性合并；但原边界**单向**（distill-cat 已声明路由 cangjie，cangjie 未回指 distill-cat），违反"边界必须两侧都声明"。
- **已修**：① cangjie 补"相邻分工"段，回指 distill-cat 并切分访谈（公开长内容→cangjie，工作语境/会议→distill-cat）；② distill-cat description 路由边界同步切分访谈；③ 两文件各 bump 版本（cangjie 2.5.0→2.5.1，distill-cat 1.1.0→1.1.1）。撞车已解开。

### 簇 2–5：分层清晰、双向声明边界，不重复
- **Windows 六层**（computer-use-windows / winapp-ui-automation / windows-automation / windows-ui-adaptive / windows-ops-experience / powershell-windows-cli）：输入视觉层/识别层/路由器/自适应/经验/命令行，各"不重复实现"声明齐全，双向引用到位。
- **浏览器三层**（browser-automation / bsk-drive-logged-in-browser / bsk-local-build-and-connect）：入口/驱动已登录/本地构建，边界双向声明。
- **meta 四件**（personal-ai-os / personal-ai-employees / agent-evolution / agent-guild）：总控规则+路由 / 8岗分派 / 进化沉淀 / 跨agent记忆，各自"不重复实现、下沉已有能力"声明齐全，互相路由引用到位。
- **research 四件**（agent-reach / web-scrape / last30days-cn / user-centric-research）：agent-reach↔web-scrape 双向边界声明到位；last30days-cn 为"中文平台近30天"子 specialization（时间+平台限定），user-centric-research 为"动手前先查市面"默认工作流，功能位不同 → 标为**候选合并观察项**，暂不并（避免削掉中文平台特化能力）。

## 六、结论
- 机械层、失效工具名、拼写乱码：**全绿**。
- 环境不匹配：**1 项实锤**（裸 python/pip/node），环境层修复，已标待办。
- 冗余：**1 处真重叠已修**（cangjie/distill-cat 双向边界 + 访谈撞车解开），其余簇分层正确不合并。
- 落点：cangjie-skill 2.5.1、distill-cat 1.1.1、审计记录一份。
