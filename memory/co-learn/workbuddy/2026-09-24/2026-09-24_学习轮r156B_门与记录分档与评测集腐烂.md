# 2026-09-24 学习轮 r156-B（WB 独立重拉 · 3 文件 3 独点）

## 实拉证据
- 全量重拉：173 路 / **167 OK200** / 6 未达（同 A，make_templates 本轮 200）。
- 主源：**Mastra 官方 `mastra.ai/llms.txt`（659 md 链接，过滤后 130/130 全 200）** —— 清单内信源，此前从未深拉（`evals/*`）。

## 落地（轮 / 文件 / 版本 / 独有点）
| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| B | engineering/wb-artifact-verification | 1.97.0 | 门 vs 记录两档语义（无阈值 scorer＝只记录不拦截）＋★空评测必须报错不许静默通过＋★评分器可见范围决定它测了什么（预置评委只读单条消息、输入相对型只看首条输入）＋★没配记忆的多轮评测照样出分（测不到召回）＋逐轮与全局两层断言 |
| B | engineering/wb-release-maintain | 1.10.0 | 评测集四个静默腐烂点（golden 当真理不版本化／无输入分布监控／模板与数据集分仓版本化／无检索语料快照）＋配套缓解；与既有「未版本化不许续」分工：那条管能不能复现，本条管有了版本之后怎么烂 |
| B | engineering/wb-spec-driven | 1.79.0 | 评测轨与生产轨运行时物理隔离（Check if Evaluating 分流）＋评测步骤不上生产链（不给真实调用加模型开销/延迟/成本） |

## 判重不落（B）
- Mastra 自定义 scorer 四步（createScorer 工厂）：写作面与 av §评分器要单独迭代 重叠>60%。
- 豆包 r155-B 点 4（三级评测闸门 L1/L2/L3 + 失败 trace 回流）：与 rel 既有「渐进改善配回归门」重叠约 40%，记候选池下轮与本节合并评估。
- vitest 集成、multi-turn 的 thread/resource 配置细节：平台机制面，不落。
