# 2026-09-24 学习轮 r159-C 责任链上的「我不处理」/ 拦截信号退化 / 历史加工是替换语义

轮次：r159-C｜信源 173 路全量实拉 **167 OK200**（6 未达同上）
主源深拉：Pydantic AI `ai.pydantic.dev/llms.txt` 过滤 `capabilities` → **35/35 OK200**；补拉 Google ADK `google.github.io/adk-docs/llms.txt` 过滤 `evaluate|callbacks|artifacts` → **9/9 OK200**
（清单内信源；此前 r157-C 拉 tool-search、r158-A 拉 durable_execution/on-demand/compaction、r158-A 拉 evals，**handle-deferred / reinject-system-prompt / process-history / raise-content-filter-error 四页为本轮首读**）

## 一、实拉证据（主源页 + 原文要点）

| 页 | 关键原文 |
|---|---|
| `pydantic.dev/docs/ai/capabilities/handle-deferred-tool-calls` | 需审批或外部执行的调用**默认暂停并返回 `DeferredToolRequests`**；该能力拦截后就地解决并自动继续；handler 可返回**部分结果**，或返回 `None` 表示**这轮我不处理** → **交给链上下一个同类型能力**，始终无人接的**继续上浮为挂起输出**；handler 可同步可异步 |
| `pydantic.dev/docs/ai/capabilities/raise-content-filter-error` | **默认只在响应为空时才抛** `ContentFilterError`；厂商若同时返回了部分文本或拒答话术，**这段文本成为普通 agent 输出，不报错不告警**；可显式开启严格模式（只要 `finish_reason='content_filter'` 一律抛错） |
| `pydantic.dev/docs/ai/capabilities/process-history` | 处理器返回的列表**整体替换**运行的消息历史（要留原件须先复制）；**多个按注册顺序**依次作用；它是 `before_model_request` 钩子的薄封装，**只能改"送进去什么"，不能短路模型调用**（要短路须直接用钩子） |
| `pydantic.dev/docs/ai/capabilities/reinject-system-prompt` | 历史里**已有** `SystemPromptPart` 时**默认 no-op**（多 agent 交接与用户自管提示保持权威）；`replace_existing=True` 才先剥离再前置 —— **历史来自不可信来源时必须让服务端提示赢**；**没有这个能力，配了 system_prompt 的 agent 在历史不含系统提示时会静默无提示运行** |

## 二、落地独点（2 文件 3 组）

| 文件 | 版本 | 独点 | 层级 |
|---|---|---|---|
| `engineering/wb-execute-discipline` | 3.01.16 | ①**「我不处理」是一等信号**：责任链上返回空＝交给下一个（不是错误），部分处理与全部处理都是合法态，**顺序即优先级**且不会自动生效 ②**被拦下的信号默认会消失**：只有空响应才算拦截，带解释的拒答会退化成普通输出 → 靠"有没有报错"判断有没有被拦会漏掉最常见的一类；严格模式是显式选择；下游不能把默认档当严格档 | 工作流 |
| `defaults/wb-context-compressor` | 3.62.0 | 历史加工三则：**替换不是追加**（"取最近五条"会顺手丢掉系统提示）；**多个按注册顺序**（先脱敏后裁剪 vs 反之结果不同）；**加工层不能短路模型调用**（能力边界看它包的是哪个钩子）；**系统提示缺失是静默失效**，两种权威策略对应两种信任假设（历史可信让它做主，来自前端/库/压缩管线则必须覆写） | 工作流 + 工具 |

## 三、判非重复理由

- **vs r157-B「配了不等于生效」（注册位置决定哪一半工作）**：那条管**注册位置**；本轮管**传入历史里根本不含系统提示**这一独立失效原因，并给出两种权威优先级的显式选择。增量 ≥65%。
- **vs r158-A「压缩是可见性边界」**：那条管**运行内压缩之后要重新广播**；本轮管**外部传入的历史里长期指令可能压根不在**。增量 ≥70%。
- **vs 豆包 r159-A 审批门**：那条管**要不要人批**；本轮管**谁能批、批不了怎么沿链传递**。增量 ≥70%。
- **vs ed 既有错误分型**：那条管**自己判断错误类型**；本轮指出**"拦截"这一类信号在默认档下根本不产生错误**。增量 ≥85%。

## 四、判重不落

- `durable_execution/*`（temporal/dbos/prefect/restate/kitaru/airflow 7 页）：与 r158-A 持久执行面重叠 >60%。
- `capabilities/on-demand`、`tool-search`、`compaction`：r157-C / r158-A 已落。
- ADK `evaluate`/`artifacts` 9 页：评测标准与产物存储示例，与 av/已有面重叠，不计。

## 五、三件套评估

本轮落 cc（历史加工与系统提示权威）。三件套功能覆盖完整，未增删成员。
