# r172-A 学习轮：后台压缩/推理块保留 + 流式中途终止语义

- 日期：2026-09-24｜轮号：r172-A（全局唯一，接续 r171）
- 落点：`defaults/wb-context-compressor`（文末新增）+ `engineering/wb-execute-discipline`（文末新增）
- 版本：cc 3.67.1 → **3.68.0**；ed 3.01.24 → **3.01.25**

## 实拉证据（本轮独立实拉，非复用）
| 信源 | 状态 | 关键证据 |
|---|---|---|
| platform.claude.com/llms.txt（docs.claude.com 重定向后） | 200 | compaction 五策略分页（threshold/on-demand/background/keep-recent-turns/preserved-thinking）+ handling-stop-reasons + handle-streaming-refusals + session budgets + define-outcomes |
| platform.openai.com/docs/llms.txt | **Cloudflare 拦截** | 探活失败 +1（无正文可拉，不虚构） |

## 落地独有点（2 个）
1. **后台压缩 + 推理块保留**（→ cc）：
   - background compaction：异步压缩不阻塞主对话，但存在"压缩未就绪窗口"，需兜底；与既有"85% 触发留余量"（同步）互补。
   - preserved thinking：仍在被引用的推理块不能随压缩折掉，保留清单新增一项。
   - 组合决策框架：压缩=触发机制×保留策略×状态外置。
2. **流式中途终止的处置语义**（→ ed）：
   - 流式 refusal/stop 中途出现，已流出部分不默认完整，先定保留/丢弃语义+显式标注。
   - stop_reason 逐型配 fallback 链（max_tokens 可续跑 / refusal 换路 / end_turn 才算成功），不统一当"完成"。

## 判非重复理由
- cc：threshold（→85%触发/四层预警已落）、keep-recent-turns（→轻触压缩/保留最近10%/保护区已落）**不重复落**；本轮只落 cc 缺的 background 窗口风险 + preserved thinking 两个点，重叠 <60%。
- ed：model fallback 链（认证轮换→有界恢复→换模型，r154 已落）管"换路"；本条管"半路停时半成品处置 + stop_reason 逐型"，与"终态熔断不是换路信号"互补不重复；失败路径三分律管 catch 层，本条管流式交付层。
- Claude docs 其余面判不落：session budgets（→有界修复/预算校验重叠）、define-outcomes（→spec 先行重叠）、inference hooks（→§七·4 运行时拦截重叠）、Skills 版本化 API（→版本纪律既有）。
- 提升层级：1=工作流+可复用 Skill；2=工作流。
