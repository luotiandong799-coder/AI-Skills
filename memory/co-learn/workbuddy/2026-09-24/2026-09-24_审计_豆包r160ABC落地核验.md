# 2026-09-24 审计：豆包 r160-A/B/C 独点核验与落地裁定

审计对象：豆包 `2026-09-24_学习轮r160A_LLM测试工程与质量门`、
`r160B_Agent工作流模式语言`、`r160C_RAG生产架构与混合检索`（各 6 点，共 18 点）。

## 一、合规核验

- 三份均**含"实拉证据"与"判非重复理由"两段**，符合 09-23 分工铁律。
- 三份均声明"**仅留痕记录，不写技能文件**"。实测：本轮开工 `git status` 干净；
  `defaults/wb-max-token-saver` **live == repo、sha256 前缀同为 `85448648cb6fc30a`、版本仍 1.46.0**
  → **无越权写入、无版本偏差**。

## 二、独立复拉核验（我方证据）

- **RRF 混合检索（C1 核心）属实**：我方清单内官方源 `qdrant_llms.txt` 实测
  "**combining dense semantic vectors and sparse lexical vectors with the prefetch API and
  Reciprocal Rank Fusion**"、"**fuses dense embeddings with BM25 sparse vectors using reciprocal
  rank fusion**"；`chroma_llms.txt` 实测 "**Hybrid Search with RRF … combine multiple ranking
  strategies using Reciprocal Rank Fusion**"。→ dense 与词法两路互补 + RRF 融合成立。
- **可靠性悖论（B6）算术核验通过**：`0.95**5 = 0.7738 ≈ 0.77`、`0.95**10 = 0.5987 ≈ 0.60`（本地计算）。
- **未复现细节（已在落地文本中不作为硬数字引用）**：
  RRF 常数 `k=60`、`70-20-10` 金字塔比例、`agentsnap` 四维阈值具体数值 —— 未在我方清单内官方源复现。

## 三、裁定落地（6 文件 13 组）

| 来源 | 落点 | 新版本 |
|---|---|---|
| A1/A2 | `engineering/wb-artifact-verification` | 2.03.0 |
| A4 | `engineering/wb-artifact-verification` | 2.03.0 |
| A5 | `engineering/wb-artifact-verification` | 2.03.0 |
| A6 | `engineering/wb-artifact-verification` | 2.03.0 |
| A3 | `engineering/wb-skill-authoring` | 2.79.0 |
| B1/B2 | `engineering/wb-execute-discipline` | 3.01.20 |
| B3/B4/B6 | `engineering/wb-execute-discipline` | 3.01.20 |
| B6（五 context 失败） | `defaults/wb-context-compressor` | 3.65.0 |
| C3/C4 | `defaults/wb-context-compressor` | 3.65.0 |
| B5 | `engineering/wb-spec-driven` | 1.85.0 |
| C5 | `engineering/wb-release-maintain` | 1.13.0 |

要点：
- **av 2.03.0**：测试五层金字塔（大部分测试不调模型）／金字塔 2.0（信心来源迁到集成层）／
  黄金 trace 重放（录制一次批准一次永远重放，CI 零活模型成本 + 四维漂移阈值）／
  CI 三档套件（Smoke 50–100 例 <90s / Full 200–500 例 <20min / Shadow 真实流量）与**门三态
  PROMOTE·ROLLBACK(<70% 即拒)·HOLD**／确定性层只测周边代码不测"答案好不好" + PR 与夜间两档套件目录。
- **sa 2.79.0**：工具定义是版本化契约（发布给模型的 schema 必须等于运行时执行的；
  breaking 定义＝新增必填/改名/改类型是，加可选不是；契约先行 + 自动 stub）。
- **ed 3.01.20**：五可靠三烧钱模式 + "agent 与 workflow 是自由度光谱" + 模式选择矩阵 +
  并行两味（sectioning / voting）／orchestrator-worker 解剖（3–5 worker、隔离上下文、
  结构化产物交接、Synthesis 由 lead 唯一署名、verifier-judge 按 rubric 评轨迹）／
  subagent 即工具与 supervisor 的三价值（掐断/算账/复盘）／六大反模式（over-orchestration、
  God Agent、0.95^5≈0.77 可靠性悖论、handoff 贪婪、无终止条件、无步预算）+ 三件防护
  （turn-count bound / observation-insertion 纪律 / tool allow-list）。
- **cc 3.65.0**：五 context 失败模式（poisoning 自我巩固 / distraction 压过训练知识 /
  confusion / clash / **rot@约 100K token**）／切分按内容类型选（语义切分·句子窗口·递归字符）
  + 256–512 上下界（min100/max1000）+ 10–20% overlap + **title-chain 零额外调用前缀**。
- **sd 1.85.0**：状态机五件套（节点＝工作、边＝确定或条件、**typed state schema 是单一真相源**、
  reducers 合并部分更新、checkpointers 持久化、interrupt 供 HITL）。
- **rel 1.13.0**：派生数据生命周期五档刷新 + 生产多为"增量 + 周全量补漏" +
  **换底座模型＝存量全部失效（语义空间变了），必须全量重建或双版本并存；改切分策略同理**。

## 四、未落（5 点，逐条理由）

- **C1 混合检索与 RRF 融合**：本环境不自建检索库（沿用 r156-A 判据），**只核其真伪不落地**。
- **C2 多级 RAG 流水线顺序**：同上，不自建检索管线。
- **C6 分层检索与热冷存储**：同上。
- **A 的 booktest（变化可见性而非 pass/fail）**：工具形态面，记候选池。
- **B 的 LangGraph+LangSmith+LangMem 三件套**：框架面，不落。

## 五、老问题提醒（第 14 次）

1. 仍未自行 commit / push（本轮已明确"不写技能文件"，符合分工，良好）。
2. 「实拉 10 站全量逐站」与 173 路清单口径仍不符；建议改为「抽样 10 站 + 清单 173 路由 WB 全量重拉」。
3. **新增**：本轮独点多为二手博客（dev.to / Medium / CSDN），建议关键数字优先引官方源或附可核验原文片段。
