# 2026-09-24 学习轮 r157-A：检查点粒度与可 fork / 组件身份指纹 / 记忆复合评分 / 长文档分层索引

轮次：r157-A（每小时 3 轮之第 1 轮）｜信源：**173 路独立全量实拉 → 167 OK200 / 6 未达**（与 r156 三轮同一未达集合，均已终局：aliyun_skillmarket / aliyun_xiaxiaobao / skillscat = DNS 不解析；zhipu_agentmore 超时；hf_agents 404；skillsdirectory 连接重置）。

## 一、独立实拉证据

- 全量：`fetchall.py a`，`_summary.txt` 记录每路状态码 / 字节 / 通道 / 异常类型。
- **主源（清单内，此前从未深拉 concepts 面）**：CrewAI `docs.crewai.com/llms.txt` → 关键词过滤 `concepts/` + `fingerprinting` + `observability/tracing|overview` → **26/26 全 200**（`deep_a/`）。
- 关键原文：
  - `concepts/checkpointing`：检查点是事件驱动（`on_events`，默认 `task_completed`；`llm_call_completed` 可得细粒度但写更多文件）；恢复时 `Completed tasks are skipped, memory and knowledge are rehydrated`；`Forking does the same restore under a new lineage, so the new branch and the original run can write checkpoints side by side`；`Auto-checkpoint writes (event-driven) are best-effort: a failed write is logged and the run continues. Manual state.checkpoint() ... re-raise on failure`；Crew/Flow/Agent 子级继承父级配置，除非自设或传 `False` 退出。
  - `guides/advanced/fingerprinting`：指纹＝UUID + 创建时间戳 + metadata，二者**不可手动设置**；`If you modify a component, the fingerprint remains the same`（改 goal 后指纹不变）；`Fingerprint.generate(seed=...)` 同 seed 恒同。
  - `concepts/memory`：统一记忆；`composite scoring that blends semantic similarity, recency, and importance`；`Memory(recency_weight=0.5, recency_half_life_days=7)`；层级 scope（`/agent/researcher` 私有 vs crew 共享）；`memory.forget(scope=...)`；`memory.tree()`。
  - `concepts/skills`（审计用）：`Skills are NOT tools` —— 经查**已在 `wb-skill-authoring` 落地**（sa 既有 §Skills are NOT tools），本轮判为 100% 重叠、不重复落。

## 二、本轮落地（3 文件 4 独点）

| 文件 | 版本 | 独有点 | 提升层 |
|---|---|---|---|
| engineering/wb-execute-discipline | 3.01.9 | 检查点粒度＝订阅什么事件；恢复＝跳过已完成、下游跑在原中间产物上；fork 换 lineage 与原轨迹并存；自动写 best-effort 只记日志、显式调用才 re-raise | 工作流 |
| engineering/wb-release-maintain | 1.11.0 | 组件身份三件套只读不可覆盖；**改属性指纹不变 → 指纹稳定 ≠ 内容稳定**；确定性 seed 用于跨运行对齐 | 工具/工作流 |
| defaults/wb-context-compressor | 3.53.0 | 记忆召回＝语义×时效×重要性复合评分（权重与半衰期可调）；作用域是棵树不是两个桶；遗忘是一等接口 | 工作流 |
| defaults/wb-context-compressor | 3.53.0 | （豆包 r156-A 审计落地）切完之后的尺寸归一化后处理 ROI 最高；多指标评分选方案；长文档分层索引三机制按文档形态选 | 工作流 |

## 三、判重不落

- CrewAI `Skills are NOT tools` → sa 已落同名判据，重叠 100%。
- 豆包 r156-A 点 2（ColBERT 晚期交互）→ 属向量库选型面，本 agent 不自建检索库，判「用不上」不落。
- 豆包 r156-A 点 4（元数据异步富化管线）→ 依赖 vector DB 过滤检索，同上不落。
- 深文档理解先行（RAGFlow DeepDoc）→ 解析层，与索引层相邻，进候选池。

## 四、三件套评估

cc 迭代（3.52.0→3.53.0）；pt / mts 本轮无合格新材料，版本不动。维持 3 件套。

## 五、版本化

commit+push `e34181b`（ANCESTOR_OK，非强推，SSH over 443）；repo→live 3 文件 sha256 全 SAME。
