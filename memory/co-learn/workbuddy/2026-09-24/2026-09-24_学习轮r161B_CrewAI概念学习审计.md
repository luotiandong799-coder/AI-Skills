# r161-B · WorkBuddy 审计记录（CrewAI 概念/学习面）

- 轮次：r161-B（每小时触发连跑 3 轮之第 2 轮；A/B/C 各独立实拉、独立落地、独立记录）
- 实拉时间：2026-09-24
- 主源：CrewAI 官方 `docs.crewai.com/v1.15.22` llms.txt → deep 拉取 `concepts,learn,guides` 共 85 篇正文
- 全量探活：fetchall b 覆盖 173 路信源，OK200=167（非 200 见 run_b.log `_summary.txt`，已逐站留痕，非 200 多为代理拦截/限流，非站点消亡）

## 一、逐站实拉证据（节选关键源）
| 源 | 状态 | 本轮取到 |
|---|---|---|
| CrewAI concepts（agents/tasks/flows/memory/knowledge/tools/training/testing） | 200 | 80/20 任务优先、显式 IO、滑窗、作用域记忆、知识源替代大 prompt、显式 ToolFailure |
| CrewAI learn（llm-selection-guide/coding-agents/human-in-loop/litellm-removal/execution-hooks） | 200 | 按任务选模型、fail-fast 误配置、生产前审视注入提示 |
| CrewAI guides/advanced/customizing-prompts | 200 | 默认系统指令注入、最小化改动切片 |
| 其余 171 路（Agno/Pydantic/Mastra/等，见 run_b） | 167 OK200 | 本轮 B 重点在 CrewAI，其余仅探活留痕，未做新深拉 |

## 二、筛选（只取底层可复用方法论）
- ❌ 不计：frontend/generative-UI/agentic-UI、CopilotKit 集成、CLI scaffold、`crewai create` 等 UI 层与工具包装。
- ✅ 取：3 个可复用于 wb-* 技能系统的底层判据。

## 三、判非重复理由（每个落地点独立核验）
1. **80/20 任务优先 + 显式 IO（→ wb-spec-driven）**
   已有 spec-driven 强调"先立规约"，但未明确"80% 精力放任务设计、任务必须显式声明 inputs/outputs、先顺序流程后层级"。属新增操作点，重叠 <60%。
2. **显式失败信号，不靠文本猜（→ wb-artifact-verification）**
   已有 av 2.04 讲"空结果不是没有 / 检索取证四纪律"；本点讲"工具失败必须显式 ToolFailure/isError/抛异常，框架绝不看字符串像错误就判失败；配置误配要 fail-fast 抛 ValueError 而非静默打到错端点；生产前审视被注入提示"。是另一维度的验证纪律，不重叠。
3. **长上下文先外置知识源 + 作用域记忆树（→ wb-context-compressor）**
   已有 cc 3.66 讲"大对象外置生命周期/指针唯一"；本点讲"用 Knowledge Sources / RAG 替代大 prompt、respect_context_window 默认滑窗、记忆按 scope 分树 + 只读切片"。属上下文压缩的可操作前置，且 CrewAI 给出"库变小 ≠ 读变便宜"与 r161-A Agno 结论互证，新增点。

## 四、落地独点（见本轮交付四列表格）
- wb-spec-driven 1.85.0 → 1.86.0
- wb-artifact-verification 2.04.0 → 2.05.0
- wb-context-compressor 3.66.0 → 3.67.0

## 五、三件套评估（本轮）
三件套（ponytail/max-token-saver/context-compressor）本轮未改 ponytail 与 max-token-saver：
- context-compressor 已随本轮回合升级（见上）。
- ponytail：CrewAI 的"先顺序后层级 / 最小化改动切片"与其 YAGNI 一致，属互相印证，无新增判据，故不 bump。
- max-token-saver：本轮无新压缩点需固化。

## 六、审计结论
B 轮非缩水：主源换为 CrewAI（与 A 轮 Agno 不同源），三个落地点均来自本轮实拉正文、均附可核验出处，且与 A 轮所落三技能的点位不重复（A 落 sa/av/cc 的"名字不是权限/空结果取证/大对象外置"，B 落 spec/av/cc 的"任务优先/显式失败信号/知识源外置+作用域记忆"）。
