# 2026-09-23 学习轮 r153-A · WorkBuddy 审计

> 本轮主题：**检索面的过滤基数 / 正样本分数上界 / 数值兼容破坏面**
> 主源：Pinecone 官方 `docs.pinecone.io`（清单内 165 路之一，此前从未深拉；子索引 `_llms/pinecone-database/guides.md` 103 页全量实拉，**103/103 全 200**）
> 执行线：WorkBuddy（WB）。分工：豆包产出实拉证据+独点清单+判非重复理由；WB 核验独点真伪/判重复/确认落地版本/复核推送。

## 一、实拉证据（独立进程，非复用前轮）

- 全量：`fetchall.py a` → **165 路 / 152 OK200 / 13 未达**（未达：dify_market / make / fullstackskills_gh / aliyun_skillmarket / aliyun_xiaxiaobao / zhipu_agentmore / gpt_store_discover / hf_agents / waytoagi_fastgpt / guangzhengli / skillscat / skillsdirectory / bedrock_agentcore_llms）。
- 主源深拉：`https://docs.pinecone.io/_llms/pinecone-database/guides.md` → 103 页全 200，原文落 `yt/r153wb/deep_a/`。
  - 注意：顶层 `llms.txt` 只给**子索引**（Database 554 页拆成 `guides.md` 103 + `reference.md` 363），globs 关键字过滤会漏 → **取页前先看子索引层级**（第 7 次印证取页形态规律）。

| # | 页面 | 关键原文（英文原样） |
|---|---|---|
| 1 | `guides/index-data/design-for-multitenancy.md` | `Instead of passing thousands of user IDs, this filter uses only 2 group identifiers to achieve the same access control`；`Avoid filtering by large lists of individual user IDs`；三档模式表（完全隔离→一索引+每租户一 namespace「最快」/ 大租户含子组→一索引+子组 namespace / 跨租户共享→共享 namespace+按组 ID 过滤「要扫整个共享 namespace，更慢更贵」） |
| 2 | `guides/optimize/increase-relevance.md` | `the same string embedded as a query and as a passage produces two different vectors`；`querying for the exact text "JetBlue Flights and Extras" against a record containing that same text can score around 0.58`；`Scores in roughly the 0.5-0.8 range for identical short strings are normal`；`This setting degrades true semantic search` + 只能索引级全局改、无法按请求切换 |
| 3 | `guides/search/rerank-results.md` | `cohere-rerank-3.5` 2026-07-01 废弃、2026-08-31 起由 `cohere-rerank-4-fast` 承接；`Because cohere-rerank-4-fast returns different relevance scores, re-tune any hard-coded score thresholds against it`；同表内 `truncate` 默认值：`bge-reranker-v2-m3` = `NONE`（超长报错）vs `pinecone-rerank-v0` = `END`（静默截断） |
| 4 | `guides/search/filter-by-metadata.md` | 文本匹配算子 `$match_phrase` / `$match_all` / `$match_any` 只在读侧可用：`A filtered documents/update or documents/delete rejects them with a 400 and takes plain metadata filters only` |

## 二、落地汇报（强制四列表格 · 轮 / 文件 / 版本 / 独有点）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | defaults/wb-context-compressor | **3.47.0** | 过滤条件的**基数**决定检索成本：按「组」过滤不按「个体」（千个 user ID → 2 个组标识，同权限）；三档隔离模式的成本差异由「每次查询扫多大」决定（共享区恒定全扫） |
| A | engineering/wb-artifact-verification | **1.92.0** | 「**正样本不必然满分**」：同串查询 vs 段落走两条嵌入路径 → 完全相同短串 0.5–0.8 分是常态；任何「分数 ≥ X 算命中」的判据**必须先在已知完全匹配的样本对上标定上界**；精确型工作负载该**换通道**（sparse/FTS）而不是把全局开关拧到精确档（那会整体降低语义检索）；跨管线比名次不比分数 |
| A | engineering/wb-release-maintain | **1.7.0** | 升级破坏面要分「**接口兼容**」与「**数值兼容**」两面，数值面**不报错**（200 + 字段齐全，只有分数分布变了）→ 唯一抓手是「**硬编码数值清单**」（阈值/超时/重试上限/退避基数/候选数量/截断策略）；同一参数名在同一语义槽位可给**相反默认值**（`NONE` 报错 vs `END` 静默截断）；同一能力走不同路径破坏面不共享 |

## 三、逐点判重（双键：来源标识 + 概念关键词）

| 候选点 | 来源 | 判定 | 落点 |
|---|---|---|---|
| 过滤条件基数决定扫描量、按组不按个体 | Pinecone design-for-multitenancy | **新增**（cc §混合检索 系列管「多路召回怎么合并」，是**融合**环节；本条管**收窄/过滤**环节。cc §状态的作用域写在键名前缀 管「隔离键怎么命名」，不管「过滤基数」。r150-A 判不落的是「多租户按 payload 分区」＝部署结构面，非本条的**基数→成本**判据） | cc ✅ |
| 同一字符串作查询/段落分数只有 0.5–0.8，且路径不可按请求切 | Pinecone increase-relevance | **新增**（av §可调阈值的前置条件 管**元规则**「默认值必须注明由什么标定、换方法阈值作废」；本条是该元规则在检索上的**具体形态**——标定夹具＝已知完全匹配样本对，且**必须预期上界 <1**。av §检索类评测分三层 管「测哪一层」，本条管「这一层阈值从哪来」） | av ✅ |
| 换模型后分数分布变、硬编码阈值必须重调 | Pinecone rerank-results | **扩增**（rel §Dependency Guard 现有三查是跨度/受影响 API/破坏面分级，提到「默认值」但**无「数值兼容」这一独立面**、无「硬编码数值清单」这个动作；rel §兼容性要在两个面上分别判 管「存量引用会不会自动跟着变」，非数值面） | rel ✅ |
| 读侧算子 ≠ 写侧算子（文本匹配过滤器写操作被 400 拒） | Pinecone filter-by-metadata | **判不落**（与 exec 既有「批量删除先取 ID」纪律重叠 >60%；仅作 §正样本上界 条内的语境补充） | — |
| 单索引 hybrid 需查询时归一化 dense/sparse（凸组合 alpha） | Pinecone hybrid-search/single-index | **判不落**（cc §混合检索管线参数 / §混合检索与评估 已有 RRF 与 alpha 调参三条，重叠 >60%） | — |
| namespace 每租户隔离 / 分片副本 / 量化参数 | Pinecone | **判不落**（产品运维面，与「只取底层方法论」筛选规则不符） | — |

## 四、功能套件检查（三件套）

- 本轮三件套覆盖：**A → wb-context-compressor 3.46.0 → 3.47.0**（过滤基数）；pt / mts 本轮无合格新材料。
- 结论：**维持 1–N 件套基数不变（当前 = pt + mts + cc 三件）**；不硬凑。

## 五、垃圾清理

- 本轮产物全落 D 盘 `yt/r153wb/`（脚本 fetchall.py / deep.py / land_a.py / audit_full.py / sync_live.py / probe_*.py、三轮抓取 a/b/c、深拉 deep_a 103 页、日志）；**C 盘无新增**；无临时文件残留。
