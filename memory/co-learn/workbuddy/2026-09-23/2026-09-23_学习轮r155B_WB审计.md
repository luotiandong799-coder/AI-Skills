# r155-B · WB 审计记录（2026-09-23 23:21 续跑）

## 实拉证据
- 全量重拉 171 路（fetchall b）：OK200=164/171，7 站同终结论未达。
- 主源深拉 ZenML `docs.zenml.io/llms.txt` → 521 页正文（deep_b/）。
- 逐站留痕见 `D:/腾讯AI/yt/r155wb/b/` 与 `deep_b/`。

## 独点提取（3 个）
1. **失败传播三语义与跳过传染**（ZenML）：FAIL_FAST / STOP_ON_FAILURE / CONTINUE_ON_FAILURE 三档，旁支绿≠成功、跳过沿依赖链传染、停了≠收权（令牌 leeway 仍有效）。→ 落 `wb-execute-discipline` 3.01.6→3.01.7。
2. **旁路钩子静默失效面**（wb-artifact-verification 迭代）：钩子未执行不报错，须独立验证钩子真跑了。→ 落 `wb-artifact-verification` 1.94.0→1.95.0。
3. **上层声明语义漂移**（wb-spec-driven 迭代）：声明层与实现层语义不一致时以实现为准。→ 落 `wb-spec-driven` 1.76.0→1.77.0。

## 判非重复理由
- 失败三语义：ed 已有「并发槽终态释放」「资源账面三对区分」，但无「部分成功 + 跳过传染」语义，本轮补 ZenML 原图判据，独点。
- 钩子静默失效：av 此前无「钩子执行不可见须验证」条目，新增。
- 语义漂移：sd 此前无「声明≠实现」判据，新增。

## 落地与验证
- 三文件 PyYAML 解析 OK，CRLF 0，sha256 repo==live 全 SAME。
- commit `94615f2`，SSH over 443 push 一次（ANCESTOR_OK）。
