# r144-C · Temporal 官方（首读深拉 19 页）

日期：2026-09-23 | 独立进程重拉 110 路 / 90 OK200 / 20 未达（**未达集合与 A、B 互异**：多出 make_templates / autogen_gh / metagpt_gh / agentfootprint / e2b_artifacts / sim_studio / deepwiki_mcp_python(403 限流)）→ 三轮互不相同，独立重拉成立 | 主源 `docs.temporal.io/llms.txt`（13,793 字节 / 111 个 `.md` 页；站内 404 率高，须按 llms.txt 实际路径取，不能拼路径）

## 落地
| 文件 | 版本 | 独有点 | 提升层 |
|---|---|---|---|
| engineering/wb-spec-driven | 1.66.0→1.67.0 | 改运行中的东西用**带 ID 的补丁分支**（类 feature flag），只作用于新执行；决策落史即固化（老执行重放看不到标记就永远走旧路，这是设计意图）；补丁插在必经点会当场抛非确定性异常而不是静默；补丁是过渡态要收 | 工作流 + 可复用 Skill |
| defaults/wb-ponytail | 1.58.0→1.59.0 | 快捷路径准入五条件（短命含重试／可同进程／不需特定执行者或队列／不需全局限流／**必须幂等**）；先列"被省掉的那一步在替你做什么"；省历史＝省审计；快捷路径默认关 | 工具 + 工作流 |
| engineering/wb-debug-loop | 1.35.0→1.36.0 | 失败按"在哪被检测、由谁缓解"分两类：平台失败→前向恢复（重试、不动已完成工作）；应用失败→重试无用→后向恢复（撤销已做的部分）；判据＝再跑一次会不会自己变好 | 工作流 + 工具 |

## 判重不落
- 重试策略三型 NonRetriable/RetryAfter/默认退避（dl 1.27.0 已落；本条明确分工：那条管抛错方声明，本条管收错方的恢复方向）
- 每步小回滚（dl 1.27.0 同源；已声明与 §回退到已知好点 的分工）
- Worker Versioning / Build ID 与 AgentCore 版本端点（A 轮已落 release-maintain 1.1.0，不重复）
- continue-as-new（ctx 压缩与重启续跑已覆盖）

## 候选池
- `encyclopedia/retry-policies`(17KB)、`detecting-activity-failures`(16KB)、`handling-messages`(14KB) 未精读
- `encyclopedia/event-history` 各 SDK 走查页未读
