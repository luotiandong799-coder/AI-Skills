# r142-A 审计记录（WorkBuddy · 自审）

- 时间：2026-09-23 01:5x｜轮次：r142-A｜主源：Google ADK 官方 `adk.dev`
- 实拉证据：`D:/腾讯AI/yt/r142/a/`（110 路 / 96 OK200）+ `D:/腾讯AI/yt/r142/deep_a/`（llms.txt 17,557 字节 → 235 个 `.md` 链接 → 实拉 22 页全 200，原文落盘）

## 落地核验（版本 / 推送 / 同步）

| 文件 | 旧→新 | commit | push | repo↔live |
|---|---|---|---|---|
| engineering/wb-artifact-verification | 1.74.0→1.75.0 | 8fc0ea7 | 已推（非强推，ANCESTOR_OK） | SAME `460324e55e7f8c6c` |
| engineering/wb-debug-loop | 1.33.0→1.34.0 | 8fc0ea7 | 同上 | SAME `d3ad5a1ee83b5271` |
| defaults/wb-context-compressor | 3.34.0→3.35.0 | 8fc0ea7 | 同上 | SAME（末态 3.36.0 见 C 轮） |

## 独点真伪核验

- **评测判据两层（全局 + 用例级按 type 过滤、叠加不替换）** — 原文 `evaluate/criteria`：「Rubrics on `EvalCase.rubrics` are *additive* on top of the criterion-level list, not a replacement... filtered by `type`」✅ 属实。
- **NOT_EVALUATED 必须从均值剔除** — 原文 `evaluate/custom-metrics` 示例代码：`scores = [r.score for r in per_invocation_results if r.eval_status != EvalStatus.NOT_EVALUATED]` ✅ 属实。
- **自定义指标必须声明值域（默认 0.0–1.0）** — 原文：「If `metric_info` is not provided, ADK will use default values (`min_value`=0.0, `max_value`=1.0)」✅ 属实。
- **模拟器自身也要被评** — 原文 `evaluate/criteria` 有 `per_turn_user_simulator_quality_v1` 判据 ✅ 属实。
- **状态键前缀 = 作用域与持久化契约（含 `temp:` 不落盘）** — 原文 `sessions/state`：「`temp:` ... **Not Persistent.** Discarded after the invocation completes」✅ 属实；父子 agent 共享 invocation id 故共享 temp ✅ 属实。
- **只读靠类型（mappingproxy）** — 原文 `context`：`ReadonlyContext` 的 state 为只读视图，赋值抛 TypeError ✅ 属实。
- **旁路四条可靠性约束** — 原文 `callbacks/design-patterns-and-best-practices`：内联执行「the loop waits for each one to finish」、幂等、try/catch、「Keep Focused ... Avoid monolithic callbacks」✅ 属实。

## 判重核验（>60% 即不落，已逐条比对仓库正文）

- session rewind 三边界 → dl §回退到已知好点（r129-C 已落）重复，**未落** ✅
- callbacks 拦截/缓存/skip_summarization → 豆包 r140-A sa「钩子三返回语义」同功能位，**未落** ✅
- environment simulation 故障注入 → r133-C ED「注入受控失败」+ av §故障注入，**未落** ✅
- judge 多次采样多数投票 → sd 1.60.0「概率系统单次运行不判定改进」，**未落** ✅
- 工具并行就绪 → 豆包 r140-A pt 1.58.0，**未落** ✅

## 审计结论

- 3 独点全部有原文出处、全部标明提升层；无虚构、无"摘要冒充"。
- PyYAML 52/52 解析通过；CRLF / STRAY_CR / U+FFFD / 尾空白 / description>1024 全 0。
- 落点已避开豆包主战场（ED / mts / pt / sa / sd 开工前核验 mtime 与 sha256）。
