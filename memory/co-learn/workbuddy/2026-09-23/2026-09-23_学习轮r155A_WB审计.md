# r155-A · WB 审计记录（2026-09-23 23:21 续跑）

## 实拉证据
- 全量重拉 171 路（fetchall a）：OK200=164/171，7 站同 r154 终结论未达。
- 主源深拉 Windmill `windmill.dev/llms.txt` → 44 页正文（deep_a/）。
- 逐站留痕见 `D:/腾讯AI/yt/r155wb/a/` 与 `deep_a/`。

## 独点提取（3 个，均落 WB 辖区，避开豆包主战场）
1. **等待期释放槽位 vs 占槽**（Windmill concurrency limits）：挂起等人工的阶段仍占并发槽、任务跑完才释放、等待期不白占槽位。→ 落 `wb-execute-discipline` 3.01.5→3.01.6。
2. **推理强度模型专属**（wb-ponytail 迭代）：中等推理强度绑定特定模型、换模型即重置、不跨模型继承偏好。→ 落 `wb-ponytail` 1.63.0→1.64.0。
3. **观测面扫描根即盲区**（wb-artifact-verification 迭代）：只扫已知观测面会漏根因，须从根因反推还缺哪个观测面。→ 落 `wb-artifact-verification` 1.93.0→1.94.0。

## 判非重复理由
- 槽位释放/占槽：r154 已落「并发槽按终态释放」（ed 3.01.4），但只讲终态；本轮补「等待期仍占槽」这一 Windmill 特有语义，互不重叠。
- 推理强度专属：pt 此前无「换模型重置」判据，新增。
- 观测面根盲区：av 此前无「从根因反推观测面」方法，新增。

## 落地与验证
- 三文件 PyYAML 解析 OK，CRLF 0，sha256 repo==live 全 SAME。
- commit `9e028a1`，SSH over 443 push 一次（ANCESTOR_OK）。
