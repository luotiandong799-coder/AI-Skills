# 2026-09-23 学习轮 r151-C（Fireworks：按负载定超时 / 错误语义重试 / schema 可校验输出）

## 实拉证据（全量 165 路 + fireworks 深拉）
- **全量抓取**：`fetchall.py c` → 165 路 / 122 OK200 / 43 未达（独立进程取证，未达集合与 A/B 互异）。
- **主源深拉（清单外新信源首读）**：Fireworks 官方 `docs.fireworks.ai/llms.txt`（110 链接）→ 深拉 **110/110 页全 200**：`guides/reliability`、`guides/inference-error-codes`、`structured-responses/structured-response-formatting`、`guides/function-calling`、`guides/prompt-caching`、`guides/batch-inference`、`fine-tuning/agentic-rl`、`guides/security_compliance/*` 等。
- 逐站留痕：`c/` + `deep_c/` 110 份原文。

## 独点清单 + 判非重复理由（3 点落地 rel/dl/av）
1. **按负载类型定超时与异步 job 生命周期**（→ rel 1.5.0→1.6.0）：interactive 30-60s / agentic 5-30min / batch 60s 异步；connect/read 超时分设；长任务提交即返回 job id 异步取。判非重复：rel §准入闸门 管"谁能上线"，本条管"不同动作配多长超时、同步还是异步"。
2. **重试前按错误语义分类**（→ dl 1.45.0→1.46.0）：429/5xx 可重试带退避；400/401/404/422 永久错误去修别循环。判非重复：dl §失败按换 token/改契约分两类 管"模型侧恢复"，本条管"传输/协议层错误按语义分流"。
3. **让模型输出机器可校验：schema 格式化响应**（→ av 1.90.0→1.91.0）：用 JSON Schema 钉死输出形状，解析/校验失败是"一等失败"非静默。判非重复：av §结构检查三态 管"解析失败/不合 schema/合规"分类，本条管"从源头用 schema 把输出钉成机器可校验形状"。

## 落地汇报（强制四列表格）
| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| C | engineering/wb-release-maintain | 1.6.0 | 按负载类型定超时（交互/agentic/batch 分档）+ connect/read 分设 + 长任务走 job 生命周期异步 |
| C | engineering/wb-debug-loop | 1.46.0 | 重试前按错误语义分类：429/5xx 退避重试，400/401/404/422 去修别循环 |
| C | engineering/wb-artifact-verification | 1.91.0 | 模型输出机器可校验：schema 格式化响应，校验失败是一等失败非静默 |

## 三件套评估（C）
- 本轮落 rel/dl/av，**避开 mts/ED**。三件套 **维持 3 件套**；r151 三轮三件套覆盖 A→无 / B→无 / C→无，未增减。

## 审计
- 7 文件 CRLF=0 / STRAY_CR=0 / U+FFFD=0 / 尾空白=0 / description≤1024（rel329/av739/dl696）。ISSUES=0。
- repo→live 7/7 SAME。版本化随 A 同 commit `024ae90` 推送。

## r151 三轮合计落地汇报
| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | engineering/wb-artifact-verification | 1.90.0 | 评测可比性：每次 run 冻结 harness+模型 |
| A | engineering/wb-debug-loop | 1.45.0 | 回退路由韧性：fallback 是依赖层能力 |
| A | engineering/wb-spec-driven | 1.74.0 | 评测即规格：跑前冻结四问 |
| B | defaults/wb-context-compressor | 3.45.0 | 记忆外置 git 仓库、按需引用不内联 |
| B | defaults/wb-ponytail | 1.62.0 | 共享知识引用不复制 |
| B | engineering/wb-skill-authoring | 2.67.0 | 跨 agent 共享记忆须显式同步协议 |
| C | engineering/wb-release-maintain | 1.6.0 | 按负载定超时 + 异步 job 生命周期 |
| C | engineering/wb-debug-loop | 1.46.0 | 错误语义分类重试 |
| C | engineering/wb-artifact-verification | 1.91.0 | schema 格式化使输出机器可校验 |
