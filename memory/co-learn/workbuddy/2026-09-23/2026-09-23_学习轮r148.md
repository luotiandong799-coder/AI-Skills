# 学习轮 r148 — Ragas 评测方法论落地（WorkBuddy 审计视图）

- 轮号：r148（全局唯一，不按日重置）
- 日期：2026-09-23
- 信源：Ragas 官方 docs（`docs.ragas.io` llms.txt 全量深拉 50/50 页成功；Phoenix/Arize 因 llms.txt 链接格式未被正则匹配 0 文件，留待下轮补相对路径逻辑）
- 学习方式：单轮（用户点名"再学习一轮"=单轮，非连跑）
- 筛选铁律：已删 4 类技能（代码库架构重构 / 需求拆工单 / 多媒体取证 / 浏览器扩展源码构建）同源内容**不学不落地**（用户 2026-09-23 指令）；本轮 3 点均不属此类。

## 落地汇报（四列表格）

| 轮 | 文件（技能） | 版本 | 独有点 |
|---|---|---|---|
| r148 | engineering/wb-artifact-verification | 1.84.0 → 1.85.0 | 面向方面的二值评测（Aspect Critique 法）：DiscreteMetric 二值 + strictness 2~4 自一致性 + `MetricResult(value, reason)` 必须带理由；与 §失败分诊九类分类互补（分类管"哪里错"，二值闸门管"这一项过没过"） |
| r148 | engineering/wb-execute-discipline | 3.00.0 → 3.01.0 | 长任务可控终止（Cancellation 模式）：`return_executor=True` 拿可取消执行器 + `cancel()`/`is_cancelled()` 轮询 + partial results；线程安全 + 协作式（任务须周期查取消标志）；与 §超时即终态互补（终态上报前先优雅取消保住进度） |
| r148 | defaults/wb-context-compressor | 3.40.0 → 3.41.0 | 确定性调用的精确匹配缓存（Exact-Match Caching）：同 prompt+同参数→同输出才缓存（DiskCacheBackend），省成本 50~60%+提速 60x+可复现；反模式=非确定 prompt / temp>0.7 / streaming / 实时；与 §不缓存验证结论互补（结论不存、原始响应可存） |

## 非重叠判定（落地前逐项核对）

- av 现有"失败分诊用九类分类，反对仅用对/错二值"——本轮是**单一维度轻量闸门的建设方法**（strictness + reason），角度不同，重叠 <30%。
- ed 现有"超时/成本熔断是终态不换路"——本轮是**终态到达前的协作式中途取消 + 保留 partial results**，机制不同，重叠 <20%。
- cc 现有"不缓存验证结论（重启须重验）"——本轮是**可缓存确定性调用的原始响应 + 反模式清单**，对象不同（结论 vs 原始响应），重叠 <30%。
- 三目标技能互不相同（av / ed / cc），跨技能零重叠。

## 证据（实拉原文）

- Aspect Critic：`strictness` 理想区间 2~4，LLM 评估 + 自一致性；返回 `MetricResult(value, reason)`。
- Cancellation：`return_executor=True` 拿 Executor；`cancel()` / `is_cancelled()` / `results()`（取消时返回 partial results）；线程安全 + cooperative（任务须周期查取消标志）；生产必设超时。
- Caching：Exact Match；`DiskCacheBackend`；命中=同 prompt+同参数+同响应结构；反模式=非确定 prompt / temp>0.7 / streaming / 实时；收益 50~60% 省成本、60x 提速、可复现。

## 审计结果（落地后）

- 3 文件：CRLF=0、行尾空白=0、PyYAML 解析 frontmatter 通过、版本号正确、新章节已写入。
- 提交：`f5bb19e`（cac850f..f5bb19e），仅 3 文件 +25/-4。
- repo → live 全量 sha256：**3/3 MATCH**。
- 推送前 ancestor 校验：ANCESTOR_OK（本轮无并发提交，无需 rebase）。

## 下轮待办（可选）

- 修复 deep.py 相对路径逻辑，补拉 Phoenix/Arize llms.txt（参考 Opik 坑）。

## 收尾

- 本轮未新增 D 盘垃圾：`r148/` 为轮次工作目录（实拉原文+脚本），按规约**不删**；`land.py` 留作该轮落地脚本。
