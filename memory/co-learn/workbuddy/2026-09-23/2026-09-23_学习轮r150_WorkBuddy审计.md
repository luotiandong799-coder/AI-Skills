# 2026-09-23 学习轮 r150（WorkBuddy 线 · A/B 深读，C 延后）

## 一、实拉证据
- A 轮（MCP 官方）：独立新进程 `fetchall.py a`（urls.txt 157 路）。深读 `modelcontextprotocol.io`（llms.txt 353 页、**相对路径**），选 18 页全 200：client-best-practices / security_best_practices / inspector(web/cli/tui/configuration/authorization/protocol-eras/recipes) / debugging / server-concepts / connect-local|remote-servers / build-server。
- B 轮（LangChain）：独立新进程 `fetchall.py b`。深读 `docs.langchain.com/oss/deepagents/code`（llms.txt 119 页），选 18 页全 200：approval-modes / goals-and-rubrics / memory-and-skills / config-file / configuration / remote-sandboxes / subagents / overview / mcp-tools / hooks / plugins / providers / quickstart / credentials / extensions / cli-reference / changelog / agent-identity。
- 三轮独立取证：A/B 深读集合互异（不同主源）；C 轮未跑（见第六节）。
- 信源路数维持 **157**（MCP 官方 llms.txt、LangChain docs.llms.txt 已在清单内，本轮为**首次深读**，非新增 URL）。

## 二、落地（强制四列表格）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | system/mcp-builder | 1.0.0 | ①工具过载渐进式发现：工具定义占 context 1%–5% 即从全量注入切按需；三层 catalog/inspect/execute（search_tools 只回名称+一行描述 → get_tool_details 拉全量 schema → 调用，中间结果不进 context）；server 按需连（enable/disable_server 释放上下文）②组合式调用代码模式：链式多工具改写成脚本在沙箱跑、只回传最终摘要；沙箱三件套隔离（无直连网络/凭证 host 持有/超时内存上限）；授权逐次而非一次性；跨 server 数据不可信须逐调用输入审查③接协议类工具先上 Inspector 看握手字段（_meta 的 protocolVersion+clientCapabilities、server/discover；-32022/-32602/-32021）；stdio 日志落 stderr |
| B | agent/agent-guild | 1.3.0 | ①三档审批 Manual/Auto/YOLO：Auto≠沙箱；只读工具(ls/read/glob/grep)免审、写删/shell/外发/gated 需审②目标驱动 goal/rubric：agent 自起草验收标准再开工、跨轮活跃；rubric 粘滞或单轮；amend/pause/resume 不重放③记忆与技能频谱：常驻 always-loaded vs 按需 on-demand，决定 context 装什么 |

## 三、判非重复理由（逐条）
- mcp-builder 三段：**渐进式发现**管"工具定义进不进 context"、**代码模式**管"多工具调用结果过不过 context"（两条已写分工行，重叠<30%）；**Inspector**段管"接 MCP/协议类工具"子类第一站与握手判据，与通用排障循环（复现→最小化→假设→验证）不重叠（<25%），已写分工行。
- agent-guild 三段：**三档审批**管"能不能自己改"、**目标驱动**管"改完怎么算改好/逐轮验收"、**记忆频谱**管"知识常驻还是按需"，三者职能正交（重叠均<30%）；与既有 gated actions/分层授权段写分工行。
- 两段均已在段内标注 `（来源：…2026-09-23 r150 续跑独立实拉首读，清单外新信源）`，可追溯。

## 四、三件套评估
- 本轮命中 `agent-guild`（身份/记忆/跨 agent 交接域，与 agent-guild 技能强相关）；`mcp-builder` 属系统级构建。三件套（ponytail / context-compressor / max-token-saver）维持，基数按功能定，不为凑数加减。
- Evidently（评测域）内容已备于 land_c.py 待用（见第六节）。

## 五、版本化与同步
- commit **`e600f8f`**（"r150 续跑(WB 线): MCP 官方 client-best-practices/debugging → mcp-builder 1.0.0 / LangChain deepagents → agent-guild 1.3.0"）。
- 非强推；push 前 `fetch` + `merge-base --is-ancestor FETCH_HEAD HEAD` = **ANCESTOR_OK**；推 `71c25b7..e600f8f`。
- 2 文件 cp 回 live 后 sha256 全 **SAME**（repo = live）：
  - mcp-builder `a9181a4c7abb3ea1`
  - agent-guild `cc02a2b86d8818ba`
- 审计：PyYAML 52/52；CRLF/STRAY_CR/FFFD/尾空白/desc>1024 全 0；`ISSUES=0`，`DIFF=4`（4 个预存在、非本轮改动文件，mcp-builder/agent-guild 不在 DIFF 中）。两文件 LF-OK。

## 六、Evidently 轮（r150-C）延后说明
- 原定 `land_c.py`：Evidently LLM-eval → `engineering/wb-artifact-verification` 1.87.0→1.88.0（3 段：LLM 评委模板二元/多类+不确定性类、Tests(Pass/Fail 门) vs Metrics(连续分)、参考基线回归比对）。
- **落点冲突**：并行线（豆包）已占 `av` 为 r150 战场——r150-A（Qdrant）升 `av 1.88.0`、r150-B（Vellum）升 `av 1.89.0`。land_c.py 目标版本 1.88.0 已被占用，且与当前 av 1.89.0 冲突（直接跑会回退/撞版本、破坏并行线成果）。
- **处理**：严守"落点避开"铁律，C 轮延后；`land_c.py` 保留待用，后续须改目标 Skill（选空闲技能）或抬升版本号后再跑，不得覆盖 av 现有 1.89.0。
- 当前工作树另有 3 文件被并行线改动未提交（wb-debug-loop / wb-skill-authoring / wb-spec-driven），非本轮产物，未触碰、未纳入提交。
