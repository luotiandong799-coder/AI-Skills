# 2026-09-23 学习轮 r149-A（WorkBuddy 线 · 三轮第 1 轮）

## 一、实拉证据
- 独立新进程 `fetchall.py a`：**147 路 / 127 OK200 / 20 未达**（未达集合：dify_market 404、make 403、fullstackskills_gh 404、aliyun_skillmarket / aliyun_xiaxiaobao / zhipu_agentmore / gpt_store / gpt_store_discover / hf_agents / hf_skills / skillscat / mistral_llms（代理隧道 502）、waytoagi_fastgpt 302 环、guangzhengli 404、the_neuron 403、skillsdirectory SSL EOF、bedrock_agentcore_llms 404、openai_platform_llms 403、openai_cookbook_llms 403、voltagent_llms SSL EOF）。
- **主源（清单外新信源深读）**：Prefect 官方 `docs.prefect.io`（llms.txt 330 页，**绝对 URL**）→ 深拉 7/7 全 200：`concepts/caching`、`advanced/caching`、`advanced/results`、`advanced/transactions`、`concepts/artifacts`、`how-to-guides/workflows/artifacts`、`how-to-guides/workflows/retry-flow-runs`。

## 二、落地（强制四列表格）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | defaults/wb-context-compressor | 3.42.0 | 复用能力有底座（缓存依赖"结果持久化"，底座默认关闭→缓存永不命中且不报错）；默认值分三级（全局→父级→本级，显式优先但全局能覆盖父级）；复用范围上限由存储可达性决定（本地存储只在同一 run 内可达，换容器即失效） |
| A | engineering/wb-spec-driven | 1.70.0 | 复用键的确定性等级分层（任务级默认确定性哈希可复用 / 流程级默认随机 UUID 默认不可复用）；模板键让文件名即语义；同键两来源时模板键胜出、键函数被静默忽略 |
| A | engineering/wb-artifact-verification | 1.86.0 | 读持久化记录两道判断有顺序（先"能不能解码"后"值对不对"，解码失败是一等失败不是值错）；记录须自带写入方版本/序列化器/过期时间；手工反序列化绕过读取通道=同时绕过过期检查与锁 |

## 三、判非重复理由（逐条）
- "缓存键包含运行 ID 故默认只在 run 内去重" → 与 `cc §确定性调用的精确匹配缓存`（管"什么调用可缓存"+反模式）成**互补**：本条管"底座开关 + 默认层级 + 存储可达性"，重叠 <35%，已写分工行。
- "键的确定性等级" → 与 `sd §8 缓存与资产分层`（管按项目隔离防污染）重叠 <30%，已写分工行。
- "同键两来源静默忽略其一" → 与 `pt §同义别名`（讲两个别名同时设置无害）方向相反（那是"共存"、这是"互斥取一"），重叠 <35%。
- "持久化记录自带元数据 + 绕过读取通道即绕过校验" → 与 `av §评级绑定具体版本`（管评级绑版本）重叠 <35%，已写分工行。

## 四、三件套评估
- 本轮命中 `wb-context-compressor`（升 3.42.0）；`wb-ponytail` / `wb-max-token-saver` 无新料 → **维持 3 件套**（基数按功能定，不为凑数加减）。

## 五、版本化与同步
- commit **`1b32a69`**，非强推，push 前 `fetch` + `merge-base --is-ancestor` = **ANCESTOR_OK**。
- 3 文件 cp 回 live 后 sha256 全 **SAME**（cc `9a51e48e43ba20e9` / sd `fca391f26909c687` / av `8baceea511dacc03`）。
