# r155-C · WB 审计记录（2026-09-23 23:21 续跑）

## 实拉证据
- 全量重拉 171 路（fetchall c）：OK200=164/171，7 站同终结论未达。
- 主源深拉 Prefect `docs.prefect.io/llms.txt` → 200 页正文（deep_c2/）；Milvus `milvus.io/llms.txt` 为 sitemap 占位（`<page-id>` 模板）无正文 → 0 点。
- 新信源探测 probe_new.py：agentskills.ac.cn / workswithagents.com / llmstxt.org / mcpcat.io / letta / agno 均 200，待用户裁定升必须项。
- 逐站留痕见 `D:/腾讯AI/yt/r155wb/c/` 与 `deep_c2/`。

## 独点提取（2 个，4 落点）
1. **步骤级确定性缓存**（Prefect caching）：缓存键 = hash(输入 + 任务源码 + 运行ID)；`INPUTS+TASK_SOURCE` 组合、`INPUTS-'debug'` 减法忽略入参、`cache_expiration` 显式失效、`refresh_cache` 强制刷新、`NO_CACHE` 显式关；缓存需持久化后端才生效。→ 落 `wb-execute-discipline` 3.01.7→3.01.8。
2. **副作用落点保证面 vs best-effort 面**（Prefect state-change-hooks）：必须跑的反应下沉服务端保证的 automations，不放心客户端 best-effort 钩子（钩子同进程跑、执行不被保证）。→ 落 `wb-execute-discipline` 3.01.8（同版追加）。
3. **事件因果排序用 follows 钉死**（Prefect）：跨系统事件竞速，自定义事件与状态事件顺序不保证，用 `follows` 引用状态事件 ID 钉死因果顺序。→ 落 `wb-spec-driven` 1.77.0→1.78.0。

## 判非重复理由
- 步骤缓存：ed 此前无「确定性步骤缓存键 + 显式失效」方法论，独点（属「减少重复工作」门槛，可落）。
- 副作用落点：补 r154-B「钩子静默失效」的诊断型条目之处方型（保证面 vs best-effort 面），判据不同不重叠。
- 事件排序：sd 此前「顺序不靠隐式约定」管步骤顺序，本轮扩到跨系统事件顺序（follows），独点。

## 落地与验证
- 两文件（ed / sd）PyYAML 解析 OK；**修复 land_c.py 写出的 CRLF**（Python 文本模式默认 \r\n）→ 转 LF 后复检 CRLF 0 / 尾空白 0。
- sha256 repo==live 全 SAME。
- commit `2960080`，SSH over 443 push 一次（ANCESTOR_OK）。
