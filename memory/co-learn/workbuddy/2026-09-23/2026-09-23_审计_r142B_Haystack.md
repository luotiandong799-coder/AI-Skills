# r142-B 审计记录（WorkBuddy · 自审）

- 时间：2026-09-23 02:0x｜轮次：r142-B｜主源：Haystack 官方 `docs.haystack.deepset.ai`
- 实拉证据：`D:/腾讯AI/yt/r142/b/`（110 路 / 97 OK200，未达集合与 A 轮互异）+ `D:/腾讯AI/yt/r142/deep_b/_llms_raw.txt`（**2,651,505 字节、458 个文件块的整站机器可读全量**）→ `deep_b/pages/`（按 `// File:` 切分，实读 12 个概念页）

## 落地核验

| 文件 | 旧→新 | commit | push | repo↔live |
|---|---|---|---|---|
| engineering/wb-debug-loop | 1.34.0→1.35.0 | 36ebbc3 | 已推（非强推，ANCESTOR_OK） | SAME `d3ad5a1ee83b5271`（末态） |
| engineering/wb-spec-driven | 1.64.0→1.65.0 | 36ebbc3 | 同上 | SAME（末态 1.66.0 见 C 轮） |
| writing/wb-doc-writing | 1.8.0→1.9.0 | 36ebbc3 | 同上 | SAME（末态 1.10.0 见 C 轮） |

## 独点真伪核验

- **断点坐标 = 环节 + 第 N 次访问** — 原文 `concepts/pipelines/pipeline-breakpoints`：「Create a `Breakpoint` by specifying the component name and the visit count at which to trigger it... The default `visit_count` value is 0」✅ 属实。
- **失败自动留最后一个有效状态、可续跑** — 原文：「Pipelines automatically create a snapshot of the last valid state if a run fails. The snapshot contains inputs, visit counts, and intermediate outputs... resume execution from that checkpoint instead of restarting」✅ 属实。
- **快照落盘默认关闭** — 原文：「Snapshot file saving to disk is **disabled by default**」需设 `HAYSTACK_PIPELINE_SNAPSHOT_SAVE_ENABLED`；「When `snapshot_callback` is provided, file-saving is skipped」✅ 属实。
- **不得就地改输入** — 原文 `concepts/components/custom-components`：「do not change the component's inputs directly... The original input values might be reused by other components or by later pipeline steps」✅ 属实。
- **改法分两档（浅替换 / 深拷贝）** — 原文：「When only one or a few fields... use `dataclasses.replace()`... When you need to modify nested mutable structures... use a full deep copy」✅ 属实。
- **能力文档契约 Overview 三问** — 原文 `_templates/component-template`：「*How is it different from this other very similar component? Which one do you choose?*」✅ 属实；模板字段含 Mandatory init variables / Mandatory run variables / Output variables ✅ 属实。
- **文档改动三情况** — 原文 `AGENTS`：「Edit ... pages only for outdated, incorrect, or materially useful guidance... avoids churn」✅ 属实。

## 判重核验

- content tracing 默认关 / `include_outputs_from` → dl §诊断通道分离 + §观测 opt-in（r138-A/B）重复，**未落** ✅
- SuperComponent 收敛接口与同名消歧 → sa 2.50.0 / 2.57.0 已覆盖，**未落** ✅
- 输入输出显式声明 → sa 2.51.0「显式契约不靠推断」已落，**未落** ✅
- secret-management / 凭证存会话态 → ctx §凭据只验三件事 + av §凭证出沙箱，**未落** ✅

## 审计结论

- 3 独点均有官方原文出处；Haystack 全站 llms.txt 属**此前只探活未读**的新深度，非复用旧结论。
- 开工前已核验 sd / sa / doc-writing 三处 repo 与 live sha256 全 SAME，无并发未推内容被覆盖。
