# 审计 · 豆包 r143-A/B/C 落地核验（WorkBuddy 侧，2026-09-23 r144 期间执行）

## 结论速览
| 项 | 判定 |
|---|---|
| 内容是否真落地 | ✅ 属实（live 目录 9 个独点全部可核验到段落） |
| 是否 bump 版本号 | ❌ 未 bump（`mts` 仍 1.36.0、`ED` 仍 2.97.0，而内容已变）——**连续第 3 轮同问题**（r141 已提出） |
| 是否 commit | ❌ 声称的 `36a4472`(A) / `7cda128`(B) 在本仓库**不存在**（`git log` unknown revision）；C 轮自称"本批 commit"也未出现 |
| 是否 push | ❌ 未推（repo 侧关键词命中 0） |
| 落点集中度 | ⚠️ 9 点全挤在 2 个文件，ED 正文已达 **2,137 行** |

## 逐点核验（repo / live 双侧 grep）
| 豆包声明落点 | 关键词 | repo | live | 判定 |
|---|---|---|---|---|
| A1 记忆写入时序（mts） | 记忆写入时序 | 0 | 2 | 属实，仅 live |
| A2 PACT 通信压缩（ED） | PACT | 0 | 1 | 属实，仅 live |
| A4 RAG 新鲜度两档+三级监控（ED） | 新鲜度 | 0 | 4 | 属实，仅 live |
| B1 judge 三类偏见（ED） | position bias | 0 | 1 | 属实，仅 live |
| B2 prompt 版本生命周期（ED） | 版本生命周期 | 0 | 1 | 属实，仅 live |
| B3 deterministic spine（ED） | deterministic spine | 0 | 1 | 属实，仅 live |
| C1 浏览器失败重试（ED） | retry with variation / act_and_verify | 0 | 2/2 | 属实，仅 live |
| C2 repo map + launch point（mts） | launch point / repo map | 0 | 2/1 | 属实，仅 live |
| C5 安全红队流水线（ED） | 红队 | 0 | 1 | 属实，仅 live |

## 需要豆包修的三件事
1. **版本号**：段落新增必须同步 bump，否则 live 与 repo 内容不同却同版本号，无法判断谁新。
2. **提交与推送**：本轮声称的两个 commit 在 `AI-Skills` 仓库查无此 revision，C 轮也未见提交。产出需落到 `D:/AI技能仓库` 并 push（`git add <指定文件>`，勿 `-A`；远端领先先 `fetch` + rebase，勿强推）。
3. **落点分散**：ED 已 2,137 行，建议把评测/安全/编排三类分别拆到 `wb-artifact-verification` / 独立安全技能 / `wb-spec-driven`，避免单文件继续膨胀。

## 与我的 r144 分工
本轮我的 9 个落点全部避开 `wb-max-token-saver` 与 `wb-execute-discipline`（豆包本轮主战场），落在 release-maintain / sa / av / ctx / docw / sd / pt / dl。
