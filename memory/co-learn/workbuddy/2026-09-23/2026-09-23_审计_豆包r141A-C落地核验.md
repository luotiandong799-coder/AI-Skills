# 豆包 r141-A/B/C 落地核验（WorkBuddy 审计侧）

- 被审对象：`D:\腾讯AI\每日学习日志\豆包_输出\2026-09-23\` 下 r141A / r141B / r141C 三份实拉记录
- 审计方式：读豆包产出 → 到 live 技能目录与版本仓库实证核验（不采信其自述）

## 核验结果

| 豆包轮 | 声称落点 | live 实证 | 版本 | 是否已推 GitHub |
|---|---|---|---|---|
| r141-A | 检索排序 + eval 数据集纪律 | ✅ `engineering/wb-execute-discipline` 有「Eval 数据集纪律：golden dataset 取自真实生产失败，pass^k 一致性优于最好通过率」段 | live 2.97.0 | ❌ 未推 |
| r141-B | system prompt 分层预算 | ✅ 同文件有编排器上下文预算分配段（§1874 附近） | live 2.97.0 | ❌ 未推 |
| r141-C | 输出执行边界 + 基准分数读数 | ✅ 同文件有「AI 生成输出按"陌生人上传"对待」+「基准分数不可迁移：lab 到生产有 37% gap」两段 | live 2.97.0 | ❌ 未推 |

## 发现的问题（需豆包修正）

1. **三轮落地都没升版本号**：repo 与 live 的 `wb-execute-discipline` 都是 **2.97.0**，但 sha256 不同（live 领先）→ 加了 4 个新段却没 bump 版本，版本化纪律失效，后续无法从版本号判断是否含这些内容。
2. **三轮内容均未推送 GitHub**：`CONTENT_DIFF(2)` 中 `engineering/wb-execute-discipline` 与 `defaults/wb-max-token-saver` 两项持续为 live 领先 repo。真源仍只在本地，不符合"改动同步 AI-Skills 仓库"的用户规则。
3. **落点过度集中**：r141 三轮全部落在 `wb-execute-discipline` 一个文件，该文件正文已超 2000 行、章节 60+。建议按主题外迁（评测类 → `wb-artifact-verification`，提示词预算类 → `wb-context-compressor`）。

## 本轮我的应对

- 三轮落点全部避开 `wb-execute-discipline` 与 `wb-max-token-saver`，避免与豆包未推内容互相覆盖。
- 我的三轮均按"改文件必 bump 版本 + 必须 push"执行，9 次落地 3 次非强推 push（`8fc0ea7` / `36ebbc3` / `af4a252`）。
