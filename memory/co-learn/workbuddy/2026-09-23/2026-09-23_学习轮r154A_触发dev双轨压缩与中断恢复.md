# 2026-09-23 学习轮 r154-A（WorkBuddy 侧独立实拉 + 落地）

轮次：r154-A ｜ 开工：`fetch origin main` → HEAD=`875ec20`（r153 收尾），远端==本地，豆包已出 r153A/B/C（其三份均声明落在 mts 之外、按 09-23 新规只留痕不写技能文件）→ 本轮轮号 r154，落点全避开 mts/ED 已占位。

## 一、实拉证据（174 路全量，逐站留痕）
- `fetchall.py a` → **174 路 / 145 OK200 / 29 未达**；未达集合（逐站名留痕见 `yt/r154wb/a/_summary.txt`）：dify_market·dify_gh·n8n_gh·flowise_gh·langflow_gh·activepieces_gh·make·anthropic_skills_gh·anthropic_skills_dir·fullstackskills_gh·aliyun_skillmarket·aliyun_xiaxiaobao·zhipu_agentmore·openclaw_gh·langchain_gh·gpt_store_discover·openai_agents_gh·autogen_gh·metagpt_gh·hf_agents·waytoagi_fastgpt·guangzhengli·agentfootprint·e2b_artifacts·sim_studio·skillscat·skillsdirectory·bedrock_agentcore_llms·deepwiki_mcp_python（+1 条 host 名截断项）。
- **★主源（清单内 165→174 路，此前从未深拉）**：**Trigger.dev 官方 `trigger.dev/docs/llms.txt`**，`deep.py` 按 `ai-chat` 过滤 → **43/44 全 200**（唯一失败 `ai-chat/anatomy.md`：WinError 10054，连接被重置）。
- 原文片段（可核）：`They are not competing; they compose.`（两轨压缩关系）／`Neither changes what your agent has accumulated, so on its own the next turn re-sends the whole transcript again and the token saving is lost.`（原生压缩单用）／`the handle is a cache miss on the other provider`（可携带性）／`starts from the summary rather than re-reading the whole transcript and summarising it again`（压缩产物持久化）／`The recovery doesn't need any customer-side persistence to avoid duplicate processing`（OOM 恢复）／`skips records belonging to turns that already completed`（游标过滤）／`does not expose generic retry options` + `retrying an LLM-driven loop on non-OOM errors tends to be expensive and side-effecting`（重试准入）／`The naive approach — log every tool part you see — over-counts.`（副作用多算）。

## 二、落地（3 文件 3 独点）
| 文件 | 版本 | 独有点 |
|---|---|---|
| `defaults/wb-context-compressor` | 3.48.0 → **3.49.0** | 压缩分两轨（provider-native 单请求内 / framework 跨轮持久），只有后者可携带；原生压缩必须把 appliedEdits 镜像回存储否则下轮全量重发；模型消息 vs UI 消息分离；压缩产物落盘且被撤销触及即失效 |
| `engineering/wb-debug-loop` | 1.48.0 → **1.49.0** | 被杀死的进程恢复用「完成标记做游标」（扫最后一条 turn-complete 时间戳 → 过滤 in 流），无需业务方幂等表；只有环境杀死类失败允许自动重试；被中断那轮要拼回链上（取消≠丢弃）；半成品标 final:false |
| `engineering/wb-artifact-verification` | 1.92.0 → **1.93.0** | 副作用「恰好一次」的锚是「这个 toolCallId 是否已结算」，不是「这条消息见过」；必须在合并前钩子读取（合并后永远查不到）；两条合并路径两个比对基准 |

## 三、判非重复（不落项）
- **provider-native 压缩 vs framework 压缩**：不落——已作为上表 cc 条目落地。
- **大 payload ~1MiB per-record cap + ID 引用模式**：**判重不落**——cc §工具大返回处理（r152-B，truncate/spill/summarize）重叠 >60%，同属"工具返回尺寸治理"。
- **HITL 暂停不占 CPU / 等待不计入 maxDuration**：**判重不落**——与 r153-C dl「等不算跑」、r154-A dl 中断恢复重叠 >60%。
- **Trusted edge signals（边缘代理独占命名空间键做不可伪造输入）**：**本轮不落**，记入候选池（归属 sa 或 cc 的安全面），下轮评估。
- **recovery boot 的 onRecoveryBoot 政策钩子（丢弃半成品 / 合成工具结果 / 恢复横幅）**：**本轮不落**，与已落的"拼接回链"同段，属同一条的延伸。

## 四、三件套评估
- `wb-context-compressor`：**本升 3.49.0**（双轨压缩 + 模型/UI 消息分离 + 压缩产物持久化）。
- `wb-ponytail` / `wb-max-token-saver`：本轮无合格新材料 → 版本不动，不硬凑。
- 结论：**维持 3 件套**。

## 五、版本化
- 先落 live → cp 到 `D:/AI技能仓库` → sha256 三文件全 **SAME** → commit **`5b15b37`** → `git fetch` + `merge-base --is-ancestor` = **ANCESTOR_OK** → push `875ec20..5b15b37`（SSH over 443，非强推、无重试、不补推）。
