# r142-C 审计记录（WorkBuddy · 自审）

- 时间：2026-09-23 02:1x｜轮次：r142-C｜主源：OpenHands 官方 `docs.all-hands.dev` / `docs.openhands.dev`
- 实拉证据：`D:/腾讯AI/yt/r142/c/`（110 路 / 94 OK200，未达集合与 A、B 均互异：`e2b_artifacts`、`sim_studio` 未达，`deepwiki_mcp_python` 遭 GitHub 403 限流）+ `D:/腾讯AI/yt/r142/deep_c/`（llms.txt 46,920 字节 / 267 个 `.md` 链接 → 实拉 24 页全 200）

## 落地核验

| 文件 | 旧→新 | commit | push | repo↔live |
|---|---|---|---|---|
| defaults/wb-context-compressor | 3.35.0→3.36.0 | af4a252 | 已推（非强推，ANCESTOR_OK） | SAME `633bce756e90a5c0` |
| writing/wb-doc-writing | 1.9.0→1.10.0 | af4a252 | 同上 | SAME `2df08037b0fc9945` |
| engineering/wb-spec-driven | 1.65.0→1.66.0 | af4a252 | 同上 | SAME `3237d0b7b3b6548b` |

## 独点真伪核验

- **压缩三段结构（keep_first / 中间 / 最近）** — 原文 `sdk/guides/context-condenser`：「always keeps the first `keep_first` events (system prompts, initial user messages) to preserve important context」+「Keeps recent messages intact」✅ 属实。
- **按事件条数阈值触发** — 原文：「triggers when there are more than `max_context_length` events in the conversation history」✅ 属实。
- **压缩调用单独打 usage_id** — 原文示例 `llm.model_copy(update={"usage_id": "condenser"})` ✅ 属实。
- **文档详略由读者三档决定** — 原文 `overview/skills/creating`：「Who will use this skill? — Just you (keep it simple) / Your team (add more documentation) / Public sharing (comprehensive examples)」✅ 属实。
- **正文 1,500–2,000 词** — 原文「SKILL.md content: Core concepts and workflows (1,500-2,000 words ideal)」✅ 属实。
- **长期目标不 fork 历史** — 原文 `sdk/guides/agent-server/conversation-goals`：「It does **not** create a special conversation type, a fork, or a separate history」✅ 属实。
- **capped 是终态且不可续** — 原文：「it only resumes statuses that are not terminal; a goal with `status="complete"` or `status="capped"` is not resumable」✅ 属实。
- **驱动状态与会话事件同存** — 原文：「These status events are persisted with the conversation events, so resume can work after a server restart」✅ 属实。
- **停止优雅** — 原文：「Stopping is graceful. If a model call is already in flight, it may finish before the conversation becomes idle」✅ 属实。

## 判重核验

- UNKNOWN 一等风险级 + confirm_unknown 开关 → av 核心判据「不确定是一等判定，不得默认通过」重叠 >60%，**未落** ✅
- 技能创建的触发词 / 渐进披露 / references 外置 / 祈使句 → sa 已全覆盖，**未落** ✅
- prompting best practices（具体 / 给位置 / ≤100 行）→ sd §规约先行 + §任务自包含 已落，**未落** ✅
- event sourcing / tool-system / mcp / llm-profiles / sandboxes → 平台与 API 面，**未落** ✅

## 审计结论

- 3 独点均有官方原文出处；三轮未达集合互不相同，独立重拉成立。
- 全轮共 9 次落地 / 5 个文件；PyYAML 52/52；CRLF / STRAY_CR / U+FFFD / 尾空白 / description>1024 全 0。
- `CONTENT_DIFF(2)`：`defaults/wb-max-token-saver`、`engineering/wb-execute-discipline` —— live 领先 repo，属豆包 r141-A/B/C 未推内容，本轮三轮落点已全部避开。
