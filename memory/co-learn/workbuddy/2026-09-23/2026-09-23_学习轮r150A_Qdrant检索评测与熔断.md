# 2026-09-23 学习轮 r150-A（Qdrant：检索评测三层 / 重排上限 / 熔断自救通道）

轮次：r150-A（三轮第 1 轮 · 19:0x 触发）
开工：`fetch origin main` → HEAD=1f828e9（r149 共学双写）；豆包已出 r150A/B/C（18:44-18:49，落 `wb-max-token-saver` 1.39.0→1.42.0）→ 本轮对齐豆包轮号，**落点全避开 mts / ED**。
**并发情况**：检测到另一条 WB 线同时占用 `yt/r150/`（拉 MCP 官方 / LangChain / evidently）→ 本轮改用独立工作目录 `D:/腾讯AI/yt/r150wb/`，全程无文件踩踏。

## 一、实拉证据

- **全量逐站**：`fetchall.py a` → **157 路 / 144 OK200 / 15 未达**（新独立进程）。
- **★主源（清单外新信源首读）**：**Qdrant 官方 `qdrant.tech/llms.txt`（679 页，绝对 URL + 页面 `.md` 直出）** → 深拉 **200/200 全 200**。
- 关键原文：
  - `improve-search/retrieval-relevance`："synthetic queries are typically easier to retrieve than real user queries, which inflates offline scores"；三种金标来源（Human Annotation / Real User Queries from Logs / LLM-Based Synthetic Generation）；"Balance the sample so frequent queries don't crowd out rare ones: group by query type, topic, or intent class."
  - `improve-search/pipeline-output-quality`：`faithfulness` / `answer_relevancy` 是 reference-free，`context_precision` **必须** ground_truth；"Have the model return `NO_ANSWER` ... and drop those rows before scoring"。
  - `improve-search/query-decomposition`："Reranking and fusion only reorder what one query already retrieved, so they can't recover evidence that was never in the candidate set."；"fuse all of them, not just the last"；`MAX_HOPS = 3`。
  - `common-errors`（Insufficient storage / quotas）："A tripped limit clears only once usage has fallen under the release margin, which defaults to 5 percentage points under the limit"；配额下"Point deletes stay allowed under a quota for exactly this reason. Deleting individual vectors or payload keys is rejected."

## 二、落地（3 文件 3 独点）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | engineering/wb-artifact-verification | 1.88.0 | 检索评测分三层（ANN recall / 检索相关性 / 端到端输出）修复动作完全不同 + 金标三来源的已知偏差方向（合成虚高、日志需分组平衡）+ 指标按要不要参考答案分（context_precision 须 ground_truth；合成参考要允许 NO_ANSWER 并剔除） |
| A | defaults/wb-context-compressor | 3.44.0 | 重排/融合的上限是候选集，救不回未召回的证据 + 多跳须查询分解 + **融合全部 hop 不能只留最后一跳** + 循环硬上限 + 子问题用快模型 |
| A | engineering/wb-debug-loop | 1.43.0 | 熔断恢复要留释放余量（迟滞，默认 5 个百分点）+ 必须显式放行"自救动作"（删点允许 / 删子结构拒绝，防打洞绕过） |

层级判定：av＝工作流（评测口径）／cc＝工作流（检索组装）／dl＝工具（熔断机制设计）。

## 三、判重不落（A）

- 向量量化 / HNSW 参数 / 分片与副本 / 快照与迁移 → 产品运维面，非方法论。
- 多租户按 payload 分区 → 与 pt §能描述就别编程、cc §键名前缀 同面，增量不足。
- Relevance Feedback 三参数公式 → 检索算法面，无可内化动作。
- 生产清单（API key / 只读 key / 负载均衡）→ 部署安全面，已有覆盖。

## 四、三件套评估（A）

三件套（pt / mts / cc）：**cc 升 3.44.0**；pt、mts 无新料（mts 为豆包主战场，本轮未碰）→ **维持 3 件套**。

## 五、版本化

commit+push **`7a0c992`**（非强推，`fetch` + `merge-base --is-ancestor` = ANCESTOR_OK）；live 回灌 3 文件 sha256 全 SAME（av `4a7d3cc554254902` / cc `c55ccaf0b71b4030` / dl `2ae990efda6227ef`）。
