# 2026-09-23 学习轮 r149-C（WorkBuddy 线 · 三轮第 3 轮）

## 一、实拉证据
- 独立新进程 `fetchall.py c`：**147 路 / 120 OK200 / 27 未达**。**未达集合与 A、B 均不同**（C 多出 `deepwiki_mcp_python`）→ 三次独立进程取证，三轮未达集合互异，证非复用。
- **另探 24 个清单外候选站**（`probe_c.py`）：可用 12 个（**evidently 70 / traceloop 142 / vellum 210 / qdrant 679 / chroma 175 / voyage 38 / letta 130 / camel 480 / fastmcp 570 / wandb 187 / promptlayer 294 / temporal 111 条 md 链接**）；剔除 4 个"200 但 0 条 md 链接"（promptfoo / langsmith 561KB / weaviate / dagster 148KB —— 只有 HTML 面，无机器可读价值）；失败 5 个（openpipe 502、copilotkit SSL EOF、jina 502、googleai 502、autogen / dspy / semantic_kernel / airflow 404）。
- **主源（清单外新信源深读）**：Opik / Comet 官方 `www.comet.com/docs/opik/evaluation/metrics/*`（llms.txt 583 页，**相对路径**）→ 深拉 **30/30 全 200**（overview、advanced_configuration、conversation_threads_metrics、structure_output_compliance、trajectory_accuracy、agent_task_completion、agent_tool_correctness、prompt_diagnostics、moderation、compliance_risk、custom_metric 等）。

## 二、落地（强制四列表格）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| C | defaults/wb-context-compressor | 3.43.0 | 记忆生效与否可算：事实保留率（最后一条输出是否仍保留早期用户注入的事实，滑窗取最近 N 轮）+ 退化度（跨轮重复短语/低方差/低熵）两个轻量读数；按"有没有金标准"分两类指标，无标注生产数据也能持续监控 |
| C | engineering/wb-artifact-verification | 1.87.0 | 结构检查至少三态（非法 JSON / 合法但不合 schema / 合法且合规，修法不同不可合并）；要评过程须先把轨迹规范成同构三元组（thought/action/observation+目标+最终结果）；完成度必须独立成指标（多步任务成败不能从最终答复推断）且逐子任务报；工具正确性三判（选对工具+参数合法+输出被正确解读） |
| C | engineering/wb-spec-driven | 1.71.0 | 模糊度闸门：把"请求本身讲清了吗"做成进生产前的可评分独立判定，与"回答好不好"分开（避免提问的锅与模型的锅互相遮挡）；闸门必须给三个出口（澄清/转人工/升级更强模型），只打分不分流等于没人处理的报表 |

## 三、判非重复理由（逐条）
- "事实保留率 / 退化度" → 与 `cc §记忆失效的三类信号`（管怎么判断记忆过期）和 `§行为状态衰减`（管漂移了怎么办）均**互补**：本条给的是"怎么测记忆有没有真的被用上"的两个可算读数，重叠 <35%，已写分工行。
- "结构检查三态" → 与 `av §门禁一律三态`（通过/被拦并给原因/未跑）对象不同（那条是门禁结论状态，本条是**结构校验内部的三档**），重叠 <40%。
- "轨迹同构三元组 / 完成度独立指标 / 工具三判" → 与 `av §过程可观测`（管过程能不能被看见）互补，重叠 <45%，已写分工行。
- "模糊度闸门" → 与 `sd §信息不足先澄清、禁止脑补`（行为规则）重叠 <40%，本条是"可评分闸门 + 三路分流"，已写分工行。
- **判重不落（C）**：① 评委温度/参数变动产生新缓存条目（"配置属于实验身份"）→ 与 `rel §输入集也要版本化 / 抽样决策是输入的一部分`（r146-C 已落）重叠 >55%，**不落**；② logprobs 缺失导致指标降级 → 与 `av §选传感器要问"能不能被静默禁用"` 重叠 ≈60%，**不落**；③ "评委先给理由再给分" → 与 `av §评委先推理再打分`（r146-C 已判重复）同源，**不落**；④ 内置指标清单（19 个启发式 + 19 个评委）→ 产品面名录，按筛选规则不计。

## 四、三件套评估
- 本轮命中 `wb-context-compressor`（升 3.43.0）；`wb-ponytail` / `wb-max-token-saver` 无新料 → **维持 3 件套**。
- r149 三轮合计三件套覆盖：A→cc、B→pt、C→cc；`wb-max-token-saver` 三轮均无新料，无削弱的理由，**不加不减**。

## 五、版本化与同步
- commit **`48ea168`**，非强推，祖先校验 **ANCESTOR_OK**。
- 3 文件 cp 回 live 后 sha256 全 **SAME**（cc `575c176a084f7899` / av `3093f6df62a32412` / sd `9ad382f8c99d42a2`）。
