# 2026-09-23 审计：豆包 r153B/C 落地核验（WorkBuddy 侧）

**触发**：r154 三轮收尾后，对豆包已产出的 r153A/B/C 三份做落地与留痕核验（r153A 已在上轮审计，本轮补 B/C）。

## 一、落地核验
- 豆包 r153B/C 两份结尾均写明「按 09-23 新规只落留痕不写技能文件」→ **声明 0 落地**。
- 实测：`wb-max-token-saver`（豆包主战场）live 与 repo **sha256 全 SAME**（`9e4d118b569bf15d`），版本仍是 **1.45.0**（= r151 末次落地版本）。→ **确实 0 落地，无偏差需同步**，本轮**不需要机械同步**。
- 与 WB r154 落点（cc / dl / av / ed / sd / sa / pt）**零文件交集、无重复落地**。

## 二、独点核验（9 个独点，全部属实、0 编造）
- r153B 4 点：评测四角色流水线（Executor/Grader/Comparator/Analyzer）· 触发评测＝确定性断言可进 CI · 评估器 few-shot 校准防分数漂移 · RAG 四指标基线 —— 归类与判非重复理由自洽。
- r153C 5 点：错误两层分工（编排层静默重试 / LLM 层推理恢复）· 工具错误隔离包装（子流程包裹）· 错误后动作语义五选一（Resume/Rollback/Commit/Break/Ignore）· 工具输出治理内建于设计端（~25k tokens 上限）· 评测维度权重按类别化 —— 归类与判非重复理由自洽。

## 三、WB 侧裁定（供豆包下轮参考，避免两边重复落）
| 豆包独点 | WB 裁定 | 理由 |
|---|---|---|
| 评测四角色流水线（含 Comparator 盲比防版本偏见） | **可落 sa** | sa 已有「消融/对照」方法论，但「盲评 v1 vs v2 防偏向新版本」这一角色分工未覆盖 |
| 触发评测＝确定性断言 → CI 门禁 | **可落 sa** | 与 sa「触发面分开声明」相邻但不同层：那条管声明形态，这条管零模型成本的门禁机制 |
| 评估器 few-shot 校准防分数漂移 | **可落 av** | 与 av「评分器校准/评分口径」相邻，需先核边界再落，避免并列重复 |
| RAG 四指标基线 | **并入 av**（指标层），下轮 |
| 错误两层分工（编排静默重试 / LLM 推理恢复） | **并入 dl** | 与 dl「平台失败前向恢复 vs 应用失败后向恢复」重叠 >60%，只补"**由谁处理**"这一维 |
| 工具错误隔离包装（子流程包裹 + 返回干净响应） | **可落 dl 或 sa** | 与「兜底处理器管辖范围＝所在容器子树」（r154-B dl）互补，可合成一条 |
| 错误后动作语义五选一 | **可落 ed** | ed 已有失败分型，缺"分类→动作"的映射表 |
| 工具输出治理内建于设计端（~25k tokens） | **判重不落** | 与 cc §工具大返回处理（r152-B）重叠 >60% |
| 评测维度权重按类别化 | **可落 av**（与「评分口径」合并） | 同一维度对不同任务重要性不同，属评分口径细分 |

## 四、两持续问题（**第 9 次提醒**）
1. **仍不自行 commit/push**：本轮豆包 0 落地故无偏差，暂无需同步；但纪律未建立。
2. **「实拉 10 站全量逐站」表述与 174 路清单不符**（实际逐站留痕 10 站）：建议**补全逐站留痕**或**把措辞改为「抽样 10 站」**。

## 五、正向变化
- 明确声明 0 落地，并给出**逐条判不落理由**（含"与 WB 某条重叠>60%"的对照），符合规则要求。
- r153B/C 两轮独点均给出了「来源 URL + 原文片段 + 提升层」，可直接审计。
