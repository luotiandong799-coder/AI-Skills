# 豆包 r145-A/B/C 落地核验（WorkBuddy 审计）

> 审计时间：2026-09-23 04:5x｜审计对象：`每日学习日志/豆包_输出/2026-09-23/` 下 r145A/B/C 三份
> 审计方式：读豆包产出 → 到 live `D:\腾讯AI\skills` 逐点 grep 核验 → 查版本号 → 查是否入版本控制

## 一、结论（一句话）

**三份产出内容属实、独点全部可核验（无编造），但三个老问题一个没改：版本不 bump（连续第 4 轮）、改动不入版本控制、12 个点全挤在 2 个文件里。**

## 二、逐点核验

| 轮 | 豆包声称的独点 | 落点文件 | 核验结果 |
|---|---|---|---|
| A | 错误处理两层分治（节点级瞬时就地重试 + 工作流级死信/告警，attempt_count 让重试次数变数据） | ED | ✅ 存在（与既有 §DLQ 三要素 / §节点级错误语义 相邻，分工已注明） |
| A | 精简优先实证（Claude Code 删 80% system prompt 无评估损失） | mts | ✅ 存在（§精简优先的实证，L417） |
| A | 注入顺序：关键信息放首尾（lost in the middle，U 型） | mts | ⚠️ **重复落地**——mts §L376「检索上下文排序与预算：top-few 硬预算 + 关键放首尾（lost in the middle）」已完整覆盖同一判据，L423 又写了一次，两处重叠 >60% |
| B | 记忆读取按需 + 摘要锚点（Oblivion / HiGMem） | mts | ✅ 存在（Oblivion 1 / HiGMem 2 处） |
| B | 确定性产物管线（类型化 IR → Schema 机器验证 → 编译渲染） | ED | ✅ 存在（archify 2 / Schema 验证 3 处） |
| B | 上下文演化三角色（Generator/Reflector/Curator 增量 deltas） | mts | ✅ 存在（Curator 1 处） |
| B | 多工具冲突时先定义权威来源 | ED | ✅ 存在（权威来源 2 处） |
| B | AI 动作成功后固化（成功交互序列缓存 → 零 LLM 重放） | ED | ✅ 存在（action cach 1 处） |
| C | 动作单一定义多入口复用（defineAction + Zod 一次验证） | ED | ✅ 存在（defineAction 1 处） |
| C | 回合状态机（每轮先判要不要工具）+ 模型/harness 责任分离 | ED | ✅ 存在（回合状态机 1 处） |
| C | 规划上下文隔离（TDP：重规划限定活动子任务，错误不传播） | ED | ✅ 存在（TDP 1 处） |
| C | 稀疏腿优先 SPLADE（幻觉 31%→43%，管线不变换腿） | ED | ✅ 存在（SPLADE 5 处） |

**12 点全部属实，0 点编造。**

## 三、三个必须修的问题（按严重度）

1. **版本号不 bump —— 连续第 4 轮**（r141 首次提出）
   - live `engineering/wb-execute-discipline` **仍 2.97.0**
   - live `defaults/wb-max-token-saver` **仍 1.36.0**
   - 三轮共落地 12 个独点，版本号一个没动 → **无法从版本号判断技能是否变过**，回退与比对全部失效。
2. **改动不入版本控制**
   - 三轮 push 的祖先校验均 ANCESTOR_OK，说明 `AI-Skills` 远端**没有豆包的任何新提交**
   - repo 与 live 的 sha256 比对仍报 `CONTENT_DIFF(2) = [wb-max-token-saver, wb-execute-discipline]` → 改动只存在于 live
   - **后果**：live 是 junction 目录、无 git 历史，一旦被覆盖就永久丢失；也无法让其他 agent 拉到。
3. **A 轮「注入顺序」是重复落地**
   - mts L376 已有「关键放首尾（lost in the middle）」完整段落，A 轮又在 L423 加了一条同源的
   - **处置建议**：合并成一段，删掉后写的那条；判重时不能只比「注入什么 vs 注入顺序」，要先 grep 文件里是否已含该结论。

## 四、结构性风险（非本轮新增，但已到临界）

- `wb-execute-discipline` 正文 **2,231 行**（r143 时 2,137，本轮 +94）。
- 本轮 12 点里 **9 点落在 ED**，mts 3 点。
- **建议拆分**：把「错误处理 / 死信 / 重试层级」拆到 `wb-debug-loop`；「产物校验 / 输出断言 / 结构化 IR」拆到 `wb-artifact-verification`；「多 agent 接口契约 / 动作复用 / 回合状态机」拆到 `wb-spec-driven`。ED 只留「执行纪律」本体（零省略、失败攻坚、覆盖度）。

## 五、本轮 WorkBuddy 侧对照

| 轮 | 我落的点 | 文件 | 版本 |
|---|---|---|---|
| A | 私有/共享记忆双仓分离、共享仓可发技能、worktree 并发 | agent/agent-guild | 1.1.0 |
| A | 常驻目录树非内容、默认无向量索引、不常驻须显式点名、体检三查 | defaults/wb-context-compressor | 3.38.0 |
| A | 子代理 frontmatter 与技能同构、默认全开、按职能分型含只读 | engineering/wb-skill-authoring | 2.61.0 |
| B | 压缩项 opaque、两种压缩模式、剪枝随串联方式相反、压缩输出不得再加工 | defaults/wb-context-compressor | 3.39.0 |
| B | 重试粒度是步骤、幂等键收敛重复、状态与执行同 journal | engineering/wb-debug-loop | 1.37.0 |
| B | 昂贵并发声明天花板、等待用挂起、同一状态先定写权 | engineering/wb-spec-driven | 1.68.0 |
| C | 回滚两维三档、快照粒度=一次工具调用、影子仓隔离、回滚成本决定自主权 | engineering/wb-debug-loop | 1.38.0 |
| C | 记忆库六文件分工、更新触发条件、全量复审 | agent/agent-guild | 1.2.0 |
| C | eval 四条反模式、三类边界例、测量不稳不许调优 | engineering/wb-artifact-verification | 1.78.0 |

**9 点全部 bump 版本 + commit + push**（`5fbf2bd` / `b86b23f` / `1ead7ee`，三次均非强推、祖先校验 ANCESTOR_OK），并已回灌 live（全量 sha256 校验 SAME）。
