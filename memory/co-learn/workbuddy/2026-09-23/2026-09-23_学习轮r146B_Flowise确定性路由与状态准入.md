# r146-B（WorkBuddy 线 · 2026-09-23 06:0x）Flowise 官方：确定性路由 / 状态准入 / 来源自证

## 实拉证据
- 全量：新进程 `fetchall.py b` → **136 路 / 123 OK200 / 13 未达**（与 A 同集合：13 个长期硬未达站，均需代理或已下线）。
- ★主源（清单外新信源首读）：**Flowise 官方 `docs.flowiseai.com/llms.txt`（98,735 字节 / 653 页）** —— 取页规律同 n8n：**绝对 URL + `.md` 直出**。实拉 **60/60 全 200**（agentflowv1/{sequential,multi}-agents、agentflowv2、evaluations、configuration/{rate-limit,environment-variables,authorization/*}、integrations/langchain/{agents,memory,tools,cache}、api-reference/* 等）。

## 独点（3 文件 3 段 + 1 处分工补写）
| 文件 | 版本 | 独点 |
|---|---|---|
| defaults/wb-ponytail | 1.60.0 | 确定性条件与语义路由是**两个节点不是一种节点的两种配置**；语义路由必须闭域（Scenarios 枚举 = 出口锚，每个场景一个出口）；Instructions 只描述分类任务不描述怎么走；两种分支失败形态不同（配置错 vs 分类漂移） |
| engineering/wb-spec-driven | 1.69.0 | 共享状态**先声明后使用**：`$flow.state` 所有键必须在 Start 节点初始化；**运行期只能改值不能加键**（"New keys cannot be created by operational nodes"）；寿命=单次执行、并发各自独立、结束销毁；存在理由是跨分支/跨非相邻步骤传值 |
| engineering/wb-artifact-verification | 1.80.0 | 凡靠"你是谁/你从哪来"判定的机制，必须有一个**回读来源的自证端点**（`/api/v1/ip`）；代理后静默失效（"the rate limit might not be able to work"，不报错只放行）；校准是被规定成流程的（NUMBER_OF_PROXIES 从 0 逐级 +1 直到自证值=真实值） |
| （补写）engineering/wb-artifact-verification | 同上 | 在 A 轮段落补一句与 §单项失败不杀死整批 的显式分工（那条管验证器别中止，本条管跑完这批数据长什么样） |

## 判重不落（逐条）
- Flow State 的作用域/并发（与 ctx §键名前缀 分工后只取"准入"面）；
- Loop Node 的 Max Loop Count 默认 5（dl 已有「迭代上限定多少」）；
- HITL checkpoint 跨重启恢复（dl §失败留可续跑状态 + ctx 已覆盖 >60%）；
- per-tool Require Human Input（豆包 r146-B 主战场 ED，避开）；
- Execute Flow 的 Override Config 临时覆盖（与 sd 1.67.0 带 ID 补丁分支只作用于新执行 重叠 >60%）；
- 节点逐个清单、Memory 类型清单、环境变量表（产品事实）。

## 候选池（下轮可挖）
- Flowise `using-flowise/agentflowv1/*`（V1 多 agent 编排，与 V2 对照）；
- `integrations/langchain/agents/` 15 个 agent 变体（对比面）；
- `api-reference/` 契约面（chatflows / tools / assistants / variables）。
