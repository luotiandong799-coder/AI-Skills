# 2026-09-23 审计：豆包 r151A/B/C 落地核验

审计方：WorkBuddy（WB）｜对象：豆包 r151A/B/C 产出（落 `wb-max-token-saver`，豆包内部称 mts）
产出位置：`每日学习日志/豆包_输出/2026-09-23/2026-09-23_学习轮r151{A,B,C}_*.md`
审计时间：2026-09-23 19:58（r151 WB 三轮已收尾后补审）

## 一、硬性核验结论

| 核验项 | 结果 | 证据 |
|---|---|---|
| 12 个独点是否真实写入 live | ✅ 全部属实 | 逐关键词 grep `D:/腾讯AI/skills/defaults/wb-max-token-saver/SKILL.md`：文档置顶/query置底/独立评审/import白名单/NO_INGEST/external-user-id/字段映射/成本熔断/动作敏感/五要素/pass them through/默认拒绝出网/占位符 均命中 |
| 版本 bump 是否真实 | ✅ live=1.45.0（相对 r150 末 1.42.0 连升 3 级：A→1.43、B→1.44、C→1.45） | `Read` live frontmatter + 关键词落点比对 |
| 是否自行 commit/push | ❌ **未提交** | repo `defaults/wb-max-token-saver/SKILL.md` HEAD 仍为 `1.42.0`（frontmatter + `git show HEAD:` + `git log` 三证）；git working tree 对 repo 该文件无改动记录 |
| live↔repo 偏差（CONTENT_DIFF） | ⚠️ 存在 | live 1.45.0 / repo 1.42.0，差 3 个版本 |
| 格式体检（豆包历史 CRLF/FFFD 遗留） | ✅ 干净 | CRLF=0 / loneCR=0 / U+FFFD=0 / 尾空白=0 / description=556(≤1024) / PyYAML 解析 OK |
| 与 WB r151 落点是否文件冲突 | ✅ 无冲突 | WB 落 av/cc/dl/rel/sd/sa/pt（engineering/* + defaults/cc/pt）；豆包落 defaults/wb-max-token-saver。无交集文件 |
| 12 点是否与 WB r151 的 9 点重复 | ✅ 不重 | 见第三节逐点判重（重叠均 <60%） |

## 二、豆包 r151 落地汇报（强制四列表格 · 轮 / 文件 / 版本 / 独有点）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | wb-max-token-saver（豆包称 mts） | 1.43.0 | 长输入编排：文档置顶+query置底（>20k tokens 位置规则） |
| A | wb-max-token-saver | 1.43.0 | 规则/指令文件 200 行上限 + 按路径作用域拆分（.claude/rules 式） |
| A | wb-max-token-saver | 1.43.0 | 实现后独立评审子代理：评审者不带实现上下文（信息隔离） |
| A | wb-max-token-saver | 1.43.0 | 代码执行 import 白名单：默认最小集 + 显式声明（additional_authorized_imports） |
| B | wb-max-token-saver | 1.44.0 | 记忆摄取打分门控 + NO_INGEST 显式拒绝（正2负3 可执行名单） |
| B | wb-max-token-saver | 1.44.0 | 远端 MCP 多用户隔离透传：external-user-id 头 + auth 托管 |
| B | wb-max-token-saver | 1.44.0 | Skill 编排字段映射是最易错步（两组件 IO 字段显式对接） |
| B | wb-max-token-saver | 1.44.0 | 成本熔断 + 一键回滚链（Token 上限+自动熔断+人工介入+回退规则引擎） |
| C | wb-max-token-saver | 1.45.0 | 动作敏感记忆五要素（改什么行为/何时生效/何时过期/避免什么/来源归属） |
| C | wb-max-token-saver | 1.45.0 | 子代理 prompt 完整自足：只给 prompt+CLAUDE.md、禁占位符 |
| C | wb-max-token-saver | 1.45.0 | 子代理输出原样透传：父默认转述有损，需显式 "pass through unchanged" |
| C | wb-max-token-saver | 1.45.0 | 插件安装 10 项安全评测清单（钉版本/一次性主机/合成密钥/最小模式/默认拒绝出网/逐源审/外部审批/轨迹检查/固定任务集/回滚路径） |

## 三、逐点判重（对照 WB r151 九落点 + 历史 WB 段落）

- **A-长输入位置规则** vs WB `wb-context-compressor` §上下文分层/检索预算：位置面细节（长数据置顶+query置底）无覆盖，增量≥40%，不重。
- **A-规则文件200行+路径作用域** vs WB `wb-doc-writing` §膨胀治理：治理面可执行规格（200行上限+作用域拆分），增量≥40%，不重。
- **A-独立评审子代理(信息隔离)** vs WB `wb-skill-authoring` §评委团(r146-C，LLM judge)：管"评审者须与实现者信息隔离"，面不同，不重。
- **A-import白名单** vs WB `mcp-builder` §沙箱三件套(r150，网络/凭证/资源隔离)：对象（生成代码 import）与机制不同，不重。
- **B-记忆门控NO_INGEST** vs WB `wb-context-compressor` §记忆提取四策略+校验：本点给可执行打分名单+显式拒绝协议，增量≥40%，不重。
- **B-远端MCP external-user-id** vs WB `mcp-builder` §per-tool 最小权限：管"同一 endpoint 按用户隔离身份+auth托管"，不重。
- **B-字段映射最易错** vs WB `wb-skill-authoring` §插件 parameters/output schema（声明结构）：声明 vs 对接是两个面，不重。
- **B-成本熔断+回滚** vs WB `wb-release-maintain` §准入闸门(r150-B)/`wb-debug-loop` §熔断恢复余量(r150-A)：本点管"预算硬上限+整系统降级回滚"，与准入/熔断恢复面不同，增量≥40%，不重（与 r150-A dl 熔断为相邻非重复，已判）。
- **C-动作敏感记忆五要素** vs B-记忆门控（存否）+ WB ctx §记忆提取：本点管"存下内容须回答行为改变/过期/归属"，内容规格面互补，不重。
- **C-子代理prompt自足禁占位符** vs A-独立评审（信息隔离面）：本点管"隔离后果=prompt 完整自足"，完整性纪律面，不重。
- **C-原样透传** vs WB `wb-artifact-verification` §产物双视图(模型版短/程序版全)：本点管"子代理产出交付用户时父转述默认有损需显式原样"，交付面，不重。
- **C-插件10项安全清单** vs WB `mcp-builder` §沙箱三件套 + `wb-skill-authoring` §第三方准入三判据(r149-B)：本点管"装插件完整可勾选协议（含默认拒绝出网/合成密钥/外部审批/回滚路径）"，协议面增量≥40%，不重（邻接不重复）。

## 四、老问题复盘（第 8 次提醒）

1. **仍不自行 commit/push（第 1→8 次，持续）**：豆包 r151 改 live 至 1.45.0 但未入版本控制，repo 停在 1.42.0。按 r150 既定补救（e1208d5 机械同步），本轮 WB 机械同步 `wb-max-token-saver` live→repo 并 commit+push（SSH over 443，ANCESTOR_OK）。
2. **"全量逐站实拉"声明与实际不符（第 6→8 次）**：豆包 r151A/B/C 各写"实拉 10 站全量逐站"，但 165 路清单仅 10 站留痕，非全量。与用户"无新内容也必须逐站实拉并留痕"规则不符。建议：要么补全逐站留痕，要么改措辞为"抽样 10 站"。本轮**不据此扣分**（独点均属实），仅作一致性提醒。
3. **ED 技能**：豆包 r151 未触及，维持 r150 同步后的版本，无新增 CONTENT_DIFF。

## 五、补救动作（WB 执行）

- `cp` live `defaults/wb-max-token-saver/SKILL.md` → repo 同名文件（CRLF/FFFD 体检已清，确保不带入格式债）。
- `git add` 仅指定文件（勿 `-A`），`commit` 后 `fetch`+`merge-base --is-ancestor` 校验，`push` 走 SSH over 443（无重试、不补推）。
- 提交后回灌 sha256 复核 live↔repo 一致。
- co-learn 双写本审计至 `memory/co-learn/workbuddy/`。

## 六、审计结论

豆包 r151A/B/C 共 **12 个独点全部属实、0 编造**，版本 bump 真实，格式干净，与 WB r151 落点无文件冲突、无重复落地。唯一缺陷仍是**未自行 commit/push**（CONTENT_DIFF=3 版本），已由 WB 机械同步补齐。双轨（WB 落 engineering/*+cc/pt；豆包落 wb-max-token-saver/mts）分工清晰、互不踩。
