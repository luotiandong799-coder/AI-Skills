# r145-B 学习记录（WorkBuddy）

> 日期：2026-09-23｜轮次：r145-B（三轮第 2 轮，独立重拉）｜信源：**115 路 / 102 OK200 / 13 未达**（与 A 轮同一未达集合，B 轮另探 32 个候选站点）
> 主源（本轮**清单外新信源首读**）：**OpenAI 官方 `platform.openai.com/docs/llms.txt`（42,056 B / 229 页）**·**Restate 官方 `docs.restate.dev/llms.txt`（33,290 B / 218 页）**
> 深拉：61 页全 200（OpenAI 31 + Restate 30）

## 一、本轮落地

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| B | defaults/wb-context-compressor | 3.39.0 | 压缩项官方定义为 opaque / 不可人读（只承诺能续跑不承诺可审计）+ 服务端自动 vs 独立端点两种模式的取舍 + 剪枝规则由串联方式决定（数组链可剪 / ID 链禁剪）+ 压缩输出即权威下一窗口不得再加工 |
| B | engineering/wb-debug-loop | 1.37.0 | 重试粒度是步骤不是整段（journal 跳过已完成步骤）+ 幂等键让重复请求收敛成一次 + 状态与执行同 journal 才永不同步 + 续跑的前提是进度在进程外 |
| B | engineering/wb-spec-driven | 1.68.0 | 昂贵并发必须声明天花板（AI 调用直接换算成花费）+ 等待外部事件用挂起不占执行位 + 同一份状态先定谁有写权（单写者） |

## 二、★清单外新发现站点（本轮最重要产出，建议计入"必须项"）

| 站点 | 探测结果 | 说明 |
|---|---|---|
| **`platform.openai.com/docs/llms.txt`** | 200 / 42,056 B / **229 页** | **OpenAI 官方 API 文档站，且 `/api/docs/<path>.md` 直出清洁 Markdown**。此前只有 Agents SDK 的 `openai.github.io` 入口，平台 API 面（function calling / structured outputs / reasoning / compaction / evals / computer use）一直没机器可读入口 —— 这次补齐。**本轮唯一被深挖出落地点的主源**。 |
| **`docs.restate.dev/llms.txt`** | 200 / 33,290 B / **218 页** | Restate = Durable Execution 引擎（Temporal 同类但另一个实现）。journal + replay + 幂等键 + 单写者 + 流控，方法论密度高且**与 Temporal 的表述不同源**，可互补。 |
| `trigger.dev/docs/llms.txt` | 200 / 61,917 B / 328 页 | 已入库未深读（TypeScript durable workflow） |
| `docs.cline.bot/llms.txt` | 200 / 18,871 B / 112 页 | 已入库未深读（Cline agent harness） |
| `docs.windsurf.com/llms.txt` | 200 / 24,156 B / 101 页 | 已入库未深读 |
| `kilocode.ai/docs/llms.txt` | 200 / 1,409,995 B / 10 页 | 已入库未深读（体量异常大，疑含全文） |
| `gptme.org/docs/llms.txt` | 200 / 12,610 B / 77 页 | 已入库未深读 |
| `cookbook.openai.com/llms.txt` | 200 / 14,001 B / 55 页 | 已入库未深读 |

**清单 115 → 123 路**（`D:/腾讯AI/yt/r145/urls.txt`）；删 0。
**探活失败**：`docs.dapr.io`、`learn.microsoft.com/semantic-kernel`、`learn.microsoft.com/agent-framework`、`docs.roocode.com`、`docs.continue.dev`、`opencode.ai/docs`、`aider.chat`、`inngest.com/docs` —— 均无 llms.txt（404）。

## 三、逐站实拉证据（选摘，全量见 `D:/腾讯AI/yt/r145/b/_summary.txt`）

- `developers.openai.com/api/docs/guides/compaction.md` 200 / 14,272 B —— 压缩项 opaque、两种模式、剪枝规则、不得再加工
- `developers.openai.com/api/docs/guides/conversation-state` 200 / 28,787 B —— previous_response_id / Compaction 独立页指向
- `docs.restate.dev/foundations/key-concepts` 200 / 8,956 B —— journal+replay、幂等键、单写者、流控、FaaS 挂起
- `docs.restate.dev/develop/ts/{durable-steps,error-handling,state,external-events,concurrent-tasks,testing}` 200（3.8–11.5 KB）
- `docs.restate.dev/use-cases/ai-agents` 200 / 5,515 B｜`tour/agents` 200 / 32,360 B
- 未达 13 路（与 A 轮一致）：`dify_market`(404) / `make`(403) / `fullstackskills_gh`(404) / `aliyun_skillmarket`+`aliyun_xiaxiaobao`+`zhipu_agentmore`(连接被重置) / `gpt_store_discover`(404) / `hf_agents`(404) / `waytoagi_fastgpt`(302 循环) / `guangzhengli`(404) / `skillscat`+`skillsdirectory`(SSL EOF) / `bedrock_agentcore_llms`(404)

## 四、判重不落（逐条理由）

| 点 | 来源 | 判非重复理由（→ 不落） |
|---|---|---|
| Conversation API / `previous_response_id` 链接 | OpenAI | 具体 API 用法，非方法论；只有"剪枝规则随串联方式变"被抽出落地 |
| 上下文窗口 token 计量（input+output+reasoning） | OpenAI | 与 ctx §自动触发须确认计量口径 同源 >60% |
| computer use / code interpreter / file search / apply_patch | OpenAI | 产品工具面，非 agent 技能系统底层 |
| agent builder / agents API 架构与配置 | OpenAI | 平台配置事实，无可复用判据 |
| Restate 三种服务类型、部署形态、序列化、serving | Restate | 产品/运维面 |
| Restate SDK 语言特定写法（TS/Java/Kotlin/Go/Rust） | Restate | 语言绑定面 |
| Restate 集群共识、Kafka 集成 | Restate | 基础设施面，本机单 agent 场景不适用 |
| FaaS 挂起机制细节 | Restate | 只取"等待不占执行位"这个判据，实现细节不落 |

## 五、候选池（下轮可接）

1. OpenAI `guides/evaluation-best-practices`（23,604 B，已拉未读）
2. OpenAI `guides/agents-api-sessions-events`（20,236 B，已拉未读）
3. OpenAI `guides/error-codes`（32,170 B）
4. Restate `tour/workflows`（90,586 B）、`develop/ts/concurrent-tasks`（5,566 B）
5. `trigger.dev/docs`、`docs.cline.bot` 两个新入库站点的首读

## 六、三件套评估（B）

`wb-context-compressor` 升 3.39.0（输入侧：压缩产物形态与剪枝规则）。`wb-ponytail` / `wb-max-token-saver` 本轮仍无新料 → **维持 3 件套**。
