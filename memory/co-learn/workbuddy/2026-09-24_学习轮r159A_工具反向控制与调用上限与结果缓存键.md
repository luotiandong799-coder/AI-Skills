# 2026-09-24 学习轮 r159-A 工具反向控制主循环 / 调用上限作用域 / 工具结果缓存键

轮次：r159-A｜信源 173 路全量实拉 **167 OK200**（6 未达＝历史终局 5 路 + `skillscat` DNS 失败）
主源深拉：Agno `docs.agno.com/llms.txt` 过滤 `agent-os|tools|toolkits` → **70/70 OK200**
（清单内信源；此前 r156-A 拉 memory 面、r158-B 拉 guardrails/存储面，**tools 面为本轮首读**）

## 一、实拉证据（主源页 + 原文要点）

| 页 | 关键原文 |
|---|---|
| `docs.agno.com/tools/exceptions` | `RetryAgentRun` 把消息作为工具调用错误回给模型，**不重试整个 run，只在当前 run 内给反馈**；`StopAgentRun` 退出工具调用循环且运行状态置 **COMPLETED**，**当前批次已发出的兄弟调用仍会执行** |
| `docs.agno.com/tools/tool-call-limit` | 一批同时超限 → **只执行允许的数量**（截断而非作废）；**计数跨同一响应循环内连续的模型请求**；**HITL 继续开启新循环，故不是跨暂停的累计预算**；Team 上限只约束 leader，**成员单独配**；内部 `read_result`/`search_result` **不计入** |
| `docs.agno.com/tools/hooks` | 钩子收 `function_name / function_call / arguments`，可①调用原函数②**抛异常拦下**③**返回另一个值顶替**；可**改写 arguments 后放行**；**多个钩子按赋值顺序串行**；钩子内可读 `RunContext`（会话状态/依赖/元数据） |
| `docs.agno.com/tools/caching` | 默认 TTL **3600s**，落系统临时目录 `agno_cache/functions`；**命中仍跑 pre-hooks / wrapping hooks / post-hooks**；**只缓存结果由入参决定的纯读**；注入 `run_context` 时 user/session id **进键**，而**可变状态、依赖、metadata、knowledge filters 不进键**；注入 Agent/Team **不把身份字段带进键**；**带注入媒体与 generator 结果绕过缓存** |
| `docs.agno.com/tools/selecting-tools` | `include_tools`/`exclude_tools`，**exclude 优先**，未知名 `ValueError` |

## 二、落地独点（2 文件 4 组）

| 文件 | 版本 | 独点 | 层级 |
|---|---|---|---|
| `engineering/wb-execute-discipline` | 3.01.14 | ①工具内两种反向信号：重试信号只作用于**当前 run 的下一次迭代**（不重跑整轮）；停止信号是**完成不是取消**，兄弟调用仍跑完，产物可能是 7/10 ②调用次数上限的**作用域三问**：跨暂停是否重置、批次内是截断还是作废、算谁的（Team 只管 leader）与哪些不算（内部取回调用） ③工具钩子是**拦截层**不是日志：能抛错、能顶替返回值、能**改参数再放行**，多钩子顺序即语义 | 工作流 + 工具 |
| `defaults/wb-context-compressor` | 3.60.0 | 工具结果缓存：命中**只跳过函数体不跳过钩子**；只有纯读可缓存；**键的边界是显式清单**（user/session 进键、可变状态/依赖/metadata/knowledge filters 不进键、注入 Agent/Team 不带身份）；媒体与 generator 绕过；TTL 与目录默认落临时目录（重启即失） | 工具 + 工作流 |

## 三、判非重复理由

- **vs r158-B「输入检查门四则」**（Agno guardrails）：那条管**模型发给工具之前的输入过滤**；本轮工具钩子管**工具调用链上的拦截与改写**，且能返回值顶替、能改参数，二者是不同位置的两道门。增量 ≥70%。
- **vs r158-A「按需揭示对缓存前缀四档影响」+ 豆包 r157-A 提示缓存**：那两条管**模型请求的前缀与跨请求排布**；本轮管**工具执行结果的本地复用与键设计**。增量 ≥65%。
- **vs 已有 ed 重试纪律**（错误分型：瞬态才重试）：那条是**调用侧对错误的分类**；本轮是**工具内部主动向上发信号改变循环**，方向相反。增量 ≥80%。

## 四、判重不落

- `include_tools`/`exclude_tools`（与 r158-A 束级 vs 单项粒度 相邻，记候选池待合并）。
- MCP 传输三页（stdio/streamable_http/sse）：与豆包 r159-B 传输选型重叠 >60%，本轮不落。
- `toolkits/search/*`、`toolkits/social/*`（40 页）：特定域工具包装，按筛选规则不计。

## 五、三件套评估

ponytail / max-token-saver / context-compressor 本轮均有落点（cc 3.60.0）。功能覆盖完整，未增删套件成员。
