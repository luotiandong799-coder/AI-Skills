# r130 学习轮（WorkBuddy）· 三轮独立实拉 · 9 次落地 5 文件

日期：2026-09-22 02:3x ｜ 三轮各独立新进程实拉 95 路（A 78 达 / B 76 达 / C 79 达，未达集合互不相同）

## 本轮新增信源（清单外新发现，均首读）

| 站点 | 是什么 | 为什么值得进必须项 |
|---|---|---|
| `mastra.ai` | Mastra 官方（TS agent 框架） | 有 `llms.txt`（653 页），**页面 URL 直接带 `.md`**，一跳直取正文；skills / workflows / evals / memory 四族方法论密度高 |
| `docs.agno.com` | Agno 官方（agent 框架 + AgentOS 运行时） | 有 `llms.txt`（3972 行）+ `llms-full.txt`；每页链接即 `.md` 清洁 Markdown，无需解析 HTML |
| `docs.haystack.deepset.ai` | Haystack 官方（deepset） | `llms.txt` 是 **llms-full 形态**（2.6MB 全站正文），一次抓取即全量原文，含 pipelines loops / snapshots / evaluation 等 |

清单 95 → **101 路**（新增 6 条：mastra_llms、mastra_skills、agno_llms、agno_evals、haystack_llms、openai_agents_llms）。

## 落地汇报（强制四列表格）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | engineering/wb-debug-loop | 1.25.0 | 旁路挂件失败必须显式选策略（降级继续 / 冒泡为失败）且无论哪种都留痕可查；不顶替原错误、不自递归 |
| A | engineering/wb-spec-driven | 1.52.0 | 委派边界四条：取消级联到子任务、产物传引用不传复述、引用内容对下游降格为数据、子代理只归档自己的一段 |
| A | engineering/wb-artifact-verification | 1.69.0 | 判定结论三层合成：门禁（必须 1.0）/ 带方向的阈值（min·max·区间）/ 裁决三值；无数值证据不出裁决 |
| B | defaults/wb-max-token-saver | 1.34.0 | 裁剪只作用于"给模型的视图"，存储里的完整历史不动：上下文可少带、事实不能少存 |
| B | engineering/wb-spec-driven | 1.53.0 | 人工介入分阻塞型与审计型；暂停状态落共享存储、续跑不依赖原进程、续跑占尝试额度 |
| B | engineering/wb-artifact-verification | 1.70.0 | 评测失败先分诊三类（判据写错 / 真回归 / 评委抖）；用例具体·稳定·一例一行为·锚定工具调用；清理只能撤新增 |
| C | engineering/wb-debug-loop | 1.26.0 | 自纠循环硬上限（执行前检查）+ 失败自动留可续跑快照；循环默认只留最后一次输出 |
| C | engineering/wb-skill-authoring | 2.46.0 | 模板/可执行内容默认沙箱；逃逸开关显式且风险写进名字（`unsafe`）；不为方便偷偷关沙箱 |
| C | engineering/wb-artifact-verification | 1.71.0 | 评测流水线与被测流水线分离：被测产出先落盘，换判据不重跑（避免重跑引入的归因噪声） |

## 三件套评估（每轮）

- A：三落点均非三件套 → 维持。
- B：**`wb-max-token-saver` 1.33.0 → 1.34.0**（裁剪只作用于视图层，事实层完整落库）。
- C：三落点均非三件套 → 维持 3 件套（基数由功能位决定，不凑数）。

## 判不落（逐条理由）

- Mastra `onIterationComplete`（每轮注入 feedback / 提前停）→ 与 sd §中途评审（TrajectoryJudge，一次一终裁）重叠 >60%。
- Mastra rubric scorer 自评迭代 → 与 av §最小指导量 + sd §中途评审 重叠 >50%。
- Mastra skill 三工具渐进披露 / references 外置 → 与 sa §渐进披露、ctx §上下文分层 已覆盖。
- Agno coordinator/specialist（协调者只见最终文本、保持上下文精简）→ 与 A 轮 sd §委派边界（引用传递）相邻，同族不重复写。
- Haystack metadata filtering / Jinja 沙箱之外的组件参数说明 → 平台实现细节。
- Haystack model-based vs statistical evaluation 的"选哪个" → 已收进 C 轮 av（接法与归因），指标定义部分不另落。

## 收尾

- 版本化：3 次非强推 push（SSH over 443、无 VPN、不重试不补推）— `c5767e1`(A) → `6fb8504`(B) → `4d32911`(C)，每次 push 前 `fetch` + `merge-base --is-ancestor` 均 OK。
- 审计：repo 45 SKILL.md，PyYAML 45/45 全解析；CRLF 0 / STRAY_CR 0 / U+FFFD 0 / TRAILING_WS 0 / description >1024 0 / 标点开头 0。
- live 同步：cp 回 `D:\腾讯AI\skills` 后全量 sha256，3 轮 9 次落地的文件 repo/live 全部 SAME；CONTENT_DIFF 2（`wb-context-compressor`、`wb-execute-discipline`）为豆包在途改动，未覆盖未代提交。
