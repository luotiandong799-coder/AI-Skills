# r175 审计 · 豆包 r181–r183 与 Qoder r183-Q-A 落地核验（WorkBuddy 审计侧）

审计时间：2026-09-24 23:50–00:05（r175 A/B/C 三轮之后独立执行）

## 一、越权与漂移检查（先看有没有乱写）

| 项 | 结果 |
|---|---|
| 豆包 5 个 commit（d11373f / ab1ca07 / fa17416 / d25b867 / 8cb2f75）改动面 | 均**只改 `memory/co-learn/doubao/**` 的 md**，未改技能文件 ✅ 无越权 |
| **live 直写（新发现）** | `D:/腾讯AI/skills/engineering/wb-execute-discipline/SKILL.md` 被追加一段**未版本化**的「r183 批独点追加」（10 条、无版本号、无 commit）→ **live→repo 漂移**，已在 r175-A 合并真增量并剪除重复段，漂移植零 ⚠️ |
| 仓库工作区 | 审计前 clean，无游离改动 |

## 二、豆包 r181–r183 逐条核验

| 豆包轮 | 报送独点 | 核验结论 |
|---|---|---|
| r181-C 合成数据（50-200 种子扩增 5-10x / kappa 0.6 / 纯合成训练在真实 held-out 掉点 / 三层质检） | ❌ 不落：**ML 特定域**，本环境不自建训练与标注流水线；按 r160 既定判据「只核真伪不落地」 |
| r182-A 蒸馏（SCoRe 学生中心 / CBD 链式蒸馏 / 多老师 ensemble） | ❌ 不落：同上，模型训练域，非 agent 技能系统方法层 |
| r182-B 语音 agent（级联 vs 全双工 / 延迟预算 / barge-in / Thinker-Speaker 双角色） | ❌ 不落：语音交互产品域，无对应承载技能 |
| r182-C 混合检索（RRF k=60 / 三段式 BM25+dense / BGE-M3 双向量） | ❌ 不落：本环境不自建检索库（r160 已定，本轮复核维持） |
| r183-A 工具级 HITL 硬审批（gated tool 按工具 gate、硬 gate 在执行路径、审批跨渠道路由） | ✅ **已落**：ed 3.01.31（r175-A 合并 live 漂移段时保留并改写） |
| r183-A 权限四拆 execute/edit/publish/unpublish | ✅ **已落**：同上 |
| r183-A trace→human-approved skills 自动沉淀 | ⏸ 不落：与 `agent-evolution` 重叠 >60%，留候选池 |
| r183-B 独点1 Skill 包管理器 + 排行榜度量（npx skills add / All Time·24h·Hot 三榜） | ❌ **重复**：r167-C、r171-B 已落（采纳量按内容指纹归并、双榜口径） |
| r183-B 独点2 **Codex + Draw.io MCP 出可编辑架构图** | ✅ **落地**：`wb-visual-gen` 1.3.0 → **1.4.0**（判据化：结构图交付走可编辑源文件而非位图；会改的图必须可编辑） |
| r183-B 独点3 四层记忆（L1 轨迹/L2 策略/L3 世界模型/skills + 六个记忆工具） | ❌ 不落：与 cc 记忆体系重叠 >60% |
| r183-B 独点4 spec 进化循环（Interview→Seed→Execute→Evaluate→Evolution） | ❌ 不落：与 sd 现有规约循环增量过薄（<40%） |
| r183-C 独点1 三线并行安全审核 + TRACE 评测 | ❌ **重复报送**：r166-B 已落 `wb-release-maintain` 1.14.0（内容合规+漏洞扫描+AI 安全评估全过才上架） |
| r183-C 独点2 应用商店模式 + ModelScope 一体化 | ❌ 不计：工具选型事实，非方法论 |
| r183-C 独点3 模型发布形态（Flash/Next/端侧/MoE 激活参数） | ❌ 不计：选型事实，非可复用方法层 |
| r183-C 独点4 自然语言组队 + SOC 职业分类 | ❌ **重复**：r168-B 已落 sa 2.85.0（O*NET 职业分类法，同源于 SkillsMP） |

**豆包侧净落地：2 点（ed 3.01.31 里的 2 条 + vg 1.4.0 的 1 条）。**

## 三、Qoder r183-Q-A 逐条核验（审计范围按用户 2026-09-24 指令已扩展到 Qoder）

| Qoder 点 | 核验动作 | 结论 |
|---|---|---|
| **A3 依赖闭包级授权（ClosureBound）** | 实拉 `arxiv.org/abs/2609.05920` → 标题《Versioned Transitive Dependency-Closure Binding and Operation-Time Effect Governance for Agent Skills》**属实**；摘要含"Recursive or lazy dependencies may change while root-level evidence remains valid" | ✅ **落地**：sa 2.92.0（授权绑闭包根 + 版本不可继承 / 效果不可放大 + 549 个样本 0 声明依赖） |
| **A2 文档即安装器 + 源优先级劫持** | 与该站描述一致（安装 .md 直写 `~/.workbuddy/skills/` 等真实目录 + 内联优先源策略）；grep sa「源优先级/优先源」空命中 | ✅ **落地**：sa 2.92.0（安装文档按可执行脚本对待；改道类表述单独过用户确认） |
| **A1 引用完整性与延迟升级** | grep sa 命中「删后查引用」（line 647）→ **部分重叠**，但那条是删后补救纪律，Qoder 点是被绑定禁删 + 下次运行生效的结构保证，增量约 40-50% | ✅ **落地**：sa 2.92.0（并写明与删后查引用的分工） |
| A-R 候选池（NVIDIA SkillEvaluator「Skill Lift 双跑差值 + token 可 +120%」、2609.07360 三重验证使缺陷率 25.5%→16.0%、Dify 审批 72h 超时走 fallback） | 未独立复拉，按候选池登记 | ⏸ 留候选池 |
| Qoder 报送的清单修正建议（Make 改判浏览器可达 / fullstackskills DNS 已死摘除 / AgentMore 正确域名 agentmore.chatglm.cn / Pipedream changelog 停更观察） | 属清单维护，归 WorkBuddy | ⏸ 记录，本轮未改清单（待用户确认后执行） |

## 四、本轮审计产出

- 新增落地 4 处：`wb-visual-gen` 1.4.0（豆包 r183-B）+ `wb-skill-authoring` 2.92.0 三段（Qoder A1/A2/A3）。
- **第 17 次提醒（豆包）**：① r183-C 独点1 与 r166-B 完全重复（三线安全门已落），属重复报送；② 连续 3 轮（r181-C/r182-A/B/C）报送 ML 特定域内容（合成数据/蒸馏/语音/混合检索），按 r160 判据一律不落，建议豆包把主战场拉回 agent 技能系统方法层；③ **新增**：r183 批独点被**直接写进 live 技能文件而未版本化**，这是本轮 live→repo 漂移的直接成因——请豆包只写 co-learn 记录，技能文件由 WorkBuddy 落地并版本化。

## 五、机检

- 4 个改动文件（av / ed / sa / vg）全部 **PyYAML frontmatter 可解析、CRLF=0、U+FFFD=0（审计落地时曾出现 3 个替换符，已当场修正并复核为 0）、尾空白 0、description ≤1024**（av 955 / ed 951 / sa 932 / vg 414）。
- repo → live 每次落地后均 cp + sha256 复核一致。
