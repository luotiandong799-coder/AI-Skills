# 2026-09-24 学习轮 r159-B 终态分型 / 痕迹过滤流水线 / 脱敏按字段名匹配

轮次：r159-B｜信源 173 路全量实拉 **166 OK200**（6 未达同上 + `make_templates` 本轮 403 波动转直连后仍未成，记探活计数）
主源深拉：Mastra `mastra.ai/llms.txt` 过滤 `reference/observability|workflows` → **70/70 OK200**
（清单内信源；r156-B 只拉 evals、r157-B 只拉 processors、r158-C 只拉 harness/time-travel，**workflows 主体与 observability 面为本轮首读**）

## 一、实拉证据（主源页 + 原文要点）

| 页 | 关键原文 |
|---|---|
| `mastra.ai/docs/workflows/error-handling` | 结果 `status` 四值：**`success` / `failed` / `suspended` / `tripwire`**；`onFinish` 收全部四态，**`onError` 只收 `failed` 与 `tripwire`（不收 `suspended`）**；结果带 `steps`，可逐步骤定位失败 |
| `mastra.ai/reference/observability/tracing/span-filtering` | 导出时过滤四阶段固定顺序：①内部 span 默认丢弃（须 `includeInternalSpans`）②`excludeSpanTypes` 按类型批发式剔除（**在处理器之前**）③`spanOutputProcessors` 变换 ④`spanFilter` 按属性/元数据/耗时/entityId 决定去留；**`spanFilter` 抛错时保留该条并记错误，避免静默丢数据**；可按导出目标分别配 |
| `.../processors/sensitive-data-filter` | 按**字段名**匹配，大小写不敏感且**忽略分隔符**（`api-key`/`apiKey`/`api_key` 同一名字）；**传 `sensitiveFields` 是替换默认清单不是追加**；默认过滤器**跑在用户 spanOutputProcessors 之后**（能盖住用户新增字段），但**预构造的 `ObservabilityInstance` 不会被追加**；三档：`full`（整值替换）/ `partial`（保留首尾各 3 字符，≤6 字符用 token）/ `indexed`（同值同令牌，**保留可关联性**）；默认字段清单含 password/token/secret/key/jwt/credential/ssn 等 |

## 二、落地独点（2 文件 3 组）

| 文件 | 版本 | 独点 | 层级 |
|---|---|---|---|
| `engineering/wb-execute-discipline` | 3.01.15 | ①**「没做完」拆两种**：`suspended`（等外部输入）与 `tripwire`（被拦下）是不同终态，把暂停归到失败会误报"挂了"；完成回调收四态、**错误回调不收暂停** → 靠错误回调告警会漏"卡在等输入" ②**痕迹过滤是四阶段流水线**：先按类型粗筛（低开销、在变换之前）再按属性细筛；**过滤器抛错要保数据**（失败姿态偏"多留"）；可按导出目标分别配（调试全量 vs 留存精简） | 工作流 + 工具 |
| `defaults/wb-context-compressor` | 3.61.0 | 脱敏四则：**按字段名匹配**（忽略大小写与分隔符，比按值匹配更稳）；**自定义清单是替换不是追加**（填两项＝只保护两项）；**默认脱敏跑在用户处理器之后、且对预构造实例不生效**；**三档风格对应三种用途**（要跨记录分析必须选保留关联性的 indexed） | 工具 + 可复用 Skill |

## 三、判非重复理由

- **vs r158-C 持久目标三结局（继续/完成/暂停）**：那条从**目标驱动**视角列结局；本轮从**工作流终态枚举**视角指出 `suspended` 与 `tripwire` 是两类不同东西，且给出回调覆盖面差异（错误回调不收暂停）。增量 ≥60%。
- **vs r152-B OTel 五类日志三告警**：那条管**记什么**；本轮管**导出时怎么裁**（顺序 + 成本档位 + 失败姿态）。增量 ≥75%。
- **vs cc 既有「暴露面最小化」表述**：此前只有原则没有判据；本轮给出**匹配面（字段名）+ 覆盖语义（替换）+ 生效范围（预构造实例不生效）+ 三档用途**四条可执行判据。增量 ≥85%。

## 四、判重不落

- `exporters/*`（14 页）：后端适配清单，运维操作面，不计。
- `migrations/upgrade-to-v1/workflows`：版本迁移操作面，不计。
- `reference/observability/feedback`、`metrics/queries`：与豆包 r159-C 评测/反馈面重叠 >60%，本轮不落（留给审计）。
- `workflows/human-in-the-loop`、`suspend-and-resume`、`snapshots`：与豆包 r159-A 审批门面重叠 >60%，本轮不落。

## 五、三件套评估

本轮落 cc（脱敏判据）。三件套功能覆盖完整，未增删成员。
