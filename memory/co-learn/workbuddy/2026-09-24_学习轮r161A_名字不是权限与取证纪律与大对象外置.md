# 2026-09-24 学习轮 r161-A 名字不是权限 / 检索取证纪律 / 大对象外置生命周期

轮次：r161-A（每小时 3 轮的第 1 轮）｜独立实拉 173 路 / 167 OK200

## 一、实拉证据

- **清单全量**：`D:/腾讯AI/yt/r161wb/fetchall.py a` → 173 路 / **167 OK200** / 6 未达（aliyun_skillmarket、aliyun_xiaxiaobao、skillscat = DNS 不解析；zhipu_agentmore 超时；hf_agents 404；skillsdirectory 重置，均为历史终局项）。
- **★主源（清单内，Agno 知识库 / 数据库 / 状态会话 / 上下文提供者面首读）**：`docs.agno.com/llms.txt` 过滤 `knowledge|database|state|sessions|history|context-providers` → 深拉 **94/95 OK200**（r156-A 只拉 memory、r158-B 只拉 guardrails 与存储、r160-A 只拉 agent-os 与 teams，本面此前未读）。
- 关键原页：`/knowledge/published-pages`、`/knowledge/concepts/search-and-retrieval/custom-retriever`、`/concepts/readers/docling-reader`、`/context-providers/providers/database`、`/context-providers/providers/overview`、`/sessions/persisting-sessions/media-storage/{overview,local,s3,gcs}`、`/sessions/session-summaries`、`/sessions/session-management`。

## 二、本轮落地（3 文件 4 组独点）

| 文件 | 版本 | 独有点 |
|---|---|---|
| engineering/wb-skill-authoring | 2.80.0 | 名字不是权限：只读是凭据属性不是命名属性（readonly_engine / query_ 前缀 / MCP 名均不构成边界）＋读写双连接特权分离＋发现辅助参数不是护栏＋隐藏入口不等于收回能力 |
| engineering/wb-artifact-verification | 2.04.0 | 检索与取证四纪律：空或截断结果不等于「没有」／不得静默替换证据／自定义检索器返回 None 使「有回答」不能证明检索成功／接口不替你改写查询／配好源不等于配好策略／白名单只守它管的那条路径 |
| defaults/wb-context-compressor | 3.66.0 | 大对象外置：只有持久化副本带指针／开关沿树向下且父不能覆盖子／外置物默认比会话活得久（先删行留孤儿）／预签名永不入库每次重签且 public 不授予访问／裸 URL 默认不下载／库变小与读变便宜是两件事 |
| defaults/wb-context-compressor | 3.66.0 | 摘要让主提示变短不等于总账变便宜：摘要自身默认读全量且有自己的输入边界／摘要与近期历史不是二选一／会话缓存只在单实例独占时成立 |

层级归属：sa 点落在**工具层**（能力边界判断）；av 点落在**工作流层**（验证取证纪律）；cc 两点落在**工具层**（存储与上下文成本）。

## 三、判重不落（逐条理由）

- **向量库与检索具体参数**（PgVector 默认余弦距离、`websearch_to_tsquery`、hybrid 的 `rrf_k=60`、chunking 各策略参数）：本环境不自建检索库，只作证据留痕，不落地。
- **Knowledge 隔离检索的「必须重建索引」**：与 rel 1.13.0 已落「改切分策略＝存量全部失效」重叠 >60%，不单列。
- **Cloud storage 适配器清单 / S3·GCS 凭据配置 / DuckDB·Pandas·Neo4j·BigQuery 工具库**：运维面与特定域包装，按筛选规则不计。
- **Learning Machine Always 模式的并发抽取与命名空间**：与 cc 已落「失败路径记忆三档 + 抽取器边界」相邻，记候选池，待合并判定。
- **on-demand history（`get_chat_history` 工具）**：与 sa 2.76.0「先读手册再动手的弹回门」重叠 >60%。
- **session 存储裁剪会连助手消息一起删**：cc 3.57.0 已落，不重复。

## 四、三件套评估

- `wb-context-compressor`：本轮迭代（3.65.0→3.66.0），新增大对象外置生命周期与摘要成本两节。
- `wb-ponytail` / `wb-max-token-saver`：本轮无合格新材料，不动版本。

## 五、审计与收尾

- repo 52 个 SKILL.md 待本轮收尾统一 PyYAML 复核；本轮 3 个落地文件写入后经 `newline='\n'` 落盘，无 CRLF。
- 产物落 `D:/腾讯AI/yt/r161wb/`，C 盘无新增。
