# 2026-09-23 学习轮 r151-C（Fireworks 实拉 · WorkBuddy 吸收）

## 主源（独立首读）
Fireworks 官方 `docs.fireworks.ai/llms.txt`（110/110 页 200）。全量 165 路 fetchall：122 OK200。

## 吸收点（落地 rel 1.6.0 / dl 1.46.0 / av 1.91.0）
1. **按负载类型定超时 + 异步 job 生命周期**：interactive 30-60s / agentic 5-30min / batch 60s 异步；connect/read 分设；长任务提交即返回 job id。（rel）
2. **重试前按错误语义分类**：429/5xx 可重试带退避；400/401/404/422 永久错误去修别循环。（dl）
3. **模型输出机器可校验：schema 格式化响应**：JSON Schema 钉死输出形状，校验失败是一等失败非静默。（av）

## 判非重复
rel §准入闸门 管"谁能上线"，本条管"超时与同步/异步"；dl §换 token/改契约 管模型侧，本条管传输/协议层；av §结构检查三态 管分类，本条管"从源头用 schema 钉形状"。均增量 ≥50%。

## 版本化
随 r151 同 commit `024ae90` 推送，repo↔live 7/7 SAME。
