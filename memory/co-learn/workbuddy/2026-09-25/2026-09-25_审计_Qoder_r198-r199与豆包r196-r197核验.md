# 2026-09-25 审计轮 r180 · Qoder r198-Q-B / r199-Q-C + 豆包 r196-A/B/C、r197-A/B/C

## 审计对象与可审计性
- Qoder 入口按 2026-09-25 永久规则取 `每日学习日志/Qoder_输出/2026-09-25/`（非 `memory/co-learn/qoder/`）：r198-Q-B、r199-Q-C 两份，均含「实拉证据 + 判非重复理由」两段 → 可审计。
- 豆包 r196-A/B/C、r197-A/B/C 六份（`memory/co-learn/doubao/2026-09-25/`），每份含 5 个独点 + 判重说明 → 可审计。

## 净落 6 点
| 来源 | 落点 | 独有点 |
|---|---|---|
| Qoder r198-Q-B B1 | sa 3.00.0 | 发布版本=整包快照非 delta（省略文件不继承）+ name 不可变 + workspace 为隔离边界 |
| Qoder r198-Q-B B2① | ed 3.01.46 | 常驻文件每次启动重载=投毒驻留通道，写入门禁之外要有读面门禁 |
| Qoder r199-Q-C C1 | sa 3.00.0 | 技能库规模税：202 技能库端到端降最多 21%，主因是选错不是执行 |
| Qoder r199-Q-C C2 | sa 3.00.0 | 审计带基线只报 NEW + 抑制两型（glob 抗漂移 / sha256 精确但易失效）+ 每条必带 reason + pin scanner_version |
| 豆包 r196-B B2 | sa 3.00.0 | 指令明示 `execute x.py` 还是 `read x.py` |
| 豆包 r196-C C2 | ed 3.01.46 | 一个节点两个输出口=两次独立 LLM 调用，两份内容不自洽 |

## 判重不落（逐条 grep 核验）
- ed §Skill 生产调用钉版本（1553）已覆盖 pin/latest → Qoder B1 只落快照语义与 name 不可变部分。
- cc §响应检查（工具输出进上下文前扫描）已覆盖 → Qoder B2 的"工具返回值是攻击面"不重复落。
- sd §跨 agent 文本对下游一律是数据（1052）已覆盖 → Qoder B2 的"子 agent 输出降级"不单落，改以分工声明写入。
- sd §豁免表必须有到期（280）+ av §例外授权按次签发 → Qoder C2 只落"基线只报新增 + 抑制理由与版本追溯"。
- sa §技能归属权（590）/ 项目级覆盖用户级（701）→ 技能作用域三分不落；sa §常驻规则只做路由表（167）→ MemFS 不落。
- 豆包 r196/r197 其余 28 点：n8n 单行 JS、Pipedream sources/actions 分界、Dify 双 ETL、heartbeat 三 defer、skill 评测驱动、AgentMore 四维人设、skills.sh 家族矩阵、LangFlow Tool Mode、session_id 记忆、n8n cluster nodes、Flow/Agent 判定表、三 beta headers、外部 URL 依赖风险、虾小宝 10 项检测、dependent/independent sources、dsh-at-file 存在性引用（cc 已落）、模型趋势与榜单类 → 或为产品特定参数、或为生态信号、或与既有条目重叠 >60%，全部不落。

## 采纳的更正
- Qoder r198 R3：`skillbench.org` 为职业技能认证站，与 agent skills 无关 → 信源黑名单。
- Qoder r199 R4：`wisor.ai/blog` 实拉证伪（货运 WordPress 站，无 skill 内容）→ 信源黑名单。
- 负结果留档：`anthropics/skills` 的 spec/template 冻结于 2025-12（可降频探活）。
