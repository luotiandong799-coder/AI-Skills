# 学习轮 r179-B：挂起双出口 / 重放打最新版不等于复现 / 提前终止要留原因（2026-09-25）

## 实拉记录（本轮独立重探，不复用 A 轮结论）
- 全量重探 173 路 → **147 OK200 / 26 BAD**（`yt/r179wb/probe_b.txt`）。未达集合与 A 轮不同（本轮新增 GitHub 域名 502：`openai_plugins` / `openai_skills` / `mcp_quickstart` / `voltagent_awesome` / `mattpocock_skills` / `agent_rigor` / `openviking` / `firecrawl_mcp` / `skillbuddy` / `awesome_agent_skills_engryan`），说明是通道波动而非站点失效。
- 主源：**Pipedream** `pipedream.com/docs/llms.txt`（26,249 字符）过滤后深拉 **14/14 OK200**（`yt/r179wb/B/`）：errors、concurrency-and-throttling、limits、environment-variables、secrets、inspect、event-history、rerun(node/py)、delay、parallel、end-workflow、data-stores、using-props。

## 落地（3 文件 3 组独点）
| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| B | engineering/wb-execute-discipline | 3.01.39 | `$.flow.suspend` 一次返回 resume_url **与** cancel_url：挂起是把决定权交出去，只给恢复链接则取消只能靠超时，而超时与被明确否决事后无法区分 |
| B | engineering/wb-artifact-verification | 2.16.0 | inspector 重放打的是**最新版本**而非出错那版：重放测"现在还错不错"、复现测"当时为什么错"；只提供打最新版则历史 bug 永远回归不了 |
| B | engineering/wb-debug-loop | 1.58.0 | 提前退出（`$.flow.exit()`）可配 reason 且原因在 inspect 可见：没走到最后一步与主动退出在留痕上必须能分开 |

## 判非重复理由（本轮被判重拦截的候选）
- **系统邮件去重（同一错误每 24h 一封）vs 自定义错误流不去重** → ed 已在 r177-审计落「通知层要节流，事件层要完整」，重叠 100%，不落。
- **测试态错误不发告警、不进 `$errors` 流** → 同一条目已含，不落。
- **Debug with AI 只发错误码/栈/步骤代码/静态配置，不发 event data** → ed line 1551「AI 调试最小数据披露」已落，重叠 100%，不落。
- **队列满直接丢事件（默认 100，付费至 10,000）** → ed 已有「队列满丢谁」，不落。
- **并发与节流 AND 语义、固定窗口节流** → ed 已有「并发不是限流」「限流要预防」，增量过薄，不落。
- **并发=1 消除竞态（同写一行互相覆盖且不报错）** → ed 已有「并发覆写」「并行写守卫」，不落。
- **Parallel 分支名是导出 key、规则顺序不影响** → sd 已有「顺序无关」「显式声明顺序」，不落。

## 提升层
工作流 / 审批（ed）、验证与回归（av）、可观测性与排障（dl）。
