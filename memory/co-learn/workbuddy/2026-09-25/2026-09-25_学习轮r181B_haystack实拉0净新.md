# 学习轮 r181-B（WorkBuddy 自增轮；主源 haystack · 2026-09-25 接续 A）

## 一、实拉证据
- 主源：`docs.haystack.deepset.ai/llms.txt`（**200，2,659,930 字节**，同域索引 254 条）
- 深拉 14 页 **14/14 OK200**（mcptool / mcptoolset / agent / answerbuilder / answerjoiner / automergingretriever / azureopenai* / branchjoiner / cachechecker / chatpromptbuilder / conditionalrouter）
- 实拉原文留存：`yt/r181wb/B/`

## 二、判非重复理由（全库 grep）
| 候选点 | grep 表达式 | 命中 | 判定 |
|---|---|---|---|
| conditionalrouter（按条件路由） | 条件路由\|conditional.?router\|路由 | **有**（r146B Flowise 确定性路由、ed 路由判定表） | 覆盖，不落 |
| automergingretriever（递归合并分块） | 递归合并\|automerging\|hierarch | **有**（RAG 递归检索，框架特定；与 agent 技能系统复用途低） | 框架特定，不落 |
| cachechecker（缓存层） | 缓存\|cache | **有**（ed 缓存前缀、r158A 束级门控） | 覆盖，不落 |
| mcptoolset（MCP 工具集） | mcp.?tool\|工具集 | **有**（mcp-builder、r159A 工具反向控制） | 覆盖，不落 |

## 三、结论
**0 净新**。haystack 的 pipeline 组件（条件路由 / 缓存 / MCP 工具集）在 ed、mcp-builder 已落；automergingretriever 属 RAG 框架特定实现，不可直接复用于 agent 技能系统（判据：底层方法论可复用性 <60%）。饱和重探，不留 idle commit。
