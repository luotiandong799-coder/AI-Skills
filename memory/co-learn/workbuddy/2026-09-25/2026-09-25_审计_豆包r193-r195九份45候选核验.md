# 审计：豆包 r193-A/B/C、r194-A/B/C、r195-A/B/C 共 9 份 45 候选（2026-09-25）

## 审计方法
每份先确认含「实拉记录 + 判重说明」两段（9 份均含，可审计）→ 逐条候选做全库关键词与语义查重 → 只落与既有条目重叠 <60% 的净新点。

## 落地（3 文件 3 组净新点）
| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| 审计 | engineering/wb-execute-discipline | 3.01.41 | 多 agent 圆桌三约束：消息必经主编聚合、禁成员直连；主编只编排、不写专业判断；结论保留每位专家独立卡片、不合并不调和——N 个 agent 退化成 1 个的特征是**趋同**，且趋同会被误读成互相印证 |
| 审计 | engineering/wb-artifact-verification | 2.17.0 | Freeze Path 钉住上游、只重算改动的下游：迭代成本＝**每次重跑覆盖范围**；上游跟着重跑既付全链路钱，又把上游随机性带进来让两次结果不可比 |
| 审计 | engineering/wb-skill-authoring | 2.97.0 | 同一份 SKILL.md 在 claude.ai / API / Claude Code 三端**各传各的不自动同步**，且 runtime 能力不同（API 无网、不能装包、只用预装包；Code 全网络；claude.ai 视配置）——可用性要标"跑在哪一面" |

## 判重不落（逐条给理由）
- r193A：A1 四层记忆（cc 已有语义/摘要/偏好/情节四策略 + 按召回成本分层）／A2 spec 驱动闭环（sd 已有 Evaluate→Evolution）／A3 Reference/Action 分治（sa 已有「参考型技能」）／A4 路由叠加层（与 A2 同族且抽象）／A5 多 runtime 适配（sa 已落跨 runtime）。
- r193B：B1 monorepo 拆包（sa 已有仓库四类分目录）／B2 三层渐进披露 token 账（cc 已落 metadata→body→resources）／B3 SkillHub TRACE（生态信息非方法论）／B4 无 skills/ 子目录不算包（sa 已有目录结构要求）／B5 跨 runtime 通用格式（已落）。
- r193C：C2 自动化六件套分工（与已落 cron 三判据同族）／C3 数据来源强制标注（cc 记忆带引用 + av 证据归属 已覆盖）／C4 WebGPU 跑 27B（模型/信息层）／C5 合规边界清单（领域特定）。**C1 落（圆桌三约束）**。
- r194A：A1 find-skills 元技能（sa 已有技能发现面）／A2 spec 硬约束清单（sa 已落 name/description/allowed-tools/500 行/引用一层深）／A3 description 反例（sa 已落）／A4 lark 生态（信息）／A5 EverMemOS（信息）。
- r194B：B2 `sys.workflow_run_id` 全链路审计（ed 已有 trace_id/run_id 族）／B3 GGUF 反超（信息）／B4 大模型周榜（信息）／B5 流式 ASR（信息）。**B1 轮数计数器未落**——与 cc「限历史轮数」（上下文裁剪）语义不同但增量较薄，记候选池。
- r194C：C2 Group 打包（ed 已有子流程）／C3 模板升级不覆盖已实例化（与已落版本分叉同族）／C4 step 命名（ed 已有名字错配静默分叉）／C5 props 语言限制（平台特定）。**C1 落（Freeze Path）**。
- r195A：A1 三层 token 账（已落）／A2 script 零 token（mts line 353 已落）／A5 reserved word（sa 已有「保留名」）。**A3/A4 落（跨 surface 不同步 + runtime 能力面）**。
- r195B：B1 SOC 职业分类（信息）／B2 本地数据 skill（已落）／B3 per-repo 记忆（cc 已有按仓库作用域）／B4 两步安装（sa 已有依赖加载可靠性）／B5 市场不认证（sa 已落「索引不是背书」）。
- r195C：C1 flush 配本地小模型（cc 已有压缩前 flush）／C2 action-sensitive memory（r178-B 已落）／C3 虾小宝三项认证 + C4 三层安全审计（sa 2.95.0 已落）／C5 50+ 客户端（信息）。

## 结论
九份合计 45 候选 → **净落 3 点**，其余 42 点均有明确已落对应条目（重叠 >60% 或属信息/平台特定层）。
