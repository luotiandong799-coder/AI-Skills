# 2026-09-25 学习轮 r180-B · LangSmith（docs.langchain.com/langsmith）实拉

## 实拉证据
- 全量重探：`fetchall.py b` → 173 / **166 OK200**（`probe_b.txt`）。
- 主源换路记录：`docs.smith.langchain.com/llms.txt` 返回 **200 但正文是 Mintlify 首页 HTML**（索引型页面不是索引）→ 按其 canonical 改走 `docs.langchain.com/llms.txt`（24,315 字符）→ `langsmith/llms.txt`（39,997 字符）→ 深拉 14 页 **14/14 OK200**（`yt/r180wb/B/`）：audit-evaluator-scores、assertions、bind-evaluator-to-dataset、code-evaluator-sdk、composite-evaluators-sdk、compare-experiment-results、conditional-tracing、annotate-traces-inline、annotation-queues、alerts、cicd-pipeline-example、create-few-shot-evaluators、dataset-transformations、online-evaluations-code。

## 独点（3 组，2 文件）
1. **验收标准写成断言而不是手写答案**：评审到错的 run 时写自然语言断言（key + comment，`must_` 前缀只是命名惯例）；**只要写了至少一条断言，输出面板就从实际输出切换成断言预览并锁定**。
2. **人工改分闭环回评测器**：`update_feedback(correction={"score":...})` 原判不抹、修正独立挂；修正自动进 corrections 数据集，再经 `{{Few-shot examples}}` 插回评测器提示词（默认 5 条）。
3. **追踪三级优先级**：`tracing_context(enabled=)` > `ls.configure(enabled=)` > 环境变量；可按合规/PII/租户/成本/特性开关在作用域内关闭；另有采样率通道（降量非全关）。

## 判非重复理由
- 1 vs av §失败驱动评测集增长：那条管**收不收进来**，本条管**"期望"以什么形式写**；"写了断言即锁住输出面板"这条机制全库 0 命中，正是防"把反例存成正例"。
- 2 vs av §不许静默纠正 / §反馈必须版本化：那两条管**纠正要披露、反馈要能追溯来源**，本条管**纠正值回流成评测器校准信号、以及原判必须保留作为对照**；grep `纠正 / 回流 / few-shot` 无同义命中。
- 3 vs cc §实例级强制是下限不可局部放松：方向相反的两类开关——强制类局部不许放松，隐私/成本类局部必须能关；本条写明三级优先级，grep 无命中。
- 判重不落：评测器绑定数据集后**不回溯旧实验**（记候选池）、反馈可挂任意中间 span（av §过程可观测要先显式打开 已覆盖）、composite evaluator 自定义聚合（ed §复合评分改权重不重跑 已覆盖）、dataset transformations（特定域）。
