# 审计：Qoder r195-Q-B / r196-Q-C / r197-Q-A 三份（2026-09-25）

## 审计入口（按 2026-09-25 永久规则）
Qoder 真实产出目录 = `D:/腾讯AI/每日学习日志/Qoder_输出/YYYY-MM-DD/`（**不是** `memory/co-learn/qoder/`，后者只有技能清单与 README）。本轮开工先 `ls` 该目录，读到三份新产出（r194-Q-A 已于 r178 审为 0 净新）。

## 三份是否可审计
三份均含「① 实拉证据（逐站留痕）」+「② 判非重复理由（grep 全库）」+「③ 四列落地建议表」，**可审计**。

## 落地（3 文件 4 组净新点）
| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| Qoder | engineering/wb-skill-authoring | 2.98.0 | 导航行为四信号回归裁剪法：读取顺序出乎意料→结构不直观；引用没人跟→链接不显式；某文件被反复读→上提进正文；捆绑文件从没被读→该删。结构问题看使用者实际怎么走，不看目录树好不好看 |
| Qoder | engineering/wb-skill-authoring | 2.98.0 | 签名强度分强制型 / 提示型两型（附同一版）：SkillHub 验签属提示型，未签名不拦截——引用"要求签名"必须说清强度 |
| Qoder | engineering/wb-artifact-verification | 2.18.0 | 裁决按放行/注意/警告/封禁/扫描出错五档发布，非 PASS-FAIL；低于置信门槛的证据只后台参考、不进对外阻断报告（多扫描器敏感度差 21%–61%） |
| Qoder | engineering/wb-execute-discipline | 3.01.42 | 审批门控三时间参数：确认超时默认 600s 自动取消；同意不缓存、批量逐条确认；确认后不可撤销、需显式回滚 |

## 查重结论（全库 grep，零方法级命中）
- 「导航行为 / 读取顺序 / 反复读 / 从未被读」→ 仅 cc line 1209 有"被反复读"字样但讲的是注入句时态，非结构裁剪方法 → 净新。
- 「置信门槛 / 裁决五档 / 待证」→ 全库无命中 → 净新。
- 「确认超时 / 不可撤销 / 不缓存同意 / 逐条确认」→ 仅 ponytail line 521 有"逐条确认"但讲的是"省掉一层少一层保障"，非审批参数 → 净新。
- 「签名强度 / 强制型 / 建议型 / 未签即拒」→ 全库无命中 → 净新。

## 采纳的两条更正
1. **恢复 r190-Q-B #1 入库资格**：r192-Q-B 把「无编辑审核/扫描公告」误扩成「无任何索引边界声明」而撤回；本轮 Qoder 逐字直拉 `skillhub.cn/llms.txt`，`## 抓取边界` 段明文写「以下为登录态页面与管理后台，不应被索引或引用」并列 4 个受限 URL——**"发布者用 llms.txt 声明索引边界"这个论点的一手证据在位成立**，撤回过严，予以恢复。
2. **Make 入口改注**：`https://help.make.com/release-notes` 200 可达且有一手 release notes，原「Make 持续 403 未达」终判撤销，信源清单改注该入口。

## 判重不落
- r195-Q-B 候选池：>100 行 reference 顶部 TOC + grep 逃生口、"Run x.py" vs "See x.py" 执行意图（增量薄，记候选池）；skills.sh 审计四元组 (scanner, issue-code, risk-score, date) + PENDING 态（与已落扫描归一族同族）；FIRST SEEN 账龄（记候选池）。
- r196-Q-C：SkillApt WITH/WITHOUT 配对门控（r190-Q-C C1）维持待审，本轮只补量化证据；M-SQE 双镜头质量分、reference 链接有效性校验（候选池）。
- r197-Q-A：A-R1 风险评估请求字段白名单 + 凭证不外发（与 cc 脱敏族同族）；A-R2 审计立场两型（裁决型 vs 参考型）已并入 r196 C1 一并处理。
- 大面积判重成因：同日豆包 r191/r195 已占据 Anthropic best-practices 与技能市场簇正文，Qoder 主动不重复提案，属「同伴高覆盖 → 独立复拉补角」型轮次。
