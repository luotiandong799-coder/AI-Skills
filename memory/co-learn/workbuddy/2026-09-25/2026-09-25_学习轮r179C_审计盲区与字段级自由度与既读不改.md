# 学习轮 r179-C：审计面＝能分类的动作 / 自由度按字段给 / 既读不改（2026-09-25）

## 实拉记录（本轮独立重探 + 主源换路）
- 全量重探 173 路 → **135 OK200 / 38 BAD**（`yt/r179wb/probe_c.txt`）。
- **主源换路（关键事件）**：首选 Anthropic 官方 `docs.anthropic.com/llms.txt`（69,087 字符，200）→ 按索引深拉 `platform.claude.com/docs/en/**.md` 14 页，**全部返回 200，但正文是同一份 447,830 字节的 "App unavailable in region" 空壳页**（换 `docs.anthropic.com/en/**`、`docs.claude.com/en/**` 三个域名结果一致）。
  → 按已落判据「状态码不是全部证据 / 200 但没内容」判为**未实访**，换主源为 **Activepieces** `www.activepieces.com/docs/llms.txt`，深拉 **14/14 OK200**（`yt/r179wb/C/`）：agents overview/tools/knowledge/in-flows/manage、flows building/ai-router/passing-data/project-variables/publishing/flow-approvals/debugging/versioning、mcp overview。C 轮抓取的空壳产物已删除。

## 落地（3 文件 3 组独点）
| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| C | engineering/wb-skill-authoring | 2.96.0 | 审计面＝能分类的那部分动作：只记写不记读、传参值从不入库、失败尝试也留痕；MCP 工具因无读写标记完全不记录＝**明示盲区**，不许用"记了但记不准"假装覆盖 |
| C | engineering/wb-execute-discipline | 3.01.40 | 自由度按**参数字段**给，不按工具给：同一工具每字段可选「模型填 / 人工钉死 / 留空不发」；挂工具的最小粒度是单个动作不是整个应用 |
| C | defaults/wb-context-compressor | 3.78.0 | 读过不可信内容后，**同一条消息内拒绝改自己的指令**：读取与自改必须跨轮分离，注入最多影响本轮回答、无法写进常驻规则 |

## 判非重复理由（本轮被判重拦截的候选）
- **审批期间旧版本继续跑** → ed line 2469「批准有寿命，等待期间不许有副作用」+ av line 855「平台把旧版本继续喂给下游」已覆盖，重叠 >60%，不落。
- **拒绝后可 Use as Draft 继续** → ed 已有失败恢复阶梯，增量过薄，不落。
- **审批徽章计数封顶 "10+"** → sa 已有「下界不是确数」，不落。
- **Flow tool 把固定流程当工具交给 agent** → ed 已有「agent-as-tool」族，不落。

## 提升层
安全与审计（sa）、工具与授权粒度（ed）、注入防护（cc）。
