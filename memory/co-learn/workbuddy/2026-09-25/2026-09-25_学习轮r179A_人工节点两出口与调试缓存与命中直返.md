# 学习轮 r179-A：人工节点两出口 / 调试缓存与执行记录分离 / 命中直返（2026-09-25）

## 实拉记录（本轮独立实拉，不复用既有结论）
- 全量探活 173 路 → **155 OK200 / 18 BAD**（`yt/r179wb/probe_a.txt`）。
- 主源：**Dify** `docs.dify.ai/llms.txt`（→ `_llms/en/cloud.md` 索引 16,452 字符）过滤后深拉 **14/14 OK200**（`yt/r179wb/A/`）：
  human-input、error-handling、error-types、var-assigner、var-aggregator、node-iteration、node-loop、version-control、annotation、step-run、var-inspect、run-history、monitor-logs、node-agent。
- 未达站点（18）：make_templates 403、openai_platform/cookbook llms 403、together 403、the_neuron 403、aliyun_skillmarket / aliyun_xiaxiaobao / zhipu_agentmore / hf_agents / hf_skills / skillscat / smolagents 502、skillsdirectory SSL、topai_sitemap 与 trigger.dev 握手超时、mistral / voltagent 不可达。

## 落地（3 文件 3 组独点）
| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| A | engineering/wb-execute-discipline | 3.01.38 | 人工节点两出口：首个响应即终局（广播给 N 人 ≠ N 人会签，先到先得）+ 超时分支是一等出口，不接则工作流静默结束（无人响应与人工拒绝是两条路径） |
| A | engineering/wb-artifact-verification | 2.15.0 | 单节点独立跑 + 检查器可编辑中间值做场景测试，但**编辑不改写 Last run 原始记录**：探索与证据两条线；Answer/End 不支持单测 |
| A | defaults/wb-context-compressor | 3.77.0 | 语义命中超阈值直返策展答案、不进生成；索引绑定产出它的 embedding 模型，换模型 = 全部作废重算 |

## 判非重复理由（本轮被判重拦截的候选）
- **失败回退三档（None / Default Value / Fail Branch）** → ed line 1278 已于 2026-09-19 落同义条目，重叠 100%，不落。
- **批内失败三策略（终止 / 错误时继续 null 占位 / 移除失败结果）** → ed line 1290 已落，且连示例 `[result-1, null, result-3]` 都一致，不落。
- **数组迭代两模式（顺序 / 并行，并行上限 10）** → ed line 1352 已落，不落。
- **互斥分支汇聚器（只合互斥分支，并行分支要用 Code/Template）** → ed line 1328 已落，不落。
- **错误分层：节点级 vs 系统级（连接/503/限流/配额）** → ed line 654「400/404/409 不重试，500 安全重试」+ 1788「不可重试错误显式标记」已覆盖同一判据，重叠 >60%，不落。
- **换 embedding 模型 = 重建** → wb-release-maintain line 159 已落同一条，本轮只以"命中直返形态下的表现"作为 cc 新条的从属判据引用，不单独成条。
- **人工决策用稳定 action_id 不用显示文案** → sa line 1613「引用必须用不可变标识」已覆盖，不落。
- **Loop 三终止源 / 循环变量循环外可读** → ed 已有「循环必须有显式终止条件」「refine 循环默认无上限」，增量过薄，不落。

## 提升层
工作流 / 审批（ed）、验证与调试（av）、上下文与生成路径（cc）。
