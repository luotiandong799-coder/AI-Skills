# r146-A（WorkBuddy 线 · 2026-09-23 05:5x）n8n 官方：失败声明 / dirty 失效传播 / 批量占位

## 实拉证据
- 全量：新进程 `fetchall.py a` → **136 路 / 123 OK200 / 13 未达**（dify_market 404、make 403、aliyun×2 10054/10053、zhipu 10054、gpt_store 404、hf_agents 404、waytoagi 302 死循环、guangzhengli 404、skillscat/skillsdirectory SSL EOF、bedrock_agentcore_llms 404、fullstackskills_gh 404）。
- ★主源（清单外新信源首读）：**n8n 官方 `docs.n8n.io/llms.txt`（285,345 字节 / 1,356 页）** —— 修正两点取页规律：①llms.txt 里是**绝对 URL**（`https://docs.n8n.io/<path>.md`），不是相对路径；②页面 `.md` 直出 200。实拉 88 页全 200（70 页关键词筛选 + 18 页定向补拉：handle-errors-gracefully / break-workflows-into-smaller-parts / understand-dirty-nodes / item-linking-errors / error-handling 节点参考 / redact-execution-data 等）。

## 独点（3 文件 3 段）
| 文件 | 版本 | 独点 |
|---|---|---|
| engineering/wb-debug-loop | 1.39.0 | 失败声明：官方 `failure:{cause}` 六值（前三自愈 / 后三需人动手）；**抛错方只报原因不报对策**（"n8n owns any behavior derived from the declaration"）；**分类不出就原样抛**（猜错比没有更坏）；等待提示只给可自愈三类（retryAfterMs / resetsAtEpochMs）；错误负载要声明哪些字段必然缺失（execution.id/url 需入库、retryOf 仅重试有、触发点不同结构不同） |
| defaults/wb-context-compressor | 3.40.0 | dirty 节点：成功过的产物会因编辑变陈旧；失效标记规则 = 改参数标该节点 / 插删标其后第一个 / 加连接标目标 / **环内任一脏则环首也脏** / **子节点脏则父链到根全脏**；清除只有重跑一条路 |
| engineering/wb-artifact-verification | 1.79.0 | 批量失败归属到条：错误带 `itemIndex` 挂到具体条目；`continueOnFail()` 时**产出 `{error:...}` 占位并保持 `pairedItem` 指回原条目**；产出条数须与输入条数一致 |

## 判重不落（逐条）
- Error Workflow / Error Trigger 机制（平台功能面，只抽了错误负载缺省条件）；
- Sub-workflow 输入契约三档 Define using fields / JSON example / Accept all data（与 sa §输入输出显式声明 重叠 >60%）；
- Partial execution / 执行类型（产品事实）；
- OTel 原生 / Log streaming（观测工具面，与已有观测条目同面）；
- queue mode / 二进制数据处理 / 执行数据留存（部署配置面）；
- redact-execution-data 的脱敏字段清单（安全配置面，与 ctx §凭据三件事 同面）。

## 候选池（下轮可挖）
- n8n `connect/create-nodes/` 全系列（节点开发契约，55 页未读）；
- `build/work-with-data/` 数据映射与 item linking 全系列；
- `integrate-ai/` AI agent 节点族（未读）。
