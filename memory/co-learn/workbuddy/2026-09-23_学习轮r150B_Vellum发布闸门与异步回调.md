# 2026-09-23 学习轮 r150-B（Vellum：准入闸门动作级硬约束 / 异步回调对账 / 失败是一等输出端口）

轮次：r150-B（三轮第 2 轮 · 独立重拉）
落点继续避开豆包主战场（mts / ED）；本轮碰 rel / av / sd。

## 一、实拉证据

- **全量逐站**：`fetchall.py b` → **157 路 / 144 OK200 / 15 未达**（新独立进程，与 A 同集合）。
- **★主源（清单外新信源首读）**：**Vellum 官方 `docs.vellum.ai/llms.txt`（210 页，绝对 URL + `.md` 直出；llms.txt 头部明说"append `.md` to the page URL"）** → 全量 **210/210 全 200**。
- 关键原文：
  - `product/deployments/release-reviews`："A protected Release Tag cannot be assigned ... unless that Release has at least one approval from a Reviewer and no outstanding change requests."
  - `product/workflows/advanced/long-running-workflows`："Use the async execution endpoint to initiate your Workflow and immediately receive an `execution_id` for correlation. You can also provide your own `external_id` (such as a job_id, content_id, document_id...)"；webhook 认证三选一，官方"we recommend using HMAC authentication"；事件三态 `workflow.execution.initiated` / `.fulfilled` / `.rejected`。
  - `product/workflows/common-architectures/prompt-retry`："Prompt nodes support two selectable outputs - one from the model in case of a valid output and one in case of a non deterministic error"；条件节点判错误输出非空 → 计数节点读 Execution Counter 与上限（3）比较 → 回环或退出。
  - `common-architectures/fallback-models`：模型选择可由 Templating Node 的表达式动态决定。

## 二、落地（3 文件 3 独点）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| B | engineering/wb-release-maintain | 1.5.0 | 关键环境准入闸门做成**动作级硬约束**（受保护标签：无 approval 或有未决 change request 就打不上）+ 判据"绕过的成本＝完成它的成本" + 审批是"有批准且无未决"两条件的与 + 留可审计审批链 |
| B | engineering/wb-artifact-verification | 1.89.0 | 异步回调最大的失败是"对不上号"→ 发起时埋调用方自有 `external_id` + 回调认证选 HMAC（静态 key 泄露后无法区分伪造）+ 生命周期事件三态，只订阅"完成"会漏掉"被拒" |
| B | engineering/wb-spec-driven | 1.72.0 | 失败是节点的**一等输出端口**（错误输出可连线，错误处理退化成普通编排）+ 重试上界由显式执行计数器给出，不能靠"循环几次自然停" + 凡"可能需要换一个"的东西先问是不是可计算输入 |

层级判定：rel＝工作流（发布治理）／av＝工具（异步对账与回调认证）／sd＝工作流（流程契约）。

## 三、判重不落（B）

- 动态模型回退链（fallback models 四类目的）→ 与 mts §换挡顺序 / ED §换挡检测（豆包主战场）重叠 >55%，不落。
- 人工升级独立出口（Human Escalation 两个 Final Output）→ 豆包 r150-B 已落"过升级治理"到 mts，同面不落。
- 批量执行自动排队 / webhook 事件订阅配置 → 平台机制面。
- Workflow 节点类型清单（17 页 nodes）→ 产品事实。

## 四、三件套评估（B）

三件套（pt / mts / cc）本轮**均无新料**（落点 rel / av / sd）→ **维持 3 件套**。

## 五、版本化

commit+push **`71c25b7`**（非强推，ANCESTOR_OK）；live 回灌 3 文件 sha256 全 SAME（rel `8ffa0c51c26717b3` / av `66e50024e013a6d1` / sd `f8a14a2c03983853`）。
