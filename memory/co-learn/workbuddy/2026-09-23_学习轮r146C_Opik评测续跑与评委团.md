# r146-C（WorkBuddy 线 · 2026-09-23 06:1x）Opik/Comet 官方：评测续跑 / 评委团 / 输入集版本化

## 实拉证据
- 全量：新进程 `fetchall.py c` → **136 路 / 123 OK200 / 13 未达**（与 A、B 同集合 —— 三轮未达集合一致，独立进程成立但未达集合本轮无差异，以"另探 27 站"作为本轮差异佐证）。
- **另探 27 个候选站点**：新发现带 llms.txt 且有 md 链接的官方站 9 个 —— **Opik/Comet(584 md)**、**Prefect(330)**、**mcp_spec(353，已在库)**、**Helicone(214)**、**chainlit(122)**、**voltagent(67)**、**braintrust(65)**、**agentops(49)**、**instructor(71)**、**langfuse(21)**；探活失败 4 个：dspy(SSL EOF)、zep(SSL EOF)、autogen_ms(404)、semantic_kernel(404)、kubiya(SSL EOF)。
- ★主源（**清单外全新信源首读**）：**Opik / Comet 官方 `www.comet.com/docs/opik/llms.txt`（106,314 字节 / 583 页）** —— 评测与可观测专站，方法论密度高。实拉 **55/55 全 200**（evaluation/{concepts,metrics/*,advanced/*}、tracing/*、development/{optimization-runs/*,prompt-library/*}）。

## 独点（3 文件 3 段）
| 文件 | 版本 | 独点 |
|---|---|---|
| engineering/wb-artifact-verification | 1.81.0 | 评测续跑：已完成 run 原样保留（不重跑 task）、只重放未完成；**改了 task/metrics 就必须全量重跑**（续跑会抹掉"哪个 run 用哪个版本"）；另两种不该续的场景（重打分 / 加样本）工具不同；**续跑门票 = 版本化数据集**（否则 raise `ExperimentNotResumable`），自定义 sampler 还需本地 checkpoint 且同机 |
| engineering/wb-skill-authoring | 2.62.0 | 评委团 `LLMJuriesJudge`：单指标抓不住多维质量时才合成 ensemble；**子分必须留**（`metadata["judge_scores"]`）——只报均值是不可诊断的分数；**所有评委须同输入签名**；温度/模型/追踪等配置留在各评委自身，集成层只做聚合 |
| engineering/wb-release-maintain | 1.2.0 | 版本化不只管产物：**输入集（评测集）也要版本化**，否则不许续跑/复现；**抽样决策也是输入的一部分**（自定义 sampler 需落盘 checkpoint，且同机）；版本命名空间须显式（Opik 2.0 起 datasets/experiments 为 project-scoped） |

## 判重不落（逐条）
- Test Suites 的 suite 级 / item 级断言叠加（av 1.75.0「评测判据两层、叠加不替换」已覆盖 >60%）；
- `runs_per_item` / `pass_threshold` 三层通过逻辑（sd L1085「概率系统一次运行不能判定改进 + repetitions」已覆盖 >60%）；
- G-Eval 先起草步骤再打分（av §评委先推理再打分后丢弃推理 同源）；
- 启发式指标清单 / 对话级启发式（产品指标面，且"长会话用例"已在 av 边界例里）；
- Agent trajectory 需先 tracing（av §过程可观测 已覆盖）；
- Prompt library / 优化器算法 / Ollie 助手（产品功能面）。

## 候选池（下轮可挖）
- Opik `evaluation/metrics/*` 逐指标（moderation / compliance_risk / prompt_diagnostics 未读）；
- **Prefect 官方文档（330 md，工作流编排与重试语义）** —— 与 Temporal/Restate 可三方对照；
- Helicone(214)、chainlit(122)、braintrust(65)。
