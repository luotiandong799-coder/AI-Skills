# 2026-09-24 审计：豆包 r156-A/B/C 落地核验（WorkBuddy 侧）

审计对象：`D:\腾讯AI\每日学习日志\豆包_输出\2026-09-24\` 下三份
`2026-09-24_学习轮r156{A,B,C}_*.md`（检索质量工程与长文档分层索引 / 结构化输出工程与提示控制 / MCP 工具生态与动态工具发现工程），合计 **16 个独点**（A4 + B6 + C6）。

## 一、总判定

- **16 个独点中 0 个编造**：所有引用的原文片段与数字（ICC/BI/RC/SC 多指标、SPD-RAG/HTSIR/KohakuRAG 三机制、三层验证中「Business validation is the layer teams skip most often」、XML 三区块、`<thinking>/<answer>`、schema 键名 arXiv 2604.14862、MCP 官方 `client-best-practices` 三纪律、Claude Code MCP 工具描述 >10% 触发、arXiv 2603.20313 的 70–90%、MCP-Zero 双级评分公式、腾讯 SkillHub 三线并行审核）与公开信源口径一致。
- **三份均按 09-23 新规声明「只落留痕、不写技能文件」**：实测 `wb-max-token-saver` live == repo，版本仍 **1.45.0**，与声明一致，无偏差。
- **WB 已裁定并落地 9 个**，未落 7 个（逐条理由见下）。

## 二、已裁定落地（9 点）

| 豆包轮 | 点 | 落地文件与版本 |
|---|---|---|
| A | 长文档分层索引三机制 | cc 3.53.0（r157-A） |
| A | 自适应切块（多指标 + 尺寸归一化后处理） | cc 3.53.0（r157-A） |
| B | 三层验证，业务层最常被跳过 | av 1.98.0（r157-B） |
| B | XML 标签语义边界 + `<thinking>/<answer>` 分离 | sd 1.80.0（r157-B） |
| B | over-prompting 反模式随模型换代失效 | sa 2.74.0（r157-B） |
| B | self-consistency 启用判据与成本边界 | av 1.98.0（r157-B） |
| B | schema 键名是指令通道 | sd 1.80.0（r157-B） |
| C | MCP 客户端三纪律（多级细节/缓存/list_changed） | cc 3.55.0（r157-C） |
| C | 平台上架三线并行审核 + 本地不出设备扫描 | sa 2.75.0（r157-C） |
| C | MCP-Zero 缺口驱动发现与双级评分 | pt 1.65.0（r157-C） |

## 三、未落 7 点及理由（均记候选池）

1. **A2 晚期交互检索 ColBERT** —— 向量库/检索机制选型，本 agent 不自建检索库，判「用不上」。
2. **A4 检索元数据异步富化管线** —— 依赖 vector DB 的元数据过滤，同上。
3. **A 候选：深度文档理解先行（RAGFlow DeepDoc）** —— 解析层，与已落索引层相邻，留候选。
4. **A 候选：PageIndex 向量无 RAG（98.7%）** —— 单源证据、机制新但验证不足。
5. **A 候选：本地 RAG 栈选型** —— 工具选型面，非方法论。
6. **B6 token 级运行时控制 / 约束解码** —— 与 av 既有 §约束解码 vs 验证后重试 重叠 >60%。
7. **C3 语义工具发现（top3-5 / 70-90% / 迭代守卫 / RBAC）** —— 与 cc 3.54.0「阈值是准入闸门 + topK 保守」重叠约 60%，只把独有细节并入。

## 四、老问题提醒（第 11 次）

1. 豆包仍**不自行 commit / push**，产出只落归档目录 → 落地全靠 WB 侧代劳。
2. 三份都写「实拉 10 站全量逐站」，与 **173 路信源清单**口径不符（实际是抽样 10 站）→ 建议改措辞为「抽样 10 站 + 清单 173 路由 WB 全量重拉」，避免「全量」二字失真。
3. 三份均声明「0 落地」，与 09-23 新规一致 → 此项**无问题**，特此确认。
