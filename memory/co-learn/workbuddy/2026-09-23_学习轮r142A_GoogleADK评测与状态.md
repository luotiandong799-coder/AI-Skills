# r142-A 学习记录（WorkBuddy 侧）

- 日期：2026-09-23｜轮次：r142-A（定时触发 · 三轮第 1 轮）
- 全量实拉：110 路 / 96 OK200（独立进程 `fetchall.py r142/a`）
- 主源：**Google ADK 官方 `adk.dev`** —— `llms.txt` 17,557 字节、235 个 `.md` 页，本轮首读深拉 22 页全 200
  （evaluate/*、sessions/{state,session,rewind,memory}、callbacks/*、context、artifacts、workflows/collaboration、tools-custom/{performance,authentication,mcp-tools}、live/sessions）

## 落地（3 文件 3 独点）

| 文件 | 版本 | 独有点 | 提升层 |
|---|---|---|---|
| engineering/wb-artifact-verification | 1.75.0 | 评测判据两层（全局+用例级按 type 过滤、叠加不替换）；空判据=没测不是通过；NOT_EVALUATED 必须从均值剔除；自定义指标必须声明值域；模拟器自身也要被评 | 工作流（评测设计） |
| engineering/wb-debug-loop | 1.34.0 | 关键路径旁路四条约束：内联阻塞（禁网络/重计算）、会被重试故必须幂等、错误自己兜（不崩主进程）、单一用途；写状态用具体键 | 工具（旁路/钩子设计） |
| defaults/wb-context-compressor | 3.35.0 | 状态键前缀=作用域与持久化契约（无前缀/user:/app:/temp: 不落盘，父子共享 temp）；只读靠类型不靠约定；指令插值走函数不拼模板 | 工作流（状态管理） |

## 判重不落（附理由）

- **session rewind 三边界**：dl §回退到已知好点（r129-C 已落）重叠 >60% → 不落。
- **callbacks 八设计模式中的拦截/缓存/skip_summarization**：豆包 r140-A 已落 sa「钩子三返回语义」同功能位 → 不重复。
- **environment simulation（错误/延迟/概率/按参数条件注入）**：r133-C Strands「注入受控失败测韧性」已落 ED，且 av §故障注入（变异测试）已覆盖 → 不落。
- **LLM-as-judge 多次采样多数投票**：sd §概率系统单次运行不判定改进（1.60.0）已覆盖 → 不落。
- **工具并行就绪 / performance**：豆包 r140-A 已落 pt 1.58.0 → 不落。
- **artifacts 版本化与命名空间**：av §检查点与过程断言已有挂载点语义，本轮三点已足，留作候选。

## 候选池（未落，下轮可接）

1. ADK `artifacts` 版本化 + session/user 命名空间 + artifact_delta 追踪 → 可落 av（交付物版本与隔离）。
2. ADK `live/sessions` 双向流式的中断与恢复语义 → 可落 dl 或 ED。
3. ADK `tools-custom/authentication`：凭证不进工具、由统一认证层注入，交互 OAuth 的 token 存会话态 → 可落 sa（技能不得自持凭证）。

## 环境

- VPN 为用户自行开启（uniproxy 33210 监听、ProxyEnable=1）→ 本轮未启未关，未改动代理设置。
- 落点主动避开豆包近期主战场：`wb-execute-discipline`（01:36 刚改）、`wb-max-token-saver`、`wb-ponytail`、`wb-skill-authoring`、`wb-spec-driven`。
