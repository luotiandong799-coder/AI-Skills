# r144-B · Anthropic 官方 docs.claude.com（首读深拉 24 页）

日期：2026-09-23 | 独立进程重拉 110 路 / 97 OK200 / 13 未达（未达集合与 A 一致，均为需代理/已下线站） | 主源 `docs.claude.com/llms.txt`（68,721 字节 / 634 页，`/en/docs/<path>.md` 直出清洁 Markdown；`platform.claude.com` 仍不可达）

## 落地
| 文件 | 版本 | 独有点 | 提升层 |
|---|---|---|---|
| engineering/wb-artifact-verification | 1.76.0→1.77.0 | 评测设计三原则 + **题量优先于单题质量**（原话 Prioritize volume over quality）+ 边界例四类必列、"连人类都难达成一致的模糊用例"必须单列不得并入硬判据 + 判分能否自动化在题目设计阶段决定 + 评委先推理再打分然后把推理丢掉 | 工作流 + 工具 |
| defaults/wb-context-compressor | 3.36.0→3.37.0 | 自动触发须先证明"它读的数=它想管的量"（缓存读/内部调用让 usage 虚高→压缩提前触发，用独立计数端点校准）+ 压缩不得切在在途动作（会先抽走 tool use 块使动作重来一次）+ 清理是策展不是省钱 | 工具 + 工作流 |
| writing/wb-doc-writing | 1.10.0→1.11.0 | 禁"某日期之后改用新办法"这类绝对日期条件句 → 当前方法正文 + 折叠的 Old patterns；模板严格度按消费方分档（机器解析→ALWAYS 写死，人读→只给骨架）；术语一词一概念 | 可复用 Skill + 输出 |

## 判重不落
- 自由度三档（sa §控制度按脆弱性逐段校准 已落，同源）
- description 第三人称 / 动名词命名 / 术语一致（sa §787/§791 已落）
- 委派三模式（并行/专业/升级）（sd §1.58/1.59 已覆盖 >60%）
- tool search / programmatic calling / prompt caching / context editing 四手段（ctx 3.35/3.36 已覆盖工具定义与结果占面）

## 候选池
- `context-editing`(104KB)、`develop-tests`(137KB)、`structured-outputs`(110KB)、`prompt-caching`(158KB) 只精读了关键段
- `build-a-tool-using-agent`(163KB) 未读
