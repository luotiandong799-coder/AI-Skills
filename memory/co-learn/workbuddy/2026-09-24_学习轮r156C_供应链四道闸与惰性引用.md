# 2026-09-24 学习轮 r156-C（WB 独立重拉 · 3 文件 3 独点）

## 实拉证据
- 全量重拉：173 路 / **167 OK200** / 6 未达（三轮一致）。
- 主源：**Modal 官方 `modal.com/docs/llms.txt`（307 md 链接，过滤后 62/62 全 200）** —— 清单内信源，此前只读过 `guide/concurrent-inputs`，本轮换 `sandbox-snapshots / secrets / volumes / webhook-timeouts`。

## 落地（轮 / 文件 / 版本 / 独有点）
| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| C | engineering/wb-skill-authoring | 2.73.0 | 技能供应链四道闸：装前四步审查（含「读文件+联网」合成风险与重定向域名核验）／静态审计查载体（注释·Markdown·不可见 Unicode·example 代码块，量化基线 13.4% critical、36.82% 至少一个安全问题、恶意样本 91% 走注入）／★装后只读挂载防篡改（静态扫描替代不了）／★clean 徽章不是安全结论（80% 行为偏差、混淆绕过 8 个扫描器 >80%） |
| C | defaults/wb-context-compressor | 3.52.0 | ★惰性引用（Image.from_id 构造不报错，错误推迟到使用点）＋★快照覆盖面边界（根文件系统快照不含挂载卷）＋同起点 fork 多副本＋多来源同名键按列表顺序静默覆盖＋凭据不是大对象存储（32,768 字符上限，超限走卷） |
| C | engineering/wb-debug-loop | 1.51.0 | ★端到端超时由最严一层决定（HTTP 150s / 函数超时 / 客户端 20 跳 vs urllib 4 跳≈12.5min）＋★长任务改「spawn 返回 call_id + 轮询 get(timeout=0) 回 202」而非调大超时＋隐式上限还包括重定向次数与 CORS |

## 判重不落（C）
- 快照保留期默认值变更（v1.5 起 FS 快照无期→30 天，可 `ttl=None` 退出）：与 rel §同参数名可给相反默认值 同面，记候选池。
- Endpoint 默认需认证 vs Web Function 默认公开：与 sa §默认开还是默认关是隔离真伪的分水岭 重叠>60%，不重复落。
- Modal Secret 的 CLI/dotenv 用法：平台操作面，不落。
