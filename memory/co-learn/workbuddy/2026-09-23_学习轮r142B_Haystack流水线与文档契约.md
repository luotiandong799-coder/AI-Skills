# r142-B 学习记录（WorkBuddy 侧）

- 日期：2026-09-23｜轮次：r142-B（定时触发 · 三轮第 2 轮）
- 独立重拉：新进程 `fetchall.py r142/b` → **110 路 / 97 OK200 / 13 未达**（未达集合与 A 轮的 14 个互异：B 轮多打回 `make`，A 轮的 `mattpocock_skills` 等在 B 轮可达）→ 证明确为独立进程、非复用上一轮结论。
- 主源：**Haystack 官方 `docs.haystack.deepset.ai/llms.txt`** —— **2,651,505 字节、458 个文件块的整站机器可读全量**（此前只做探活，未读）。重点首读：`concepts/pipelines/{breakpoints,loops,creating-pipelines,smart-pipeline-connections,serialization}`、`concepts/components/{custom-components,supercomponents}`、`concepts/{agents,secret-management}`、`development/{logging,tracing}`、`optimization/evaluation/*`、仓库 `AGENTS.md` 与 `_templates/component-template`。

## 落地（3 文件 3 独点）

| 文件 | 版本 | 独有点 | 提升层 |
|---|---|---|---|
| engineering/wb-debug-loop | 1.35.0 | 断点坐标＝"环节 + 第 N 次访问"；停下来要拿到完整现场（输入+中间输出+访问计数）；失败自动留最后一个可续跑状态；快照落盘默认关、走回调不写盘 | 工作流（可恢复性） |
| engineering/wb-spec-driven | 1.65.0 | 步骤不得就地改输入（共享输入污染下游且污染点远离报错点）；改法按改动面分两档（少字段浅替换 / 嵌套或大改才深拷贝）；是否改入参属于契约要写进说明 | 工作流（步骤契约） |
| writing/wb-doc-writing | 1.9.0 | 能力/组件文档固定契约：Overview 答三问（做什么 / 与相近的怎么选 / 坑在哪）；必填项分 init 与 run 两栏；摘录配置必须链权威参考；文档只在"过时/有错/实质增益"三种情况下改 | 可复用 Skill（文档契约） |

## 判重不落（附理由）

- **content tracing 默认关闭、按需 `include_outputs_from` 打开中间输出**：dl §诊断通道分离 + §观测 opt-in（r138-A/B 已落）重叠 >60% → 不落。
- **SuperComponent 收敛接口 + 同名输出显式映射消歧**：sa 2.50.0「一个子代理一个独立入口」+ 2.57.0 聚合前缀消歧已覆盖 → 不落。
- **custom component 的输入输出显式声明**：sa 2.51.0「对外能力契约显式声明不靠推断」已落 → 不落。
- **multi-agent-systems / agents 概念页**：平台 API 面为主，无独立于 sd §委派已落内容的方法论 → 不落。
- **secret-management / 凭证存会话态**：ctx §凭据只验三件事 + av §凭证出沙箱 已覆盖 → 不落。

## 候选池（未落，下轮可接）

1. Haystack `pipeline-loops`（循环与终止条件 / 最大迭代）→ 可落 sd 或 ED。
2. Haystack `smart-pipeline-connections`（按类型自动连接、类型不匹配才报错）→ 可落 sa（接口自动推导的边界）。
3. Haystack `optimization/evaluation/{model-based,statistical}`（统计评测 vs 模型评测的分工）→ 可落 av。
4. Haystack `serialization`（流水线 YAML 序列化与跨环境复现）→ 可落 sd。

## 环境

- VPN 仍为用户自行开启（uniproxy 33210 监听、ProxyEnable=1）→ 未启未关，未改动代理设置。
- 落点避开豆包近期主战场 `wb-execute-discipline` / `wb-max-token-saver` / `wb-ponytail`；落地前已核验 sd、sa、doc-writing 三处 repo 与 live sha256 全 SAME，无并发未推内容。
