# 2026-09-23 学习轮 r150-C（CAMEL-AI：任务一等对象 / 互操作兜底默认开 / 两类超时默认值相反）

轮次：r150-C（三轮第 3 轮 · 独立重拉 + 另探 30 站）
落点继续避开豆包主战场（mts / ED）；本轮碰 sd / sa / dl。**并发线 e600f8f（MCP/LangChain）已在 71c25b7 之上，本轮基于其快进，无冲突。**

## 一、实拉证据

- **全量逐站**：`fetchall.py c` → **157 路 / 130 OK200 / 29 未达**（新独立进程；未达集合与 A、B 不同，C 轮多出 14 站瞬时失败，佐证三次独立进程）。
- **★主源（清单外新信源首读）**：**CAMEL-AI 官方 `docs.camel-ai.org/llms.txt`（480 页，绝对 URL）** → 全量 **480/480 全 200**。
- 关键原文：
  - `key_modules/tasks`：Task 属性 `content / id / state / type / parent / subtasks / result`；`state` 枚举 `"OPEN", "RUNNING", "DONE", "FAILED", "DELETED"`；`set_state` — "Recursively set the state of the task and its subtasks."；方法含 `decompose` / `compose` / `get_depth`（root depth is 1）/ `get_running_task`。
  - `key_modules/workforce`：`graceful_shutdown_timeout: float = 15.0`；`task_timeout_seconds: Optional[float] = None`；`share_memory: bool = False`；`use_structured_output_handler: bool = True` — "Enables structured output handling so models without native JSON + tool-calling can still interop reliably."
- **另探 30 站（`probe_c.py`）**：可用且 md 链接 >100 的新站 **openrouter(459) / convex(399) / fireworks(388) / together(366) / trigger.dev(327) / phidata(307) / modal(306) / pinecone(146)**；**剔除 4 个"200 但 0 条 md 链接"**（replicate / llamaindex(247KB) / julep / weaviate(2 条)）；失败 8（windmill / superagent / supabase / motia / milvus / langfuse / kestra / kaiban / groq / crawl4ai / aisdk 404·400）。**清单 157 → 165 路**。

## 二、落地（3 文件 3 独点）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| C | engineering/wb-spec-driven | 1.73.0 | 任务是一等对象：状态是**闭合五态枚举**（没做完要分"没开始/在做/失败"）+ 父改状态**递归到子树**（子全 DONE 而父 RUNNING 是不自洽）+ decompose/compose **成对提供**（只拆不合则信息回不来）+ 能自查深度与当前运行节点 |
| C | engineering/wb-skill-authoring | 2.66.0 | 互操作能力要有**默认开启的兜底层**（让不具备原生 JSON/tool-calling 的模型也能参与）+ 判据"能力 A 才能参与 → 没有 A 的参与者怎么办" + 兜底层只做协议翻译不做语义猜测 |
| C | engineering/wb-debug-loop | 1.44.0 | "收尾的超时"与"干活的超时"是两个旋钮且**默认值取向相反**：收尾宽限默认给具体值（否则优雅退不出只能强杀），干活超时默认不限（误杀长任务代价更高）+ 两者要能分开设 |

层级判定：sd＝工作流（任务契约）／sa＝工具（互操作）／dl＝工具（超时设计）。

## 三、判重不落（C）

- `share_memory` 默认关 → 与 cc §底座默认关（r149-A）同源，不落。
- Workforce callbacks 观察生命周期 → sa §组件生命周期暴露为可订阅事件（r147-B）重叠 >60%，不落。
- RolePlaying worker（双 agent 辩论）→ sa §评委团（r146-C）同面，不落。
- AgentPool 复用实例 → pt §引依赖 / ED 资源面，增量不足。
- 480 页里 450 页为 API reference → 产品事实。

## 四、三件套评估（C）

三件套（pt / mts / cc）本轮无新料 → **维持 3 件套**。r150 三轮三件套覆盖：A→cc、B→无、C→无；**三轮均未对三件套做增减**。

## 五、版本化

commit+push **`0619b7a`**（非强推，ANCESTOR_OK；基于并发线 e600f8f 快进）；live 回灌 3 文件 sha256 全 SAME（sd `f4d6edaf4a9260ae` / sa `3d5c8c4c42259a00` / dl `ad9784135044d445`）。豆包侧 mts 1.42.0 机械同步入版本控制 → **`e1208d5`**。
