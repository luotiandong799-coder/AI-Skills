# 学习轮 r121（2026-09-20 17:5x · 单轮：遵循 r109「不用B轮」+ 工作记忆「以最近用户指令为准」）

## 本轮落点
- 仓库已是最新（HEAD==FETCH_HEAD，落后 0，无需 rebase）。豆包线最新批为 r111（全 0 独点），无 r112+ 新内容 → 落点全部避开 `wb-execute-discipline`，零冲突。
- 信源面：topaiskills.com `/tutorials` 列表最新文仍为 09-18（r117 已深读），无新发布；The Batch 最新 issue-371（r120 已落地 agent 安全）。**补读 r120 降到探活级、未深读的 7 篇 topaiskills Azure 实操文**（prisma-skills-faq / azure-aigateway-procedural / azure-deploy-procedural / azure-storage-procedural / azure-quotas-procedural / azure-resource-lookup-vs-visualizer / azure-planner-skills-comparison），避免漏方法论。

## 实落（2 文件）
- **engineering/wb-artifact-verification 1.58.0→1.59.0**（来源 topaiskills azure-deploy-procedural + azure-planner-skills-comparison + azure-quotas-procedural，2026-09-20 实读）：
  1. 验证闸以"证据"为准不以"状态位/记忆"为准：状态位（Validated）须配含实际命令+时间戳的 Proof 段，空 Proof＝绕过；写校验结论权威与执行动作权威须分离（azure-deploy 禁止手动改状态，只有 azure-validate 能写）；闸过了要贴实际输出，绝不凭记忆声称过了（azure-planner Phase 6「proving the gate, not asserting it」）。
  2. 异步/最终一致动作后必须回查确认真的成了（azure-deploy 部署后回查 RBAC 角色真存在）；RBAC 传播延迟要在授予与使用间插"等待+复核"闸（跳过→900s 超时）；一步式 `azd up` 静默绕过该闸。
  3. "无限制/None/No Limit" 不是放行许可，是"检查器不覆盖此项"的缺失信号（azure-quotas 门户 "No Limit" 实为配额 API 不覆盖）。
  4. 堆叠管控步骤顺序即逻辑（auth→cache→limit→safety→backend→metrics，调序静默改行为）。
- **defaults/wb-ponytail 1.44.0→1.45.0**（来源 topaiskills azure-aigateway-procedural，2026-09-20 实读）：互斥/不兼容能力必须显式取舍（语义缓存与 token 计量均不兼容流式，须主动二选一）；堆叠步骤顺序即逻辑；选型按任务真正需要的产出物不按安装量/流行度（resource-lookup vs visualizer「the artifact decides」，流行度不是适用度）。

## 判不落（下轮别再取）
- Prisma：pack-counter / lockstep（sa 2.28/2.31 已落）、负向边界 upfront（sa §负向边界）、silent runtime fallback bunx --bun（av 1.56.0 §默认值只对入口成立）、v7 枚举 removed items（sa 2.30 §正负清单）、priority-tiered reference（sa §按能力分行）— 均重叠 >60%。
- aigateway：ManagedIdentity vs DefaultAzureCredential by env（av 1.58.0）、on-error 干净 400（av 1.58.0 部分）、NEVER use MCP 不可靠用 CLI（sa §显式禁止清单 r116）→ 重叠。
- storage/quotas/planner/lookup 的其余点（负向边界、auth by env、NEVER use MCP、20% buffer、compatible matrix 诚实上限、Match job not install count、secret 不进图）→ 分别与 sa/av 已落内容重叠 >60% 或纯领域事实。

## 三件套评估（r121）
- wb-artifact-verification 升 1.59.0（验证闸证据化/权责分离/事后回查/缺失信号，落在本技能功能位）。
- wb-ponytail 升 1.45.0（互斥能力显式取舍，落在本技能取舍功能位）。
- wb-max-token-saver / wb-context-compressor 本轮无合格新材料，不硬凑 → 维持 3 件套。

## 审计
- repo 36 SKILL.md PyYAML 全解析 OK（0 坏 frontmatter）；CRLF 0 / STRAY_CR 0 / FFFD 0 / TRAILING_WS 0。
- 同步 live 后全量 sha256：36 比对 0 差异，仅 live-only browser-skill（已知）。

## 信源增删
- 0 增 0 删（75 路稳定）。topaiskills `/tutorials` 连续多轮无新文章；The Batch issue-371 已落地。

## 收尾
- 未启 VPN（ProxyEnable=1 / ProxyServer http://127.0.0.1:33210 / 用户开启态 → 不关、不还原）；baidu 200 / api.github 200；ipconfig /flushdns rc=0；tmp 清理（audit_r121.py 留档，cl_*.py 为周度缓存清理脚本与本任务无关）；C 盘无新增。

## 下轮提示
- ①井已近干，topaiskills 7 篇 Azure 实操文已全深读，新增独点阈值继续抬高；②落地改 repo → push → cp live → sha256；③`git add` 只加目标文件勿 `-A`；④远端被前推用 rebase 不用强推；⑤豆包线若再往 live ed 写 CRLF，cp 后必跑 sha256；⑥ed 356KB+，拆分待办保持观察（未到 400KB 阈值、无互斥功能位）。
