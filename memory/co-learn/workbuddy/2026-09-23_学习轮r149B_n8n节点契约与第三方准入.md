# 2026-09-23 学习轮 r149-B（WorkBuddy 线 · 三轮第 2 轮）

## 一、实拉证据
- 独立新进程 `fetchall.py b`：**147 路 / 121 OK200 / 27 未达**。**未达集合与 A 轮不同**（A 20 个 / B 27 个；B 多出 mattpocock_skills、agent_rigor、openviking、firecrawl_mcp、skillbuddy、awesome_agent_skills_engryan 六个瞬时失败）→ 两次独立进程取证，非复用 A 结论。
- **主源（清单外新信源深读）**：n8n 官方 `docs.n8n.io/connect/create-nodes/*`（llms.txt 1,356 页，绝对 URL）→ 深拉 **36/36 全 200**（节点构建风格选型、代码规范、版本化、item linking、错误处理、node linter、verification guidelines、测试与部署全套）。

## 二、落地（强制四列表格）

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| B | engineering/wb-release-maintain | 1.4.0 | 兼容性要分两个面判（存量引用钉死在旧版本→"不破坏存量"免费成立，真风险面只剩新建/新用户拿到的默认行为）；版本化能力上限由早期实现风格决定（声明式不支持完整版本化）；一份版本区间特性声明同时驱动展示层与执行层，逐版本列举必然漂移 |
| B | engineering/wb-skill-authoring | 2.65.0 | 第三方产物进门三判据：可溯源（CI 出证 + provenance 强于人为声明，npm 与仓库作者须一致）/ 无旁路（禁读环境变量与文件系统、输入全走显式参数 → 审计面 = 参数表）/ 单一职责（一包一服务，代理层不受理） |
| B | defaults/wb-ponytail | 1.61.0 | 能描述就别编程：默认走声明式，四条例外穷举（触发器/非 REST/需数据变换/需按特性在代码分支）；"需要代码分支"本身就是升级理由；标识与文案分离——跨操作复用内部标识保住用户已填数据，前提是同一时刻只有一个字段可见 |

## 三、判非重复理由（逐条）
- "旧引用钉旧版" → 与 `rel §破坏性变更放最前`（管怎么告知用户）方向不同（本条管先判定有没有破坏存量），重叠 <30%，已写分工行。
- "无旁路=审计面等于参数表" → 与 `sa §信任是第三方授予的、不是自我声明的`（管"已审计"信号由谁签发）重叠 <40%，本条给的是三条可机械核对的进门判据。
- "能描述就别编程" → 与 `pt §不要用尚未定义契约的声明式文件假装安全`（管"契约不存在时别造没人校验的声明文件"）**方向相反但对象不同**，已在正文写明确分工行，避免读者混淆。
- **判重不落（B）**：① n8n "不要就地改传入数据" → 与 `sd §步骤不要就地改它的输入`（Haystack 源，r142-B 已落）重叠 >70%，**判重复不落**；② "少引入第三方 SDK、用宿主内建通道" → 与 `pt §引依赖：先榨干已有的，再 vet、pin` 重叠 >60%，**判重复不落**；③ item linking / pairedItem → 与 `av §占位保配对（r146-A）` 同源，**不落**；④ 五类 artifact 类型与 UI 渲染 → 产品/UI 面，按筛选规则不计。

## 四、三件套评估
- 本轮命中 `wb-ponytail`（升 1.61.0）；`wb-context-compressor` / `wb-max-token-saver` 无新料 → **维持 3 件套**。

## 五、版本化与同步
- commit **`55e706a`**，非强推，祖先校验 **ANCESTOR_OK**。
- 3 文件 cp 回 live 后 sha256 全 **SAME**（rel `9257ba46c5bcbecd` / sa `f6ef19748bd9b87c` / pt `f571458f270c2e86`）。
