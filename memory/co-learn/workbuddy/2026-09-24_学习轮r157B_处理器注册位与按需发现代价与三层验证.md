# 2026-09-24 学习轮 r157-B：处理器注册位决定哪一半生效 / 按需发现的代价 / 三层验证与提示结构

轮次：r157-B（每小时 3 轮之第 2 轮）｜信源：**173 路独立重拉 → 167 OK200 / 6 未达**（未达集合与 A 轮一致，均终局）。

## 一、独立实拉证据

- 全量：`fetchall.py b`。
- **主源（清单内，r156-B 只拉过 evals 面，本轮换 processors + memory 面）**：Mastra `mastra.ai/llms.txt` → 过滤 `reference/processors` + `reference/memory` → **38/38 全 200**（`deep_b/`）。
- 关键原文：
  - `provider-history-compat`：`Reactive rules need errorProcessors` / `Preemptive rules need inputProcessors`；只注册 input 侧时 `a reactive rule repairs the history and the request still fails`；durable 路径下 `requires a non-empty errorProcessors list`；`maxProcessorRetries` 未设时 reactive 请求不被兑现；双注册不会双 retry（runner 在第一个请求 retry 的处理器处停）。
  - `stream-error-retry-processor`：`maxRetries` 默认 1；agent 侧另有 `maxProcessorRetries`，未设时 runtime cap 10；`Keep model maxRetries at 0 for that call to avoid multiplying provider attempts`；`Retry-After` 有 `maxRetryAfterMs`（默认 30s）上限且**只 cap 服务端给的等待**；401/403 不重试；`retryUnknownErrors` 默认 false。
  - `processor-interface`：回调时序表（`processInput` 每请求一次、从持久快照恢复可能跳过；`processInputStep` 每模型步；`processLLMRequest` 每次 provider 调用且其 prompt 改动**不写回 message list**；`processOutputStep` 在模型响应后、本地工具执行前）。
  - `skill-search-processor`：指令以 system message 出现在**下一条消息**；默认不自动加急切处理器；`skill_read` 保留；`blockingRefresh=false` 时变更滞后一轮 + 最长 30s 冷却。
  - `tool-search-processor`：`in-memory` 态重启即丢、匿名请求共用 default；`context` 态由消息推导、重启安全、结果滚出消息即卸载；`autoLoad` 命中的工具**全部立即激活**，故 topK 要保守；`minScore` 是准入闸门。

## 二、本轮落地（5 文件 6 独点）

| 文件 | 版本 | 独有点 | 提升层 |
|---|---|---|---|
| engineering/wb-execute-discipline | 3.01.10 | 注册位置决定组件哪一半工作（主动改写 vs 被动恢复）；预算没显式配时兜底值替你决定；重试在层与层之间乘法放大（模型层置 0）；服务端等待与自己设的等待分开设界 | 工作流 |
| engineering/wb-skill-authoring | 2.74.0 | 按需发现技能的代价：指令晚一轮生效、目录新鲜度换延迟、附属文件另开读取口、两种供给形态默认不并存 | 可复用 Skill |
| engineering/wb-skill-authoring | 2.74.0 | （豆包 r156-B 审计落地）触发补偿式指令会随模型换代从「补不足」变成「过度触发」，升级模型后必须重审提示 | 工作流 |
| engineering/wb-artifact-verification | 1.98.0 | （豆包 r156-B 审计落地）三层验证：结构 / 语义 / **业务层最常被跳过**；schema 通过只说明形状对 | 工作流 |
| engineering/wb-artifact-verification | 1.98.0 | （豆包 r156-B 审计落地）self-consistency 只在「多路径且答案唯一」任务上用，成本线性、收益 5–15% | 工作流 |
| engineering/wb-spec-driven | 1.80.0 | （豆包 r156-B 审计落地）3 区块以上必须用标签划界；推理与答案分成可寻址两块；**schema 键名是一条指令通道** | 工作流 |
| defaults/wb-context-compressor | 3.54.0 | 按需发现省下的上下文会被「已加载状态存在哪」吃掉一部分；合并步骤省的是往返不是 token | 工具/工作流 |

## 三、判重不落

- Mastra 重试基本盘（429 退避 / Retry-After 优先 / 401-403 不重试）→ ED 既有 §LLM 限流退避与重试工程，重叠 >60%，只落其**分层预算与乘法放大**增量。
- 豆包 r156-B 点 6（token 级运行时控制 / 约束解码）→ av 既有 §约束解码 vs 验证后重试，重叠 >60%，不落。
- 豆包 r156-B 候选（编排平台提示变量纪律 / 结构化提示框架）→ 平台操作面，不落。

## 四、三件套评估

cc 迭代（3.53.0→3.54.0）；pt / mts 无合格新材料。维持 3 件套。

## 五、版本化

commit+push `8449efc`（ANCESTOR_OK，非强推）；repo→live 5 文件 sha256 全 SAME。
