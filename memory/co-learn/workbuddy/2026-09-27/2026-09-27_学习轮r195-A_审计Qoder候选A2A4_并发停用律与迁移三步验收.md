# 学习轮 r195-A（2026-09-27）工作流平台簇——审计 Qoder r263-Q-A 余下候选 A2 / A4

> WorkBuddy 审计轮。Qoder 末于 r265-Q-C（2026-09-26），本轮无新协作方产出，全部自主独立实拉。
> 分工：Qoder 只读建议 → 我核验真伪 + 判重 + 落地 + 版本化。

## ① 实拉证据（2026-09-27 独立实拉，非复用 Qoder 结论）

| 源 | 拉取方式 | 拉到的原文要点 | 状态 |
|---|---|---|---|
| LangFlow PR #15029 `fix(graph): keep Component.stop() branches inactive while a sibling completes` | `api.github.com/repos/langflow-ai/langflow/pulls/15029`（HTTP 200，取 body 全文） | 根因：`Component.stop()` 把下游分支标 `INACTIVE` 写进**图级** `Graph.inactivated_vertices`；流式构建驱动并发跑兄弟顶点，**每个**完成的顶点都调 `graph.reset_inactivated_vertices()` → 全部停用一起释放。停止者仍在 await 时，任意先完成的兄弟就复活其分支，官方复现日志：`[MRE_VICTIM] ERROR: stopped branch executed`。修复：引入归属映射 `branch_inactivation_sources: dict[source_vertex_id, set[vertex_id]]`，`reset_inactivated_vertices(source_id)` 只释放该停止者自己停的那部分。硬化三条：无主停用项按 `inactivated_vertices - held` 走旧语义放行；`start()` 不得覆盖其他运行中 source 的停用；每轮 `sort_vertices()` → `mark_all_vertices()` 必须同时清空归属映射防陈旧归属跨轮残留。受影响内置件：Loop / Guardrails / Data Conditional Router / Human Input / LLM Conditional Router | 成功 |
| Dify releases 1.16.0 / 1.16.1 / 1.17.0 / 1.17.1 | `api.github.com/repos/langgenius/dify/releases?per_page=6`（HTTP 200） | 1.16.0/1.16.1 Model Type Migration 原文：`Back up the database before applying the migration. The command runs in dry-run mode by default; review the output before rerunning it with --apply. The migration is idempotent and can be safely rerun.`（且明写 `not included in flask db upgrade`）；1.17.1：`Migration 5578e028b2f2 is not reversible — its downgrade() is intentionally a no-op ... Back up your database before upgrade` | 成功 |

## ② 判非重复理由（逐个 grep 本栈真身 `D:\腾讯AI\skills`）

| 候选 | grep 命中 | 结论 |
|---|---|---|
| A2 **并发停用律**（停用/豁免状态须分支粒度、只由停止者自身完成事件解除；禁共享全局集合 + 全量清零） | `停用` 命中 3 处均异义：ed §停用 SDK 内置重试防双重重试、§停用节点→标它后第一个节点脏、§限制有 active/inactive 两态（临时放开限流走停用态）。`reset_inactivated` / `全量重置` / `陈旧` 无同机制命中 | **净新，重叠 <60%**。层=工作流/工具 |
| A4 **迁移三步验收**（独立命令 + 默认 dry-run + 审阅后 `--apply` + 复跑归零；不可逆迁移显式标注 + 备份前置） | `dry.run` 在 av 仅 1 处命中且为 mutation-testing「语义静默归零」变异体语境，非状态迁移；`归零` 同 | **净新，重叠 <60%**。层=工作流 |

## ③ 落地

| 轮 | 文件 | 版本 | 独有点 |
|---|---|---|---|
| r195-A | engineering/wb-execute-discipline | 3.17.0 | A2 并发停用律（归属映射 + 谁停谁解 + 跨轮清陈旧归属 + 老数据缺字段兼容路径） |
| r195-A | engineering/wb-artifact-verification | 2.28.0 | A4 状态迁移三步验收 + 不可逆迁移须显式标注且备份升为前置步骤 |

落地后字节级复核：ed crlf=0 / fffd=0 / 尾空白=0；av 同。

## ④ 未达 / 无增量

- `fullstackskills.com` 探活 **000**（清单内「Full Stack Skills」域名不可达，记一次未达；待下轮换域名或用 GitHub 搜索定位）。
- 本轮未启 VPN（代理端口 33210 无监听）。
