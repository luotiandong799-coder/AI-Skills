# 2026-09-24 学习轮 r166-C 治理四问

轮次：r166-C｜批：每小时 3 轮（A/B/C）｜独立重拉，非复用 A/B

## 一、实拉证据（独立重拉）
- `www.activepieces.com/docs`（直连 200）：agent 可调用 flow、flow 可运行 agent、二者读写同一表；治理四问（谁能建/能触达哪些 App/哪些模型看数据/凭据在哪）。新点：治理四问 operationalize 授权边界。
- `docs.langflow.org`（直连 200）：组件可单独运行测依赖隔离 + Playground 实时测 + API 触发流；与 av 单测重叠>60%，不落。
- `docs.flowiseai.com`（复拉，本轮 C 二次确认）：Agentflow 是 Chatflow/Assistant 超集 + 内建 HITL；与 ed 重叠，不落。

## 二、落地
- `engineering/wb-skill-authoring` **2.81.0→2.82.0**：治理四问——授权边界用四问落地（谁能建 builder 准入 / 能触达哪些 App 作用域 / 哪些模型可见数据 暴露面 / 凭据存在哪 secret 落点）；边界落在凭据/作用域/数据可见性三层，不在工具可见性或命名；任一答不出=边界未定义默认不可信。提升层：工具（授权边界）。与 sa 2.80「名字不是权限」重叠<60%，增量≥55%。

## 三、判重不落
- Langflow 组件隔离单测：av 已落。
- Flowise Agentflow 超集/HITL：ed 编排已覆盖。

## 四、三件套评估
- pt/mts/cc：本轮无新材。

## 五、来源可信
- Activepieces / Langflow / Flowise 直连 200。
