# r175-C 技能内动态注入的执行语义 + 技能内容生命周期 + context: fork 术语陷阱（Claude Code 官方 skills.md 实拉）

实拉时间：2026-09-24 23:38–23:50（r175 三轮中的 C 轮，主源与 A/B 均不同，逐站重拉）

## 实拉证据（逐站）

1. `docs.claude.com/en/docs/agents-and-tools/agent-skills/best-practices.md` → **200 但内容是「App unavailable in region」区域封锁页**（400,000 B HTML，`<title>App unavailable in region</title>`）——**200 不等于拿到正文，必须看内容判定**，本轮记为区域封锁（非淘汰）。同路径 `overview.md`、`platform.claude.com/.../best-practices.md` 同为区域页。
2. `code.claude.com/docs/en/skills.md` → **200，117,414 B 真正文**（r173 已验证通道，本轮复通）。
3. `code.claude.com/docs/en/sub-agents.md`（117,754 B，200）、`code.claude.com/docs/en/slash-commands.md`（117,414 B，200）——辅助核对。
4. `code.claude.com/docs/en/agent-skills.md` → 404（路径不存在，探活+1）。
5. 上一轮已下载未消费的 `advanced-tool-use` / `code-execution-with-mcp` / `a-postmortem-of-three-recent-issues` / `how-we-contain-claude` —— 本轮审读后判重叠或增量过薄，不落（见下）。

原文留存 `D:/腾讯AI/yt/r175wb/C/`。

## 关键原文摘录（可核验）

- 「The `` !`<command>` `` syntax runs shell commands **before the skill content is sent to Claude**. The command output replaces the placeholder, so Claude receives actual data, not the command itself.」
- 「A failed command **aborts the entire skill invocation**, not just its own placeholder. Claude never sees the skill content for that invocation.」
- 「any non-zero exit code counts as a failure. One carveout applies: Claude Code treats **exit code 1 from search and comparison commands as a normal result** and injects their output. Exit codes of 2 or higher fail even for those commands.」
- 「`shell: bash` when bash isn't available: the invocation fails **before any command runs**… `Skill <name> requires bash (shell: bash in frontmatter) but Git Bash was not found`」
- 生命周期：「the rendered `SKILL.md` content enters the conversation as a single message and **stays there across later turns**. This persistence applies to the skill's instructions, not its permissions: an `allowed-tools` grant **clears when you send your next message**. Claude Code does **not re-read the skill file on later turns**」；「When the rendered content is identical to the copy already in context, Claude Code adds a short note that the skill is already loaded rather than a second copy of the content.」
- 「a skill with `context: fork` **doesn't run in a fork of the current conversation**, which would hand the subagent everything you've discussed so far… The subagent doesn't see your conversation history, so the skill's instructions **have to stand on their own**.」

## 独点清单（1 个落地点）

### 落点 → `wb-skill-authoring` 2.90.0 → **2.91.0**
技能内动态注入与内容生命周期：注入在正文送达模型前执行（拿到的是数据不是命令）／**注入失败作废整次调用**（模型看不到正文，只报 `Shell command failed for pattern`）／退出码口径（默认非 0 即失败，唯一豁免：搜索比较类 exit 1 视正常）／shell 可移植性硬边界（钉 `shell: bash` 在无 Git Bash 的机器上调用前即失败）／渲染正文"进入即常驻、后续轮次不重读文件"（贯穿性要求必须写成 standing instructions；内容完全一致时只补"已加载"短注、不塞第二份）／**常驻的是内容不是权限**（allowed-tools 授权在用户下条消息时清除）／`context: fork` 术语陷阱（不是 fork 会话，子代理看不到历史 → 技能指令必须自包含；真需要历史应 fork 对话）。
**提升层：可复用 Skill。**

## 判非重复理由（本轮逐条）

- sa §渐进披露（description 常驻 → SKILL.md → references）管**加载时机与文件分层**；本轮管**同一份渲染内容在会话里的驻留与失效**（不重读、幂等短注、权限随轮次清除），重叠 <60%。
- sa §触发权/轮级授权（allowed-tools 下一轮清除）已覆盖授权续期；本段只取其与"内容常驻"的**对照**（别把内容还在当成权限还在），未重复展开。
- sd 已有 Namespace 前缀防同名覆盖（r171-C）；本轮未落同名解析（Claude 的 sync/命令同名规则为产品细节，判不落）。
- **判不落**：`code-execution-with-mcp`（编排写进代码、中间结果不进上下文）与 cc「大对象外置」+ sa「defer 挂载粒度」重叠 >60%；`advanced-tool-use` 的 Tool Search/deferred loading 与 sa line 1918 重叠 >60%（B 轮已判一次，C 轮复核维持）；`a-postmortem-of-three-recent-issues`（事故复盘）与 `how-we-contain-claude`（沙箱约束）为 Anthropic 自身工程实践，抽不出可复用的 agent 技能系统方法层独点，留候选池。
- **新教训（写入下轮提示）**：`docs.claude.com` 返回 200 但正文是区域封锁页 → **探活必须看内容不看状态码**；正文通道走 `code.claude.com/docs/en/<page>.md`。
