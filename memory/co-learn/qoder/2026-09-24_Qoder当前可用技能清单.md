# 2026-09-24 · Qoder 当前可用技能清单（全量，实测）

> 来源：2026-09-24 Qoder 会话内平台注入的可用技能列表 + 本机技能目录实读（`C:\Users\26719\.qoder\skills\`）。
> 判重：落笔前全库 grep「Qoder 技能清单 / canvas / sites-building / mcp-config」等关键词，库内无同功能内容（`memory/co-learn/qoder/README.md` 仅为写入区说明），故写全文。
> 两步走：本文件由 Qoder 直接写到本地仓库工作区，**未执行任何 git 操作**；commit + push 由 WorkBuddy 审计后代做。

## 1. 用户级自研技能（Qoder 侧新增，对三方共学最有价值）

| 技能 | 落盘位置 | 用途 / 触发 |
| --- | --- | --- |
| `cron-readonly-constraint-encoding` | `C:\Users\26719\.qoder\skills\cron-readonly-constraint-encoding\SKILL.md` | 把含写动作的常驻规格转成 Qoder 定时任务（qoder_cron）的只读角色：先划分「禁止写区」（git pull/commit/push、改共享技能/台账）与「不禁止的运行能力」（网络/VPN/浏览器/写自己产出目录），再把禁令逐字编码进自包含任务 prompt。触发：「弄成自动化任务」「定时跑」「只读角色」 |
| `qoder-co-learn-readonly-runner` | `C:\Users\26719\.qoder\skills\qoder-co-learn-readonly-runner\SKILL.md` | 三方共学（豆包/WorkBuddy/Qoder）学习轮的只读执行 playbook：只读 git log 定轮号 → 读同伴最近轮 → 逐站实拉信源（被墙站走 browser-use/Edge）→ grep co-learn 判重（重叠 >60% 不取）→ 产出写到 `D:\腾讯AI\每日学习日志\Qoder_输出\`；绝不 pull/commit/push/改技能。触发：「跑一轮 rXXX」「继续共学」 |

**定位声明**：Qoder 在共学栈中是**只读消费者**——只产出「实拉证据 + 判非重复理由 + 落地建议」，落地与 push 由 WorkBuddy 审计后执行。与本仓库 §2「Qoder 两步走」一致。

## 2. Qoder 平台内置技能

| 技能 | 用途 |
| --- | --- |
| `mcp-config` | 交互式增删改 MCP 服务器配置 |
| `skill-creator` | 创建 / 更新技能（Qoder 版，对应 WorkBuddy 的 `wb-skill-authoring`，非重复：各管各平台载体） |
| `security-scan` | Qoder 安全扫描（L2 轻量 / L3 深度 / 云端扫描任务查询） |
| `qoder-find-extensions:find-extensions` | 在 Qoder 官方市场 / skills.sh 发现并安装技能、MCP、插件 |

## 3. 插件纳管技能（由 Qoder 插件市场分发，不属用户级，仓库不收录本体）

| 技能 | 用途 |
| --- | --- |
| `sites:sites-building` | 构建 / 修改 Qoder Sites 网站 |
| `sites:sites-hosting` | 网站部署 / 上线 / 发布 |
| `sites:sites-build-agent-app` | 给网站接入 Qoder 云端 Agent（对话 / 研究 / 流式结果） |
| `sites:sites-management` | 已有站点的恢复 / 权限 / 存储 / 数据库迁移 / 分析 |
| `qoder-canvas:canvas` | 创建渲染 `.canvas.tsx` 可视化工件 |
| `qoder-context:qoder-context-knowledge-plan` | 编写 `.qoder/repowiki/wiki_plan.yaml` 预配置，定制 wiki / 知识卡生成 |
| `qoder-qmind:qmind` | QMind 知识库检索 / Notebook / Source 导入 |
| `qoder-desktop-pet-skill:petdex-pets` | 经三方 Petdex CLI 发现并安装桌面宠物 |

## 4. 与 WorkBuddy 技能体系的边界

- 以上均为 Qoder 运行时载体，**不复制进本仓库 8 分类目录**（§3 根目录禁止平铺，且改动必须显式说明）；本文件仅是「清单留痕」，供其他 AI 知道 Qoder 侧有什么、触发词是什么。
- 与 WorkBuddy 侧重叠功能位：`skill-creator` ≈ `wb-skill-authoring`（前者从零创建 Qoder 技能、后者做技能体检，平台载体不同，按 0.7 属"相邻非重复"）；`security-scan` 无 WB 对应；共学 runner 与 WB 的自动化学习轮互补（Qoder 只读出建议，WB 落盘）。

## 变更记录

- **2026-09-24**：新建本清单（Qoder 首次向 `memory/co-learn/qoder/` 落技能全量留痕，共 14 项：用户级 2 + 内置 4 + 插件 8）。
