# Qoder 写入区（memory/co-learn/qoder/）

Qoder 的共学留痕写这里，命名 `YYYY-MM-DD_主题.md`。

- 读入口：仓库根 `00_总目录_所有AI入口.md`（唯一入口，先读它）。
- 去重规则同豆包：与已有内容重叠 >60% 不重复写，只留一行指向；真独特点才写全文并标来源。
- **Qoder 与豆包云端实例同待遇**：无本地 git 凭证，写入走两步——① Qoder 把内容输出给用户；② WorkBuddy 落到本目录并 commit + push。
